// AUTO-GENERATED by scripts/generate.ts from .generated-specs (docs.slack.dev JSON twins). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  SlackProtocol,
  type SlackOpError,
  type SlackOpContext,
} from "../protocol.ts";
import { SlackError, SlackRateLimited } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { SlackOpError, SlackOpContext };

export interface OauthAccessRequest {
  /** Issued when you created your application. If possible, avoid sending `client_id` and `client_secret` as parameters in your request and instead supply the Client ID and Client Secret using the HTTP Basic authentication scheme. */
  client_id?: string;
  /** Issued when you created your application. If possible, avoid sending `client_id` and `client_secret` as parameters in your request and instead supply the Client ID and Client Secret using the HTTP Basic authentication scheme. */
  client_secret?: string | Redacted.Redacted<string>;
  /** The `code` param returned via the OAuth callback. */
  code?: string;
  /** This must match the originally submitted URI (if one was sent). */
  redirect_uri?: string;
  /** Request the user to add your app only to a single channel. Only valid with a [legacy workspace app](https://docs.slack.dev/changelog/2021-03-workspace-apps-to-retire-in-august-2021). */
  single_channel?: boolean;
}
export const OauthAccessRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    client_id: S.optional(S.String),
    client_secret: S.optional(S.String.pipe(T.SensitiveValue({}))),
    code: S.optional(S.String),
    redirect_uri: S.optional(S.String),
    single_channel: S.optional(S.Boolean),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/oauth.access",
      code: 200,
      contentType: "form-urlencoded",
    }),
  ),
).annotate({
  identifier: "OauthAccessRequest",
}) as any as S.Schema<OauthAccessRequest>;

export interface OauthAccessResponseAuthorizingUser {
  user_id?: string;
  app_home?: string | null;
}
export const OauthAccessResponseAuthorizingUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    user_id: S.optional(S.String),
    app_home: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "OauthAccessResponseAuthorizingUser",
}) as any as S.Schema<OauthAccessResponseAuthorizingUser>;

export type OauthAccessResponseInstallerUser =
  OauthAccessResponseAuthorizingUser;
export const OauthAccessResponseInstallerUser =
  OauthAccessResponseAuthorizingUser;

export type OauthAccessResponseScopesAppHomeList = Array<string>;
export const OauthAccessResponseScopesAppHomeList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<OauthAccessResponseScopesAppHomeList>;

export type OauthAccessResponseScopesTeamList = Array<string>;
export const OauthAccessResponseScopesTeamList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<OauthAccessResponseScopesTeamList>;

export type OauthAccessResponseScopesChannelList = Array<string>;
export const OauthAccessResponseScopesChannelList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<OauthAccessResponseScopesChannelList>;

export type OauthAccessResponseScopesGroupList = Array<string>;
export const OauthAccessResponseScopesGroupList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<OauthAccessResponseScopesGroupList>;

export type OauthAccessResponseScopesMpimList = Array<string>;
export const OauthAccessResponseScopesMpimList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<OauthAccessResponseScopesMpimList>;

export type OauthAccessResponseScopesImList = Array<string>;
export const OauthAccessResponseScopesImList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<OauthAccessResponseScopesImList>;

export interface OauthAccessResponseScopes {
  app_home?: OauthAccessResponseScopesAppHomeList;
  team?: OauthAccessResponseScopesTeamList;
  channel?: OauthAccessResponseScopesChannelList;
  group?: OauthAccessResponseScopesGroupList;
  mpim?: OauthAccessResponseScopesMpimList;
  im?: OauthAccessResponseScopesImList;
}
export const OauthAccessResponseScopes = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    app_home: S.optional(OauthAccessResponseScopesAppHomeList),
    team: S.optional(OauthAccessResponseScopesTeamList),
    channel: S.optional(OauthAccessResponseScopesChannelList),
    group: S.optional(OauthAccessResponseScopesGroupList),
    mpim: S.optional(OauthAccessResponseScopesMpimList),
    im: S.optional(OauthAccessResponseScopesImList),
  }),
).annotate({
  identifier: "OauthAccessResponseScopes",
}) as any as S.Schema<OauthAccessResponseScopes>;

export interface OauthAccessResponseBot {
  bot_user_id?: string;
  bot_access_token?: string;
  scope?: string;
}
export const OauthAccessResponseBot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bot_user_id: S.optional(S.String),
    bot_access_token: S.optional(S.String),
    scope: S.optional(S.String),
  }),
).annotate({
  identifier: "OauthAccessResponseBot",
}) as any as S.Schema<OauthAccessResponseBot>;

export interface OauthAccessResponseUser {
  name?: string;
  id?: string;
  email?: string;
  image_24?: string;
  image_32?: string;
  image_48?: string;
  image_72?: string;
  image_192?: string;
  image_512?: string;
  scope?: string;
  access_token?: string | Redacted.Redacted<string>;
  token_type?: string;
}
export const OauthAccessResponseUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    id: S.optional(S.String),
    email: S.optional(S.String),
    image_24: S.optional(S.String),
    image_32: S.optional(S.String),
    image_48: S.optional(S.String),
    image_72: S.optional(S.String),
    image_192: S.optional(S.String),
    image_512: S.optional(S.String),
    scope: S.optional(S.String),
    access_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
    token_type: S.optional(S.String),
  }),
).annotate({
  identifier: "OauthAccessResponseUser",
}) as any as S.Schema<OauthAccessResponseUser>;

export interface OauthAccessResponseTeam {
  id?: string;
  name?: string;
  domain?: string;
  image_34?: string;
  image_44?: string;
  image_68?: string;
  image_88?: string;
  image_102?: string;
  image_132?: string;
  image_230?: string;
  image_default?: boolean;
  image_original?: string;
}
export const OauthAccessResponseTeam = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    name: S.optional(S.String),
    domain: S.optional(S.String),
    image_34: S.optional(S.String),
    image_44: S.optional(S.String),
    image_68: S.optional(S.String),
    image_88: S.optional(S.String),
    image_102: S.optional(S.String),
    image_132: S.optional(S.String),
    image_230: S.optional(S.String),
    image_default: S.optional(S.Boolean),
    image_original: S.optional(S.String),
  }),
).annotate({
  identifier: "OauthAccessResponseTeam",
}) as any as S.Schema<OauthAccessResponseTeam>;

export interface OauthAccessResponseIncomingWebhook {
  channel?: string;
  channel_id?: string;
  configuration_url?: string;
  url?: string;
}
export const OauthAccessResponseIncomingWebhook = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.optional(S.String),
    channel_id: S.optional(S.String),
    configuration_url: S.optional(S.String),
    url: S.optional(S.String),
  }),
).annotate({
  identifier: "OauthAccessResponseIncomingWebhook",
}) as any as S.Schema<OauthAccessResponseIncomingWebhook>;

export interface OauthAccessResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  token_type?: string;
  app_id?: string;
  app_user_id?: string;
  team_name?: string;
  team_id: string;
  access_token: string | Redacted.Redacted<string>;
  authorizing_user?: OauthAccessResponseAuthorizingUser;
  installer_user?: OauthAccessResponseAuthorizingUser;
  scopes?: OauthAccessResponseScopes;
  scope?: string;
  user_id?: string;
  bot?: OauthAccessResponseBot;
  refresh_token?: string | Redacted.Redacted<string>;
  expires_in?: number;
  user?: OauthAccessResponseUser;
  team?: OauthAccessResponseTeam;
  enterprise_id?: string | null;
  incoming_webhook?: OauthAccessResponseIncomingWebhook;
  single_channel_id?: string;
}
export const OauthAccessResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    token_type: S.optional(S.String),
    app_id: S.optional(S.String),
    app_user_id: S.optional(S.String),
    team_name: S.optional(S.String),
    team_id: S.String,
    access_token: S.String.pipe(T.SensitiveValue({})),
    authorizing_user: S.optional(OauthAccessResponseAuthorizingUser),
    installer_user: S.optional(OauthAccessResponseAuthorizingUser),
    scopes: S.optional(OauthAccessResponseScopes),
    scope: S.optional(S.String),
    user_id: S.optional(S.String),
    bot: S.optional(OauthAccessResponseBot),
    refresh_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
    expires_in: S.optional(S.Number),
    user: S.optional(OauthAccessResponseUser),
    team: S.optional(OauthAccessResponseTeam),
    enterprise_id: S.optional(S.NullOr(S.String)),
    incoming_webhook: S.optional(OauthAccessResponseIncomingWebhook),
    single_channel_id: S.optional(S.String),
  }),
).annotate({
  identifier: "OauthAccessResponse",
}) as any as S.Schema<OauthAccessResponse>;

export interface V2AccessRequest {
  /** Issued when you created your application. If possible, avoid sending `client_id` and `client_secret` as parameters in your request and instead supply the Client ID and Client Secret using the HTTP Basic authentication scheme. */
  client_id?: string;
  /** Issued when you created your application. If possible, avoid sending `client_id` and `client_secret` as parameters in your request and instead supply the Client ID and Client Secret using the HTTP Basic authentication scheme. */
  client_secret?: string | Redacted.Redacted<string>;
  /** The `code` param returned via the OAuth callback. */
  code?: string;
  /** The code_verifier param used to generate the code_challenge originally. Used for PKCE. */
  code_verifier?: string;
  /** This must match the originally submitted URI (if one was sent). */
  redirect_uri?: string;
  /** The `grant_type` param as described in the OAuth spec. */
  grant_type?: string;
  /** The `refresh_token` param as described in the OAuth spec. */
  refresh_token?: string | Redacted.Redacted<string>;
  /** Identity assertion JWT authorization grant */
  assertion?: string;
}
export const V2AccessRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    client_id: S.optional(S.String),
    client_secret: S.optional(S.String.pipe(T.SensitiveValue({}))),
    code: S.optional(S.String),
    code_verifier: S.optional(S.String),
    redirect_uri: S.optional(S.String),
    grant_type: S.optional(S.String),
    refresh_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
    assertion: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/oauth.v2.access",
      code: 200,
      contentType: "form-urlencoded",
    }),
  ),
).annotate({
  identifier: "V2AccessRequest",
}) as any as S.Schema<V2AccessRequest>;

/** The type of access token. Only present if a bot token was requested at the start of the auth flow. */
export type V2AccessResponseTokenType = "granular_bot";
export const V2AccessResponseTokenType = /*@__PURE__*/ S.String;

/** Details about the installing user, and the user token if one was requested at the start of the auth flow. */
export interface V2AccessResponseTeam {
  /** ID of the team into which the app is being installed or re-installed. */
  id: string;
  /** Name of the team into which the app is being installed or re-installed. */
  name?: string;
}
export const V2AccessResponseTeam = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.optional(S.String),
  }),
).annotate({
  identifier: "V2AccessResponseTeam",
}) as any as S.Schema<V2AccessResponseTeam>;

/** Details about the installing team, and the user token if one was requested at the start of the auth flow. */
export interface V2AccessResponseEnterprise {
  /** ID of the enterprise into which the app is being installed or re-installed. */
  id: string;
  /** Name of the enterprise into which the app is being installed or re-installed. */
  name?: string;
}
export const V2AccessResponseEnterprise = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.optional(S.String),
  }),
).annotate({
  identifier: "V2AccessResponseEnterprise",
}) as any as S.Schema<V2AccessResponseEnterprise>;

/** The type of the access token. Only present if a user token was requested at the start of the auth flow. */
export type V2AccessResponseAuthedUserTokenType = "user";
export const V2AccessResponseAuthedUserTokenType = /*@__PURE__*/ S.String;

/** Details about the installing user, and the user token if one was requested at the start of the auth flow. */
export interface V2AccessResponseAuthedUser {
  /** ID of the installing user. If a user token was requested, this is also the ID of the user to which the user token authenticates. */
  id: string;
  /** A user token. Only presented if requested at the start of the auth flow. */
  access_token?: string | Redacted.Redacted<string>;
  /** The type of the access token. Only present if a user token was requested at the start of the auth flow. */
  token_type?: V2AccessResponseAuthedUserTokenType;
  /** Comma-separated list of scopes granted to the user token. Only present if a user token was requested at the start of the auth flow. */
  scope?: string;
  /** Time until this credential expires, if it expires at all. */
  expires_in?: number;
  /** Refresh token for this credential. */
  refresh_token?: string | Redacted.Redacted<string>;
}
export const V2AccessResponseAuthedUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    access_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
    token_type: S.optional(V2AccessResponseAuthedUserTokenType),
    scope: S.optional(S.String),
    expires_in: S.optional(S.Number),
    refresh_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
  }),
).annotate({
  identifier: "V2AccessResponseAuthedUser",
}) as any as S.Schema<V2AccessResponseAuthedUser>;

/** Details about a newly-created incoming webhook. Only present if an incoming webhook was requested at the start of the auth flow. */
export interface V2AccessResponseIncomingWebhook {
  /** Name of channel. */
  channel: string;
  /** Encoded ID of channel. */
  channel_id: string;
  /** URL for managing the webhook. */
  configuration_url: string;
  /** Endpoint for triggering the wehbook. */
  url: string;
}
export const V2AccessResponseIncomingWebhook = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.String,
    channel_id: S.String,
    configuration_url: S.String,
    url: S.String,
  }),
).annotate({
  identifier: "V2AccessResponseIncomingWebhook",
}) as any as S.Schema<V2AccessResponseIncomingWebhook>;

export interface V2AccessResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  /** A bot access token. Only present if requested at the start of the auth flow. */
  access_token?: string | Redacted.Redacted<string>;
  /** The type of access token. Only present if a bot token was requested at the start of the auth flow. */
  token_type?: V2AccessResponseTokenType;
  /** Comma-separated list of scopes granted to the token. Only present if a bot token was requested at the start of the auth flow. */
  scope?: string;
  /** The user ID of the bot user corresponding to the token. Only present if a bot token was requested at the start of the auth flow. */
  bot_user_id?: string;
  /** The user ID of the bot user corresponding to the token. Only present during a refresh_token grant_type for a user access token. */
  user_id?: string;
  /** ID of the app being installed or re-installed. */
  app_id: string;
  /** Boolean flag indicating whether the generated token is Enterprise-level token or not. */
  is_enterprise_install?: boolean;
  /** Time until this credential expires, if it expires at all. */
  expires_in?: number;
  /** Refresh token for this credential. */
  refresh_token?: string | Redacted.Redacted<string>;
  team: V2AccessResponseTeam | null;
  enterprise: V2AccessResponseEnterprise | null;
  /** Details about the installing user, and the user token if one was requested at the start of the auth flow. */
  authed_user?: V2AccessResponseAuthedUser;
  /** Details about a newly-created incoming webhook. Only present if an incoming webhook was requested at the start of the auth flow. */
  incoming_webhook?: V2AccessResponseIncomingWebhook;
}
export const V2AccessResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    access_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
    token_type: S.optional(V2AccessResponseTokenType),
    scope: S.optional(S.String),
    bot_user_id: S.optional(S.String),
    user_id: S.optional(S.String),
    app_id: S.String,
    is_enterprise_install: S.optional(S.Boolean),
    expires_in: S.optional(S.Number),
    refresh_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
    team: S.NullOr(V2AccessResponseTeam),
    enterprise: S.NullOr(V2AccessResponseEnterprise),
    authed_user: S.optional(V2AccessResponseAuthedUser),
    incoming_webhook: S.optional(V2AccessResponseIncomingWebhook),
  }),
).annotate({
  identifier: "V2AccessResponse",
}) as any as S.Schema<V2AccessResponse>;

export interface V2ExchangeRequest {
  /** Issued when you created your application. */
  client_id: string;
  /** Issued when you created your application. */
  client_secret: string | Redacted.Redacted<string>;
}
export const V2ExchangeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    client_id: S.String,
    client_secret: S.String.pipe(T.SensitiveValue({})),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/oauth.v2.exchange",
      code: 200,
      contentType: "form-urlencoded",
    }),
  ),
).annotate({
  identifier: "V2ExchangeRequest",
}) as any as S.Schema<V2ExchangeRequest>;

/** The type of access token. */
export type V2ExchangeResponseTokenType = "bot" | "user";
export const V2ExchangeResponseTokenType = /*@__PURE__*/ S.String;

export interface V2ExchangeResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  app_id: string;
  /** The type of access token. */
  token_type: V2ExchangeResponseTokenType;
  /** Comma-separated list of scopes granted to the token. */
  scope: string;
  /** The user ID of the bot user corresponding to the token. Only present if a legacy bot access token is being exchanged. */
  bot_user_id?: string;
  /** The user ID of the user corresponding to the token. Only present if a legacy user access token is being exchanged. */
  user_id?: string;
  /** A new xoxe.xox(b|p) expiring access token to replace the legacy token. */
  access_token: string | Redacted.Redacted<string>;
  /** Duration (in seconds) until the new access token expires. */
  expires_in: number;
  /** The xoxe refresh token to obtain a new access token after the one returned expires. */
  refresh_token: string | Redacted.Redacted<string>;
  team?: unknown | null;
  enterprise?: unknown | null;
}
export const V2ExchangeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    app_id: S.String,
    token_type: V2ExchangeResponseTokenType,
    scope: S.String,
    bot_user_id: S.optional(S.String),
    user_id: S.optional(S.String),
    access_token: S.String.pipe(T.SensitiveValue({})),
    expires_in: S.Number,
    refresh_token: S.String.pipe(T.SensitiveValue({})),
    team: S.optional(S.NullOr(S.Unknown)),
    enterprise: S.optional(S.NullOr(S.Unknown)),
  }),
).annotate({
  identifier: "V2ExchangeResponse",
}) as any as S.Schema<V2ExchangeResponse>;

export interface V2UserAccessRequest {
  /** Issued when you created your application. If possible, avoid sending `client_id` and `client_secret` as parameters in your request and instead supply the Client ID and Client Secret using the HTTP Basic authentication scheme. */
  client_id?: string;
  /** Issued when you created your application. If possible, avoid sending `client_id` and `client_secret` as parameters in your request and instead supply the Client ID and Client Secret using the HTTP Basic authentication scheme. */
  client_secret?: string | Redacted.Redacted<string>;
  /** The `code` param returned via the OAuth callback. */
  code?: string;
  /** The code_verifier param used to generate the code_challenge originally. Used for PKCE. */
  code_verifier?: string;
  /** This must match the originally submitted URI (if one was sent). */
  redirect_uri?: string;
  /** The `grant_type` param as described in the OAuth spec. */
  grant_type?: string;
  /** The `refresh_token` param as described in the OAuth spec. */
  refresh_token?: string | Redacted.Redacted<string>;
  /** Identity assertion JWT authorization grant. */
  assertion?: string;
}
export const V2UserAccessRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    client_id: S.optional(S.String),
    client_secret: S.optional(S.String.pipe(T.SensitiveValue({}))),
    code: S.optional(S.String),
    code_verifier: S.optional(S.String),
    redirect_uri: S.optional(S.String),
    grant_type: S.optional(S.String),
    refresh_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
    assertion: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/oauth.v2.user.access",
      code: 200,
      contentType: "form-urlencoded",
    }),
  ),
).annotate({
  identifier: "V2UserAccessRequest",
}) as any as S.Schema<V2UserAccessRequest>;

/** The type of access token. */
export type V2UserAccessResponseTokenType = "user";
export const V2UserAccessResponseTokenType = /*@__PURE__*/ S.String;

/** Details about the team. */
export type V2UserAccessResponseTeam = V2AccessResponseTeam;
export const V2UserAccessResponseTeam = V2AccessResponseTeam;

/** Details about the enterprise. */
export type V2UserAccessResponseEnterprise = V2AccessResponseEnterprise;
export const V2UserAccessResponseEnterprise = V2AccessResponseEnterprise;

export interface V2UserAccessResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  /** A user access token. */
  access_token: string | Redacted.Redacted<string>;
  /** The type of access token. */
  token_type: V2UserAccessResponseTokenType;
  /** Comma-separated list of scopes granted to the token. */
  scope?: string;
  /** The user ID of the user corresponding to the token. */
  user_id: string;
  /** ID of the app being installed or re-installed. */
  app_id: string;
  /** Boolean flag indicating whether the generated token is enterprise level token or not */
  is_enterprise_install: boolean;
  /** Time until this credential expires, if it expires at all */
  expires_in?: number;
  /** Refresh token for this credential */
  refresh_token?: string | Redacted.Redacted<string>;
  team: V2AccessResponseTeam | null;
  enterprise: V2AccessResponseEnterprise | null;
}
export const V2UserAccessResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    access_token: S.String.pipe(T.SensitiveValue({})),
    token_type: V2UserAccessResponseTokenType,
    scope: S.optional(S.String),
    user_id: S.String,
    app_id: S.String,
    is_enterprise_install: S.Boolean,
    expires_in: S.optional(S.Number),
    refresh_token: S.optional(S.String.pipe(T.SensitiveValue({}))),
    team: S.NullOr(V2AccessResponseTeam),
    enterprise: S.NullOr(V2AccessResponseEnterprise),
  }),
).annotate({
  identifier: "V2UserAccessResponse",
}) as any as S.Schema<V2UserAccessResponse>;

export type OauthAccessError = SlackOpError;
/** Exchanges a temporary OAuth verifier code for an access token. Rate limit tier: 4 Method-specific errors (the `error` slug on the SlackError): - `bad_client_secret` — Value passed for `client_secret` was invalid. - `bad_redirect_uri` — Value passed for `redirect_uri` did not match the `redirect_uri` in the original request. - `code_already_used` — Value passed for `code` was already exchanged. - `internal_error` — Something went wrong during app installation. - `invalid_client_id` — Value passed for `client_id` was invalid. - `invalid_code` — Value passed for `code` was invalid. - `invalid_token` — Invalid refresh token. - `missing_resource` — Missing permission resource. - `ratelimited` — Too many requests made in succession. - `oauth_authorization_url_mismatch` — The OAuth flow was initiated on an incorrect version of the authorization url. The flow must be initiated via /oauth/authorize. - `user_email_unverified` — The users email is unverified See https://docs.slack.dev/reference/methods/oauth.access */
export const oauthAccess: API.OperationMethod<
  OauthAccessRequest,
  OauthAccessResponse,
  OauthAccessError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: OauthAccessRequest,
  output: OauthAccessResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type V2AccessError = SlackOpError;
/** Exchanges a temporary OAuth verifier code for an access token. Rate limit tier: 5 Method-specific errors (the `error` slug on the SlackError): - `bad_client_secret` — Value passed for `client_secret` was invalid. - `bad_redirect_uri` — Value passed for `redirect_uri` did not match the `redirect_uri` in the original request. - `cannot_install_an_org_installed_app` — Returned when the the org-installed app cannot be installed on a workspace. - `invalid_client_id` — Value passed for `client_id` was invalid. - `invalid_code` — Value passed for `code` was invalid. - `invalid_grant_type` — Value passed for `grant_type` was invalid. - `invalid_refresh_token` — The given refresh token is invalid. - `no_scopes` — Missing `scope` in the request. - `oauth_authorization_url_mismatch` — The OAuth flow was initiated on an incorrect version of the authorization url. The flow must be initiated via /oauth/v2/authorize. - `preview_feature_not_available` — Returned when the API method is not yet available on the team in context. - `user_email_unverified` — The users email is unverified. - `pkce_not_allowed` — The app is not allowed to use the PKCE flow. - `invalid_code_verifier` — The code_verifier is invalid. See https://docs.slack.dev/reference/methods/oauth.v2.access */
export const v2Access: API.OperationMethod<
  V2AccessRequest,
  V2AccessResponse,
  V2AccessError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: V2AccessRequest,
  output: V2AccessResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type V2ExchangeError = SlackOpError;
/** Exchanges a legacy access token for a new expiring access token and refresh token Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `authorization_not_found` — The underlying authorization for this token was revoked or is invalid. - `bad_client_secret` — Value passed for `client_secret` was invalid. - `internal_error` — A server-side error occurred. - `invalid_client_id` — Value passed for `client_id` was invalid. - `invalid_token` — The legacy token provided cannot be exchanged for a new pair of token credentials. - `token_already_exchanged` — This token has already been exchanged for a pair of token credentials. - `token_rotation_not_enabled` — The app does not have token rotation enabled. See https://docs.slack.dev/reference/methods/oauth.v2.exchange */
export const v2Exchange: API.OperationMethod<
  V2ExchangeRequest,
  V2ExchangeResponse,
  V2ExchangeError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: V2ExchangeRequest,
  output: V2ExchangeResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type V2UserAccessError = SlackOpError;
/** Exchanges a temporary OAuth verifier code for a user access token. Rate limit tier: 5 Method-specific errors (the `error` slug on the SlackError): - `bad_client_secret` — Value passed for `client_secret` was invalid. - `bad_redirect_uri` — Value passed for `redirect_uri` did not match the `redirect_uri` in the original request. - `cannot_install_an_org_installed_app` — Returned when the the org-installed app cannot be installed on a workspace. - `invalid_client_id` — Value passed for `client_id` was invalid. - `invalid_code` — Value passed for `code` was invalid. - `invalid_grant_type` — Value passed for `grant_type` was invalid. - `invalid_refresh_token` — The given refresh token is invalid. - `no_scopes` — Missing `scope` in the request. - `no_user_scopes` — Missing user `scope` in the auth request. - `not_implemented` — Method not yet supported - `oauth_authorization_url_mismatch` — The OAuth flow was initiated on an incorrect version of the authorization url. The flow must be initiated via /oauth/v2/authorize . - `preview_feature_not_available` — Returned when the API method is not yet available on the team in context. - `user_email_unverified` — The users email is unverified See https://docs.slack.dev/reference/methods/oauth.v2.user.access */
export const v2UserAccess: API.OperationMethod<
  V2UserAccessRequest,
  V2UserAccessResponse,
  V2UserAccessError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: V2UserAccessRequest,
  output: V2UserAccessResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));
