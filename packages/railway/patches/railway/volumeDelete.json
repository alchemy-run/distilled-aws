{
  "description": "volumeDelete returns Boolean! so the baked document must not select subfields.",
  "patches": [
    {
      "op": "replace",
      "path": "/shapes/com.railway.api#DeleteVolumeRequest/traits/com.railway.graphql#operation/query",
      "value": "mutation volumeDelete($volumeId: String!) {\n  volumeDelete(volumeId: $volumeId)\n}"
    }
  ]
}
