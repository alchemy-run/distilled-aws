{
  "description": "projectDelete returns Boolean! so the baked document must not select subfields.",
  "patches": [
    {
      "op": "replace",
      "path": "/shapes/com.railway.api#DeleteProjectRequest/traits/com.railway.graphql#operation/query",
      "value": "mutation projectDelete($id: String!) {\n  projectDelete(id: $id)\n}"
    }
  ]
}
