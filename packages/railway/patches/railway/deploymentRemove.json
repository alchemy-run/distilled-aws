{
  "description": "deploymentRemove returns Boolean! so the baked document must not select subfields.",
  "patches": [
    {
      "op": "replace",
      "path": "/shapes/com.railway.api#RemoveDeploymentRequest/traits/com.railway.graphql#operation/query",
      "value": "mutation deploymentRemove($id: String!) {\n  deploymentRemove(id: $id)\n}"
    }
  ]
}
