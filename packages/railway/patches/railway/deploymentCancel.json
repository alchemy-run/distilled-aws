{
  "description": "deploymentCancel returns Boolean! so the baked document must not select subfields.",
  "patches": [
    {
      "op": "replace",
      "path": "/shapes/com.railway.api#CancelDeploymentRequest/traits/com.railway.graphql#operation/query",
      "value": "mutation deploymentCancel($id: String!) {\n  deploymentCancel(id: $id)\n}"
    }
  ]
}
