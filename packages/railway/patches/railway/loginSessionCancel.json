{
  "description": "loginSessionCancel returns Boolean! so the baked document must not select subfields.",
  "patches": [
    {
      "op": "replace",
      "path": "/shapes/com.railway.api#CancelLoginSessionRequest/traits/com.railway.graphql#operation/query",
      "value": "mutation loginSessionCancel($code: String!) {\n  loginSessionCancel(code: $code)\n}"
    }
  ]
}
