// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

/** * `PostHogWarehouse` - PostHog warehouse * `Redshift` - Redshift * `Snowflake` - Snowflake * `BigQuery` - BigQuery * `Postgres` - Postgres * `Databricks` - Databricks * `AzureBlob` - Azure Blob * `S3` - S3 */
export type ExternalDataDestinationTypeEnum =
  | "PostHogWarehouse"
  | "Redshift"
  | "Snowflake"
  | "BigQuery"
  | "Postgres"
  | "Databricks"
  | "AzureBlob"
  | "S3";
export const ExternalDataDestinationTypeEnum = S.String;

export interface CreateExternalDataDestinationRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Where synced rows are written. The PostHog warehouse is managed for you, so you cannot create one here. * `PostHogWarehouse` - PostHog warehouse * `Redshift` - Redshift * `Snowflake` - Snowflake * `BigQuery` - BigQuery * `Postgres` - Postgres * `Databricks` - Databricks * `AzureBlob` - Azure Blob * `S3` - S3 */
  type: ExternalDataDestinationTypeEnum | (string & {});
  /** Human-readable name shown when picking destinations for a source or table. */
  name: string;
  /** Settings for this destination: target database, schema or dataset, bucket and prefix, file format. Credentials are not stored here. They live on the linked integration. */
  config?: unknown;
  /** Integration holding this destination's credentials. Required for every type except the PostHog warehouse. */
  integration?: number | null;
}
export const CreateExternalDataDestinationRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      type: ExternalDataDestinationTypeEnum,
      name: S.String,
      config: S.optional(S.Unknown),
      integration: S.optional(S.NullOr(S.Number)),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/external_data_destinations/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "CreateExternalDataDestinationRequest",
}) as any as S.Schema<CreateExternalDataDestinationRequest>;

export interface ExternalDataDestination {
  id: string;
  /** Where synced rows are written. The PostHog warehouse is managed for you, so you cannot create one here. * `PostHogWarehouse` - PostHog warehouse * `Redshift` - Redshift * `Snowflake` - Snowflake * `BigQuery` - BigQuery * `Postgres` - Postgres * `Databricks` - Databricks * `AzureBlob` - Azure Blob * `S3` - S3 */
  type: ExternalDataDestinationTypeEnum;
  /** Human-readable name shown when picking destinations for a source or table. */
  name: string;
  /** Settings for this destination: target database, schema or dataset, bucket and prefix, file format. Credentials are not stored here. They live on the linked integration. */
  config?: unknown;
  /** Integration holding this destination's credentials. Required for every type except the PostHog warehouse. */
  integration?: number | null;
  /** Whether this is the managed PostHog warehouse destination. */
  is_posthog_warehouse: boolean;
  created_at: string;
  created_by: number | null;
  updated_at: string | null;
}
export const ExternalDataDestination = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    type: ExternalDataDestinationTypeEnum,
    name: S.String,
    config: S.optional(S.Unknown),
    integration: S.optional(S.NullOr(S.Number)),
    is_posthog_warehouse: S.Boolean,
    created_at: S.String,
    created_by: S.NullOr(S.Number),
    updated_at: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ExternalDataDestination",
}) as any as S.Schema<ExternalDataDestination>;

export interface ExternalDataDestinationsDestroyRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this external data destination. */
  id: string;
}
export const ExternalDataDestinationsDestroyRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/api/projects/{project_id}/external_data_destinations/{id}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ExternalDataDestinationsDestroyRequest",
}) as any as S.Schema<ExternalDataDestinationsDestroyRequest>;

export interface ExternalDataDestinationsDestroyResponse {}
export const ExternalDataDestinationsDestroyResponse = /*@__PURE__*/ S.suspend(
  () => S.Struct({}),
).annotate({
  identifier: "ExternalDataDestinationsDestroyResponse",
}) as any as S.Schema<ExternalDataDestinationsDestroyResponse>;

export interface GetExternalDataDestinationRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this external data destination. */
  id: string;
}
export const GetExternalDataDestinationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/external_data_destinations/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetExternalDataDestinationRequest",
}) as any as S.Schema<GetExternalDataDestinationRequest>;

export interface ListExternalDataDestinationsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Number of results to return per page. */
  limit?: number;
  /** The initial index from which to return the results. */
  offset?: number;
}
export const ListExternalDataDestinationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/external_data_destinations/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListExternalDataDestinationsRequest",
}) as any as S.Schema<ListExternalDataDestinationsRequest>;

export type PaginatedExternalDataDestinationListResultsList =
  Array<ExternalDataDestination>;
export const PaginatedExternalDataDestinationListResultsList =
  /*@__PURE__*/ S.Array(
    ExternalDataDestination,
  ) as any as S.Schema<PaginatedExternalDataDestinationListResultsList>;

export interface PaginatedExternalDataDestinationList {
  count: number;
  next?: string | null;
  previous?: string | null;
  results: PaginatedExternalDataDestinationListResultsList;
}
export const PaginatedExternalDataDestinationList = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      count: S.Number,
      next: S.optional(S.NullOr(S.String)),
      previous: S.optional(S.NullOr(S.String)),
      results: PaginatedExternalDataDestinationListResultsList,
    }),
).annotate({
  identifier: "PaginatedExternalDataDestinationList",
}) as any as S.Schema<PaginatedExternalDataDestinationList>;

export interface UpdateExternalDataDestinationRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this external data destination. */
  id: string;
  /** Where synced rows are written. The PostHog warehouse is managed for you, so you cannot create one here. * `PostHogWarehouse` - PostHog warehouse * `Redshift` - Redshift * `Snowflake` - Snowflake * `BigQuery` - BigQuery * `Postgres` - Postgres * `Databricks` - Databricks * `AzureBlob` - Azure Blob * `S3` - S3 */
  type: ExternalDataDestinationTypeEnum | (string & {});
  /** Human-readable name shown when picking destinations for a source or table. */
  name: string;
  /** Settings for this destination: target database, schema or dataset, bucket and prefix, file format. Credentials are not stored here. They live on the linked integration. */
  config?: unknown;
  /** Integration holding this destination's credentials. Required for every type except the PostHog warehouse. */
  integration?: number | null;
}
export const UpdateExternalDataDestinationRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      type: ExternalDataDestinationTypeEnum,
      name: S.String,
      config: S.optional(S.Unknown),
      integration: S.optional(S.NullOr(S.Number)),
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/api/projects/{project_id}/external_data_destinations/{id}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "UpdateExternalDataDestinationRequest",
}) as any as S.Schema<UpdateExternalDataDestinationRequest>;

export interface UpdateExternalDataDestinationsPartialRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this external data destination. */
  id: string;
  /** Where synced rows are written. The PostHog warehouse is managed for you, so you cannot create one here. * `PostHogWarehouse` - PostHog warehouse * `Redshift` - Redshift * `Snowflake` - Snowflake * `BigQuery` - BigQuery * `Postgres` - Postgres * `Databricks` - Databricks * `AzureBlob` - Azure Blob * `S3` - S3 */
  type?: ExternalDataDestinationTypeEnum | (string & {});
  /** Human-readable name shown when picking destinations for a source or table. */
  name?: string;
  /** Settings for this destination: target database, schema or dataset, bucket and prefix, file format. Credentials are not stored here. They live on the linked integration. */
  config?: unknown;
  /** Integration holding this destination's credentials. Required for every type except the PostHog warehouse. */
  integration?: number | null;
}
export const UpdateExternalDataDestinationsPartialRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      type: S.optional(ExternalDataDestinationTypeEnum),
      name: S.optional(S.String),
      config: S.optional(S.Unknown),
      integration: S.optional(S.NullOr(S.Number)),
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/api/projects/{project_id}/external_data_destinations/{id}/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "UpdateExternalDataDestinationsPartialRequest",
  }) as any as S.Schema<UpdateExternalDataDestinationsPartialRequest>;

export type CreateExternalDataDestinationError = PosthogOpError;
/** Manage where warehouse sources write their synced rows. A destination can be attached to several sources, or to a single table on a source. Credentials come from an integration, so one connection can be reused across syncs. */
export const createExternalDataDestination: API.OperationMethod<
  CreateExternalDataDestinationRequest,
  ExternalDataDestination,
  CreateExternalDataDestinationError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateExternalDataDestinationRequest,
  output: ExternalDataDestination,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ExternalDataDestinationsDestroyError = PosthogOpError;
/** Soft-delete, and detach it from everything that syncs to it. Runs already in flight keep their own snapshot of the destination, so deleting one never strands a run part-way through. The next run of each affected table resolves without it. */
export const externalDataDestinationsDestroy: API.OperationMethod<
  ExternalDataDestinationsDestroyRequest,
  ExternalDataDestinationsDestroyResponse,
  ExternalDataDestinationsDestroyError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExternalDataDestinationsDestroyRequest,
  output: ExternalDataDestinationsDestroyResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type GetExternalDataDestinationError = PosthogOpError;
/** Manage where warehouse sources write their synced rows. A destination can be attached to several sources, or to a single table on a source. Credentials come from an integration, so one connection can be reused across syncs. */
export const getExternalDataDestination: API.OperationMethod<
  GetExternalDataDestinationRequest,
  ExternalDataDestination,
  GetExternalDataDestinationError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetExternalDataDestinationRequest,
  output: ExternalDataDestination,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ListExternalDataDestinationsError = PosthogOpError;
/** Manage where warehouse sources write their synced rows. A destination can be attached to several sources, or to a single table on a source. Credentials come from an integration, so one connection can be reused across syncs. */
export const listExternalDataDestinations: API.OperationMethod<
  ListExternalDataDestinationsRequest,
  PaginatedExternalDataDestinationList,
  ListExternalDataDestinationsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListExternalDataDestinationsRequest,
  output: PaginatedExternalDataDestinationList,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type UpdateExternalDataDestinationError = PosthogOpError;
/** Manage where warehouse sources write their synced rows. A destination can be attached to several sources, or to a single table on a source. Credentials come from an integration, so one connection can be reused across syncs. */
export const updateExternalDataDestination: API.OperationMethod<
  UpdateExternalDataDestinationRequest,
  ExternalDataDestination,
  UpdateExternalDataDestinationError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateExternalDataDestinationRequest,
  output: ExternalDataDestination,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type UpdateExternalDataDestinationsPartialError = PosthogOpError;
/** Manage where warehouse sources write their synced rows. A destination can be attached to several sources, or to a single table on a source. Credentials come from an integration, so one connection can be reused across syncs. */
export const updateExternalDataDestinationsPartial: API.OperationMethod<
  UpdateExternalDataDestinationsPartialRequest,
  ExternalDataDestination,
  UpdateExternalDataDestinationsPartialError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateExternalDataDestinationsPartialRequest,
  output: ExternalDataDestination,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
