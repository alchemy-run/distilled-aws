// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-paypal). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  PaypalProtocol,
  type PaypalOpError,
  type PaypalOpContext,
} from "../protocol.ts";
import { UnknownPaypalError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { PaypalOpError, PaypalOpContext };

/** The type of landing page to display on the PayPal site for user checkout. To use the non-PayPal account landing page, set to `Billing`. To use the PayPal account login landing page, set to `Login`. */
export type FlowConfigLandingPageType = "login" | "billing";
export const FlowConfigLandingPageType = S.String;

/** Presents either the **Continue** or **Pay Now** checkout flow to the customer.<br/><br/>Default is **Continue**, or `user_action=continue`. When you do not know the final payment amount, accept this default flow, which redirects the customer to the PayPal payment page with the **Continue** button. When the customer clicks **Continue**, the customer can change the payment amount.<br/><br/> When you know the final payment amount, set `user_action=commit` to choose the **Pay Now** flow, which redirects the customer to the PayPal payment page with the **Pay Now** button. When the customer clicks **Pay Now**, the payment is processed immediately. */
export type FlowConfigUserAction = "COMMIT";
export const FlowConfigUserAction = S.String;

/** The HTTP method to use to redirect the customer to a return URL. Value is `GET` or `POST`. */
export type FlowConfigReturnUriHttpMethod = "GET" | "POST";
export const FlowConfigReturnUriHttpMethod = S.String;

/** The flow configuration parameters. */
export interface FlowConfig {
  /** The type of landing page to display on the PayPal site for user checkout. To use the non-PayPal account landing page, set to `Billing`. To use the PayPal account login landing page, set to `Login`. */
  landing_page_type?: FlowConfigLandingPageType | (string & {});
  /** The merchant site URL to display after a bank transfer payment. In Germany, valid only for the Giropay payment method or bank transfer payment method. */
  bank_txn_pending_url?: string;
  /** Presents either the **Continue** or **Pay Now** checkout flow to the customer.<br/><br/>Default is **Continue**, or `user_action=continue`. When you do not know the final payment amount, accept this default flow, which redirects the customer to the PayPal payment page with the **Continue** button. When the customer clicks **Continue**, the customer can change the payment amount.<br/><br/> When you know the final payment amount, set `user_action=commit` to choose the **Pay Now** flow, which redirects the customer to the PayPal payment page with the **Pay Now** button. When the customer clicks **Pay Now**, the payment is processed immediately. */
  user_action?: FlowConfigUserAction | (string & {});
  /** The HTTP method to use to redirect the customer to a return URL. Value is `GET` or `POST`. */
  return_uri_http_method?: FlowConfigReturnUriHttpMethod | (string & {});
}
export const FlowConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    landing_page_type: S.optional(FlowConfigLandingPageType),
    bank_txn_pending_url: S.optional(S.String),
    user_action: S.optional(FlowConfigUserAction),
    return_uri_http_method: S.optional(FlowConfigReturnUriHttpMethod),
  }),
).annotate({ identifier: "FlowConfig" }) as any as S.Schema<FlowConfig>;

/** The input field customization parameters. */
export interface InputFields {
  /** Indicates whether PayPal displays shipping address fields on the experience pages. Value is:<ul><li><code>0</code>. Displays the shipping address on the PayPal pages.</li><li><code>1</code>. Redacts shipping address fields from the PayPal pages. For digital goods, this field is required and must be <code>1</code>.</li><li><code>2</code>. Gets the shipping address from the customer's account profile.</li></ul> */
  no_shipping?: number;
  /** Indicates whether to display the shipping address that is passed to this call rather than the one on file for this buyer on the PayPal experience pages. Value is:<ul><li><code>0</code>. Displays the shipping address on file.</li><li><code>1</code>. Displays the shipping address specified in this call. the customer cannot edit this shipping address.</li></ul> */
  address_override?: number;
}
export const InputFields = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    no_shipping: S.optional(S.Number),
    address_override: S.optional(S.Number),
  }),
).annotate({ identifier: "InputFields" }) as any as S.Schema<InputFields>;

/** The style and presentation parameters. */
export interface Presentation {
  /** A label that overrides the business name in the PayPal account on the PayPal pages. Character length and limitations: 127 single-byte alphanumeric characters. */
  brand_name?: string;
  /** A URL to the logo image. A valid media type is `.gif`, `.jpg`, or `.png`. The image's maximum width is 190 pixels and maximum height is 60 pixels. PayPal crops images that are larger. PayPal places your logo image at the top of the cart review area. PayPal recommends that you store the image on a secure (HTTPS) server. Otherwise, web browsers display a message that checkout pages contain non-secure items. Character length and limitations: 127 single-byte alphanumeric characters. */
  logo_image?: string;
  /** The locale of pages that the PayPal payment experience displays. A valid value is `AU`, `AT`, `BE`, `BR`, `CA`, `CH`, `CN`, `DE`, `ES`, `GB`, `FR`, `IT`, `NL`, `PL`, `PT`, `RU`, or `US`. A 5-character code is also valid for languages in these countries: `da_DK`, `he_IL`, `id_ID`, `ja_JP`, `no_NO`, `pt_BR`, `ru_RU`, `sv_SE`, `th_TH`, `zh_CN`, `zh_HK`, or `zh_TW`. */
  locale_code?: string;
}
export const Presentation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    brand_name: S.optional(S.String),
    logo_image: S.optional(S.String),
    locale_code: S.optional(S.String),
  }),
).annotate({ identifier: "Presentation" }) as any as S.Schema<Presentation>;

export interface CreateWebProfileRequest {
  /** The server stores keys for three hours. */
  payPalRequestId: string;
  /** The web experience profile name. Must be unique for a set of profiles for a merchant. */
  name: string;
  /** Indicates whether the profile persists for three hours or permanently. To persist the profile permanently, set to `false`. To persist the profile for three hours, set to `true`. */
  temporary?: boolean;
  flow_config?: FlowConfig;
  input_fields?: InputFields;
  presentation?: Presentation;
}
export const CreateWebProfileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    payPalRequestId: S.String.pipe(T.Header("PayPal-Request-Id")),
    name: S.String,
    temporary: S.optional(S.Boolean),
    flow_config: S.optional(FlowConfig),
    input_fields: S.optional(InputFields),
    presentation: S.optional(Presentation),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/payment-experience/web-profiles",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateWebProfileRequest",
}) as any as S.Schema<CreateWebProfileRequest>;

/** A payment web experience profile. */
export interface WebProfile {
  /** The ID of the web experience profile. */
  id?: string;
  /** The web experience profile name. Must be unique for a set of profiles for a merchant. */
  name: string;
  /** Indicates whether the profile persists for three hours or permanently. To persist the profile permanently, set to `false`. To persist the profile for three hours, set to `true`. */
  temporary?: boolean;
  flow_config?: FlowConfig;
  input_fields?: InputFields;
  presentation?: Presentation;
}
export const WebProfile = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    name: S.String,
    temporary: S.optional(S.Boolean),
    flow_config: S.optional(FlowConfig),
    input_fields: S.optional(InputFields),
    presentation: S.optional(Presentation),
  }),
).annotate({ identifier: "WebProfile" }) as any as S.Schema<WebProfile>;

export interface DeleteWebProfileRequest {
  /** The ID of the profile to delete. */
  id: string;
}
export const DeleteWebProfileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v1/payment-experience/web-profiles/{id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteWebProfileRequest",
}) as any as S.Schema<DeleteWebProfileRequest>;

export interface DeleteWebProfileResponse {}
export const DeleteWebProfileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteWebProfileResponse",
}) as any as S.Schema<DeleteWebProfileResponse>;

export interface GetWebProfileRequest {
  /** The ID of the profile to delete. */
  id: string;
}
export const GetWebProfileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v1/payment-experience/web-profiles/{id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetWebProfileRequest",
}) as any as S.Schema<GetWebProfileRequest>;

export interface UpdateWebProfileRequest {
  /** The ID of the profile to delete. */
  id: string;
  /** The web experience profile name. Must be unique for a set of profiles for a merchant. */
  name: string;
  /** Indicates whether the profile persists for three hours or permanently. To persist the profile permanently, set to `false`. To persist the profile for three hours, set to `true`. */
  temporary?: boolean;
  flow_config?: FlowConfig;
  input_fields?: InputFields;
  presentation?: Presentation;
}
export const UpdateWebProfileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    name: S.String,
    temporary: S.optional(S.Boolean),
    flow_config: S.optional(FlowConfig),
    input_fields: S.optional(InputFields),
    presentation: S.optional(Presentation),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/v1/payment-experience/web-profiles/{id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateWebProfileRequest",
}) as any as S.Schema<UpdateWebProfileRequest>;

export interface UpdateWebProfileResponse {}
export const UpdateWebProfileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UpdateWebProfileResponse",
}) as any as S.Schema<UpdateWebProfileResponse>;

/** The operation. */
export type PatchOp = "add" | "remove" | "replace" | "move" | "copy" | "test";
export const PatchOp = S.String;

/** The JSON patch object to apply partial updates to resources. */
export interface Patch {
  /** The operation. */
  op: PatchOp | (string & {});
  /** The <a href="https://tools.ietf.org/html/rfc6901">JSON Pointer</a> to the target document location at which to complete the operation. */
  path?: string;
  /** The value to apply. The <code>remove</code> operation does not require a value. */
  value?: unknown | null;
  /** The <a href="https://tools.ietf.org/html/rfc6901">JSON Pointer</a> to the target document location from which to move the value. Required for the <code>move</code> operation. */
  from?: string;
}
export const Patch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    op: PatchOp,
    path: S.optional(S.String),
    value: S.optional(S.NullOr(S.Unknown)),
    from: S.optional(S.String),
  }),
).annotate({ identifier: "Patch" }) as any as S.Schema<Patch>;

/** An array of JSON patch objects to apply partial updates to resources. */
export type PatchRequest = Array<Patch>;
export const PatchRequest = /*@__PURE__*/ S.Array(
  Patch,
) as any as S.Schema<PatchRequest>;

export interface UpdateWebProfilePartialRequest {
  /** The ID of the profile to delete. */
  id: string;
  body?: PatchRequest;
}
export const UpdateWebProfilePartialRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    body: S.optional(PatchRequest.pipe(T.HttpBody())),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/v1/payment-experience/web-profiles/{id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateWebProfilePartialRequest",
}) as any as S.Schema<UpdateWebProfilePartialRequest>;

export interface UpdateWebProfilePartialResponse {}
export const UpdateWebProfilePartialResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UpdateWebProfilePartialResponse",
}) as any as S.Schema<UpdateWebProfilePartialResponse>;

export interface WebProfileGetListRequest {}
export const WebProfileGetListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({
      method: "GET",
      uri: "/v1/payment-experience/web-profiles",
      code: 200,
    }),
  ),
).annotate({
  identifier: "WebProfileGetListRequest",
}) as any as S.Schema<WebProfileGetListRequest>;

/** An array of web profiles. */
export type WebProfileList = Array<WebProfile>;
export const WebProfileList = /*@__PURE__*/ S.Array(
  WebProfile,
) as any as S.Schema<WebProfileList>;

export type WebProfileGetListResponse = WebProfileList;
export const WebProfileGetListResponse = /*@__PURE__*/ S.suspend(() =>
  WebProfileList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "WebProfileGetListResponse",
}) as any as S.Schema<WebProfileGetListResponse>;

export type CreateWebProfileError = PaypalOpError;
/** Create web experience profile Creates a web experience profile. In the JSON request body, specify the profile name and details. */
export const createWebProfile: API.OperationMethod<
  CreateWebProfileRequest,
  WebProfile,
  CreateWebProfileError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateWebProfileRequest,
  output: WebProfile,
  errors: [UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type DeleteWebProfileError = PaypalOpError;
/** Delete web experience profile Deletes a web experience profile, by ID. */
export const deleteWebProfile: API.OperationMethod<
  DeleteWebProfileRequest,
  DeleteWebProfileResponse,
  DeleteWebProfileError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteWebProfileRequest,
  output: DeleteWebProfileResponse,
  errors: [UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type GetWebProfileError = PaypalOpError;
/** Show web experience profile details by ID Shows details for a web experience profile, by ID. */
export const getWebProfile: API.OperationMethod<
  GetWebProfileRequest,
  WebProfile,
  GetWebProfileError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetWebProfileRequest,
  output: WebProfile,
  errors: [UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type UpdateWebProfileError = PaypalOpError;
/** Update web experience profile Updates a web experience profile, by ID. In the JSON request body, specify the profile details. If your request omits any profile parameters, any previously set values for those parameters are removed. */
export const updateWebProfile: API.OperationMethod<
  UpdateWebProfileRequest,
  UpdateWebProfileResponse,
  UpdateWebProfileError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateWebProfileRequest,
  output: UpdateWebProfileResponse,
  errors: [UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type UpdateWebProfilePartialError = PaypalOpError;
/** Partially update web experience profile Partially-updates a web experience profile, by ID. In the JSON request body, specify a patch object, the path of the profile location to update, and a new value. */
export const updateWebProfilePartial: API.OperationMethod<
  UpdateWebProfilePartialRequest,
  UpdateWebProfilePartialResponse,
  UpdateWebProfilePartialError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateWebProfilePartialRequest,
  output: UpdateWebProfilePartialResponse,
  errors: [UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type WebProfileGetListError = PaypalOpError;
/** List web experience profiles Lists the latest 20 web experience profiles for a merchant or subject. To show details for these or additional profiles, you can show web experience profile details by ID. */
export const webProfileGetList: API.OperationMethod<
  WebProfileGetListRequest,
  WebProfileGetListResponse,
  WebProfileGetListError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WebProfileGetListRequest,
  output: WebProfileGetListResponse,
  errors: [UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));
