// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-docker/specs/swagger.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  DockerProtocol,
  type DockerOpError,
  type DockerOpContext,
} from "../protocol.ts";
import { UnknownDockerError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { DockerOpError, DockerOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface ArchiveContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Resource in the container’s filesystem to archive. */
  path: string;
}
export const ArchiveContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({ method: "GET", uri: "/containers/{id}/archive", code: 200 }),
  ),
).annotate({
  identifier: "ArchiveContainerRequest",
}) as any as S.Schema<ArchiveContainerRequest>;

export interface ArchiveContainerResponse {}
export const ArchiveContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ArchiveContainerResponse",
}) as any as S.Schema<ArchiveContainerResponse>;

export interface AttachContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Override the key sequence for detaching a container.Format is a single character `[a-Z]` or `ctrl-<value>` where `<value>` is one of: `a-z`, `@`, `^`, `[`, `,` or `_`. */
  detachKeys?: string;
  /** Replay previous logs from the container. This is useful for attaching to a container that has started and you want to output everything since the container started. If `stream` is also enabled, once all the previous output has been returned, it will seamlessly transition into streaming current output. */
  logs?: boolean;
  /** Stream attached streams from the time the request was made onwards. */
  stream?: boolean;
  /** Attach to `stdin` */
  stdin?: boolean;
  /** Attach to `stdout` */
  stdout?: boolean;
  /** Attach to `stderr` */
  stderr?: boolean;
}
export const AttachContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    detachKeys: S.optional(S.String.pipe(T.Query())),
    logs: S.optional(S.Boolean.pipe(T.Query())),
    stream: S.optional(S.Boolean.pipe(T.Query())),
    stdin: S.optional(S.Boolean.pipe(T.Query())),
    stdout: S.optional(S.Boolean.pipe(T.Query())),
    stderr: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "POST", uri: "/containers/{id}/attach", code: 200 }),
  ),
).annotate({
  identifier: "AttachContainerRequest",
}) as any as S.Schema<AttachContainerRequest>;

export interface AttachContainerResponse {}
export const AttachContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "AttachContainerResponse",
}) as any as S.Schema<AttachContainerResponse>;

export interface BuildPruneRequest {
  /** Amount of disk space in bytes to keep for cache */
  reserved_space?: number;
  /** Maximum amount of disk space allowed to keep for cache */
  max_used_space?: number;
  /** Target amount of free disk space after pruning */
  min_free_space?: number;
  /** Remove all types of build cache */
  all?: boolean;
  /** A JSON encoded value of the filters (a `map[string][]string`) to process on the list of build cache objects. Available filters: - `until=<timestamp>` remove cache older than `<timestamp>`. The `<timestamp>` can be Unix timestamps, date formatted timestamps, or Go duration strings (e.g. `10m`, `1h30m`) computed relative to the daemon's local time. - `id=<id>` - `parent=<id>` - `type=<string>` - `description=<string>` - `inuse` - `shared` - `private` */
  filters?: string;
}
export const BuildPruneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    reserved_space: S.optional(S.Number.pipe(T.Query("reserved-space"))),
    max_used_space: S.optional(S.Number.pipe(T.Query("max-used-space"))),
    min_free_space: S.optional(S.Number.pipe(T.Query("min-free-space"))),
    all: S.optional(S.Boolean.pipe(T.Query())),
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/build/prune", code: 200 })),
).annotate({
  identifier: "BuildPruneRequest",
}) as any as S.Schema<BuildPruneRequest>;

export type BuildPruneResponseCachesDeletedList = Array<string>;
export const BuildPruneResponseCachesDeletedList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<BuildPruneResponseCachesDeletedList>;

export interface BuildPruneResponse {
  CachesDeleted?: BuildPruneResponseCachesDeletedList;
  /** Disk space reclaimed in bytes */
  SpaceReclaimed?: number;
}
export const BuildPruneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    CachesDeleted: S.optional(BuildPruneResponseCachesDeletedList),
    SpaceReclaimed: S.optional(S.Number),
  }),
).annotate({
  identifier: "BuildPruneResponse",
}) as any as S.Schema<BuildPruneResponse>;

/** An object mapping ports to an empty object in the form: `{"<port>/<tcp|udp|sctp>": {}}` */
export type CommitImageRequestExposedPortsMap = {
  [key: string]: unknown | undefined;
};
export const CommitImageRequestExposedPortsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CommitImageRequestExposedPortsMap>;

/** A list of environment variables to set inside the container in the form `["VAR=value", ...]`. A variable without `=` is removed from the environment, rather than to have an empty value. */
export type CommitImageRequestEnvList = Array<string>;
export const CommitImageRequestEnvList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CommitImageRequestEnvList>;

/** Command to run specified as a string or an array of strings. */
export type CommitImageRequestCmdList = Array<string>;
export const CommitImageRequestCmdList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CommitImageRequestCmdList>;

/** The test to perform. Possible values are: - `[]` inherit healthcheck from image or parent image - `["NONE"]` disable healthcheck - `["CMD", args...]` exec arguments directly - `["CMD-SHELL", command]` run command with system's default shell A non-zero exit code indicates a failed healthcheck: - `0` healthy - `1` unhealthy - `2` reserved (treated as unhealthy) - other values: error running probe */
export type HealthConfigTestList = Array<string>;
export const HealthConfigTestList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HealthConfigTestList>;

/** A test to perform to check that the container is healthy. Healthcheck commands should be side-effect free. */
export interface HealthConfig {
  /** The test to perform. Possible values are: - `[]` inherit healthcheck from image or parent image - `["NONE"]` disable healthcheck - `["CMD", args...]` exec arguments directly - `["CMD-SHELL", command]` run command with system's default shell A non-zero exit code indicates a failed healthcheck: - `0` healthy - `1` unhealthy - `2` reserved (treated as unhealthy) - other values: error running probe */
  Test?: HealthConfigTestList;
  /** The time to wait between checks in nanoseconds. It should be 0 or at least 1000000 (1 ms). 0 means inherit. */
  Interval?: number;
  /** The time to wait before considering the check to have hung. It should be 0 or at least 1000000 (1 ms). 0 means inherit. If the health check command does not complete within this timeout, the check is considered failed and the health check process is forcibly terminated without a graceful shutdown. */
  Timeout?: number;
  /** The number of consecutive failures needed to consider a container as unhealthy. 0 means inherit. */
  Retries?: number;
  /** Start period for the container to initialize before starting health-retries countdown in nanoseconds. It should be 0 or at least 1000000 (1 ms). 0 means inherit. */
  StartPeriod?: number;
  /** The time to wait between checks in nanoseconds during the start period. It should be 0 or at least 1000000 (1 ms). 0 means inherit. */
  StartInterval?: number;
}
export const HealthConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Test: S.optional(HealthConfigTestList),
    Interval: S.optional(S.Number),
    Timeout: S.optional(S.Number),
    Retries: S.optional(S.Number),
    StartPeriod: S.optional(S.Number),
    StartInterval: S.optional(S.Number),
  }),
).annotate({ identifier: "HealthConfig" }) as any as S.Schema<HealthConfig>;

/** An object mapping mount point paths inside the container to empty objects. */
export type CommitImageRequestVolumesMap = {
  [key: string]: unknown | undefined;
};
export const CommitImageRequestVolumesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CommitImageRequestVolumesMap>;

/** The entry point for the container as a string or an array of strings. If the array consists of exactly one empty string (`[""]`) then the entry point is reset to system default (i.e., the entry point used by docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`). */
export type CommitImageRequestEntrypointList = Array<string>;
export const CommitImageRequestEntrypointList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CommitImageRequestEntrypointList>;

/** `ONBUILD` metadata that were defined in the image's `Dockerfile`. */
export type CommitImageRequestOnBuildList = Array<string>;
export const CommitImageRequestOnBuildList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CommitImageRequestOnBuildList>;

/** User-defined key/value metadata. */
export type CommitImageRequestLabelsMap = { [key: string]: string | undefined };
export const CommitImageRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CommitImageRequestLabelsMap>;

/** Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell. */
export type CommitImageRequestShellList = Array<string>;
export const CommitImageRequestShellList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CommitImageRequestShellList>;

export interface CommitImageRequest {
  /** The ID or name of the container to commit */
  container?: string;
  /** Repository name for the created image */
  repo?: string;
  /** Tag name for the create image */
  tag?: string;
  /** Commit message */
  comment?: string;
  /** Author of the image (e.g., `John Hannibal Smith <hannibal@a-team.com>`) */
  author?: string;
  /** Whether to pause the container before committing */
  pause?: boolean;
  /** `Dockerfile` instructions to apply while committing */
  changes?: string;
  /** The hostname to use for the container, as a valid RFC 1123 hostname. */
  Hostname?: string;
  /** The domain name to use for the container. */
  Domainname?: string;
  /** Commands run as this user inside the container. If omitted, commands run as the user specified in the image the container was started from. Can be either user-name or UID, and optional group-name or GID, separated by a colon (`<user-name|UID>[<:group-name|GID>]`). */
  User?: string;
  /** Whether to attach to `stdin`. */
  AttachStdin?: boolean;
  /** Whether to attach to `stdout`. */
  AttachStdout?: boolean;
  /** Whether to attach to `stderr`. */
  AttachStderr?: boolean;
  /** An object mapping ports to an empty object in the form: `{"<port>/<tcp|udp|sctp>": {}}` */
  ExposedPorts?: CommitImageRequestExposedPortsMap | null;
  /** Attach standard streams to a TTY, including `stdin` if it is not closed. */
  Tty?: boolean;
  /** Open `stdin` */
  OpenStdin?: boolean;
  /** Close `stdin` after one attached client disconnects */
  StdinOnce?: boolean;
  /** A list of environment variables to set inside the container in the form `["VAR=value", ...]`. A variable without `=` is removed from the environment, rather than to have an empty value. */
  Env?: CommitImageRequestEnvList;
  /** Command to run specified as a string or an array of strings. */
  Cmd?: CommitImageRequestCmdList;
  Healthcheck?: HealthConfig;
  /** Command is already escaped (Windows only) */
  ArgsEscaped?: boolean | null;
  /** The name (or reference) of the image to use when creating the container, or which was used when the container was created. */
  Image?: string;
  /** An object mapping mount point paths inside the container to empty objects. */
  Volumes?: CommitImageRequestVolumesMap;
  /** The working directory for commands to run in. */
  WorkingDir?: string;
  /** The entry point for the container as a string or an array of strings. If the array consists of exactly one empty string (`[""]`) then the entry point is reset to system default (i.e., the entry point used by docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`). */
  Entrypoint?: CommitImageRequestEntrypointList;
  /** Disable networking for the container. */
  NetworkDisabled?: boolean | null;
  /** `ONBUILD` metadata that were defined in the image's `Dockerfile`. */
  OnBuild?: CommitImageRequestOnBuildList | null;
  /** User-defined key/value metadata. */
  Labels?: CommitImageRequestLabelsMap;
  /** Signal to stop a container as a string or unsigned integer. */
  StopSignal?: string | null;
  /** Timeout to stop a container in seconds. If omitted, the daemon-wide default (`default-stop-timeout`) is used. */
  StopTimeout?: number | null;
  /** Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell. */
  Shell?: CommitImageRequestShellList | null;
}
export const CommitImageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    container: S.optional(S.String.pipe(T.Query())),
    repo: S.optional(S.String.pipe(T.Query())),
    tag: S.optional(S.String.pipe(T.Query())),
    comment: S.optional(S.String.pipe(T.Query())),
    author: S.optional(S.String.pipe(T.Query())),
    pause: S.optional(S.Boolean.pipe(T.Query())),
    changes: S.optional(S.String.pipe(T.Query())),
    Hostname: S.optional(S.String),
    Domainname: S.optional(S.String),
    User: S.optional(S.String),
    AttachStdin: S.optional(S.Boolean),
    AttachStdout: S.optional(S.Boolean),
    AttachStderr: S.optional(S.Boolean),
    ExposedPorts: S.optional(S.NullOr(CommitImageRequestExposedPortsMap)),
    Tty: S.optional(S.Boolean),
    OpenStdin: S.optional(S.Boolean),
    StdinOnce: S.optional(S.Boolean),
    Env: S.optional(CommitImageRequestEnvList),
    Cmd: S.optional(CommitImageRequestCmdList),
    Healthcheck: S.optional(HealthConfig),
    ArgsEscaped: S.optional(S.NullOr(S.Boolean)),
    Image: S.optional(S.String),
    Volumes: S.optional(CommitImageRequestVolumesMap),
    WorkingDir: S.optional(S.String),
    Entrypoint: S.optional(CommitImageRequestEntrypointList),
    NetworkDisabled: S.optional(S.NullOr(S.Boolean)),
    OnBuild: S.optional(S.NullOr(CommitImageRequestOnBuildList)),
    Labels: S.optional(CommitImageRequestLabelsMap),
    StopSignal: S.optional(S.NullOr(S.String)),
    StopTimeout: S.optional(S.NullOr(S.Number)),
    Shell: S.optional(S.NullOr(CommitImageRequestShellList)),
  }).pipe(T.Http({ method: "POST", uri: "/commit", code: 200 })),
).annotate({
  identifier: "CommitImageRequest",
}) as any as S.Schema<CommitImageRequest>;

/** Response to an API call that returns just an Id */
export interface IDResponse {
  /** The id of the newly created object. */
  Id: string;
}
export const IDResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.String,
  }),
).annotate({ identifier: "IDResponse" }) as any as S.Schema<IDResponse>;

export interface ConfigInspectRequest {
  /** ID of the config */
  id: string;
}
export const ConfigInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/configs/{id}", code: 200 })),
).annotate({
  identifier: "ConfigInspectRequest",
}) as any as S.Schema<ConfigInspectRequest>;

/** The version number of the object such as node, service, etc. This is needed to avoid conflicting writes. The client must send the version number along with the modified specification when updating these objects. This approach ensures safe concurrency and determinism in that the change on the object may not be applied if the version number has changed from the last read. In other words, if two update requests specify the same base version, only one of the requests can succeed. As a result, two separate update requests that happen at the same time will not unintentionally overwrite each other. */
export interface ObjectVersion {
  Index?: number;
}
export const ObjectVersion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Index: S.optional(S.Number),
  }),
).annotate({ identifier: "ObjectVersion" }) as any as S.Schema<ObjectVersion>;

/** User-defined key/value metadata. */
export type ConfigSpecLabelsMap = { [key: string]: string | undefined };
export const ConfigSpecLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ConfigSpecLabelsMap>;

/** Key/value map of driver-specific options. */
export type DriverOptionsMap = { [key: string]: string | undefined };
export const DriverOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<DriverOptionsMap>;

/** Driver represents a driver (network, logging, secrets). */
export interface Driver {
  /** Name of the driver. */
  Name: string;
  /** Key/value map of driver-specific options. */
  Options?: DriverOptionsMap;
}
export const Driver = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Options: S.optional(DriverOptionsMap),
  }),
).annotate({ identifier: "Driver" }) as any as S.Schema<Driver>;

export interface ConfigSpec {
  /** User-defined name of the config. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: ConfigSpecLabelsMap;
  /** Data is the data to store as a config, formatted as a standard base64-encoded ([RFC 4648](https://tools.ietf.org/html/rfc4648#section-4)) string. The maximum allowed size is 1000KB, as defined in [MaxConfigSize](https://pkg.go.dev/github.com/moby/swarmkit/v2@v2.0.0-20250103191802-8c1959736554/manager/controlapi#MaxConfigSize). */
  Data?: string;
  /** Templating driver, if applicable Templating controls whether and how to evaluate the config payload as a template. If no driver is set, no templating is used. */
  Templating?: Driver;
}
export const ConfigSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Labels: S.optional(ConfigSpecLabelsMap),
    Data: S.optional(S.String),
    Templating: S.optional(Driver),
  }),
).annotate({ identifier: "ConfigSpec" }) as any as S.Schema<ConfigSpec>;

export interface Config {
  ID?: string;
  Version?: ObjectVersion;
  CreatedAt?: string;
  UpdatedAt?: string;
  Spec?: ConfigSpec;
}
export const Config = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Version: S.optional(ObjectVersion),
    CreatedAt: S.optional(S.String),
    UpdatedAt: S.optional(S.String),
    Spec: S.optional(ConfigSpec),
  }),
).annotate({ identifier: "Config" }) as any as S.Schema<Config>;

export type EndpointIPAMConfigLinkLocalIPsList = Array<string>;
export const EndpointIPAMConfigLinkLocalIPsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EndpointIPAMConfigLinkLocalIPsList>;

/** EndpointIPAMConfig represents an endpoint's IPAM configuration. */
export interface EndpointIPAMConfig {
  IPv4Address?: string;
  IPv6Address?: string;
  LinkLocalIPs?: EndpointIPAMConfigLinkLocalIPsList;
}
export const EndpointIPAMConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    IPv4Address: S.optional(S.String),
    IPv6Address: S.optional(S.String),
    LinkLocalIPs: S.optional(EndpointIPAMConfigLinkLocalIPsList),
  }),
).annotate({
  identifier: "EndpointIPAMConfig",
}) as any as S.Schema<EndpointIPAMConfig>;

export type EndpointSettingsLinksList = Array<string>;
export const EndpointSettingsLinksList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EndpointSettingsLinksList>;

export type EndpointSettingsAliasesList = Array<string>;
export const EndpointSettingsAliasesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EndpointSettingsAliasesList>;

/** DriverOpts is a mapping of driver options and values. These options are passed directly to the driver and are driver specific. */
export type EndpointSettingsDriverOptsMap = {
  [key: string]: string | undefined;
};
export const EndpointSettingsDriverOptsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<EndpointSettingsDriverOptsMap>;

/** List of all DNS names an endpoint has on a specific network. This list is based on the container name, network aliases, container short ID, and hostname. These DNS names are non-fully qualified but can contain several dots. You can get fully qualified DNS names by appending `.<network-name>`. For instance, if container name is `my.ctr` and the network is named `testnet`, `DNSNames` will contain `my.ctr` and the FQDN will be `my.ctr.testnet`. */
export type EndpointSettingsDNSNamesList = Array<string>;
export const EndpointSettingsDNSNamesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EndpointSettingsDNSNamesList>;

/** Configuration for a network endpoint. */
export interface EndpointSettings {
  IPAMConfig?: EndpointIPAMConfig | null;
  Links?: EndpointSettingsLinksList;
  /** MAC address for the endpoint on this network. The network driver might ignore this parameter. */
  MacAddress?: string;
  Aliases?: EndpointSettingsAliasesList;
  /** DriverOpts is a mapping of driver options and values. These options are passed directly to the driver and are driver specific. */
  DriverOpts?: EndpointSettingsDriverOptsMap | null;
  /** This property determines which endpoint will provide the default gateway for a container. The endpoint with the highest priority will be used. If multiple endpoints have the same priority, endpoints are lexicographically sorted based on their network name, and the one that sorts first is picked. */
  GwPriority?: number;
  /** Unique ID of the network. */
  NetworkID?: string;
  /** Unique ID for the service endpoint in a Sandbox. */
  EndpointID?: string;
  /** Gateway address for this network. */
  Gateway?: string;
  /** IPv4 address. */
  IPAddress?: string;
  /** Mask length of the IPv4 address. */
  IPPrefixLen?: number;
  /** IPv6 gateway address. */
  IPv6Gateway?: string;
  /** Global IPv6 address. */
  GlobalIPv6Address?: string;
  /** Mask length of the global IPv6 address. */
  GlobalIPv6PrefixLen?: number;
  /** List of all DNS names an endpoint has on a specific network. This list is based on the container name, network aliases, container short ID, and hostname. These DNS names are non-fully qualified but can contain several dots. You can get fully qualified DNS names by appending `.<network-name>`. For instance, if container name is `my.ctr` and the network is named `testnet`, `DNSNames` will contain `my.ctr` and the FQDN will be `my.ctr.testnet`. */
  DNSNames?: EndpointSettingsDNSNamesList;
}
export const EndpointSettings = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    IPAMConfig: S.optional(S.NullOr(EndpointIPAMConfig)),
    Links: S.optional(EndpointSettingsLinksList),
    MacAddress: S.optional(S.String),
    Aliases: S.optional(EndpointSettingsAliasesList),
    DriverOpts: S.optional(S.NullOr(EndpointSettingsDriverOptsMap)),
    GwPriority: S.optional(S.Number),
    NetworkID: S.optional(S.String),
    EndpointID: S.optional(S.String),
    Gateway: S.optional(S.String),
    IPAddress: S.optional(S.String),
    IPPrefixLen: S.optional(S.Number),
    IPv6Gateway: S.optional(S.String),
    GlobalIPv6Address: S.optional(S.String),
    GlobalIPv6PrefixLen: S.optional(S.Number),
    DNSNames: S.optional(EndpointSettingsDNSNamesList),
  }),
).annotate({
  identifier: "EndpointSettings",
}) as any as S.Schema<EndpointSettings>;

export interface ConnectNetworkRequest {
  /** Network ID or name */
  id: string;
  /** The ID or name of the container to connect to the network. */
  Container: string;
  EndpointConfig?: EndpointSettings | null;
}
export const ConnectNetworkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    Container: S.String,
    EndpointConfig: S.optional(S.NullOr(EndpointSettings)),
  }).pipe(T.Http({ method: "POST", uri: "/networks/{id}/connect", code: 200 })),
).annotate({
  identifier: "ConnectNetworkRequest",
}) as any as S.Schema<ConnectNetworkRequest>;

export interface ConnectNetworkResponse {}
export const ConnectNetworkResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ConnectNetworkResponse",
}) as any as S.Schema<ConnectNetworkResponse>;

export interface ContainerAttachWebsocketRequest {
  /** ID or name of the container */
  id: string;
  /** Override the key sequence for detaching a container.Format is a single character `[a-Z]` or `ctrl-<value>` where `<value>` is one of: `a-z`, `@`, `^`, `[`, `,`, or `_`. */
  detachKeys?: string;
  /** Return logs */
  logs?: boolean;
  /** Return stream */
  stream?: boolean;
  /** Attach to `stdin` */
  stdin?: boolean;
  /** Attach to `stdout` */
  stdout?: boolean;
  /** Attach to `stderr` */
  stderr?: boolean;
}
export const ContainerAttachWebsocketRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    detachKeys: S.optional(S.String.pipe(T.Query())),
    logs: S.optional(S.Boolean.pipe(T.Query())),
    stream: S.optional(S.Boolean.pipe(T.Query())),
    stdin: S.optional(S.Boolean.pipe(T.Query())),
    stdout: S.optional(S.Boolean.pipe(T.Query())),
    stderr: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/containers/{id}/attach/ws", code: 200 }),
  ),
).annotate({
  identifier: "ContainerAttachWebsocketRequest",
}) as any as S.Schema<ContainerAttachWebsocketRequest>;

export interface ContainerAttachWebsocketResponse {}
export const ContainerAttachWebsocketResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ContainerAttachWebsocketResponse",
}) as any as S.Schema<ContainerAttachWebsocketResponse>;

export interface ContainerChangesRequest {
  /** ID or name of the container */
  id: string;
}
export const ContainerChangesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/containers/{id}/changes", code: 200 }),
  ),
).annotate({
  identifier: "ContainerChangesRequest",
}) as any as S.Schema<ContainerChangesRequest>;

/** Kind of change Can be one of: - `0`: Modified ("C") - `1`: Added ("A") - `2`: Deleted ("D") */
export type ChangeType = 0 | 1 | 2;
export const ChangeType = S.Number;

/** Change in the container's filesystem. */
export interface FilesystemChange {
  /** Path to file or directory that has changed. */
  Path: string;
  Kind: ChangeType;
}
export const FilesystemChange = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Path: S.String,
    Kind: ChangeType,
  }),
).annotate({
  identifier: "FilesystemChange",
}) as any as S.Schema<FilesystemChange>;

export type ContainerChangesResponseBodyList = Array<FilesystemChange>;
export const ContainerChangesResponseBodyList = /*@__PURE__*/ S.Array(
  FilesystemChange,
) as any as S.Schema<ContainerChangesResponseBodyList>;

export type ContainerChangesResponse = ContainerChangesResponseBodyList;
export const ContainerChangesResponse = /*@__PURE__*/ S.suspend(() =>
  ContainerChangesResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ContainerChangesResponse",
}) as any as S.Schema<ContainerChangesResponse>;

export interface ContainerInspectRequest {
  /** ID or name of the container */
  id: string;
  /** Return the size of container as fields `SizeRw` and `SizeRootFs` */
  size?: boolean;
}
export const ContainerInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    size: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/containers/{id}/json", code: 200 })),
).annotate({
  identifier: "ContainerInspectRequest",
}) as any as S.Schema<ContainerInspectRequest>;

/** The arguments to the command being run */
export type ContainerInspectResponseArgsList = Array<string>;
export const ContainerInspectResponseArgsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerInspectResponseArgsList>;

/** String representation of the container state. Can be one of "created", "running", "paused", "restarting", "removing", "exited", or "dead". */
export type ContainerStateStatus =
  | "created"
  | "running"
  | "paused"
  | "restarting"
  | "removing"
  | "exited"
  | "dead";
export const ContainerStateStatus = S.String;

/** Status is one of `none`, `starting`, `healthy` or `unhealthy` - "none" Indicates there is no healthcheck - "starting" Starting indicates that the container is not yet ready - "healthy" Healthy indicates that the container is running correctly - "unhealthy" Unhealthy indicates that the container has a problem */
export type HealthStatus = "none" | "starting" | "healthy" | "unhealthy";
export const HealthStatus = S.String;

/** HealthcheckResult stores information about a single run of a healthcheck probe */
export interface HealthcheckResult {
  /** Date and time at which this check started in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  Start?: string;
  /** Date and time at which this check ended in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  End?: string;
  /** ExitCode meanings: - `0` healthy - `1` unhealthy - `2` reserved (considered unhealthy) - other values: error running probe */
  ExitCode?: number;
  /** Output from last check */
  Output?: string;
}
export const HealthcheckResult = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Start: S.optional(S.String),
    End: S.optional(S.String),
    ExitCode: S.optional(S.Number),
    Output: S.optional(S.String),
  }),
).annotate({
  identifier: "HealthcheckResult",
}) as any as S.Schema<HealthcheckResult>;

/** Log contains the last few results (oldest first) */
export type HealthLogList = Array<HealthcheckResult>;
export const HealthLogList = /*@__PURE__*/ S.Array(
  HealthcheckResult,
) as any as S.Schema<HealthLogList>;

/** Health stores information about the container's healthcheck results. */
export interface Health {
  /** Status is one of `none`, `starting`, `healthy` or `unhealthy` - "none" Indicates there is no healthcheck - "starting" Starting indicates that the container is not yet ready - "healthy" Healthy indicates that the container is running correctly - "unhealthy" Unhealthy indicates that the container has a problem */
  Status?: HealthStatus;
  /** FailingStreak is the number of consecutive failures */
  FailingStreak?: number;
  /** Log contains the last few results (oldest first) */
  Log?: HealthLogList;
}
export const Health = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Status: S.optional(HealthStatus),
    FailingStreak: S.optional(S.Number),
    Log: S.optional(HealthLogList),
  }),
).annotate({ identifier: "Health" }) as any as S.Schema<Health>;

/** ContainerState stores container's running state. It's part of ContainerJSONBase and will be returned by the "inspect" command. */
export interface ContainerState {
  /** String representation of the container state. Can be one of "created", "running", "paused", "restarting", "removing", "exited", or "dead". */
  Status?: ContainerStateStatus;
  /** Whether this container is running. Note that a running container can be _paused_. The `Running` and `Paused` booleans are not mutually exclusive: When pausing a container (on Linux), the freezer cgroup is used to suspend all processes in the container. Freezing the process requires the process to be running. As a result, paused containers are both `Running` _and_ `Paused`. Use the `Status` field instead to determine if a container's state is "running". */
  Running?: boolean;
  /** Whether this container is paused. */
  Paused?: boolean;
  /** Whether this container is restarting. */
  Restarting?: boolean;
  /** Whether a process within this container has been killed because it ran out of memory since the container was last started. */
  OOMKilled?: boolean;
  Dead?: boolean;
  /** The process ID of this container */
  Pid?: number;
  /** The last exit code of this container */
  ExitCode?: number;
  Error?: string;
  /** The time when this container was last started. */
  StartedAt?: string;
  /** The time when this container last exited. */
  FinishedAt?: string;
  Health?: Health | null;
}
export const ContainerState = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Status: S.optional(ContainerStateStatus),
    Running: S.optional(S.Boolean),
    Paused: S.optional(S.Boolean),
    Restarting: S.optional(S.Boolean),
    OOMKilled: S.optional(S.Boolean),
    Dead: S.optional(S.Boolean),
    Pid: S.optional(S.Number),
    ExitCode: S.optional(S.Number),
    Error: S.optional(S.String),
    StartedAt: S.optional(S.String),
    FinishedAt: S.optional(S.String),
    Health: S.optional(S.NullOr(Health)),
  }),
).annotate({ identifier: "ContainerState" }) as any as S.Schema<ContainerState>;

/** List of URLs from which this object MAY be downloaded. */
export type OCIDescriptorUrlsList = Array<string>;
export const OCIDescriptorUrlsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<OCIDescriptorUrlsList>;

/** Arbitrary metadata relating to the targeted content. */
export type OCIDescriptorAnnotationsMap = { [key: string]: string | undefined };
export const OCIDescriptorAnnotationsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<OCIDescriptorAnnotationsMap>;

/** Optional field specifying an array of strings, each listing a required OS feature (for example on Windows `win32k`). */
export type OCIPlatformOsFeaturesList = Array<string>;
export const OCIPlatformOsFeaturesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<OCIPlatformOsFeaturesList>;

/** Describes the platform which the image in the manifest runs on, as defined in the [OCI Image Index Specification](https://github.com/opencontainers/image-spec/blob/v1.0.1/image-index.md). */
export interface OCIPlatform {
  /** The CPU architecture, for example `amd64` or `ppc64`. */
  architecture?: string;
  /** The operating system, for example `linux` or `windows`. */
  os?: string;
  /** Optional field specifying the operating system version, for example on Windows `10.0.19041.1165`. */
  os_version?: string;
  /** Optional field specifying an array of strings, each listing a required OS feature (for example on Windows `win32k`). */
  os_features?: OCIPlatformOsFeaturesList;
  /** Optional field specifying a variant of the CPU, for example `v7` to specify ARMv7 when architecture is `arm`. */
  variant?: string;
}
export const OCIPlatform = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    architecture: S.optional(S.String),
    os: S.optional(S.String),
    os_version: S.optional(S.String.pipe(T.Body("os.version"))),
    os_features: S.optional(
      OCIPlatformOsFeaturesList.pipe(T.Body("os.features")),
    ),
    variant: S.optional(S.String),
  }),
).annotate({ identifier: "OCIPlatform" }) as any as S.Schema<OCIPlatform>;

/** A descriptor struct containing digest, media type, and size, as defined in the [OCI Content Descriptors Specification](https://github.com/opencontainers/image-spec/blob/v1.0.1/descriptor.md). */
export interface OCIDescriptor {
  /** The media type of the object this schema refers to. */
  mediaType?: string;
  /** The digest of the targeted content. */
  digest?: string;
  /** The size in bytes of the blob. */
  size?: number;
  /** List of URLs from which this object MAY be downloaded. */
  urls?: OCIDescriptorUrlsList | null;
  /** Arbitrary metadata relating to the targeted content. */
  annotations?: OCIDescriptorAnnotationsMap | null;
  /** Data is an embedding of the targeted content. This is encoded as a base64 string when marshalled to JSON (automatically, by encoding/json). If present, Data can be used directly to avoid fetching the targeted content. */
  data?: string | null;
  platform?: OCIPlatform | null;
  /** ArtifactType is the IANA media type of this artifact. */
  artifactType?: string | null;
}
export const OCIDescriptor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    mediaType: S.optional(S.String),
    digest: S.optional(S.String),
    size: S.optional(S.Number),
    urls: S.optional(S.NullOr(OCIDescriptorUrlsList)),
    annotations: S.optional(S.NullOr(OCIDescriptorAnnotationsMap)),
    data: S.optional(S.NullOr(S.String)),
    platform: S.optional(S.NullOr(OCIPlatform)),
    artifactType: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "OCIDescriptor" }) as any as S.Schema<OCIDescriptor>;

/** IDs of exec instances that are running in the container. */
export type ContainerInspectResponseExecIDsList = Array<string>;
export const ContainerInspectResponseExecIDsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerInspectResponseExecIDsList>;

export interface HostConfigBlkioWeightDeviceItem {
  Path?: string;
  Weight?: number;
}
export const HostConfigBlkioWeightDeviceItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Path: S.optional(S.String),
    Weight: S.optional(S.Number),
  }),
).annotate({
  identifier: "HostConfigBlkioWeightDeviceItem",
}) as any as S.Schema<HostConfigBlkioWeightDeviceItem>;

/** Block IO weight (relative device weight) in the form: ``` [{"Path": "device_path", "Weight": weight}] ``` */
export type HostConfigBlkioWeightDeviceList =
  Array<HostConfigBlkioWeightDeviceItem>;
export const HostConfigBlkioWeightDeviceList = /*@__PURE__*/ S.Array(
  HostConfigBlkioWeightDeviceItem,
) as any as S.Schema<HostConfigBlkioWeightDeviceList>;

export interface ThrottleDevice {
  /** Device path */
  Path?: string;
  /** Rate */
  Rate?: number;
}
export const ThrottleDevice = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Path: S.optional(S.String),
    Rate: S.optional(S.Number),
  }),
).annotate({ identifier: "ThrottleDevice" }) as any as S.Schema<ThrottleDevice>;

/** Limit read rate (bytes per second) from a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
export type HostConfigBlkioDeviceReadBpsList = Array<ThrottleDevice>;
export const HostConfigBlkioDeviceReadBpsList = /*@__PURE__*/ S.Array(
  ThrottleDevice,
) as any as S.Schema<HostConfigBlkioDeviceReadBpsList>;

/** Limit write rate (bytes per second) to a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
export type HostConfigBlkioDeviceWriteBpsList = Array<ThrottleDevice>;
export const HostConfigBlkioDeviceWriteBpsList = /*@__PURE__*/ S.Array(
  ThrottleDevice,
) as any as S.Schema<HostConfigBlkioDeviceWriteBpsList>;

/** Limit read rate (IO per second) from a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
export type HostConfigBlkioDeviceReadIOpsList = Array<ThrottleDevice>;
export const HostConfigBlkioDeviceReadIOpsList = /*@__PURE__*/ S.Array(
  ThrottleDevice,
) as any as S.Schema<HostConfigBlkioDeviceReadIOpsList>;

/** Limit write rate (IO per second) to a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
export type HostConfigBlkioDeviceWriteIOpsList = Array<ThrottleDevice>;
export const HostConfigBlkioDeviceWriteIOpsList = /*@__PURE__*/ S.Array(
  ThrottleDevice,
) as any as S.Schema<HostConfigBlkioDeviceWriteIOpsList>;

/** A device mapping between the host and container */
export interface DeviceMapping {
  PathOnHost?: string;
  PathInContainer?: string;
  CgroupPermissions?: string;
}
export const DeviceMapping = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    PathOnHost: S.optional(S.String),
    PathInContainer: S.optional(S.String),
    CgroupPermissions: S.optional(S.String),
  }),
).annotate({ identifier: "DeviceMapping" }) as any as S.Schema<DeviceMapping>;

/** A list of devices to add to the container. */
export type HostConfigDevicesList = Array<DeviceMapping>;
export const HostConfigDevicesList = /*@__PURE__*/ S.Array(
  DeviceMapping,
) as any as S.Schema<HostConfigDevicesList>;

/** a list of cgroup rules to apply to the container */
export type HostConfigDeviceCgroupRulesList = Array<string>;
export const HostConfigDeviceCgroupRulesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigDeviceCgroupRulesList>;

export type DeviceRequestDeviceIDsList = Array<string>;
export const DeviceRequestDeviceIDsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeviceRequestDeviceIDsList>;

export type DeviceRequestCapabilitiesItemList = Array<string>;
export const DeviceRequestCapabilitiesItemList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeviceRequestCapabilitiesItemList>;

/** A list of capabilities; an OR list of AND lists of capabilities. Note that if a driver is specified the capabilities have no effect on selecting a driver as the driver name is used directly. Note that if no driver is specified the capabilities are used to select a driver with the required capabilities. */
export type DeviceRequestCapabilitiesList =
  Array<DeviceRequestCapabilitiesItemList>;
export const DeviceRequestCapabilitiesList = /*@__PURE__*/ S.Array(
  DeviceRequestCapabilitiesItemList,
) as any as S.Schema<DeviceRequestCapabilitiesList>;

/** Driver-specific options, specified as a key/value pairs. These options are passed directly to the driver. */
export type DeviceRequestOptionsMap = { [key: string]: string | undefined };
export const DeviceRequestOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<DeviceRequestOptionsMap>;

/** A request for devices to be sent to device drivers */
export interface DeviceRequest {
  /** The name of the device driver to use for this request. Note that if this is specified the capabilities are ignored when selecting a device driver. */
  Driver?: string;
  Count?: number;
  DeviceIDs?: DeviceRequestDeviceIDsList;
  /** A list of capabilities; an OR list of AND lists of capabilities. Note that if a driver is specified the capabilities have no effect on selecting a driver as the driver name is used directly. Note that if no driver is specified the capabilities are used to select a driver with the required capabilities. */
  Capabilities?: DeviceRequestCapabilitiesList;
  /** Driver-specific options, specified as a key/value pairs. These options are passed directly to the driver. */
  Options?: DeviceRequestOptionsMap;
}
export const DeviceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Driver: S.optional(S.String),
    Count: S.optional(S.Number),
    DeviceIDs: S.optional(DeviceRequestDeviceIDsList),
    Capabilities: S.optional(DeviceRequestCapabilitiesList),
    Options: S.optional(DeviceRequestOptionsMap),
  }),
).annotate({ identifier: "DeviceRequest" }) as any as S.Schema<DeviceRequest>;

/** A list of requests for devices to be sent to device drivers. */
export type HostConfigDeviceRequestsList = Array<DeviceRequest>;
export const HostConfigDeviceRequestsList = /*@__PURE__*/ S.Array(
  DeviceRequest,
) as any as S.Schema<HostConfigDeviceRequestsList>;

export interface HostConfigUlimitsItem {
  /** Name of ulimit */
  Name?: string;
  /** Soft limit */
  Soft?: number;
  /** Hard limit */
  Hard?: number;
}
export const HostConfigUlimitsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Soft: S.optional(S.Number),
    Hard: S.optional(S.Number),
  }),
).annotate({
  identifier: "HostConfigUlimitsItem",
}) as any as S.Schema<HostConfigUlimitsItem>;

/** A list of resource limits to set in the container. For example: ``` {"Name": "nofile", "Soft": 1024, "Hard": 2048} ``` */
export type HostConfigUlimitsList = Array<HostConfigUlimitsItem>;
export const HostConfigUlimitsList = /*@__PURE__*/ S.Array(
  HostConfigUlimitsItem,
) as any as S.Schema<HostConfigUlimitsList>;

/** A list of volume bindings for this container. Each volume binding is a string in one of these forms: - `host-src:container-dest[:options]` to bind-mount a host path into the container. Both `host-src`, and `container-dest` must be an _absolute_ path. - `volume-name:container-dest[:options]` to bind-mount a volume managed by a volume driver into the container. `container-dest` must be an _absolute_ path. `options` is an optional, comma-delimited list of: - `nocopy` disables automatic copying of data from the container path to the volume. The `nocopy` flag only applies to named volumes. - `[ro|rw]` mounts a volume read-only or read-write, respectively. If omitted or set to `rw`, volumes are mounted read-write. - `[z|Z]` applies SELinux labels to allow or deny multiple containers to read and write to the same volume. - `z`: a _shared_ content label is applied to the content. This label indicates that multiple containers can share the volume content, for both reading and writing. - `Z`: a _private unshared_ label is applied to the content. This label indicates that only the current container can use a private volume. Labeling systems such as SELinux require proper labels to be placed on volume content that is mounted into a container. Without a label, the security system can prevent a container's processes from using the content. By default, the labels set by the host operating system are not modified. - `[[r]shared|[r]slave|[r]private]` specifies mount [propagation behavior](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt). This only applies to bind-mounted volumes, not internal volumes or named volumes. Mount propagation requires the source mount point (the location where the source directory is mounted in the host operating system) to have the correct propagation properties. For shared volumes, the source mount point must be set to `shared`. For slave volumes, the mount must be set to either `shared` or `slave`. */
export type HostConfigBindsList = Array<string>;
export const HostConfigBindsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigBindsList>;

/** Name of the logging driver used for the container or "none" if logging is disabled. */
export type HostConfigLogConfigType =
  | "local"
  | "json-file"
  | "syslog"
  | "journald"
  | "gelf"
  | "fluentd"
  | "awslogs"
  | "splunk"
  | "etwlogs"
  | "none";
export const HostConfigLogConfigType = S.String;

/** Driver-specific configuration options for the logging driver. */
export type HostConfigLogConfigConfigMap = {
  [key: string]: string | undefined;
};
export const HostConfigLogConfigConfigMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<HostConfigLogConfigConfigMap>;

/** The logging configuration for this container */
export interface HostConfigLogConfig {
  /** Name of the logging driver used for the container or "none" if logging is disabled. */
  Type?: HostConfigLogConfigType | (string & {});
  /** Driver-specific configuration options for the logging driver. */
  Config?: HostConfigLogConfigConfigMap;
}
export const HostConfigLogConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Type: S.optional(HostConfigLogConfigType),
    Config: S.optional(HostConfigLogConfigConfigMap),
  }),
).annotate({
  identifier: "HostConfigLogConfig",
}) as any as S.Schema<HostConfigLogConfig>;

/** PortBinding represents a binding between a host IP address and a host port. */
export interface PortBinding {
  /** Host IP address that the container's port is mapped to. */
  HostIp?: string;
  /** Host port number that the container's port is mapped to. */
  HostPort?: string;
}
export const PortBinding = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    HostIp: S.optional(S.String),
    HostPort: S.optional(S.String),
  }),
).annotate({ identifier: "PortBinding" }) as any as S.Schema<PortBinding>;

export type PortMapValueList = Array<PortBinding>;
export const PortMapValueList = /*@__PURE__*/ S.Array(
  PortBinding,
) as any as S.Schema<PortMapValueList>;

/** PortMap describes the mapping of container ports to host ports, using the container's port-number and protocol as key in the format `<port>/<protocol>`, for example, `80/udp`. If a container's port is mapped for multiple protocols, separate entries are added to the mapping table. */
export type PortMap = { [key: string]: PortMapValueList | undefined };
export const PortMap = /*@__PURE__*/ S.Record(
  S.String,
  PortMapValueList,
) as any as S.Schema<PortMap>;

/** - Empty string means not to restart - `no` Do not automatically restart - `always` Always restart - `unless-stopped` Restart always except when the user has manually stopped the container - `on-failure` Restart only when the container exit code is non-zero */
export type RestartPolicyName =
  | ""
  | "no"
  | "always"
  | "unless-stopped"
  | "on-failure";
export const RestartPolicyName = S.String;

/** The behavior to apply when the container exits. The default is not to restart. An ever increasing delay (double the previous delay, starting at 100ms) is added before each restart to prevent flooding the server. */
export interface RestartPolicy {
  /** - Empty string means not to restart - `no` Do not automatically restart - `always` Always restart - `unless-stopped` Restart always except when the user has manually stopped the container - `on-failure` Restart only when the container exit code is non-zero */
  Name?: RestartPolicyName | (string & {});
  /** If `on-failure` is used, the number of times to retry before giving up. */
  MaximumRetryCount?: number;
}
export const RestartPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(RestartPolicyName),
    MaximumRetryCount: S.optional(S.Number),
  }),
).annotate({ identifier: "RestartPolicy" }) as any as S.Schema<RestartPolicy>;

/** A list of volumes to inherit from another container, specified in the form `<container name>[:<ro|rw>]`. */
export type HostConfigVolumesFromList = Array<string>;
export const HostConfigVolumesFromList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigVolumesFromList>;

/** The mount type. Available types: - `bind` a mount of a file or directory from the host into the container. - `cluster` a Swarm cluster volume. - `image` an OCI image. - `npipe` a named pipe from the host into the container. - `tmpfs` a `tmpfs`. - `volume` a docker volume with the given `Name`. */
export type MountType =
  | "bind"
  | "cluster"
  | "image"
  | "npipe"
  | "tmpfs"
  | "volume";
export const MountType = S.String;

/** A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`. */
export type MountBindOptionsPropagation =
  | "private"
  | "rprivate"
  | "shared"
  | "rshared"
  | "slave"
  | "rslave";
export const MountBindOptionsPropagation = S.String;

/** Optional configuration for the `bind` type. */
export interface MountBindOptions {
  /** A propagation mode with the value `[r]private`, `[r]shared`, or `[r]slave`. */
  Propagation?: MountBindOptionsPropagation | (string & {});
  /** Disable recursive bind mount. */
  NonRecursive?: boolean;
  /** Create mount point on host if missing */
  CreateMountpoint?: boolean;
  /** Make the mount non-recursively read-only, but still leave the mount recursive (unless NonRecursive is set to `true` in conjunction). Added in v1.44, before that version all read-only mounts were non-recursive by default. To match the previous behaviour this will default to `true` for clients on versions prior to v1.44. */
  ReadOnlyNonRecursive?: boolean;
  /** Raise an error if the mount cannot be made recursively read-only. */
  ReadOnlyForceRecursive?: boolean;
}
export const MountBindOptions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Propagation: S.optional(MountBindOptionsPropagation),
    NonRecursive: S.optional(S.Boolean),
    CreateMountpoint: S.optional(S.Boolean),
    ReadOnlyNonRecursive: S.optional(S.Boolean),
    ReadOnlyForceRecursive: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "MountBindOptions",
}) as any as S.Schema<MountBindOptions>;

/** User-defined key/value metadata. */
export type MountVolumeOptionsLabelsMap = { [key: string]: string | undefined };
export const MountVolumeOptionsLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<MountVolumeOptionsLabelsMap>;

/** key/value map of driver specific options. */
export type MountVolumeOptionsDriverConfigOptionsMap = {
  [key: string]: string | undefined;
};
export const MountVolumeOptionsDriverConfigOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<MountVolumeOptionsDriverConfigOptionsMap>;

/** Map of driver specific options */
export interface MountVolumeOptionsDriverConfig {
  /** Name of the driver to use to create the volume. */
  Name?: string;
  /** key/value map of driver specific options. */
  Options?: MountVolumeOptionsDriverConfigOptionsMap;
}
export const MountVolumeOptionsDriverConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Options: S.optional(MountVolumeOptionsDriverConfigOptionsMap),
  }),
).annotate({
  identifier: "MountVolumeOptionsDriverConfig",
}) as any as S.Schema<MountVolumeOptionsDriverConfig>;

/** Optional configuration for the `volume` type. */
export interface MountVolumeOptions {
  /** Populate volume with data from the target. */
  NoCopy?: boolean;
  /** User-defined key/value metadata. */
  Labels?: MountVolumeOptionsLabelsMap;
  /** Map of driver specific options */
  DriverConfig?: MountVolumeOptionsDriverConfig;
  /** Source path inside the volume. Must be relative without any back traversals. */
  Subpath?: string;
}
export const MountVolumeOptions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NoCopy: S.optional(S.Boolean),
    Labels: S.optional(MountVolumeOptionsLabelsMap),
    DriverConfig: S.optional(MountVolumeOptionsDriverConfig),
    Subpath: S.optional(S.String),
  }),
).annotate({
  identifier: "MountVolumeOptions",
}) as any as S.Schema<MountVolumeOptions>;

/** Optional configuration for the `image` type. */
export interface MountImageOptions {
  /** Source path inside the image. Must be relative without any back traversals. */
  Subpath?: string;
}
export const MountImageOptions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Subpath: S.optional(S.String),
  }),
).annotate({
  identifier: "MountImageOptions",
}) as any as S.Schema<MountImageOptions>;

export type MountTmpfsOptionsOptionsItemList = Array<string>;
export const MountTmpfsOptionsOptionsItemList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<MountTmpfsOptionsOptionsItemList>;

/** The options to be passed to the tmpfs mount. An array of arrays. Flag options should be provided as 1-length arrays. Other types should be provided as as 2-length arrays, where the first item is the key and the second the value. */
export type MountTmpfsOptionsOptionsList =
  Array<MountTmpfsOptionsOptionsItemList>;
export const MountTmpfsOptionsOptionsList = /*@__PURE__*/ S.Array(
  MountTmpfsOptionsOptionsItemList,
) as any as S.Schema<MountTmpfsOptionsOptionsList>;

/** Optional configuration for the `tmpfs` type. */
export interface MountTmpfsOptions {
  /** The size for the tmpfs mount in bytes. */
  SizeBytes?: number;
  /** The permission mode for the tmpfs mount in an integer. The value must not be in octal format (e.g. 755) but rather the decimal representation of the octal value (e.g. 493). */
  Mode?: number;
  /** The options to be passed to the tmpfs mount. An array of arrays. Flag options should be provided as 1-length arrays. Other types should be provided as as 2-length arrays, where the first item is the key and the second the value. */
  Options?: MountTmpfsOptionsOptionsList;
}
export const MountTmpfsOptions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    SizeBytes: S.optional(S.Number),
    Mode: S.optional(S.Number),
    Options: S.optional(MountTmpfsOptionsOptionsList),
  }),
).annotate({
  identifier: "MountTmpfsOptions",
}) as any as S.Schema<MountTmpfsOptions>;

export interface Mount {
  /** Container path. */
  Target?: string;
  /** Mount source (e.g. a volume name, a host path). The source cannot be specified when using `Type=tmpfs`. For `Type=bind`, the source path must either exist, or the `CreateMountpoint` must be set to `true` to create the source path on the host if missing. For `Type=npipe`, the pipe must exist prior to creating the container. */
  Source?: string;
  /** The mount type. Available types: - `bind` Mounts a file or directory from the host into the container. The `Source` must exist prior to creating the container. - `cluster` a Swarm cluster volume - `image` Mounts an image. - `npipe` Mounts a named pipe from the host into the container. The `Source` must exist prior to creating the container. - `tmpfs` Create a tmpfs with the given options. The mount `Source` cannot be specified for tmpfs. - `volume` Creates a volume with the given name and options (or uses a pre-existing volume with the same name and options). These are **not** removed when the container is removed. */
  Type?: MountType | (string & {});
  /** Whether the mount should be read-only. */
  ReadOnly?: boolean;
  /** The consistency requirement for the mount: `default`, `consistent`, `cached`, or `delegated`. */
  Consistency?: string;
  /** Optional configuration for the `bind` type. */
  BindOptions?: MountBindOptions;
  /** Optional configuration for the `volume` type. */
  VolumeOptions?: MountVolumeOptions;
  /** Optional configuration for the `image` type. */
  ImageOptions?: MountImageOptions;
  /** Optional configuration for the `tmpfs` type. */
  TmpfsOptions?: MountTmpfsOptions;
}
export const Mount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Target: S.optional(S.String),
    Source: S.optional(S.String),
    Type: S.optional(MountType),
    ReadOnly: S.optional(S.Boolean),
    Consistency: S.optional(S.String),
    BindOptions: S.optional(MountBindOptions),
    VolumeOptions: S.optional(MountVolumeOptions),
    ImageOptions: S.optional(MountImageOptions),
    TmpfsOptions: S.optional(MountTmpfsOptions),
  }),
).annotate({ identifier: "Mount" }) as any as S.Schema<Mount>;

/** Specification for mounts to be added to the container. */
export type HostConfigMountsList = Array<Mount>;
export const HostConfigMountsList = /*@__PURE__*/ S.Array(
  Mount,
) as any as S.Schema<HostConfigMountsList>;

/** Initial console size, as an `[height, width]` array. */
export type HostConfigConsoleSizeList = Array<number>;
export const HostConfigConsoleSizeList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<HostConfigConsoleSizeList>;

/** Arbitrary non-identifying metadata attached to container and provided to the runtime when the container is started. */
export type HostConfigAnnotationsMap = { [key: string]: string | undefined };
export const HostConfigAnnotationsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<HostConfigAnnotationsMap>;

/** A list of kernel capabilities to add to the container. Conflicts with option 'Capabilities'. */
export type HostConfigCapAddList = Array<string>;
export const HostConfigCapAddList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigCapAddList>;

/** A list of kernel capabilities to drop from the container. Conflicts with option 'Capabilities'. */
export type HostConfigCapDropList = Array<string>;
export const HostConfigCapDropList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigCapDropList>;

/** cgroup namespace mode for the container. Possible values are: - `"private"`: the container runs in its own private cgroup namespace - `"host"`: use the host system's cgroup namespace If not specified, the daemon default is used, which can either be `"private"` or `"host"`, depending on daemon version, kernel support and configuration. */
export type HostConfigCgroupnsMode = "private" | "host";
export const HostConfigCgroupnsMode = S.String;

/** A list of DNS servers for the container to use. */
export type HostConfigDnsList = Array<string>;
export const HostConfigDnsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigDnsList>;

/** A list of DNS options. */
export type HostConfigDnsOptionsList = Array<string>;
export const HostConfigDnsOptionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigDnsOptionsList>;

/** A list of DNS search domains. */
export type HostConfigDnsSearchList = Array<string>;
export const HostConfigDnsSearchList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigDnsSearchList>;

/** A list of hostnames/IP mappings to add to the container's `/etc/hosts` file. Specified in the form `["hostname:IP"]`. */
export type HostConfigExtraHostsList = Array<string>;
export const HostConfigExtraHostsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigExtraHostsList>;

/** A list of additional groups that the container process will run as. */
export type HostConfigGroupAddList = Array<string>;
export const HostConfigGroupAddList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigGroupAddList>;

/** A list of links for the container in the form `container_name:alias`. */
export type HostConfigLinksList = Array<string>;
export const HostConfigLinksList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigLinksList>;

/** A list of string values to customize labels for MLS systems, such as SELinux. */
export type HostConfigSecurityOptList = Array<string>;
export const HostConfigSecurityOptList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigSecurityOptList>;

/** Storage driver options for this container, in the form `{"size": "120G"}`. */
export type HostConfigStorageOptMap = { [key: string]: string | undefined };
export const HostConfigStorageOptMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<HostConfigStorageOptMap>;

/** A map of container directories which should be replaced by tmpfs mounts, and their corresponding mount options. For example: ``` { "/run": "rw,noexec,nosuid,size=65536k" } ``` */
export type HostConfigTmpfsMap = { [key: string]: string | undefined };
export const HostConfigTmpfsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<HostConfigTmpfsMap>;

/** A list of kernel parameters (sysctls) to set in the container. This field is omitted if not set. */
export type HostConfigSysctlsMap = { [key: string]: string | undefined };
export const HostConfigSysctlsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<HostConfigSysctlsMap>;

/** Isolation technology of the container. (Windows only) */
export type HostConfigIsolation = "default" | "process" | "hyperv" | "";
export const HostConfigIsolation = S.String;

/** The list of paths to be masked inside the container (this overrides the default set of paths). */
export type HostConfigMaskedPathsList = Array<string>;
export const HostConfigMaskedPathsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigMaskedPathsList>;

/** The list of paths to be set as read-only inside the container (this overrides the default set of paths). */
export type HostConfigReadonlyPathsList = Array<string>;
export const HostConfigReadonlyPathsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HostConfigReadonlyPathsList>;

/** Container configuration that depends on the host we are running on */
export interface HostConfig {
  /** An integer value representing this container's relative CPU weight versus other containers. */
  CpuShares?: number;
  /** Memory limit in bytes. */
  Memory?: number;
  /** Path to `cgroups` under which the container's `cgroup` is created. If the path is not absolute, the path is considered to be relative to the `cgroups` path of the init process. Cgroups are created if they do not already exist. */
  CgroupParent?: string;
  /** Block IO weight (relative weight). */
  BlkioWeight?: number;
  /** Block IO weight (relative device weight) in the form: ``` [{"Path": "device_path", "Weight": weight}] ``` */
  BlkioWeightDevice?: HostConfigBlkioWeightDeviceList;
  /** Limit read rate (bytes per second) from a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
  BlkioDeviceReadBps?: HostConfigBlkioDeviceReadBpsList;
  /** Limit write rate (bytes per second) to a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
  BlkioDeviceWriteBps?: HostConfigBlkioDeviceWriteBpsList;
  /** Limit read rate (IO per second) from a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
  BlkioDeviceReadIOps?: HostConfigBlkioDeviceReadIOpsList;
  /** Limit write rate (IO per second) to a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
  BlkioDeviceWriteIOps?: HostConfigBlkioDeviceWriteIOpsList;
  /** The length of a CPU period in microseconds. */
  CpuPeriod?: number;
  /** Microseconds of CPU time that the container can get in a CPU period. */
  CpuQuota?: number;
  /** The length of a CPU real-time period in microseconds. Set to 0 to allocate no time allocated to real-time tasks. */
  CpuRealtimePeriod?: number;
  /** The length of a CPU real-time runtime in microseconds. Set to 0 to allocate no time allocated to real-time tasks. */
  CpuRealtimeRuntime?: number;
  /** CPUs in which to allow execution (e.g., `0-3`, `0,1`). */
  CpusetCpus?: string;
  /** Memory nodes (MEMs) in which to allow execution (0-3, 0,1). Only effective on NUMA systems. */
  CpusetMems?: string;
  /** A list of devices to add to the container. */
  Devices?: HostConfigDevicesList;
  /** a list of cgroup rules to apply to the container */
  DeviceCgroupRules?: HostConfigDeviceCgroupRulesList;
  /** A list of requests for devices to be sent to device drivers. */
  DeviceRequests?: HostConfigDeviceRequestsList;
  /** Memory soft limit in bytes. */
  MemoryReservation?: number;
  /** Total memory limit (memory + swap). Set as `-1` to enable unlimited swap. */
  MemorySwap?: number;
  /** Tune a container's memory swappiness behavior. Accepts an integer between 0 and 100. */
  MemorySwappiness?: number;
  /** CPU quota in units of 10<sup>-9</sup> CPUs. */
  NanoCpus?: number;
  /** Disable OOM Killer for the container. */
  OomKillDisable?: boolean;
  /** Run an init inside the container that forwards signals and reaps processes. This field is omitted if empty, and the default (as configured on the daemon) is used. */
  Init?: boolean | null;
  /** Tune a container's PIDs limit. Set `0` or `-1` for unlimited, or `null` to not change. */
  PidsLimit?: number | null;
  /** A list of resource limits to set in the container. For example: ``` {"Name": "nofile", "Soft": 1024, "Hard": 2048} ``` */
  Ulimits?: HostConfigUlimitsList;
  /** The number of usable CPUs (Windows only). On Windows Server containers, the processor resource controls are mutually exclusive. The order of precedence is `CPUCount` first, then `CPUShares`, and `CPUPercent` last. */
  CpuCount?: number;
  /** The usable percentage of the available CPUs (Windows only). On Windows Server containers, the processor resource controls are mutually exclusive. The order of precedence is `CPUCount` first, then `CPUShares`, and `CPUPercent` last. */
  CpuPercent?: number;
  /** Maximum IOps for the container system drive (Windows only) */
  IOMaximumIOps?: number;
  /** Maximum IO in bytes per second for the container system drive (Windows only). */
  IOMaximumBandwidth?: number;
  /** A list of volume bindings for this container. Each volume binding is a string in one of these forms: - `host-src:container-dest[:options]` to bind-mount a host path into the container. Both `host-src`, and `container-dest` must be an _absolute_ path. - `volume-name:container-dest[:options]` to bind-mount a volume managed by a volume driver into the container. `container-dest` must be an _absolute_ path. `options` is an optional, comma-delimited list of: - `nocopy` disables automatic copying of data from the container path to the volume. The `nocopy` flag only applies to named volumes. - `[ro|rw]` mounts a volume read-only or read-write, respectively. If omitted or set to `rw`, volumes are mounted read-write. - `[z|Z]` applies SELinux labels to allow or deny multiple containers to read and write to the same volume. - `z`: a _shared_ content label is applied to the content. This label indicates that multiple containers can share the volume content, for both reading and writing. - `Z`: a _private unshared_ label is applied to the content. This label indicates that only the current container can use a private volume. Labeling systems such as SELinux require proper labels to be placed on volume content that is mounted into a container. Without a label, the security system can prevent a container's processes from using the content. By default, the labels set by the host operating system are not modified. - `[[r]shared|[r]slave|[r]private]` specifies mount [propagation behavior](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt). This only applies to bind-mounted volumes, not internal volumes or named volumes. Mount propagation requires the source mount point (the location where the source directory is mounted in the host operating system) to have the correct propagation properties. For shared volumes, the source mount point must be set to `shared`. For slave volumes, the mount must be set to either `shared` or `slave`. */
  Binds?: HostConfigBindsList;
  /** Path to a file where the container ID is written */
  ContainerIDFile?: string;
  /** The logging configuration for this container */
  LogConfig?: HostConfigLogConfig;
  /** Network mode to use for this container. Supported standard values are: `bridge`, `host`, `none`, and `container:<name|id>`. Any other value is taken as a custom network's name to which this container should connect to. */
  NetworkMode?: string;
  PortBindings?: PortMap;
  RestartPolicy?: RestartPolicy;
  /** Automatically remove the container when the container's process exits. This has no effect if `RestartPolicy` is set. */
  AutoRemove?: boolean;
  /** Driver that this container uses to mount volumes. */
  VolumeDriver?: string;
  /** A list of volumes to inherit from another container, specified in the form `<container name>[:<ro|rw>]`. */
  VolumesFrom?: HostConfigVolumesFromList;
  /** Specification for mounts to be added to the container. */
  Mounts?: HostConfigMountsList;
  /** Initial console size, as an `[height, width]` array. */
  ConsoleSize?: HostConfigConsoleSizeList | null;
  /** Arbitrary non-identifying metadata attached to container and provided to the runtime when the container is started. */
  Annotations?: HostConfigAnnotationsMap;
  /** A list of kernel capabilities to add to the container. Conflicts with option 'Capabilities'. */
  CapAdd?: HostConfigCapAddList;
  /** A list of kernel capabilities to drop from the container. Conflicts with option 'Capabilities'. */
  CapDrop?: HostConfigCapDropList;
  /** cgroup namespace mode for the container. Possible values are: - `"private"`: the container runs in its own private cgroup namespace - `"host"`: use the host system's cgroup namespace If not specified, the daemon default is used, which can either be `"private"` or `"host"`, depending on daemon version, kernel support and configuration. */
  CgroupnsMode?: HostConfigCgroupnsMode | (string & {});
  /** A list of DNS servers for the container to use. */
  Dns?: HostConfigDnsList;
  /** A list of DNS options. */
  DnsOptions?: HostConfigDnsOptionsList;
  /** A list of DNS search domains. */
  DnsSearch?: HostConfigDnsSearchList;
  /** A list of hostnames/IP mappings to add to the container's `/etc/hosts` file. Specified in the form `["hostname:IP"]`. */
  ExtraHosts?: HostConfigExtraHostsList;
  /** A list of additional groups that the container process will run as. */
  GroupAdd?: HostConfigGroupAddList;
  /** IPC sharing mode for the container. Possible values are: - `"none"`: own private IPC namespace, with /dev/shm not mounted - `"private"`: own private IPC namespace - `"shareable"`: own private IPC namespace, with a possibility to share it with other containers - `"container:<name|id>"`: join another (shareable) container's IPC namespace - `"host"`: use the host system's IPC namespace If not specified, daemon default is used, which can either be `"private"` or `"shareable"`, depending on daemon version and configuration. */
  IpcMode?: string;
  /** Cgroup to use for the container. */
  Cgroup?: string;
  /** A list of links for the container in the form `container_name:alias`. */
  Links?: HostConfigLinksList;
  /** An integer value containing the score given to the container in order to tune OOM killer preferences. */
  OomScoreAdj?: number;
  /** Set the PID (Process) Namespace mode for the container. It can be either: - `"container:<name|id>"`: joins another container's PID namespace - `"host"`: use the host's PID namespace inside the container */
  PidMode?: string;
  /** Gives the container full access to the host. */
  Privileged?: boolean;
  /** Allocates an ephemeral host port for all of a container's exposed ports. Ports are de-allocated when the container stops and allocated when the container starts. The allocated port might be changed when restarting the container. The port is selected from the ephemeral port range that depends on the kernel. For example, on Linux the range is defined by `/proc/sys/net/ipv4/ip_local_port_range`. */
  PublishAllPorts?: boolean;
  /** Mount the container's root filesystem as read only. */
  ReadonlyRootfs?: boolean;
  /** A list of string values to customize labels for MLS systems, such as SELinux. */
  SecurityOpt?: HostConfigSecurityOptList;
  /** Storage driver options for this container, in the form `{"size": "120G"}`. */
  StorageOpt?: HostConfigStorageOptMap;
  /** A map of container directories which should be replaced by tmpfs mounts, and their corresponding mount options. For example: ``` { "/run": "rw,noexec,nosuid,size=65536k" } ``` */
  Tmpfs?: HostConfigTmpfsMap;
  /** UTS namespace to use for the container. */
  UTSMode?: string;
  /** Sets the usernamespace mode for the container when usernamespace remapping option is enabled. */
  UsernsMode?: string;
  /** Size of `/dev/shm` in bytes. If omitted, the system uses 64MB. */
  ShmSize?: number;
  /** A list of kernel parameters (sysctls) to set in the container. This field is omitted if not set. */
  Sysctls?: HostConfigSysctlsMap | null;
  /** Runtime to use with this container. */
  Runtime?: string | null;
  /** Isolation technology of the container. (Windows only) */
  Isolation?: HostConfigIsolation | (string & {});
  /** The list of paths to be masked inside the container (this overrides the default set of paths). */
  MaskedPaths?: HostConfigMaskedPathsList;
  /** The list of paths to be set as read-only inside the container (this overrides the default set of paths). */
  ReadonlyPaths?: HostConfigReadonlyPathsList;
}
export const HostConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    CpuShares: S.optional(S.Number),
    Memory: S.optional(S.Number),
    CgroupParent: S.optional(S.String),
    BlkioWeight: S.optional(S.Number),
    BlkioWeightDevice: S.optional(HostConfigBlkioWeightDeviceList),
    BlkioDeviceReadBps: S.optional(HostConfigBlkioDeviceReadBpsList),
    BlkioDeviceWriteBps: S.optional(HostConfigBlkioDeviceWriteBpsList),
    BlkioDeviceReadIOps: S.optional(HostConfigBlkioDeviceReadIOpsList),
    BlkioDeviceWriteIOps: S.optional(HostConfigBlkioDeviceWriteIOpsList),
    CpuPeriod: S.optional(S.Number),
    CpuQuota: S.optional(S.Number),
    CpuRealtimePeriod: S.optional(S.Number),
    CpuRealtimeRuntime: S.optional(S.Number),
    CpusetCpus: S.optional(S.String),
    CpusetMems: S.optional(S.String),
    Devices: S.optional(HostConfigDevicesList),
    DeviceCgroupRules: S.optional(HostConfigDeviceCgroupRulesList),
    DeviceRequests: S.optional(HostConfigDeviceRequestsList),
    MemoryReservation: S.optional(S.Number),
    MemorySwap: S.optional(S.Number),
    MemorySwappiness: S.optional(S.Number),
    NanoCpus: S.optional(S.Number),
    OomKillDisable: S.optional(S.Boolean),
    Init: S.optional(S.NullOr(S.Boolean)),
    PidsLimit: S.optional(S.NullOr(S.Number)),
    Ulimits: S.optional(HostConfigUlimitsList),
    CpuCount: S.optional(S.Number),
    CpuPercent: S.optional(S.Number),
    IOMaximumIOps: S.optional(S.Number),
    IOMaximumBandwidth: S.optional(S.Number),
    Binds: S.optional(HostConfigBindsList),
    ContainerIDFile: S.optional(S.String),
    LogConfig: S.optional(HostConfigLogConfig),
    NetworkMode: S.optional(S.String),
    PortBindings: S.optional(PortMap),
    RestartPolicy: S.optional(RestartPolicy),
    AutoRemove: S.optional(S.Boolean),
    VolumeDriver: S.optional(S.String),
    VolumesFrom: S.optional(HostConfigVolumesFromList),
    Mounts: S.optional(HostConfigMountsList),
    ConsoleSize: S.optional(S.NullOr(HostConfigConsoleSizeList)),
    Annotations: S.optional(HostConfigAnnotationsMap),
    CapAdd: S.optional(HostConfigCapAddList),
    CapDrop: S.optional(HostConfigCapDropList),
    CgroupnsMode: S.optional(HostConfigCgroupnsMode),
    Dns: S.optional(HostConfigDnsList),
    DnsOptions: S.optional(HostConfigDnsOptionsList),
    DnsSearch: S.optional(HostConfigDnsSearchList),
    ExtraHosts: S.optional(HostConfigExtraHostsList),
    GroupAdd: S.optional(HostConfigGroupAddList),
    IpcMode: S.optional(S.String),
    Cgroup: S.optional(S.String),
    Links: S.optional(HostConfigLinksList),
    OomScoreAdj: S.optional(S.Number),
    PidMode: S.optional(S.String),
    Privileged: S.optional(S.Boolean),
    PublishAllPorts: S.optional(S.Boolean),
    ReadonlyRootfs: S.optional(S.Boolean),
    SecurityOpt: S.optional(HostConfigSecurityOptList),
    StorageOpt: S.optional(HostConfigStorageOptMap),
    Tmpfs: S.optional(HostConfigTmpfsMap),
    UTSMode: S.optional(S.String),
    UsernsMode: S.optional(S.String),
    ShmSize: S.optional(S.Number),
    Sysctls: S.optional(S.NullOr(HostConfigSysctlsMap)),
    Runtime: S.optional(S.NullOr(S.String)),
    Isolation: S.optional(HostConfigIsolation),
    MaskedPaths: S.optional(HostConfigMaskedPathsList),
    ReadonlyPaths: S.optional(HostConfigReadonlyPathsList),
  }),
).annotate({ identifier: "HostConfig" }) as any as S.Schema<HostConfig>;

/** Low-level storage metadata, provided as key/value pairs. This information is driver-specific, and depends on the storage-driver in use, and should be used for informational purposes only. */
export type DriverDataDataMap = { [key: string]: string | undefined };
export const DriverDataDataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<DriverDataDataMap>;

/** Information about the storage driver used to store the container's and image's filesystem. */
export interface DriverData {
  /** Name of the storage driver. */
  Name: string;
  /** Low-level storage metadata, provided as key/value pairs. This information is driver-specific, and depends on the storage-driver in use, and should be used for informational purposes only. */
  Data: DriverDataDataMap;
}
export const DriverData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Data: DriverDataDataMap,
  }),
).annotate({ identifier: "DriverData" }) as any as S.Schema<DriverData>;

/** Information about a snapshot backend of the container's root filesystem. */
export interface RootFSStorageSnapshot {
  /** Name of the snapshotter. */
  Name?: string;
}
export const RootFSStorageSnapshot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
  }),
).annotate({
  identifier: "RootFSStorageSnapshot",
}) as any as S.Schema<RootFSStorageSnapshot>;

/** Information about the storage used for the container's root filesystem. */
export interface RootFSStorage {
  /** Information about the snapshot used for the container's root filesystem. */
  Snapshot?: RootFSStorageSnapshot | null;
}
export const RootFSStorage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Snapshot: S.optional(S.NullOr(RootFSStorageSnapshot)),
  }),
).annotate({ identifier: "RootFSStorage" }) as any as S.Schema<RootFSStorage>;

/** Information about the storage used by the container. */
export interface Storage {
  /** Information about the storage used for the container's root filesystem. */
  RootFS?: RootFSStorage | null;
}
export const Storage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    RootFS: S.optional(S.NullOr(RootFSStorage)),
  }),
).annotate({ identifier: "Storage" }) as any as S.Schema<Storage>;

/** MountPoint represents a mount point configuration inside the container. This is used for reporting the mountpoints in use by a container. */
export interface MountPoint {
  /** The mount type: - `bind` a mount of a file or directory from the host into the container. - `cluster` a Swarm cluster volume. - `image` an OCI image. - `npipe` a named pipe from the host into the container. - `tmpfs` a `tmpfs`. - `volume` a docker volume with the given `Name`. */
  Type?: MountType;
  /** Name is the name reference to the underlying data defined by `Source` e.g., the volume name. */
  Name?: string;
  /** Source location of the mount. For volumes, this contains the storage location of the volume (within `/var/lib/docker/volumes/`). For bind-mounts, and `npipe`, this contains the source (host) part of the bind-mount. For `tmpfs` mount points, this field is empty. */
  Source?: string;
  /** Destination is the path relative to the container root (`/`) where the `Source` is mounted inside the container. */
  Destination?: string;
  /** Driver is the volume driver used to create the volume (if it is a volume). */
  Driver?: string;
  /** Mode is a comma separated list of options supplied by the user when creating the bind/volume mount. The default is platform-specific (`"z"` on Linux, empty on Windows). */
  Mode?: string;
  /** Whether the mount is mounted writable (read-write). */
  RW?: boolean;
  /** Propagation describes how mounts are propagated from the host into the mount point, and vice-versa. Refer to the [Linux kernel documentation](https://www.kernel.org/doc/Documentation/filesystems/sharedsubtree.txt) for details. This field is not used on Windows. */
  Propagation?: string;
}
export const MountPoint = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Type: S.optional(MountType),
    Name: S.optional(S.String),
    Source: S.optional(S.String),
    Destination: S.optional(S.String),
    Driver: S.optional(S.String),
    Mode: S.optional(S.String),
    RW: S.optional(S.Boolean),
    Propagation: S.optional(S.String),
  }),
).annotate({ identifier: "MountPoint" }) as any as S.Schema<MountPoint>;

/** List of mounts used by the container. */
export type ContainerInspectResponseMountsList = Array<MountPoint>;
export const ContainerInspectResponseMountsList = /*@__PURE__*/ S.Array(
  MountPoint,
) as any as S.Schema<ContainerInspectResponseMountsList>;

/** An object mapping ports to an empty object in the form: `{"<port>/<tcp|udp|sctp>": {}}` */
export type ContainerConfigExposedPortsMap = {
  [key: string]: unknown | undefined;
};
export const ContainerConfigExposedPortsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ContainerConfigExposedPortsMap>;

/** A list of environment variables to set inside the container in the form `["VAR=value", ...]`. A variable without `=` is removed from the environment, rather than to have an empty value. */
export type ContainerConfigEnvList = Array<string>;
export const ContainerConfigEnvList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerConfigEnvList>;

/** Command to run specified as a string or an array of strings. */
export type ContainerConfigCmdList = Array<string>;
export const ContainerConfigCmdList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerConfigCmdList>;

/** An object mapping mount point paths inside the container to empty objects. */
export type ContainerConfigVolumesMap = { [key: string]: unknown | undefined };
export const ContainerConfigVolumesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ContainerConfigVolumesMap>;

/** The entry point for the container as a string or an array of strings. If the array consists of exactly one empty string (`[""]`) then the entry point is reset to system default (i.e., the entry point used by docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`). */
export type ContainerConfigEntrypointList = Array<string>;
export const ContainerConfigEntrypointList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerConfigEntrypointList>;

/** `ONBUILD` metadata that were defined in the image's `Dockerfile`. */
export type ContainerConfigOnBuildList = Array<string>;
export const ContainerConfigOnBuildList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerConfigOnBuildList>;

/** User-defined key/value metadata. */
export type ContainerConfigLabelsMap = { [key: string]: string | undefined };
export const ContainerConfigLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ContainerConfigLabelsMap>;

/** Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell. */
export type ContainerConfigShellList = Array<string>;
export const ContainerConfigShellList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerConfigShellList>;

/** Configuration for a container that is portable between hosts. */
export interface ContainerConfig {
  /** The hostname to use for the container, as a valid RFC 1123 hostname. */
  Hostname?: string;
  /** The domain name to use for the container. */
  Domainname?: string;
  /** Commands run as this user inside the container. If omitted, commands run as the user specified in the image the container was started from. Can be either user-name or UID, and optional group-name or GID, separated by a colon (`<user-name|UID>[<:group-name|GID>]`). */
  User?: string;
  /** Whether to attach to `stdin`. */
  AttachStdin?: boolean;
  /** Whether to attach to `stdout`. */
  AttachStdout?: boolean;
  /** Whether to attach to `stderr`. */
  AttachStderr?: boolean;
  /** An object mapping ports to an empty object in the form: `{"<port>/<tcp|udp|sctp>": {}}` */
  ExposedPorts?: ContainerConfigExposedPortsMap | null;
  /** Attach standard streams to a TTY, including `stdin` if it is not closed. */
  Tty?: boolean;
  /** Open `stdin` */
  OpenStdin?: boolean;
  /** Close `stdin` after one attached client disconnects */
  StdinOnce?: boolean;
  /** A list of environment variables to set inside the container in the form `["VAR=value", ...]`. A variable without `=` is removed from the environment, rather than to have an empty value. */
  Env?: ContainerConfigEnvList;
  /** Command to run specified as a string or an array of strings. */
  Cmd?: ContainerConfigCmdList;
  Healthcheck?: HealthConfig;
  /** Command is already escaped (Windows only) */
  ArgsEscaped?: boolean | null;
  /** The name (or reference) of the image to use when creating the container, or which was used when the container was created. */
  Image?: string;
  /** An object mapping mount point paths inside the container to empty objects. */
  Volumes?: ContainerConfigVolumesMap;
  /** The working directory for commands to run in. */
  WorkingDir?: string;
  /** The entry point for the container as a string or an array of strings. If the array consists of exactly one empty string (`[""]`) then the entry point is reset to system default (i.e., the entry point used by docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`). */
  Entrypoint?: ContainerConfigEntrypointList;
  /** Disable networking for the container. */
  NetworkDisabled?: boolean | null;
  /** `ONBUILD` metadata that were defined in the image's `Dockerfile`. */
  OnBuild?: ContainerConfigOnBuildList | null;
  /** User-defined key/value metadata. */
  Labels?: ContainerConfigLabelsMap;
  /** Signal to stop a container as a string or unsigned integer. */
  StopSignal?: string | null;
  /** Timeout to stop a container in seconds. If omitted, the daemon-wide default (`default-stop-timeout`) is used. */
  StopTimeout?: number | null;
  /** Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell. */
  Shell?: ContainerConfigShellList | null;
}
export const ContainerConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Hostname: S.optional(S.String),
    Domainname: S.optional(S.String),
    User: S.optional(S.String),
    AttachStdin: S.optional(S.Boolean),
    AttachStdout: S.optional(S.Boolean),
    AttachStderr: S.optional(S.Boolean),
    ExposedPorts: S.optional(S.NullOr(ContainerConfigExposedPortsMap)),
    Tty: S.optional(S.Boolean),
    OpenStdin: S.optional(S.Boolean),
    StdinOnce: S.optional(S.Boolean),
    Env: S.optional(ContainerConfigEnvList),
    Cmd: S.optional(ContainerConfigCmdList),
    Healthcheck: S.optional(HealthConfig),
    ArgsEscaped: S.optional(S.NullOr(S.Boolean)),
    Image: S.optional(S.String),
    Volumes: S.optional(ContainerConfigVolumesMap),
    WorkingDir: S.optional(S.String),
    Entrypoint: S.optional(ContainerConfigEntrypointList),
    NetworkDisabled: S.optional(S.NullOr(S.Boolean)),
    OnBuild: S.optional(S.NullOr(ContainerConfigOnBuildList)),
    Labels: S.optional(ContainerConfigLabelsMap),
    StopSignal: S.optional(S.NullOr(S.String)),
    StopTimeout: S.optional(S.NullOr(S.Number)),
    Shell: S.optional(S.NullOr(ContainerConfigShellList)),
  }),
).annotate({
  identifier: "ContainerConfig",
}) as any as S.Schema<ContainerConfig>;

/** Information about all networks that the container is connected to. */
export type NetworkSettingsNetworksMap = {
  [key: string]: EndpointSettings | undefined;
};
export const NetworkSettingsNetworksMap = /*@__PURE__*/ S.Record(
  S.String,
  EndpointSettings,
) as any as S.Schema<NetworkSettingsNetworksMap>;

/** NetworkSettings exposes the network settings in the API */
export interface NetworkSettings {
  /** SandboxID uniquely represents a container's network stack. */
  SandboxID?: string;
  /** SandboxKey is the full path of the netns handle */
  SandboxKey?: string;
  Ports?: PortMap;
  /** Information about all networks that the container is connected to. */
  Networks?: NetworkSettingsNetworksMap;
}
export const NetworkSettings = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    SandboxID: S.optional(S.String),
    SandboxKey: S.optional(S.String),
    Ports: S.optional(PortMap),
    Networks: S.optional(NetworkSettingsNetworksMap),
  }),
).annotate({
  identifier: "NetworkSettings",
}) as any as S.Schema<NetworkSettings>;

export interface ContainerInspectResponse {
  /** The ID of this container as a 128-bit (64-character) hexadecimal string (32 bytes). */
  Id?: string;
  /** Date and time at which the container was created, formatted in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  Created?: string | null;
  /** The path to the command being run */
  Path?: string;
  /** The arguments to the command being run */
  Args?: ContainerInspectResponseArgsList;
  State?: ContainerState | null;
  /** The ID (digest) of the image that this container was created from. */
  Image?: string;
  /** Location of the `/etc/resolv.conf` generated for the container on the host. This file is managed through the docker daemon, and should not be accessed or modified by other tools. */
  ResolvConfPath?: string;
  /** Location of the `/etc/hostname` generated for the container on the host. This file is managed through the docker daemon, and should not be accessed or modified by other tools. */
  HostnamePath?: string;
  /** Location of the `/etc/hosts` generated for the container on the host. This file is managed through the docker daemon, and should not be accessed or modified by other tools. */
  HostsPath?: string;
  /** Location of the file used to buffer the container's logs. Depending on the logging-driver used for the container, this field may be omitted. This file is managed through the docker daemon, and should not be accessed or modified by other tools. */
  LogPath?: string | null;
  /** The name associated with this container. For historic reasons, the name may be prefixed with a forward-slash (`/`). */
  Name?: string;
  /** Number of times the container was restarted since it was created, or since daemon was started. */
  RestartCount?: number;
  /** The storage-driver used for the container's filesystem (graph-driver or snapshotter). */
  Driver?: string;
  /** The platform (operating system) for which the container was created. This field was introduced for the experimental "LCOW" (Linux Containers On Windows) features, which has been removed. In most cases, this field is equal to the host's operating system (`linux` or `windows`). */
  Platform?: string;
  /** OCI descriptor of the platform-specific manifest of the image the container was created from. Note: Only available if the daemon provides a multi-platform image store. */
  ImageManifestDescriptor?: OCIDescriptor;
  /** SELinux mount label set for the container. */
  MountLabel?: string;
  /** SELinux process label set for the container. */
  ProcessLabel?: string;
  /** The AppArmor profile set for the container. */
  AppArmorProfile?: string;
  /** IDs of exec instances that are running in the container. */
  ExecIDs?: ContainerInspectResponseExecIDsList | null;
  HostConfig?: HostConfig;
  GraphDriver?: DriverData | null;
  Storage?: Storage | null;
  /** The size of files that have been created or changed by this container. This field is omitted by default, and only set when size is requested in the API request. */
  SizeRw?: number | null;
  /** The total size of all files in the read-only layers from the image that the container uses. These layers can be shared between containers. This field is omitted by default, and only set when size is requested in the API request. */
  SizeRootFs?: number | null;
  /** List of mounts used by the container. */
  Mounts?: ContainerInspectResponseMountsList;
  Config?: ContainerConfig;
  NetworkSettings?: NetworkSettings;
}
export const ContainerInspectResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.optional(S.String),
    Created: S.optional(S.NullOr(S.String)),
    Path: S.optional(S.String),
    Args: S.optional(ContainerInspectResponseArgsList),
    State: S.optional(S.NullOr(ContainerState)),
    Image: S.optional(S.String),
    ResolvConfPath: S.optional(S.String),
    HostnamePath: S.optional(S.String),
    HostsPath: S.optional(S.String),
    LogPath: S.optional(S.NullOr(S.String)),
    Name: S.optional(S.String),
    RestartCount: S.optional(S.Number),
    Driver: S.optional(S.String),
    Platform: S.optional(S.String),
    ImageManifestDescriptor: S.optional(OCIDescriptor),
    MountLabel: S.optional(S.String),
    ProcessLabel: S.optional(S.String),
    AppArmorProfile: S.optional(S.String),
    ExecIDs: S.optional(S.NullOr(ContainerInspectResponseExecIDsList)),
    HostConfig: S.optional(HostConfig),
    GraphDriver: S.optional(S.NullOr(DriverData)),
    Storage: S.optional(S.NullOr(Storage)),
    SizeRw: S.optional(S.NullOr(S.Number)),
    SizeRootFs: S.optional(S.NullOr(S.Number)),
    Mounts: S.optional(ContainerInspectResponseMountsList),
    Config: S.optional(ContainerConfig),
    NetworkSettings: S.optional(NetworkSettings),
  }),
).annotate({
  identifier: "ContainerInspectResponse",
}) as any as S.Schema<ContainerInspectResponse>;

export interface ContainerLogsRequest {
  /** ID or name of the container */
  id: string;
  /** Keep connection after returning logs. */
  follow?: boolean;
  /** Return logs from `stdout` */
  stdout?: boolean;
  /** Return logs from `stderr` */
  stderr?: boolean;
  /** Only return logs since this time, as a UNIX timestamp */
  since?: number;
  /** Only return logs before this time, as a UNIX timestamp */
  until?: number;
  /** Add timestamps to every log line */
  timestamps?: boolean;
  /** Only return this number of log lines from the end of the logs. Specify as an integer or `all` to output all log lines. */
  tail?: string;
}
export const ContainerLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    follow: S.optional(S.Boolean.pipe(T.Query())),
    stdout: S.optional(S.Boolean.pipe(T.Query())),
    stderr: S.optional(S.Boolean.pipe(T.Query())),
    since: S.optional(S.Number.pipe(T.Query())),
    until: S.optional(S.Number.pipe(T.Query())),
    timestamps: S.optional(S.Boolean.pipe(T.Query())),
    tail: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/containers/{id}/logs", code: 200 })),
).annotate({
  identifier: "ContainerLogsRequest",
}) as any as S.Schema<ContainerLogsRequest>;

export type ContainerLogsResponse = string;
export const ContainerLogsResponse = /*@__PURE__*/ S.suspend(() =>
  S.String.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ContainerLogsResponse",
}) as any as S.Schema<ContainerLogsResponse>;

export interface ContainerPruneRequest {
  /** Filters to process on the prune list, encoded as JSON (a `map[string][]string`). Available filters: - `until=<timestamp>` Prune containers created before this timestamp. The `<timestamp>` can be Unix timestamps, date formatted timestamps, or Go duration strings (e.g. `10m`, `1h30m`) computed relative to the daemon machine’s time. - `label` (`label=<key>`, `label=<key>=<value>`, `label!=<key>`, or `label!=<key>=<value>`) Prune containers with (or without, in case `label!=...` is used) the specified labels. */
  filters?: string;
}
export const ContainerPruneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/containers/prune", code: 200 })),
).annotate({
  identifier: "ContainerPruneRequest",
}) as any as S.Schema<ContainerPruneRequest>;

/** Container IDs that were deleted */
export type ContainerPruneResponseContainersDeletedList = Array<string>;
export const ContainerPruneResponseContainersDeletedList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ContainerPruneResponseContainersDeletedList>;

export interface ContainerPruneResponse {
  /** Container IDs that were deleted */
  ContainersDeleted?: ContainerPruneResponseContainersDeletedList;
  /** Disk space reclaimed in bytes */
  SpaceReclaimed?: number;
}
export const ContainerPruneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ContainersDeleted: S.optional(ContainerPruneResponseContainersDeletedList),
    SpaceReclaimed: S.optional(S.Number),
  }),
).annotate({
  identifier: "ContainerPruneResponse",
}) as any as S.Schema<ContainerPruneResponse>;

export interface ContainerStatsRequest {
  /** ID or name of the container */
  id: string;
  /** Stream the output. If false, the stats will be output once and then it will disconnect. */
  stream?: boolean;
  /** Only get a single stat instead of waiting for 2 cycles. Must be used with `stream=false`. */
  one_shot?: boolean;
}
export const ContainerStatsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    stream: S.optional(S.Boolean.pipe(T.Query())),
    one_shot: S.optional(S.Boolean.pipe(T.Query("one-shot"))),
  }).pipe(T.Http({ method: "GET", uri: "/containers/{id}/stats", code: 200 })),
).annotate({
  identifier: "ContainerStatsRequest",
}) as any as S.Schema<ContainerStatsRequest>;

/** Total CPU time (in nanoseconds) consumed per core (Linux). This field is Linux-specific when using cgroups v1. It is omitted when using cgroups v2 and Windows containers. */
export type ContainerCPUUsagePercpuUsageList = Array<number>;
export const ContainerCPUUsagePercpuUsageList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<ContainerCPUUsagePercpuUsageList>;

/** All CPU stats aggregated since container inception. */
export interface ContainerCPUUsage {
  /** Total CPU time consumed in nanoseconds (Linux) or 100's of nanoseconds (Windows). */
  total_usage?: number;
  /** Total CPU time (in nanoseconds) consumed per core (Linux). This field is Linux-specific when using cgroups v1. It is omitted when using cgroups v2 and Windows containers. */
  percpu_usage?: ContainerCPUUsagePercpuUsageList | null;
  /** Time (in nanoseconds) spent by tasks of the cgroup in kernel mode (Linux), or time spent (in 100's of nanoseconds) by all container processes in kernel mode (Windows). Not populated for Windows containers using Hyper-V isolation. */
  usage_in_kernelmode?: number;
  /** Time (in nanoseconds) spent by tasks of the cgroup in user mode (Linux), or time spent (in 100's of nanoseconds) by all container processes in kernel mode (Windows). Not populated for Windows containers using Hyper-V isolation. */
  usage_in_usermode?: number;
}
export const ContainerCPUUsage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total_usage: S.optional(S.Number),
    percpu_usage: S.optional(S.NullOr(ContainerCPUUsagePercpuUsageList)),
    usage_in_kernelmode: S.optional(S.Number),
    usage_in_usermode: S.optional(S.Number),
  }),
).annotate({
  identifier: "ContainerCPUUsage",
}) as any as S.Schema<ContainerCPUUsage>;

/** CPU throttling stats of the container. This type is Linux-specific and omitted for Windows containers. */
export interface ContainerThrottlingData {
  /** Number of periods with throttling active. */
  periods?: number;
  /** Number of periods when the container hit its throttling limit. */
  throttled_periods?: number;
  /** Aggregated time (in nanoseconds) the container was throttled for. */
  throttled_time?: number;
}
export const ContainerThrottlingData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    periods: S.optional(S.Number),
    throttled_periods: S.optional(S.Number),
    throttled_time: S.optional(S.Number),
  }),
).annotate({
  identifier: "ContainerThrottlingData",
}) as any as S.Schema<ContainerThrottlingData>;

/** CPU related info of the container */
export interface ContainerCPUStats {
  cpu_usage?: ContainerCPUUsage | null;
  /** System Usage. This field is Linux-specific and omitted for Windows containers. */
  system_cpu_usage?: number | null;
  /** Number of online CPUs. This field is Linux-specific and omitted for Windows containers. */
  online_cpus?: number | null;
  throttling_data?: ContainerThrottlingData | null;
}
export const ContainerCPUStats = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cpu_usage: S.optional(S.NullOr(ContainerCPUUsage)),
    system_cpu_usage: S.optional(S.NullOr(S.Number)),
    online_cpus: S.optional(S.NullOr(S.Number)),
    throttling_data: S.optional(S.NullOr(ContainerThrottlingData)),
  }),
).annotate({
  identifier: "ContainerCPUStats",
}) as any as S.Schema<ContainerCPUStats>;

/** All the stats exported via memory.stat. The fields in this object differ between cgroups v1 and v2. On cgroups v1, fields such as `cache`, `rss`, `mapped_file` are available. On cgroups v2, fields such as `file`, `anon`, `inactive_file` are available. This field is Linux-specific and omitted for Windows containers. */
export type ContainerMemoryStatsStatsMap = {
  [key: string]: number | undefined;
};
export const ContainerMemoryStatsStatsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Number,
) as any as S.Schema<ContainerMemoryStatsStatsMap>;

/** Aggregates all memory stats since container inception on Linux. Windows returns stats for commit and private working set only. */
export interface ContainerMemoryStats {
  /** Current `res_counter` usage for memory. This field is Linux-specific and omitted for Windows containers. */
  usage?: number | null;
  /** Maximum usage ever recorded. This field is Linux-specific and only supported on cgroups v1. It is omitted when using cgroups v2 and for Windows containers. */
  max_usage?: number | null;
  /** All the stats exported via memory.stat. The fields in this object differ between cgroups v1 and v2. On cgroups v1, fields such as `cache`, `rss`, `mapped_file` are available. On cgroups v2, fields such as `file`, `anon`, `inactive_file` are available. This field is Linux-specific and omitted for Windows containers. */
  stats?: ContainerMemoryStatsStatsMap;
  /** Number of times memory usage hits limits. This field is Linux-specific and only supported on cgroups v1. It is omitted when using cgroups v2 and for Windows containers. */
  failcnt?: number | null;
  /** This field is Linux-specific and omitted for Windows containers. */
  limit?: number | null;
  /** Committed bytes. This field is Windows-specific and omitted for Linux containers. */
  commitbytes?: number | null;
  /** Peak committed bytes. This field is Windows-specific and omitted for Linux containers. */
  commitpeakbytes?: number | null;
  /** Private working set. This field is Windows-specific and omitted for Linux containers. */
  privateworkingset?: number | null;
}
export const ContainerMemoryStats = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    usage: S.optional(S.NullOr(S.Number)),
    max_usage: S.optional(S.NullOr(S.Number)),
    stats: S.optional(ContainerMemoryStatsStatsMap),
    failcnt: S.optional(S.NullOr(S.Number)),
    limit: S.optional(S.NullOr(S.Number)),
    commitbytes: S.optional(S.NullOr(S.Number)),
    commitpeakbytes: S.optional(S.NullOr(S.Number)),
    privateworkingset: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "ContainerMemoryStats",
}) as any as S.Schema<ContainerMemoryStats>;

/** Aggregates the network stats of one container */
export interface ContainerNetworkStats {
  /** Bytes received. Windows and Linux. */
  rx_bytes?: number;
  /** Packets received. Windows and Linux. */
  rx_packets?: number;
  /** Received errors. Not used on Windows. This field is Linux-specific and always zero for Windows containers. */
  rx_errors?: number;
  /** Incoming packets dropped. Windows and Linux. */
  rx_dropped?: number;
  /** Bytes sent. Windows and Linux. */
  tx_bytes?: number;
  /** Packets sent. Windows and Linux. */
  tx_packets?: number;
  /** Sent errors. Not used on Windows. This field is Linux-specific and always zero for Windows containers. */
  tx_errors?: number;
  /** Outgoing packets dropped. Windows and Linux. */
  tx_dropped?: number;
  /** Endpoint ID. Not used on Linux. This field is Windows-specific and omitted for Linux containers. */
  endpoint_id?: string | null;
  /** Instance ID. Not used on Linux. This field is Windows-specific and omitted for Linux containers. */
  instance_id?: string | null;
}
export const ContainerNetworkStats = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    rx_bytes: S.optional(S.Number),
    rx_packets: S.optional(S.Number),
    rx_errors: S.optional(S.Number),
    rx_dropped: S.optional(S.Number),
    tx_bytes: S.optional(S.Number),
    tx_packets: S.optional(S.Number),
    tx_errors: S.optional(S.Number),
    tx_dropped: S.optional(S.Number),
    endpoint_id: S.optional(S.NullOr(S.String)),
    instance_id: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "ContainerNetworkStats",
}) as any as S.Schema<ContainerNetworkStats>;

/** Network statistics for the container per interface. This field is omitted if the container has no networking enabled. */
export type ContainerStatsResponseNetworksMap = {
  [key: string]: ContainerNetworkStats | undefined;
};
export const ContainerStatsResponseNetworksMap = /*@__PURE__*/ S.Record(
  S.String,
  ContainerNetworkStats,
) as any as S.Schema<ContainerStatsResponseNetworksMap>;

/** PidsStats contains Linux-specific stats of a container's process-IDs (PIDs). This type is Linux-specific and omitted for Windows containers. */
export interface ContainerPidsStats {
  /** Current is the number of PIDs in the cgroup. */
  current?: number | null;
  /** Limit is the hard limit on the number of pids in the cgroup. A "Limit" of 0 means that there is no limit. */
  limit?: number | null;
}
export const ContainerPidsStats = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    current: S.optional(S.NullOr(S.Number)),
    limit: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "ContainerPidsStats",
}) as any as S.Schema<ContainerPidsStats>;

/** Blkio stats entry. This type is Linux-specific and omitted for Windows containers. */
export interface ContainerBlkioStatEntry {
  major?: number;
  minor?: number;
  op?: string;
  value?: number;
}
export const ContainerBlkioStatEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    major: S.optional(S.Number),
    minor: S.optional(S.Number),
    op: S.optional(S.String),
    value: S.optional(S.Number),
  }),
).annotate({
  identifier: "ContainerBlkioStatEntry",
}) as any as S.Schema<ContainerBlkioStatEntry>;

export type ContainerBlkioStatsIoServiceBytesRecursiveList =
  Array<ContainerBlkioStatEntry>;
export const ContainerBlkioStatsIoServiceBytesRecursiveList =
  /*@__PURE__*/ S.Array(
    ContainerBlkioStatEntry,
  ) as any as S.Schema<ContainerBlkioStatsIoServiceBytesRecursiveList>;

/** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
export type ContainerBlkioStatsIoServicedRecursiveList =
  Array<ContainerBlkioStatEntry>;
export const ContainerBlkioStatsIoServicedRecursiveList = /*@__PURE__*/ S.Array(
  ContainerBlkioStatEntry,
) as any as S.Schema<ContainerBlkioStatsIoServicedRecursiveList>;

/** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
export type ContainerBlkioStatsIoQueueRecursiveList =
  Array<ContainerBlkioStatEntry>;
export const ContainerBlkioStatsIoQueueRecursiveList = /*@__PURE__*/ S.Array(
  ContainerBlkioStatEntry,
) as any as S.Schema<ContainerBlkioStatsIoQueueRecursiveList>;

/** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
export type ContainerBlkioStatsIoServiceTimeRecursiveList =
  Array<ContainerBlkioStatEntry>;
export const ContainerBlkioStatsIoServiceTimeRecursiveList =
  /*@__PURE__*/ S.Array(
    ContainerBlkioStatEntry,
  ) as any as S.Schema<ContainerBlkioStatsIoServiceTimeRecursiveList>;

/** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
export type ContainerBlkioStatsIoWaitTimeRecursiveList =
  Array<ContainerBlkioStatEntry>;
export const ContainerBlkioStatsIoWaitTimeRecursiveList = /*@__PURE__*/ S.Array(
  ContainerBlkioStatEntry,
) as any as S.Schema<ContainerBlkioStatsIoWaitTimeRecursiveList>;

/** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
export type ContainerBlkioStatsIoMergedRecursiveList =
  Array<ContainerBlkioStatEntry>;
export const ContainerBlkioStatsIoMergedRecursiveList = /*@__PURE__*/ S.Array(
  ContainerBlkioStatEntry,
) as any as S.Schema<ContainerBlkioStatsIoMergedRecursiveList>;

/** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
export type ContainerBlkioStatsIoTimeRecursiveList =
  Array<ContainerBlkioStatEntry>;
export const ContainerBlkioStatsIoTimeRecursiveList = /*@__PURE__*/ S.Array(
  ContainerBlkioStatEntry,
) as any as S.Schema<ContainerBlkioStatsIoTimeRecursiveList>;

/** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
export type ContainerBlkioStatsSectorsRecursiveList =
  Array<ContainerBlkioStatEntry>;
export const ContainerBlkioStatsSectorsRecursiveList = /*@__PURE__*/ S.Array(
  ContainerBlkioStatEntry,
) as any as S.Schema<ContainerBlkioStatsSectorsRecursiveList>;

/** BlkioStats stores all IO service stats for data read and write. This type is Linux-specific and holds many fields that are specific to cgroups v1. On a cgroup v2 host, all fields other than `io_service_bytes_recursive` are omitted or `null`. This type is only populated on Linux and omitted for Windows containers. */
export interface ContainerBlkioStats {
  io_service_bytes_recursive?: ContainerBlkioStatsIoServiceBytesRecursiveList;
  /** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
  io_serviced_recursive?: ContainerBlkioStatsIoServicedRecursiveList | null;
  /** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
  io_queue_recursive?: ContainerBlkioStatsIoQueueRecursiveList | null;
  /** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
  io_service_time_recursive?: ContainerBlkioStatsIoServiceTimeRecursiveList | null;
  /** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
  io_wait_time_recursive?: ContainerBlkioStatsIoWaitTimeRecursiveList | null;
  /** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
  io_merged_recursive?: ContainerBlkioStatsIoMergedRecursiveList | null;
  /** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
  io_time_recursive?: ContainerBlkioStatsIoTimeRecursiveList | null;
  /** This field is only available when using Linux containers with cgroups v1. It is omitted or `null` when using cgroups v2. */
  sectors_recursive?: ContainerBlkioStatsSectorsRecursiveList | null;
}
export const ContainerBlkioStats = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    io_service_bytes_recursive: S.optional(
      ContainerBlkioStatsIoServiceBytesRecursiveList,
    ),
    io_serviced_recursive: S.optional(
      S.NullOr(ContainerBlkioStatsIoServicedRecursiveList),
    ),
    io_queue_recursive: S.optional(
      S.NullOr(ContainerBlkioStatsIoQueueRecursiveList),
    ),
    io_service_time_recursive: S.optional(
      S.NullOr(ContainerBlkioStatsIoServiceTimeRecursiveList),
    ),
    io_wait_time_recursive: S.optional(
      S.NullOr(ContainerBlkioStatsIoWaitTimeRecursiveList),
    ),
    io_merged_recursive: S.optional(
      S.NullOr(ContainerBlkioStatsIoMergedRecursiveList),
    ),
    io_time_recursive: S.optional(
      S.NullOr(ContainerBlkioStatsIoTimeRecursiveList),
    ),
    sectors_recursive: S.optional(
      S.NullOr(ContainerBlkioStatsSectorsRecursiveList),
    ),
  }),
).annotate({
  identifier: "ContainerBlkioStats",
}) as any as S.Schema<ContainerBlkioStats>;

/** StorageStats is the disk I/O stats for read/write on Windows. This type is Windows-specific and omitted for Linux containers. */
export interface ContainerStorageStats {
  read_count_normalized?: number | null;
  read_size_bytes?: number | null;
  write_count_normalized?: number | null;
  write_size_bytes?: number | null;
}
export const ContainerStorageStats = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    read_count_normalized: S.optional(S.NullOr(S.Number)),
    read_size_bytes: S.optional(S.NullOr(S.Number)),
    write_count_normalized: S.optional(S.NullOr(S.Number)),
    write_size_bytes: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "ContainerStorageStats",
}) as any as S.Schema<ContainerStorageStats>;

/** Statistics sample for a container. */
export interface ContainerStatsResponse {
  /** ID of the container for which the stats were collected. */
  id?: string | null;
  /** Name of the container for which the stats were collected. */
  name?: string | null;
  /** OSType is the OS of the container ("linux" or "windows") to allow platform-specific handling of stats. */
  os_type?: string | null;
  /** Date and time at which this sample was collected. The value is formatted as [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) with nano-seconds. */
  read?: string;
  cpu_stats?: ContainerCPUStats | null;
  memory_stats?: ContainerMemoryStats;
  /** Network statistics for the container per interface. This field is omitted if the container has no networking enabled. */
  networks?: ContainerStatsResponseNetworksMap | null;
  pids_stats?: ContainerPidsStats | null;
  blkio_stats?: ContainerBlkioStats | null;
  /** The number of processors on the system. This field is Windows-specific and always zero for Linux containers. */
  num_procs?: number;
  storage_stats?: ContainerStorageStats | null;
  /** Date and time at which this first sample was collected. This field is not propagated if the "one-shot" option is set. If the "one-shot" option is set, this field may be omitted, empty, or set to a default date (`0001-01-01T00:00:00Z`). The value is formatted as [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) with nano-seconds. */
  preread?: string;
  precpu_stats?: ContainerCPUStats | null;
}
export const ContainerStatsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.NullOr(S.String)),
    name: S.optional(S.NullOr(S.String)),
    os_type: S.optional(S.NullOr(S.String)),
    read: S.optional(S.String),
    cpu_stats: S.optional(S.NullOr(ContainerCPUStats)),
    memory_stats: S.optional(ContainerMemoryStats),
    networks: S.optional(S.NullOr(ContainerStatsResponseNetworksMap)),
    pids_stats: S.optional(S.NullOr(ContainerPidsStats)),
    blkio_stats: S.optional(S.NullOr(ContainerBlkioStats)),
    num_procs: S.optional(S.Number),
    storage_stats: S.optional(S.NullOr(ContainerStorageStats)),
    preread: S.optional(S.String),
    precpu_stats: S.optional(S.NullOr(ContainerCPUStats)),
  }),
).annotate({
  identifier: "ContainerStatsResponse",
}) as any as S.Schema<ContainerStatsResponse>;

export interface ContainerTopRequest {
  /** ID or name of the container */
  id: string;
  /** The arguments to pass to `ps`. For example, `aux` */
  ps_args?: string;
}
export const ContainerTopRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    ps_args: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/containers/{id}/top", code: 200 })),
).annotate({
  identifier: "ContainerTopRequest",
}) as any as S.Schema<ContainerTopRequest>;

/** The ps column titles */
export type ContainerTopResponseTitlesList = Array<string>;
export const ContainerTopResponseTitlesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerTopResponseTitlesList>;

export type ContainerTopResponseProcessesItemList = Array<string>;
export const ContainerTopResponseProcessesItemList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerTopResponseProcessesItemList>;

/** Each process running in the container, where each process is an array of values corresponding to the titles. */
export type ContainerTopResponseProcessesList =
  Array<ContainerTopResponseProcessesItemList>;
export const ContainerTopResponseProcessesList = /*@__PURE__*/ S.Array(
  ContainerTopResponseProcessesItemList,
) as any as S.Schema<ContainerTopResponseProcessesList>;

/** Container "top" response. */
export interface ContainerTopResponse {
  /** The ps column titles */
  Titles?: ContainerTopResponseTitlesList;
  /** Each process running in the container, where each process is an array of values corresponding to the titles. */
  Processes?: ContainerTopResponseProcessesList;
}
export const ContainerTopResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Titles: S.optional(ContainerTopResponseTitlesList),
    Processes: S.optional(ContainerTopResponseProcessesList),
  }),
).annotate({
  identifier: "ContainerTopResponse",
}) as any as S.Schema<ContainerTopResponse>;

/** User-defined key/value metadata. */
export type CreateConfigRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const CreateConfigRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateConfigRequestLabelsMap>;

export interface CreateConfigRequest {
  /** User-defined name of the config. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: CreateConfigRequestLabelsMap;
  /** Data is the data to store as a config, formatted as a standard base64-encoded ([RFC 4648](https://tools.ietf.org/html/rfc4648#section-4)) string. The maximum allowed size is 1000KB, as defined in [MaxConfigSize](https://pkg.go.dev/github.com/moby/swarmkit/v2@v2.0.0-20250103191802-8c1959736554/manager/controlapi#MaxConfigSize). */
  Data?: string;
  /** Templating driver, if applicable Templating controls whether and how to evaluate the config payload as a template. If no driver is set, no templating is used. */
  Templating?: Driver;
}
export const CreateConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Labels: S.optional(CreateConfigRequestLabelsMap),
    Data: S.optional(S.String),
    Templating: S.optional(Driver),
  }).pipe(T.Http({ method: "POST", uri: "/configs/create", code: 200 })),
).annotate({
  identifier: "CreateConfigRequest",
}) as any as S.Schema<CreateConfigRequest>;

/** An object mapping ports to an empty object in the form: `{"<port>/<tcp|udp|sctp>": {}}` */
export type CreateContainerRequestExposedPortsMap = {
  [key: string]: unknown | undefined;
};
export const CreateContainerRequestExposedPortsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateContainerRequestExposedPortsMap>;

/** A list of environment variables to set inside the container in the form `["VAR=value", ...]`. A variable without `=` is removed from the environment, rather than to have an empty value. */
export type CreateContainerRequestEnvList = Array<string>;
export const CreateContainerRequestEnvList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateContainerRequestEnvList>;

/** Command to run specified as a string or an array of strings. */
export type CreateContainerRequestCmdList = Array<string>;
export const CreateContainerRequestCmdList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateContainerRequestCmdList>;

/** An object mapping mount point paths inside the container to empty objects. */
export type CreateContainerRequestVolumesMap = {
  [key: string]: unknown | undefined;
};
export const CreateContainerRequestVolumesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateContainerRequestVolumesMap>;

/** The entry point for the container as a string or an array of strings. If the array consists of exactly one empty string (`[""]`) then the entry point is reset to system default (i.e., the entry point used by docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`). */
export type CreateContainerRequestEntrypointList = Array<string>;
export const CreateContainerRequestEntrypointList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateContainerRequestEntrypointList>;

/** `ONBUILD` metadata that were defined in the image's `Dockerfile`. */
export type CreateContainerRequestOnBuildList = Array<string>;
export const CreateContainerRequestOnBuildList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateContainerRequestOnBuildList>;

/** User-defined key/value metadata. */
export type CreateContainerRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const CreateContainerRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateContainerRequestLabelsMap>;

/** Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell. */
export type CreateContainerRequestShellList = Array<string>;
export const CreateContainerRequestShellList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateContainerRequestShellList>;

/** A mapping of network name to endpoint configuration for that network. The endpoint configuration can be left empty to connect to that network with no particular endpoint configuration. */
export type NetworkingConfigEndpointsConfigMap = {
  [key: string]: EndpointSettings | undefined;
};
export const NetworkingConfigEndpointsConfigMap = /*@__PURE__*/ S.Record(
  S.String,
  EndpointSettings,
) as any as S.Schema<NetworkingConfigEndpointsConfigMap>;

/** NetworkingConfig represents the container's networking configuration for each of its interfaces. It is used for the networking configs specified in the `docker create` and `docker network connect` commands. */
export interface NetworkingConfig {
  /** A mapping of network name to endpoint configuration for that network. The endpoint configuration can be left empty to connect to that network with no particular endpoint configuration. */
  EndpointsConfig?: NetworkingConfigEndpointsConfigMap;
}
export const NetworkingConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    EndpointsConfig: S.optional(NetworkingConfigEndpointsConfigMap),
  }),
).annotate({
  identifier: "NetworkingConfig",
}) as any as S.Schema<NetworkingConfig>;

export interface CreateContainerRequest {
  /** Assign the specified name to the container. Must match `/?[a-zA-Z0-9][a-zA-Z0-9_.-]+`. */
  name?: string;
  /** Platform in the format `os[/arch[/variant]]` used for image lookup. When specified, the daemon checks if the requested image is present in the local image cache with the given OS and Architecture, and otherwise returns a `404` status. If the option is not set, the host's native OS and Architecture are used to look up the image in the image cache. However, if no platform is passed and the given image does exist in the local image cache, but its OS or architecture does not match, the container is created with the available image, and a warning is added to the `Warnings` field in the response, for example; WARNING: The requested image's platform (linux/arm64/v8) does not match the detected host platform (linux/amd64) and no specific platform was requested */
  platform?: string;
  /** The hostname to use for the container, as a valid RFC 1123 hostname. */
  Hostname?: string;
  /** The domain name to use for the container. */
  Domainname?: string;
  /** Commands run as this user inside the container. If omitted, commands run as the user specified in the image the container was started from. Can be either user-name or UID, and optional group-name or GID, separated by a colon (`<user-name|UID>[<:group-name|GID>]`). */
  User?: string;
  /** Whether to attach to `stdin`. */
  AttachStdin?: boolean;
  /** Whether to attach to `stdout`. */
  AttachStdout?: boolean;
  /** Whether to attach to `stderr`. */
  AttachStderr?: boolean;
  /** An object mapping ports to an empty object in the form: `{"<port>/<tcp|udp|sctp>": {}}` */
  ExposedPorts?: CreateContainerRequestExposedPortsMap | null;
  /** Attach standard streams to a TTY, including `stdin` if it is not closed. */
  Tty?: boolean;
  /** Open `stdin` */
  OpenStdin?: boolean;
  /** Close `stdin` after one attached client disconnects */
  StdinOnce?: boolean;
  /** A list of environment variables to set inside the container in the form `["VAR=value", ...]`. A variable without `=` is removed from the environment, rather than to have an empty value. */
  Env?: CreateContainerRequestEnvList;
  /** Command to run specified as a string or an array of strings. */
  Cmd?: CreateContainerRequestCmdList;
  Healthcheck?: HealthConfig;
  /** Command is already escaped (Windows only) */
  ArgsEscaped?: boolean | null;
  /** The name (or reference) of the image to use when creating the container, or which was used when the container was created. */
  Image?: string;
  /** An object mapping mount point paths inside the container to empty objects. */
  Volumes?: CreateContainerRequestVolumesMap;
  /** The working directory for commands to run in. */
  WorkingDir?: string;
  /** The entry point for the container as a string or an array of strings. If the array consists of exactly one empty string (`[""]`) then the entry point is reset to system default (i.e., the entry point used by docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`). */
  Entrypoint?: CreateContainerRequestEntrypointList;
  /** Disable networking for the container. */
  NetworkDisabled?: boolean | null;
  /** `ONBUILD` metadata that were defined in the image's `Dockerfile`. */
  OnBuild?: CreateContainerRequestOnBuildList | null;
  /** User-defined key/value metadata. */
  Labels?: CreateContainerRequestLabelsMap;
  /** Signal to stop a container as a string or unsigned integer. */
  StopSignal?: string | null;
  /** Timeout to stop a container in seconds. If omitted, the daemon-wide default (`default-stop-timeout`) is used. */
  StopTimeout?: number | null;
  /** Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell. */
  Shell?: CreateContainerRequestShellList | null;
  HostConfig?: HostConfig;
  NetworkingConfig?: NetworkingConfig;
}
export const CreateContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String.pipe(T.Query())),
    platform: S.optional(S.String.pipe(T.Query())),
    Hostname: S.optional(S.String),
    Domainname: S.optional(S.String),
    User: S.optional(S.String),
    AttachStdin: S.optional(S.Boolean),
    AttachStdout: S.optional(S.Boolean),
    AttachStderr: S.optional(S.Boolean),
    ExposedPorts: S.optional(S.NullOr(CreateContainerRequestExposedPortsMap)),
    Tty: S.optional(S.Boolean),
    OpenStdin: S.optional(S.Boolean),
    StdinOnce: S.optional(S.Boolean),
    Env: S.optional(CreateContainerRequestEnvList),
    Cmd: S.optional(CreateContainerRequestCmdList),
    Healthcheck: S.optional(HealthConfig),
    ArgsEscaped: S.optional(S.NullOr(S.Boolean)),
    Image: S.optional(S.String),
    Volumes: S.optional(CreateContainerRequestVolumesMap),
    WorkingDir: S.optional(S.String),
    Entrypoint: S.optional(CreateContainerRequestEntrypointList),
    NetworkDisabled: S.optional(S.NullOr(S.Boolean)),
    OnBuild: S.optional(S.NullOr(CreateContainerRequestOnBuildList)),
    Labels: S.optional(CreateContainerRequestLabelsMap),
    StopSignal: S.optional(S.NullOr(S.String)),
    StopTimeout: S.optional(S.NullOr(S.Number)),
    Shell: S.optional(S.NullOr(CreateContainerRequestShellList)),
    HostConfig: S.optional(HostConfig),
    NetworkingConfig: S.optional(NetworkingConfig),
  }).pipe(T.Http({ method: "POST", uri: "/containers/create", code: 200 })),
).annotate({
  identifier: "CreateContainerRequest",
}) as any as S.Schema<CreateContainerRequest>;

/** Warnings encountered when creating the container */
export type ContainerCreateResponseWarningsList = Array<string>;
export const ContainerCreateResponseWarningsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerCreateResponseWarningsList>;

/** OK response to ContainerCreate operation */
export interface ContainerCreateResponse {
  /** The ID of the created container */
  Id: string;
  /** Warnings encountered when creating the container */
  Warnings: ContainerCreateResponseWarningsList;
}
export const ContainerCreateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.String,
    Warnings: ContainerCreateResponseWarningsList,
  }),
).annotate({
  identifier: "ContainerCreateResponse",
}) as any as S.Schema<ContainerCreateResponse>;

export type CreateImageRequestChangesList = Array<string>;
export const CreateImageRequestChangesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateImageRequestChangesList>;

export interface CreateImageRequest {
  /** Name of the image to pull. If the name includes a tag or digest, specific behavior applies: - If only `fromImage` includes a tag, that tag is used. - If both `fromImage` and `tag` are provided, `tag` takes precedence. - If `fromImage` includes a digest, the image is pulled by digest, and `tag` is ignored. - If neither a tag nor digest is specified, all tags are pulled. */
  fromImage?: string;
  /** Source to import. The value may be a URL from which the image can be retrieved or `-` to read the image from the request body. This parameter may only be used when importing an image. */
  fromSrc?: string;
  /** Repository name given to an image when it is imported. The repo may include a tag. This parameter may only be used when importing an image. */
  repo?: string;
  /** Tag or digest. If empty when pulling an image, this causes all tags for the given image to be pulled. */
  tag?: string;
  /** Set commit message for imported image. */
  message?: string;
  /** Apply `Dockerfile` instructions to the image that is created, for example: `changes=ENV DEBUG=true`. Note that `ENV DEBUG=true` should be URI component encoded. Supported `Dockerfile` instructions: `CMD`|`ENTRYPOINT`|`ENV`|`EXPOSE`|`ONBUILD`|`USER`|`VOLUME`|`WORKDIR` */
  changes?: CreateImageRequestChangesList;
  /** Platform in the format os[/arch[/variant]]. When used in combination with the `fromImage` option, the daemon checks if the given image is present in the local image cache with the given OS and Architecture, and otherwise attempts to pull the image. If the option is not set, the host's native OS and Architecture are used. If the given image does not exist in the local image cache, the daemon attempts to pull the image with the host's native OS and Architecture. If the given image does exists in the local image cache, but its OS or architecture does not match, a warning is produced. When used with the `fromSrc` option to import an image from an archive, this option sets the platform information for the imported image. If the option is not set, the host's native OS and Architecture are used for the imported image. */
  platform?: string;
  /** A base64url-encoded auth configuration. Refer to the [authentication section](#section/Authentication) for details. */
  xRegistryAuth?: string;
  body?: string;
}
export const CreateImageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    fromImage: S.optional(S.String.pipe(T.Query())),
    fromSrc: S.optional(S.String.pipe(T.Query())),
    repo: S.optional(S.String.pipe(T.Query())),
    tag: S.optional(S.String.pipe(T.Query())),
    message: S.optional(S.String.pipe(T.Query())),
    changes: S.optional(CreateImageRequestChangesList.pipe(T.Query())),
    platform: S.optional(S.String.pipe(T.Query())),
    xRegistryAuth: S.optional(S.String.pipe(T.Header("X-Registry-Auth"))),
    body: S.optional(S.String.pipe(T.HttpBody())),
  }).pipe(T.Http({ method: "POST", uri: "/images/create", code: 200 })),
).annotate({
  identifier: "CreateImageRequest",
}) as any as S.Schema<CreateImageRequest>;

export interface CreateImageResponse {}
export const CreateImageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "CreateImageResponse",
}) as any as S.Schema<CreateImageResponse>;

/** The config-only network source to provide the configuration for this network. */
export interface ConfigReference {
  /** The name of the config-only network that provides the network's configuration. The specified network must be an existing config-only network. Only network names are allowed, not network IDs. */
  Network?: string;
}
export const ConfigReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Network: S.optional(S.String),
  }),
).annotate({
  identifier: "ConfigReference",
}) as any as S.Schema<ConfigReference>;

export type IPAMConfigAuxiliaryAddressesMap = {
  [key: string]: string | undefined;
};
export const IPAMConfigAuxiliaryAddressesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<IPAMConfigAuxiliaryAddressesMap>;

export interface IPAMConfig {
  Subnet?: string;
  IPRange?: string;
  Gateway?: string;
  AuxiliaryAddresses?: IPAMConfigAuxiliaryAddressesMap;
}
export const IPAMConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Subnet: S.optional(S.String),
    IPRange: S.optional(S.String),
    Gateway: S.optional(S.String),
    AuxiliaryAddresses: S.optional(IPAMConfigAuxiliaryAddressesMap),
  }),
).annotate({ identifier: "IPAMConfig" }) as any as S.Schema<IPAMConfig>;

/** List of IPAM configuration options, specified as a map: ``` {"Subnet": <CIDR>, "IPRange": <CIDR>, "Gateway": <IP address>, "AuxAddress": <device_name:IP address>} ``` */
export type IPAMConfigList = Array<IPAMConfig>;
export const IPAMConfigList = /*@__PURE__*/ S.Array(
  IPAMConfig,
) as any as S.Schema<IPAMConfigList>;

/** Driver-specific options, specified as a map. */
export type IPAMOptionsMap = { [key: string]: string | undefined };
export const IPAMOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<IPAMOptionsMap>;

export interface IPAM {
  /** Name of the IPAM driver to use. */
  Driver?: string;
  /** List of IPAM configuration options, specified as a map: ``` {"Subnet": <CIDR>, "IPRange": <CIDR>, "Gateway": <IP address>, "AuxAddress": <device_name:IP address>} ``` */
  Config?: IPAMConfigList;
  /** Driver-specific options, specified as a map. */
  Options?: IPAMOptionsMap;
}
export const IPAM = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Driver: S.optional(S.String),
    Config: S.optional(IPAMConfigList),
    Options: S.optional(IPAMOptionsMap),
  }),
).annotate({ identifier: "IPAM" }) as any as S.Schema<IPAM>;

/** Network specific options to be used by the drivers. */
export type CreateNetworkRequestOptionsMap = {
  [key: string]: string | undefined;
};
export const CreateNetworkRequestOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateNetworkRequestOptionsMap>;

/** User-defined key/value metadata. */
export type CreateNetworkRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const CreateNetworkRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateNetworkRequestLabelsMap>;

export interface CreateNetworkRequest {
  /** The network's name. */
  Name: string;
  /** Name of the network driver plugin to use. */
  Driver?: string;
  /** The level at which the network exists (e.g. `swarm` for cluster-wide or `local` for machine level). */
  Scope?: string;
  /** Restrict external access to the network. */
  Internal?: boolean;
  /** Globally scoped network is manually attachable by regular containers from workers in swarm mode. */
  Attachable?: boolean;
  /** Ingress network is the network which provides the routing-mesh in swarm mode. */
  Ingress?: boolean;
  /** Creates a config-only network. Config-only networks are placeholder networks for network configurations to be used by other networks. Config-only networks cannot be used directly to run containers or services. */
  ConfigOnly?: boolean;
  /** Specifies the source which will provide the configuration for this network. The specified network must be an existing config-only network; see ConfigOnly. */
  ConfigFrom?: ConfigReference;
  /** Optional custom IP scheme for the network. */
  IPAM?: IPAM;
  /** Enable IPv4 on the network. */
  EnableIPv4?: boolean;
  /** Enable IPv6 on the network. */
  EnableIPv6?: boolean;
  /** Network specific options to be used by the drivers. */
  Options?: CreateNetworkRequestOptionsMap;
  /** User-defined key/value metadata. */
  Labels?: CreateNetworkRequestLabelsMap;
}
export const CreateNetworkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Driver: S.optional(S.String),
    Scope: S.optional(S.String),
    Internal: S.optional(S.Boolean),
    Attachable: S.optional(S.Boolean),
    Ingress: S.optional(S.Boolean),
    ConfigOnly: S.optional(S.Boolean),
    ConfigFrom: S.optional(ConfigReference),
    IPAM: S.optional(IPAM),
    EnableIPv4: S.optional(S.Boolean),
    EnableIPv6: S.optional(S.Boolean),
    Options: S.optional(CreateNetworkRequestOptionsMap),
    Labels: S.optional(CreateNetworkRequestLabelsMap),
  }).pipe(T.Http({ method: "POST", uri: "/networks/create", code: 200 })),
).annotate({
  identifier: "CreateNetworkRequest",
}) as any as S.Schema<CreateNetworkRequest>;

/** OK response to NetworkCreate operation */
export interface NetworkCreateResponse {
  /** The ID of the created network. */
  Id: string;
  /** Warnings encountered when creating the container */
  Warning: string;
}
export const NetworkCreateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.String,
    Warning: S.String,
  }),
).annotate({
  identifier: "NetworkCreateResponse",
}) as any as S.Schema<NetworkCreateResponse>;

export interface CreatePluginRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  name: string;
  body?: string;
}
export const CreatePluginRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Query()),
    body: S.optional(S.String.pipe(T.HttpBody())),
  }).pipe(T.Http({ method: "POST", uri: "/plugins/create", code: 200 })),
).annotate({
  identifier: "CreatePluginRequest",
}) as any as S.Schema<CreatePluginRequest>;

export interface CreatePluginResponse {}
export const CreatePluginResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "CreatePluginResponse",
}) as any as S.Schema<CreatePluginResponse>;

/** User-defined key/value metadata. */
export type CreateSecretRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const CreateSecretRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateSecretRequestLabelsMap>;

export interface CreateSecretRequest {
  /** User-defined name of the secret. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: CreateSecretRequestLabelsMap;
  /** Data is the data to store as a secret, formatted as a standard base64-encoded ([RFC 4648](https://tools.ietf.org/html/rfc4648#section-4)) string. It must be empty if the Driver field is set, in which case the data is loaded from an external secret store. The maximum allowed size is 500KB, as defined in [MaxSecretSize](https://pkg.go.dev/github.com/moby/swarmkit/v2@v2.0.0/api/validation#MaxSecretSize). This field is only used to _create_ a secret, and is not returned by other endpoints. */
  Data?: string;
  /** Name of the secrets driver used to fetch the secret's value from an external secret store. */
  Driver?: Driver;
  /** Templating driver, if applicable Templating controls whether and how to evaluate the config payload as a template. If no driver is set, no templating is used. */
  Templating?: Driver;
}
export const CreateSecretRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Labels: S.optional(CreateSecretRequestLabelsMap),
    Data: S.optional(S.String),
    Driver: S.optional(Driver),
    Templating: S.optional(Driver),
  }).pipe(T.Http({ method: "POST", uri: "/secrets/create", code: 200 })),
).annotate({
  identifier: "CreateSecretRequest",
}) as any as S.Schema<CreateSecretRequest>;

/** User-defined key/value metadata. */
export type CreateServiceRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const CreateServiceRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateServiceRequestLabelsMap>;

export type PluginPrivilegeValueList = Array<string>;
export const PluginPrivilegeValueList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginPrivilegeValueList>;

/** Describes a permission the user has to accept upon installing the plugin. */
export interface PluginPrivilege {
  Name?: string;
  Description?: string;
  Value?: PluginPrivilegeValueList;
}
export const PluginPrivilege = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Description: S.optional(S.String),
    Value: S.optional(PluginPrivilegeValueList),
  }),
).annotate({
  identifier: "PluginPrivilege",
}) as any as S.Schema<PluginPrivilege>;

export type TaskSpecPluginSpecPluginPrivilegeList = Array<PluginPrivilege>;
export const TaskSpecPluginSpecPluginPrivilegeList = /*@__PURE__*/ S.Array(
  PluginPrivilege,
) as any as S.Schema<TaskSpecPluginSpecPluginPrivilegeList>;

/** Plugin spec for the service. *(Experimental release only.)* <p><br /></p> > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are > mutually exclusive. PluginSpec is only used when the Runtime field > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime > field is set to `attachment`. */
export interface TaskSpecPluginSpec {
  /** The name or 'alias' to use for the plugin. */
  Name?: string;
  /** The plugin image reference to use. */
  Remote?: string;
  /** Disable the plugin once scheduled. */
  Disabled?: boolean;
  PluginPrivilege?: TaskSpecPluginSpecPluginPrivilegeList;
}
export const TaskSpecPluginSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Remote: S.optional(S.String),
    Disabled: S.optional(S.Boolean),
    PluginPrivilege: S.optional(TaskSpecPluginSpecPluginPrivilegeList),
  }),
).annotate({
  identifier: "TaskSpecPluginSpec",
}) as any as S.Schema<TaskSpecPluginSpec>;

/** User-defined key/value data. */
export type TaskSpecContainerSpecLabelsMap = {
  [key: string]: string | undefined;
};
export const TaskSpecContainerSpecLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<TaskSpecContainerSpecLabelsMap>;

/** The command to be run in the image. */
export type TaskSpecContainerSpecCommandList = Array<string>;
export const TaskSpecContainerSpecCommandList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecCommandList>;

/** Arguments to the command. */
export type TaskSpecContainerSpecArgsList = Array<string>;
export const TaskSpecContainerSpecArgsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecArgsList>;

/** A list of environment variables in the form `VAR=value`. */
export type TaskSpecContainerSpecEnvList = Array<string>;
export const TaskSpecContainerSpecEnvList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecEnvList>;

/** A list of additional groups that the container process will run as. */
export type TaskSpecContainerSpecGroupsList = Array<string>;
export const TaskSpecContainerSpecGroupsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecGroupsList>;

/** CredentialSpec for managed service account (Windows only) */
export interface TaskSpecContainerSpecPrivilegesCredentialSpec {
  /** Load credential spec from a Swarm Config with the given ID. The specified config must also be present in the Configs field with the Runtime property set. <p><br /></p> > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`, > and `CredentialSpec.Config` are mutually exclusive. */
  Config?: string;
  /** Load credential spec from this file. The file is read by the daemon, and must be present in the `CredentialSpecs` subdirectory in the docker data directory, which defaults to `C:\ProgramData\Docker\` on Windows. For example, specifying `spec.json` loads `C:\ProgramData\Docker\CredentialSpecs\spec.json`. <p><br /></p> > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`, > and `CredentialSpec.Config` are mutually exclusive. */
  File?: string;
  /** Load credential spec from this value in the Windows registry. The specified registry value must be located in: `HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Virtualization\Containers\CredentialSpecs` <p><br /></p> > **Note**: `CredentialSpec.File`, `CredentialSpec.Registry`, > and `CredentialSpec.Config` are mutually exclusive. */
  Registry?: string;
}
export const TaskSpecContainerSpecPrivilegesCredentialSpec =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      Config: S.optional(S.String),
      File: S.optional(S.String),
      Registry: S.optional(S.String),
    }),
  ).annotate({
    identifier: "TaskSpecContainerSpecPrivilegesCredentialSpec",
  }) as any as S.Schema<TaskSpecContainerSpecPrivilegesCredentialSpec>;

/** SELinux labels of the container */
export interface TaskSpecContainerSpecPrivilegesSELinuxContext {
  /** Disable SELinux */
  Disable?: boolean;
  /** SELinux user label */
  User?: string;
  /** SELinux role label */
  Role?: string;
  /** SELinux type label */
  Type?: string;
  /** SELinux level label */
  Level?: string;
}
export const TaskSpecContainerSpecPrivilegesSELinuxContext =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      Disable: S.optional(S.Boolean),
      User: S.optional(S.String),
      Role: S.optional(S.String),
      Type: S.optional(S.String),
      Level: S.optional(S.String),
    }),
  ).annotate({
    identifier: "TaskSpecContainerSpecPrivilegesSELinuxContext",
  }) as any as S.Schema<TaskSpecContainerSpecPrivilegesSELinuxContext>;

export type TaskSpecContainerSpecPrivilegesSeccompMode =
  | "default"
  | "unconfined"
  | "custom";
export const TaskSpecContainerSpecPrivilegesSeccompMode = S.String;

/** Options for configuring seccomp on the container */
export interface TaskSpecContainerSpecPrivilegesSeccomp {
  Mode?: TaskSpecContainerSpecPrivilegesSeccompMode | (string & {});
  /** The custom seccomp profile as a json object */
  Profile?: string;
}
export const TaskSpecContainerSpecPrivilegesSeccomp = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      Mode: S.optional(TaskSpecContainerSpecPrivilegesSeccompMode),
      Profile: S.optional(S.String),
    }),
).annotate({
  identifier: "TaskSpecContainerSpecPrivilegesSeccomp",
}) as any as S.Schema<TaskSpecContainerSpecPrivilegesSeccomp>;

export type TaskSpecContainerSpecPrivilegesAppArmorMode =
  | "default"
  | "disabled";
export const TaskSpecContainerSpecPrivilegesAppArmorMode = S.String;

/** Options for configuring AppArmor on the container */
export interface TaskSpecContainerSpecPrivilegesAppArmor {
  Mode?: TaskSpecContainerSpecPrivilegesAppArmorMode | (string & {});
}
export const TaskSpecContainerSpecPrivilegesAppArmor = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      Mode: S.optional(TaskSpecContainerSpecPrivilegesAppArmorMode),
    }),
).annotate({
  identifier: "TaskSpecContainerSpecPrivilegesAppArmor",
}) as any as S.Schema<TaskSpecContainerSpecPrivilegesAppArmor>;

/** Security options for the container */
export interface TaskSpecContainerSpecPrivileges {
  /** CredentialSpec for managed service account (Windows only) */
  CredentialSpec?: TaskSpecContainerSpecPrivilegesCredentialSpec;
  /** SELinux labels of the container */
  SELinuxContext?: TaskSpecContainerSpecPrivilegesSELinuxContext;
  /** Options for configuring seccomp on the container */
  Seccomp?: TaskSpecContainerSpecPrivilegesSeccomp;
  /** Options for configuring AppArmor on the container */
  AppArmor?: TaskSpecContainerSpecPrivilegesAppArmor;
  /** Configuration of the no_new_privs bit in the container */
  NoNewPrivileges?: boolean;
}
export const TaskSpecContainerSpecPrivileges = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    CredentialSpec: S.optional(TaskSpecContainerSpecPrivilegesCredentialSpec),
    SELinuxContext: S.optional(TaskSpecContainerSpecPrivilegesSELinuxContext),
    Seccomp: S.optional(TaskSpecContainerSpecPrivilegesSeccomp),
    AppArmor: S.optional(TaskSpecContainerSpecPrivilegesAppArmor),
    NoNewPrivileges: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "TaskSpecContainerSpecPrivileges",
}) as any as S.Schema<TaskSpecContainerSpecPrivileges>;

/** Specification for mounts to be added to containers created as part of the service. */
export type TaskSpecContainerSpecMountsList = Array<Mount>;
export const TaskSpecContainerSpecMountsList = /*@__PURE__*/ S.Array(
  Mount,
) as any as S.Schema<TaskSpecContainerSpecMountsList>;

/** A list of hostname/IP mappings to add to the container's `hosts` file. The format of extra hosts is specified in the [hosts(5)](http://man7.org/linux/man-pages/man5/hosts.5.html) man page: IP_address canonical_hostname [aliases...] */
export type TaskSpecContainerSpecHostsList = Array<string>;
export const TaskSpecContainerSpecHostsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecHostsList>;

/** The IP addresses of the name servers. */
export type TaskSpecContainerSpecDNSConfigNameserversList = Array<string>;
export const TaskSpecContainerSpecDNSConfigNameserversList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<TaskSpecContainerSpecDNSConfigNameserversList>;

/** A search list for host-name lookup. */
export type TaskSpecContainerSpecDNSConfigSearchList = Array<string>;
export const TaskSpecContainerSpecDNSConfigSearchList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecDNSConfigSearchList>;

/** A list of internal resolver variables to be modified (e.g., `debug`, `ndots:3`, etc.). */
export type TaskSpecContainerSpecDNSConfigOptionsList = Array<string>;
export const TaskSpecContainerSpecDNSConfigOptionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecDNSConfigOptionsList>;

/** Specification for DNS related configurations in resolver configuration file (`resolv.conf`). */
export interface TaskSpecContainerSpecDNSConfig {
  /** The IP addresses of the name servers. */
  Nameservers?: TaskSpecContainerSpecDNSConfigNameserversList;
  /** A search list for host-name lookup. */
  Search?: TaskSpecContainerSpecDNSConfigSearchList;
  /** A list of internal resolver variables to be modified (e.g., `debug`, `ndots:3`, etc.). */
  Options?: TaskSpecContainerSpecDNSConfigOptionsList;
}
export const TaskSpecContainerSpecDNSConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Nameservers: S.optional(TaskSpecContainerSpecDNSConfigNameserversList),
    Search: S.optional(TaskSpecContainerSpecDNSConfigSearchList),
    Options: S.optional(TaskSpecContainerSpecDNSConfigOptionsList),
  }),
).annotate({
  identifier: "TaskSpecContainerSpecDNSConfig",
}) as any as S.Schema<TaskSpecContainerSpecDNSConfig>;

/** File represents a specific target that is backed by a file. */
export interface TaskSpecContainerSpecSecretsItemFile {
  /** Name represents the final filename in the filesystem. */
  Name?: string;
  /** UID represents the file UID. */
  UID?: string;
  /** GID represents the file GID. */
  GID?: string;
  /** Mode represents the FileMode of the file. */
  Mode?: number;
}
export const TaskSpecContainerSpecSecretsItemFile = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      Name: S.optional(S.String),
      UID: S.optional(S.String),
      GID: S.optional(S.String),
      Mode: S.optional(S.Number),
    }),
).annotate({
  identifier: "TaskSpecContainerSpecSecretsItemFile",
}) as any as S.Schema<TaskSpecContainerSpecSecretsItemFile>;

export interface TaskSpecContainerSpecSecretsItem {
  /** File represents a specific target that is backed by a file. */
  File?: TaskSpecContainerSpecSecretsItemFile;
  /** SecretID represents the ID of the specific secret that we're referencing. */
  SecretID?: string;
  /** SecretName is the name of the secret that this references, but this is just provided for lookup/display purposes. The secret in the reference will be identified by its ID. */
  SecretName?: string;
}
export const TaskSpecContainerSpecSecretsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    File: S.optional(TaskSpecContainerSpecSecretsItemFile),
    SecretID: S.optional(S.String),
    SecretName: S.optional(S.String),
  }),
).annotate({
  identifier: "TaskSpecContainerSpecSecretsItem",
}) as any as S.Schema<TaskSpecContainerSpecSecretsItem>;

/** Secrets contains references to zero or more secrets that will be exposed to the service. */
export type TaskSpecContainerSpecSecretsList =
  Array<TaskSpecContainerSpecSecretsItem>;
export const TaskSpecContainerSpecSecretsList = /*@__PURE__*/ S.Array(
  TaskSpecContainerSpecSecretsItem,
) as any as S.Schema<TaskSpecContainerSpecSecretsList>;

/** File represents a specific target that is backed by a file. <p><br /><p> > **Note**: `Configs.File` and `Configs.Runtime` are mutually exclusive */
export type TaskSpecContainerSpecConfigsItemFile =
  TaskSpecContainerSpecSecretsItemFile;
export const TaskSpecContainerSpecConfigsItemFile =
  TaskSpecContainerSpecSecretsItemFile;

export interface TaskSpecContainerSpecConfigsItem {
  /** File represents a specific target that is backed by a file. <p><br /><p> > **Note**: `Configs.File` and `Configs.Runtime` are mutually exclusive */
  File?: TaskSpecContainerSpecSecretsItemFile;
  /** Runtime represents a target that is not mounted into the container but is used by the task <p><br /><p> > **Note**: `Configs.File` and `Configs.Runtime` are mutually > exclusive */
  Runtime?: unknown;
  /** ConfigID represents the ID of the specific config that we're referencing. */
  ConfigID?: string;
  /** ConfigName is the name of the config that this references, but this is just provided for lookup/display purposes. The config in the reference will be identified by its ID. */
  ConfigName?: string;
}
export const TaskSpecContainerSpecConfigsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    File: S.optional(TaskSpecContainerSpecSecretsItemFile),
    Runtime: S.optional(S.Unknown),
    ConfigID: S.optional(S.String),
    ConfigName: S.optional(S.String),
  }),
).annotate({
  identifier: "TaskSpecContainerSpecConfigsItem",
}) as any as S.Schema<TaskSpecContainerSpecConfigsItem>;

/** Configs contains references to zero or more configs that will be exposed to the service. */
export type TaskSpecContainerSpecConfigsList =
  Array<TaskSpecContainerSpecConfigsItem>;
export const TaskSpecContainerSpecConfigsList = /*@__PURE__*/ S.Array(
  TaskSpecContainerSpecConfigsItem,
) as any as S.Schema<TaskSpecContainerSpecConfigsList>;

/** Isolation technology of the containers running the service. (Windows only) */
export type TaskSpecContainerSpecIsolation =
  | "default"
  | "process"
  | "hyperv"
  | "";
export const TaskSpecContainerSpecIsolation = S.String;

/** Set kernel namedspaced parameters (sysctls) in the container. The Sysctls option on services accepts the same sysctls as the are supported on containers. Note that while the same sysctls are supported, no guarantees or checks are made about their suitability for a clustered environment, and it's up to the user to determine whether a given sysctl will work properly in a Service. */
export type TaskSpecContainerSpecSysctlsMap = {
  [key: string]: string | undefined;
};
export const TaskSpecContainerSpecSysctlsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<TaskSpecContainerSpecSysctlsMap>;

/** A list of kernel capabilities to add to the default set for the container. */
export type TaskSpecContainerSpecCapabilityAddList = Array<string>;
export const TaskSpecContainerSpecCapabilityAddList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecCapabilityAddList>;

/** A list of kernel capabilities to drop from the default set for the container. */
export type TaskSpecContainerSpecCapabilityDropList = Array<string>;
export const TaskSpecContainerSpecCapabilityDropList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecContainerSpecCapabilityDropList>;

export type TaskSpecContainerSpecUlimitsItem = HostConfigUlimitsItem;
export const TaskSpecContainerSpecUlimitsItem = HostConfigUlimitsItem;

/** A list of resource limits to set in the container. For example: `{"Name": "nofile", "Soft": 1024, "Hard": 2048}`" */
export type TaskSpecContainerSpecUlimitsList = Array<HostConfigUlimitsItem>;
export const TaskSpecContainerSpecUlimitsList = /*@__PURE__*/ S.Array(
  HostConfigUlimitsItem,
) as any as S.Schema<TaskSpecContainerSpecUlimitsList>;

/** Container spec for the service. <p><br /></p> > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are > mutually exclusive. PluginSpec is only used when the Runtime field > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime > field is set to `attachment`. */
export interface TaskSpecContainerSpec {
  /** The image name to use for the container */
  Image?: string;
  /** User-defined key/value data. */
  Labels?: TaskSpecContainerSpecLabelsMap;
  /** The command to be run in the image. */
  Command?: TaskSpecContainerSpecCommandList;
  /** Arguments to the command. */
  Args?: TaskSpecContainerSpecArgsList;
  /** The hostname to use for the container, as a valid [RFC 1123](https://tools.ietf.org/html/rfc1123) hostname. */
  Hostname?: string;
  /** A list of environment variables in the form `VAR=value`. */
  Env?: TaskSpecContainerSpecEnvList;
  /** The working directory for commands to run in. */
  Dir?: string;
  /** The user inside the container. */
  User?: string;
  /** A list of additional groups that the container process will run as. */
  Groups?: TaskSpecContainerSpecGroupsList;
  /** Security options for the container */
  Privileges?: TaskSpecContainerSpecPrivileges;
  /** Whether a pseudo-TTY should be allocated. */
  TTY?: boolean;
  /** Open `stdin` */
  OpenStdin?: boolean;
  /** Mount the container's root filesystem as read only. */
  ReadOnly?: boolean;
  /** Specification for mounts to be added to containers created as part of the service. */
  Mounts?: TaskSpecContainerSpecMountsList;
  /** Signal to stop the container. */
  StopSignal?: string;
  /** Amount of time to wait for the container to terminate before forcefully killing it. */
  StopGracePeriod?: number;
  HealthCheck?: HealthConfig;
  /** A list of hostname/IP mappings to add to the container's `hosts` file. The format of extra hosts is specified in the [hosts(5)](http://man7.org/linux/man-pages/man5/hosts.5.html) man page: IP_address canonical_hostname [aliases...] */
  Hosts?: TaskSpecContainerSpecHostsList;
  /** Specification for DNS related configurations in resolver configuration file (`resolv.conf`). */
  DNSConfig?: TaskSpecContainerSpecDNSConfig;
  /** Secrets contains references to zero or more secrets that will be exposed to the service. */
  Secrets?: TaskSpecContainerSpecSecretsList;
  /** An integer value containing the score given to the container in order to tune OOM killer preferences. */
  OomScoreAdj?: number;
  /** Configs contains references to zero or more configs that will be exposed to the service. */
  Configs?: TaskSpecContainerSpecConfigsList;
  /** Isolation technology of the containers running the service. (Windows only) */
  Isolation?: TaskSpecContainerSpecIsolation | (string & {});
  /** Run an init inside the container that forwards signals and reaps processes. This field is omitted if empty, and the default (as configured on the daemon) is used. */
  Init?: boolean | null;
  /** Set kernel namedspaced parameters (sysctls) in the container. The Sysctls option on services accepts the same sysctls as the are supported on containers. Note that while the same sysctls are supported, no guarantees or checks are made about their suitability for a clustered environment, and it's up to the user to determine whether a given sysctl will work properly in a Service. */
  Sysctls?: TaskSpecContainerSpecSysctlsMap;
  /** A list of kernel capabilities to add to the default set for the container. */
  CapabilityAdd?: TaskSpecContainerSpecCapabilityAddList;
  /** A list of kernel capabilities to drop from the default set for the container. */
  CapabilityDrop?: TaskSpecContainerSpecCapabilityDropList;
  /** A list of resource limits to set in the container. For example: `{"Name": "nofile", "Soft": 1024, "Hard": 2048}`" */
  Ulimits?: TaskSpecContainerSpecUlimitsList;
}
export const TaskSpecContainerSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Image: S.optional(S.String),
    Labels: S.optional(TaskSpecContainerSpecLabelsMap),
    Command: S.optional(TaskSpecContainerSpecCommandList),
    Args: S.optional(TaskSpecContainerSpecArgsList),
    Hostname: S.optional(S.String),
    Env: S.optional(TaskSpecContainerSpecEnvList),
    Dir: S.optional(S.String),
    User: S.optional(S.String),
    Groups: S.optional(TaskSpecContainerSpecGroupsList),
    Privileges: S.optional(TaskSpecContainerSpecPrivileges),
    TTY: S.optional(S.Boolean),
    OpenStdin: S.optional(S.Boolean),
    ReadOnly: S.optional(S.Boolean),
    Mounts: S.optional(TaskSpecContainerSpecMountsList),
    StopSignal: S.optional(S.String),
    StopGracePeriod: S.optional(S.Number),
    HealthCheck: S.optional(HealthConfig),
    Hosts: S.optional(TaskSpecContainerSpecHostsList),
    DNSConfig: S.optional(TaskSpecContainerSpecDNSConfig),
    Secrets: S.optional(TaskSpecContainerSpecSecretsList),
    OomScoreAdj: S.optional(S.Number),
    Configs: S.optional(TaskSpecContainerSpecConfigsList),
    Isolation: S.optional(TaskSpecContainerSpecIsolation),
    Init: S.optional(S.NullOr(S.Boolean)),
    Sysctls: S.optional(TaskSpecContainerSpecSysctlsMap),
    CapabilityAdd: S.optional(TaskSpecContainerSpecCapabilityAddList),
    CapabilityDrop: S.optional(TaskSpecContainerSpecCapabilityDropList),
    Ulimits: S.optional(TaskSpecContainerSpecUlimitsList),
  }),
).annotate({
  identifier: "TaskSpecContainerSpec",
}) as any as S.Schema<TaskSpecContainerSpec>;

/** Read-only spec type for non-swarm containers attached to swarm overlay networks. <p><br /></p> > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are > mutually exclusive. PluginSpec is only used when the Runtime field > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime > field is set to `attachment`. */
export interface TaskSpecNetworkAttachmentSpec {
  /** ID of the container represented by this task */
  ContainerID?: string;
}
export const TaskSpecNetworkAttachmentSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ContainerID: S.optional(S.String),
  }),
).annotate({
  identifier: "TaskSpecNetworkAttachmentSpec",
}) as any as S.Schema<TaskSpecNetworkAttachmentSpec>;

/** An object describing a limit on resources which can be requested by a task. */
export interface Limit {
  NanoCPUs?: number;
  MemoryBytes?: number;
  /** Limits the maximum number of PIDs in the container. Set `0` for unlimited. */
  Pids?: number;
}
export const Limit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NanoCPUs: S.optional(S.Number),
    MemoryBytes: S.optional(S.Number),
    Pids: S.optional(S.Number),
  }),
).annotate({ identifier: "Limit" }) as any as S.Schema<Limit>;

export interface GenericResourcesItemNamedResourceSpec {
  Kind?: string;
  Value?: string;
}
export const GenericResourcesItemNamedResourceSpec = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      Kind: S.optional(S.String),
      Value: S.optional(S.String),
    }),
).annotate({
  identifier: "GenericResourcesItemNamedResourceSpec",
}) as any as S.Schema<GenericResourcesItemNamedResourceSpec>;

export interface GenericResourcesItemDiscreteResourceSpec {
  Kind?: string;
  Value?: number;
}
export const GenericResourcesItemDiscreteResourceSpec = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      Kind: S.optional(S.String),
      Value: S.optional(S.Number),
    }),
).annotate({
  identifier: "GenericResourcesItemDiscreteResourceSpec",
}) as any as S.Schema<GenericResourcesItemDiscreteResourceSpec>;

export interface GenericResourcesItem {
  NamedResourceSpec?: GenericResourcesItemNamedResourceSpec;
  DiscreteResourceSpec?: GenericResourcesItemDiscreteResourceSpec;
}
export const GenericResourcesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NamedResourceSpec: S.optional(GenericResourcesItemNamedResourceSpec),
    DiscreteResourceSpec: S.optional(GenericResourcesItemDiscreteResourceSpec),
  }),
).annotate({
  identifier: "GenericResourcesItem",
}) as any as S.Schema<GenericResourcesItem>;

/** User-defined resources can be either Integer resources (e.g, `SSD=3`) or String resources (e.g, `GPU=UUID1`). */
export type GenericResources = Array<GenericResourcesItem>;
export const GenericResources = /*@__PURE__*/ S.Array(
  GenericResourcesItem,
) as any as S.Schema<GenericResources>;

/** An object describing the resources which can be advertised by a node and requested by a task. */
export interface ResourceObject {
  NanoCPUs?: number;
  MemoryBytes?: number;
  GenericResources?: GenericResources;
}
export const ResourceObject = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NanoCPUs: S.optional(S.Number),
    MemoryBytes: S.optional(S.Number),
    GenericResources: S.optional(GenericResources),
  }),
).annotate({ identifier: "ResourceObject" }) as any as S.Schema<ResourceObject>;

/** Resource requirements which apply to each individual container created as part of the service. */
export interface TaskSpecResources {
  /** Define resources limits. */
  Limits?: Limit;
  /** Define resources reservation. */
  Reservations?: ResourceObject;
  /** Amount of swap in bytes - can only be used together with a memory limit. If not specified, the default behaviour is to grant a swap space twice as big as the memory limit. Set to -1 to enable unlimited swap. */
  SwapBytes?: number | null;
  /** Tune the service's containers' memory swappiness (0 to 100). If not specified, defaults to the containers' OS' default, generally 60, or whatever value was predefined in the image. Set to -1 to unset a previously set value. */
  MemorySwappiness?: number | null;
}
export const TaskSpecResources = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Limits: S.optional(Limit),
    Reservations: S.optional(ResourceObject),
    SwapBytes: S.optional(S.NullOr(S.Number)),
    MemorySwappiness: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "TaskSpecResources",
}) as any as S.Schema<TaskSpecResources>;

/** Condition for restart. */
export type TaskSpecRestartPolicyCondition = "none" | "on-failure" | "any";
export const TaskSpecRestartPolicyCondition = S.String;

/** Specification for the restart policy which applies to containers created as part of this service. */
export interface TaskSpecRestartPolicy {
  /** Condition for restart. */
  Condition?: TaskSpecRestartPolicyCondition | (string & {});
  /** Delay between restart attempts. */
  Delay?: number;
  /** Maximum attempts to restart a given container before giving up (default value is 0, which is ignored). */
  MaxAttempts?: number;
  /** Windows is the time window used to evaluate the restart policy (default value is 0, which is unbounded). */
  Window?: number;
}
export const TaskSpecRestartPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Condition: S.optional(TaskSpecRestartPolicyCondition),
    Delay: S.optional(S.Number),
    MaxAttempts: S.optional(S.Number),
    Window: S.optional(S.Number),
  }),
).annotate({
  identifier: "TaskSpecRestartPolicy",
}) as any as S.Schema<TaskSpecRestartPolicy>;

/** An array of constraint expressions to limit the set of nodes where a task can be scheduled. Constraint expressions can either use a _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find nodes that satisfy every expression (AND match). Constraints can match node or Docker Engine labels as follows: node attribute | matches | example ---------------------|--------------------------------|----------------------------------------------- `node.id` | Node ID | `node.id==2ivku8v2gvtg4` `node.hostname` | Node hostname | `node.hostname!=node-2` `node.role` | Node role (`manager`/`worker`) | `node.role==manager` `node.platform.os` | Node operating system | `node.platform.os==windows` `node.platform.arch` | Node architecture | `node.platform.arch==x86_64` `node.labels` | User-defined node labels | `node.labels.security==high` `engine.labels` | Docker Engine's labels | `engine.labels.operatingsystem==ubuntu-24.04` `engine.labels` apply to Docker Engine labels like operating system, drivers, etc. Swarm administrators add `node.labels` for operational purposes by using the [`node update endpoint`](#operation/NodeUpdate). */
export type TaskSpecPlacementConstraintsList = Array<string>;
export const TaskSpecPlacementConstraintsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TaskSpecPlacementConstraintsList>;

export interface TaskSpecPlacementPreferencesItemSpread {
  /** label descriptor, such as `engine.labels.az`. */
  SpreadDescriptor?: string;
}
export const TaskSpecPlacementPreferencesItemSpread = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      SpreadDescriptor: S.optional(S.String),
    }),
).annotate({
  identifier: "TaskSpecPlacementPreferencesItemSpread",
}) as any as S.Schema<TaskSpecPlacementPreferencesItemSpread>;

export interface TaskSpecPlacementPreferencesItem {
  Spread?: TaskSpecPlacementPreferencesItemSpread;
}
export const TaskSpecPlacementPreferencesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Spread: S.optional(TaskSpecPlacementPreferencesItemSpread),
  }),
).annotate({
  identifier: "TaskSpecPlacementPreferencesItem",
}) as any as S.Schema<TaskSpecPlacementPreferencesItem>;

/** Preferences provide a way to make the scheduler aware of factors such as topology. They are provided in order from highest to lowest precedence. */
export type TaskSpecPlacementPreferencesList =
  Array<TaskSpecPlacementPreferencesItem>;
export const TaskSpecPlacementPreferencesList = /*@__PURE__*/ S.Array(
  TaskSpecPlacementPreferencesItem,
) as any as S.Schema<TaskSpecPlacementPreferencesList>;

/** Platform represents the platform (Arch/OS). */
export interface Platform {
  /** Architecture represents the hardware architecture (for example, `x86_64`). */
  Architecture?: string;
  /** OS represents the Operating System (for example, `linux` or `windows`). */
  OS?: string;
}
export const Platform = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Architecture: S.optional(S.String),
    OS: S.optional(S.String),
  }),
).annotate({ identifier: "Platform" }) as any as S.Schema<Platform>;

/** Platforms stores all the platforms that the service's image can run on. This field is used in the platform filter for scheduling. If empty, then the platform filter is off, meaning there are no scheduling restrictions. */
export type TaskSpecPlacementPlatformsList = Array<Platform>;
export const TaskSpecPlacementPlatformsList = /*@__PURE__*/ S.Array(
  Platform,
) as any as S.Schema<TaskSpecPlacementPlatformsList>;

export interface TaskSpecPlacement {
  /** An array of constraint expressions to limit the set of nodes where a task can be scheduled. Constraint expressions can either use a _match_ (`==`) or _exclude_ (`!=`) rule. Multiple constraints find nodes that satisfy every expression (AND match). Constraints can match node or Docker Engine labels as follows: node attribute | matches | example ---------------------|--------------------------------|----------------------------------------------- `node.id` | Node ID | `node.id==2ivku8v2gvtg4` `node.hostname` | Node hostname | `node.hostname!=node-2` `node.role` | Node role (`manager`/`worker`) | `node.role==manager` `node.platform.os` | Node operating system | `node.platform.os==windows` `node.platform.arch` | Node architecture | `node.platform.arch==x86_64` `node.labels` | User-defined node labels | `node.labels.security==high` `engine.labels` | Docker Engine's labels | `engine.labels.operatingsystem==ubuntu-24.04` `engine.labels` apply to Docker Engine labels like operating system, drivers, etc. Swarm administrators add `node.labels` for operational purposes by using the [`node update endpoint`](#operation/NodeUpdate). */
  Constraints?: TaskSpecPlacementConstraintsList;
  /** Preferences provide a way to make the scheduler aware of factors such as topology. They are provided in order from highest to lowest precedence. */
  Preferences?: TaskSpecPlacementPreferencesList;
  /** Maximum number of replicas for per node (default value is 0, which is unlimited) */
  MaxReplicas?: number;
  /** Platforms stores all the platforms that the service's image can run on. This field is used in the platform filter for scheduling. If empty, then the platform filter is off, meaning there are no scheduling restrictions. */
  Platforms?: TaskSpecPlacementPlatformsList;
}
export const TaskSpecPlacement = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Constraints: S.optional(TaskSpecPlacementConstraintsList),
    Preferences: S.optional(TaskSpecPlacementPreferencesList),
    MaxReplicas: S.optional(S.Number),
    Platforms: S.optional(TaskSpecPlacementPlatformsList),
  }),
).annotate({
  identifier: "TaskSpecPlacement",
}) as any as S.Schema<TaskSpecPlacement>;

/** Discoverable alternate names for the service on this network. */
export type NetworkAttachmentConfigAliasesList = Array<string>;
export const NetworkAttachmentConfigAliasesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<NetworkAttachmentConfigAliasesList>;

/** Driver attachment options for the network target. */
export type NetworkAttachmentConfigDriverOptsMap = {
  [key: string]: string | undefined;
};
export const NetworkAttachmentConfigDriverOptsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<NetworkAttachmentConfigDriverOptsMap>;

/** Specifies how a service should be attached to a particular network. */
export interface NetworkAttachmentConfig {
  /** The target network for attachment. Must be a network name or ID. */
  Target?: string;
  /** Discoverable alternate names for the service on this network. */
  Aliases?: NetworkAttachmentConfigAliasesList;
  /** Driver attachment options for the network target. */
  DriverOpts?: NetworkAttachmentConfigDriverOptsMap;
}
export const NetworkAttachmentConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Target: S.optional(S.String),
    Aliases: S.optional(NetworkAttachmentConfigAliasesList),
    DriverOpts: S.optional(NetworkAttachmentConfigDriverOptsMap),
  }),
).annotate({
  identifier: "NetworkAttachmentConfig",
}) as any as S.Schema<NetworkAttachmentConfig>;

/** Specifies which networks the service should attach to. */
export type TaskSpecNetworksList = Array<NetworkAttachmentConfig>;
export const TaskSpecNetworksList = /*@__PURE__*/ S.Array(
  NetworkAttachmentConfig,
) as any as S.Schema<TaskSpecNetworksList>;

export type TaskSpecLogDriverOptionsMap = { [key: string]: string | undefined };
export const TaskSpecLogDriverOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<TaskSpecLogDriverOptionsMap>;

/** Specifies the log driver to use for tasks created from this spec. If not present, the default one for the swarm will be used, finally falling back to the engine default if not specified. */
export interface TaskSpecLogDriver {
  Name?: string;
  Options?: TaskSpecLogDriverOptionsMap;
}
export const TaskSpecLogDriver = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Options: S.optional(TaskSpecLogDriverOptionsMap),
  }),
).annotate({
  identifier: "TaskSpecLogDriver",
}) as any as S.Schema<TaskSpecLogDriver>;

/** User modifiable task configuration. */
export interface TaskSpec {
  /** Plugin spec for the service. *(Experimental release only.)* <p><br /></p> > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are > mutually exclusive. PluginSpec is only used when the Runtime field > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime > field is set to `attachment`. */
  PluginSpec?: TaskSpecPluginSpec;
  /** Container spec for the service. <p><br /></p> > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are > mutually exclusive. PluginSpec is only used when the Runtime field > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime > field is set to `attachment`. */
  ContainerSpec?: TaskSpecContainerSpec;
  /** Read-only spec type for non-swarm containers attached to swarm overlay networks. <p><br /></p> > **Note**: ContainerSpec, NetworkAttachmentSpec, and PluginSpec are > mutually exclusive. PluginSpec is only used when the Runtime field > is set to `plugin`. NetworkAttachmentSpec is used when the Runtime > field is set to `attachment`. */
  NetworkAttachmentSpec?: TaskSpecNetworkAttachmentSpec;
  /** Resource requirements which apply to each individual container created as part of the service. */
  Resources?: TaskSpecResources;
  /** Specification for the restart policy which applies to containers created as part of this service. */
  RestartPolicy?: TaskSpecRestartPolicy;
  Placement?: TaskSpecPlacement;
  /** A counter that triggers an update even if no relevant parameters have been changed. */
  ForceUpdate?: number;
  /** Runtime is the type of runtime specified for the task executor. */
  Runtime?: string;
  /** Specifies which networks the service should attach to. */
  Networks?: TaskSpecNetworksList;
  /** Specifies the log driver to use for tasks created from this spec. If not present, the default one for the swarm will be used, finally falling back to the engine default if not specified. */
  LogDriver?: TaskSpecLogDriver;
}
export const TaskSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    PluginSpec: S.optional(TaskSpecPluginSpec),
    ContainerSpec: S.optional(TaskSpecContainerSpec),
    NetworkAttachmentSpec: S.optional(TaskSpecNetworkAttachmentSpec),
    Resources: S.optional(TaskSpecResources),
    RestartPolicy: S.optional(TaskSpecRestartPolicy),
    Placement: S.optional(TaskSpecPlacement),
    ForceUpdate: S.optional(S.Number),
    Runtime: S.optional(S.String),
    Networks: S.optional(TaskSpecNetworksList),
    LogDriver: S.optional(TaskSpecLogDriver),
  }),
).annotate({ identifier: "TaskSpec" }) as any as S.Schema<TaskSpec>;

export interface CreateServiceRequestModeReplicated {
  Replicas?: number;
}
export const CreateServiceRequestModeReplicated = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Replicas: S.optional(S.Number),
  }),
).annotate({
  identifier: "CreateServiceRequestModeReplicated",
}) as any as S.Schema<CreateServiceRequestModeReplicated>;

/** The mode used for services with a finite number of tasks that run to a completed state. */
export interface CreateServiceRequestModeReplicatedJob {
  /** The maximum number of replicas to run simultaneously. */
  MaxConcurrent?: number;
  /** The total number of replicas desired to reach the Completed state. If unset, will default to the value of `MaxConcurrent` */
  TotalCompletions?: number;
}
export const CreateServiceRequestModeReplicatedJob = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      MaxConcurrent: S.optional(S.Number),
      TotalCompletions: S.optional(S.Number),
    }),
).annotate({
  identifier: "CreateServiceRequestModeReplicatedJob",
}) as any as S.Schema<CreateServiceRequestModeReplicatedJob>;

/** Scheduling mode for the service. */
export interface CreateServiceRequestMode {
  Replicated?: CreateServiceRequestModeReplicated;
  Global?: unknown;
  /** The mode used for services with a finite number of tasks that run to a completed state. */
  ReplicatedJob?: CreateServiceRequestModeReplicatedJob;
  /** The mode used for services which run a task to the completed state on each valid node. */
  GlobalJob?: unknown;
}
export const CreateServiceRequestMode = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Replicated: S.optional(CreateServiceRequestModeReplicated),
    Global: S.optional(S.Unknown),
    ReplicatedJob: S.optional(CreateServiceRequestModeReplicatedJob),
    GlobalJob: S.optional(S.Unknown),
  }),
).annotate({
  identifier: "CreateServiceRequestMode",
}) as any as S.Schema<CreateServiceRequestMode>;

/** Action to take if an updated task fails to run, or stops running during the update. */
export type CreateServiceRequestUpdateConfigFailureAction =
  | "continue"
  | "pause"
  | "rollback";
export const CreateServiceRequestUpdateConfigFailureAction = S.String;

/** The order of operations when rolling out an updated task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
export type CreateServiceRequestUpdateConfigOrder =
  | "stop-first"
  | "start-first";
export const CreateServiceRequestUpdateConfigOrder = S.String;

/** Specification for the update strategy of the service. */
export interface CreateServiceRequestUpdateConfig {
  /** Maximum number of tasks to be updated in one iteration (0 means unlimited parallelism). */
  Parallelism?: number;
  /** Amount of time between updates, in nanoseconds. */
  Delay?: number;
  /** Action to take if an updated task fails to run, or stops running during the update. */
  FailureAction?: CreateServiceRequestUpdateConfigFailureAction | (string & {});
  /** Amount of time to monitor each updated task for failures, in nanoseconds. */
  Monitor?: number;
  /** The fraction of tasks that may fail during an update before the failure action is invoked, specified as a floating point number between 0 and 1. */
  MaxFailureRatio?: number;
  /** The order of operations when rolling out an updated task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
  Order?: CreateServiceRequestUpdateConfigOrder | (string & {});
}
export const CreateServiceRequestUpdateConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Parallelism: S.optional(S.Number),
    Delay: S.optional(S.Number),
    FailureAction: S.optional(CreateServiceRequestUpdateConfigFailureAction),
    Monitor: S.optional(S.Number),
    MaxFailureRatio: S.optional(S.Number),
    Order: S.optional(CreateServiceRequestUpdateConfigOrder),
  }),
).annotate({
  identifier: "CreateServiceRequestUpdateConfig",
}) as any as S.Schema<CreateServiceRequestUpdateConfig>;

/** Action to take if an rolled back task fails to run, or stops running during the rollback. */
export type CreateServiceRequestRollbackConfigFailureAction =
  | "continue"
  | "pause";
export const CreateServiceRequestRollbackConfigFailureAction = S.String;

/** The order of operations when rolling back a task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
export type CreateServiceRequestRollbackConfigOrder =
  | "stop-first"
  | "start-first";
export const CreateServiceRequestRollbackConfigOrder = S.String;

/** Specification for the rollback strategy of the service. */
export interface CreateServiceRequestRollbackConfig {
  /** Maximum number of tasks to be rolled back in one iteration (0 means unlimited parallelism). */
  Parallelism?: number;
  /** Amount of time between rollback iterations, in nanoseconds. */
  Delay?: number;
  /** Action to take if an rolled back task fails to run, or stops running during the rollback. */
  FailureAction?:
    | CreateServiceRequestRollbackConfigFailureAction
    | (string & {});
  /** Amount of time to monitor each rolled back task for failures, in nanoseconds. */
  Monitor?: number;
  /** The fraction of tasks that may fail during a rollback before the failure action is invoked, specified as a floating point number between 0 and 1. */
  MaxFailureRatio?: number;
  /** The order of operations when rolling back a task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
  Order?: CreateServiceRequestRollbackConfigOrder | (string & {});
}
export const CreateServiceRequestRollbackConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Parallelism: S.optional(S.Number),
    Delay: S.optional(S.Number),
    FailureAction: S.optional(CreateServiceRequestRollbackConfigFailureAction),
    Monitor: S.optional(S.Number),
    MaxFailureRatio: S.optional(S.Number),
    Order: S.optional(CreateServiceRequestRollbackConfigOrder),
  }),
).annotate({
  identifier: "CreateServiceRequestRollbackConfig",
}) as any as S.Schema<CreateServiceRequestRollbackConfig>;

/** Specifies which networks the service should attach to. Deprecated: This field is deprecated since v1.44. The Networks field in TaskSpec should be used instead. */
export type CreateServiceRequestNetworksList = Array<NetworkAttachmentConfig>;
export const CreateServiceRequestNetworksList = /*@__PURE__*/ S.Array(
  NetworkAttachmentConfig,
) as any as S.Schema<CreateServiceRequestNetworksList>;

/** The mode of resolution to use for internal load balancing between tasks. */
export type EndpointSpecMode = "vip" | "dnsrr";
export const EndpointSpecMode = S.String;

export type EndpointPortConfigProtocol = "tcp" | "udp" | "sctp";
export const EndpointPortConfigProtocol = S.String;

/** The mode in which port is published. <p><br /></p> - "ingress" makes the target port accessible on every node, regardless of whether there is a task for the service running on that node or not. - "host" bypasses the routing mesh and publish the port directly on the swarm node where that service is running. */
export type EndpointPortConfigPublishMode = "ingress" | "host";
export const EndpointPortConfigPublishMode = S.String;

export interface EndpointPortConfig {
  Name?: string;
  Protocol?: EndpointPortConfigProtocol | (string & {});
  /** The port inside the container. */
  TargetPort?: number;
  /** The port on the swarm hosts. */
  PublishedPort?: number;
  /** The mode in which port is published. <p><br /></p> - "ingress" makes the target port accessible on every node, regardless of whether there is a task for the service running on that node or not. - "host" bypasses the routing mesh and publish the port directly on the swarm node where that service is running. */
  PublishMode?: EndpointPortConfigPublishMode | (string & {});
}
export const EndpointPortConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Protocol: S.optional(EndpointPortConfigProtocol),
    TargetPort: S.optional(S.Number),
    PublishedPort: S.optional(S.Number),
    PublishMode: S.optional(EndpointPortConfigPublishMode),
  }),
).annotate({
  identifier: "EndpointPortConfig",
}) as any as S.Schema<EndpointPortConfig>;

/** List of exposed ports that this service is accessible on from the outside. Ports can only be provided if `vip` resolution mode is used. */
export type EndpointSpecPortsList = Array<EndpointPortConfig>;
export const EndpointSpecPortsList = /*@__PURE__*/ S.Array(
  EndpointPortConfig,
) as any as S.Schema<EndpointSpecPortsList>;

/** Properties that can be configured to access and load balance a service. */
export interface EndpointSpec {
  /** The mode of resolution to use for internal load balancing between tasks. */
  Mode?: EndpointSpecMode | (string & {});
  /** List of exposed ports that this service is accessible on from the outside. Ports can only be provided if `vip` resolution mode is used. */
  Ports?: EndpointSpecPortsList;
}
export const EndpointSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Mode: S.optional(EndpointSpecMode),
    Ports: S.optional(EndpointSpecPortsList),
  }),
).annotate({ identifier: "EndpointSpec" }) as any as S.Schema<EndpointSpec>;

export interface CreateServiceRequest {
  /** A base64url-encoded auth configuration for pulling from private registries. Refer to the [authentication section](#section/Authentication) for details. */
  xRegistryAuth?: string;
  /** Name of the service. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: CreateServiceRequestLabelsMap;
  TaskTemplate?: TaskSpec;
  /** Scheduling mode for the service. */
  Mode?: CreateServiceRequestMode;
  /** Specification for the update strategy of the service. */
  UpdateConfig?: CreateServiceRequestUpdateConfig;
  /** Specification for the rollback strategy of the service. */
  RollbackConfig?: CreateServiceRequestRollbackConfig;
  /** Specifies which networks the service should attach to. Deprecated: This field is deprecated since v1.44. The Networks field in TaskSpec should be used instead. */
  Networks?: CreateServiceRequestNetworksList;
  EndpointSpec?: EndpointSpec;
}
export const CreateServiceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    xRegistryAuth: S.optional(S.String.pipe(T.Header("X-Registry-Auth"))),
    Name: S.optional(S.String),
    Labels: S.optional(CreateServiceRequestLabelsMap),
    TaskTemplate: S.optional(TaskSpec),
    Mode: S.optional(CreateServiceRequestMode),
    UpdateConfig: S.optional(CreateServiceRequestUpdateConfig),
    RollbackConfig: S.optional(CreateServiceRequestRollbackConfig),
    Networks: S.optional(CreateServiceRequestNetworksList),
    EndpointSpec: S.optional(EndpointSpec),
  }).pipe(T.Http({ method: "POST", uri: "/services/create", code: 200 })),
).annotate({
  identifier: "CreateServiceRequest",
}) as any as S.Schema<CreateServiceRequest>;

/** Optional warning message. FIXME(thaJeztah): this should have "omitempty" in the generated type. */
export type ServiceCreateResponseWarningsList = Array<string>;
export const ServiceCreateResponseWarningsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ServiceCreateResponseWarningsList>;

/** contains the information returned to a client on the creation of a new service. */
export interface ServiceCreateResponse {
  /** The ID of the created service. */
  ID?: string;
  /** Optional warning message. FIXME(thaJeztah): this should have "omitempty" in the generated type. */
  Warnings?: ServiceCreateResponseWarningsList | null;
}
export const ServiceCreateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Warnings: S.optional(S.NullOr(ServiceCreateResponseWarningsList)),
  }),
).annotate({
  identifier: "ServiceCreateResponse",
}) as any as S.Schema<ServiceCreateResponse>;

/** A mapping of driver options and values. These options are passed directly to the driver and are driver specific. */
export type CreateVolumeRequestDriverOptsMap = {
  [key: string]: string | undefined;
};
export const CreateVolumeRequestDriverOptsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateVolumeRequestDriverOptsMap>;

/** User-defined key/value metadata. */
export type CreateVolumeRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const CreateVolumeRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateVolumeRequestLabelsMap>;

/** The set of nodes this volume can be used on at one time. - `single` The volume may only be scheduled to one node at a time. - `multi` the volume may be scheduled to any supported number of nodes at a time. */
export type ClusterVolumeSpecAccessModeScope = "single" | "multi";
export const ClusterVolumeSpecAccessModeScope = S.String;

/** The number and way that different tasks can use this volume at one time. - `none` The volume may only be used by one task at a time. - `readonly` The volume may be used by any number of tasks, but they all must mount the volume as readonly - `onewriter` The volume may be used by any number of tasks, but only one may mount it as read/write. - `all` The volume may have any number of readers and writers. */
export type ClusterVolumeSpecAccessModeSharing =
  | "none"
  | "readonly"
  | "onewriter"
  | "all";
export const ClusterVolumeSpecAccessModeSharing = S.String;

/** One cluster volume secret entry. Defines a key-value pair that is passed to the plugin. */
export interface ClusterVolumeSpecAccessModeSecretsItem {
  /** Key is the name of the key of the key-value pair passed to the plugin. */
  Key?: string;
  /** Secret is the swarm Secret object from which to read data. This can be a Secret name or ID. The Secret data is retrieved by swarm and used as the value of the key-value pair passed to the plugin. */
  Secret?: string | Redacted.Redacted<string>;
}
export const ClusterVolumeSpecAccessModeSecretsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      Key: S.optional(S.String),
      Secret: S.optional(S.String.pipe(T.SensitiveValue({}))),
    }),
).annotate({
  identifier: "ClusterVolumeSpecAccessModeSecretsItem",
}) as any as S.Schema<ClusterVolumeSpecAccessModeSecretsItem>;

/** Swarm Secrets that are passed to the CSI storage plugin when operating on this volume. */
export type ClusterVolumeSpecAccessModeSecretsList =
  Array<ClusterVolumeSpecAccessModeSecretsItem>;
export const ClusterVolumeSpecAccessModeSecretsList = /*@__PURE__*/ S.Array(
  ClusterVolumeSpecAccessModeSecretsItem,
) as any as S.Schema<ClusterVolumeSpecAccessModeSecretsList>;

export type TopologySegmentsMap = { [key: string]: string | undefined };
export const TopologySegmentsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<TopologySegmentsMap>;

/** A map of topological domains to topological segments. For in depth details, see documentation for the Topology object in the CSI specification. */
export interface Topology {
  Segments?: TopologySegmentsMap;
}
export const Topology = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Segments: S.optional(TopologySegmentsMap),
  }),
).annotate({ identifier: "Topology" }) as any as S.Schema<Topology>;

/** A list of required topologies, at least one of which the volume must be accessible from. */
export type ClusterVolumeSpecAccessModeAccessibilityRequirementsRequisiteList =
  Array<Topology>;
export const ClusterVolumeSpecAccessModeAccessibilityRequirementsRequisiteList =
  /*@__PURE__*/ S.Array(
    Topology,
  ) as any as S.Schema<ClusterVolumeSpecAccessModeAccessibilityRequirementsRequisiteList>;

/** A list of topologies that the volume should attempt to be provisioned in. */
export type ClusterVolumeSpecAccessModeAccessibilityRequirementsPreferredList =
  Array<Topology>;
export const ClusterVolumeSpecAccessModeAccessibilityRequirementsPreferredList =
  /*@__PURE__*/ S.Array(
    Topology,
  ) as any as S.Schema<ClusterVolumeSpecAccessModeAccessibilityRequirementsPreferredList>;

/** Requirements for the accessible topology of the volume. These fields are optional. For an in-depth description of what these fields mean, see the CSI specification. */
export interface ClusterVolumeSpecAccessModeAccessibilityRequirements {
  /** A list of required topologies, at least one of which the volume must be accessible from. */
  Requisite?: ClusterVolumeSpecAccessModeAccessibilityRequirementsRequisiteList;
  /** A list of topologies that the volume should attempt to be provisioned in. */
  Preferred?: ClusterVolumeSpecAccessModeAccessibilityRequirementsPreferredList;
}
export const ClusterVolumeSpecAccessModeAccessibilityRequirements =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      Requisite: S.optional(
        ClusterVolumeSpecAccessModeAccessibilityRequirementsRequisiteList,
      ),
      Preferred: S.optional(
        ClusterVolumeSpecAccessModeAccessibilityRequirementsPreferredList,
      ),
    }),
  ).annotate({
    identifier: "ClusterVolumeSpecAccessModeAccessibilityRequirements",
  }) as any as S.Schema<ClusterVolumeSpecAccessModeAccessibilityRequirements>;

/** The desired capacity that the volume should be created with. If empty, the plugin will decide the capacity. */
export interface ClusterVolumeSpecAccessModeCapacityRange {
  /** The volume must be at least this big. The value of 0 indicates an unspecified minimum */
  RequiredBytes?: number;
  /** The volume must not be bigger than this. The value of 0 indicates an unspecified maximum. */
  LimitBytes?: number;
}
export const ClusterVolumeSpecAccessModeCapacityRange = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      RequiredBytes: S.optional(S.Number),
      LimitBytes: S.optional(S.Number),
    }),
).annotate({
  identifier: "ClusterVolumeSpecAccessModeCapacityRange",
}) as any as S.Schema<ClusterVolumeSpecAccessModeCapacityRange>;

/** The availability of the volume for use in tasks. - `active` The volume is fully available for scheduling on the cluster - `pause` No new workloads should use the volume, but existing workloads are not stopped. - `drain` All workloads using this volume should be stopped and rescheduled, and no new ones should be started. */
export type ClusterVolumeSpecAccessModeAvailability =
  | "active"
  | "pause"
  | "drain";
export const ClusterVolumeSpecAccessModeAvailability = S.String;

/** Defines how the volume is used by tasks. */
export interface ClusterVolumeSpecAccessMode {
  /** The set of nodes this volume can be used on at one time. - `single` The volume may only be scheduled to one node at a time. - `multi` the volume may be scheduled to any supported number of nodes at a time. */
  Scope?: ClusterVolumeSpecAccessModeScope | (string & {});
  /** The number and way that different tasks can use this volume at one time. - `none` The volume may only be used by one task at a time. - `readonly` The volume may be used by any number of tasks, but they all must mount the volume as readonly - `onewriter` The volume may be used by any number of tasks, but only one may mount it as read/write. - `all` The volume may have any number of readers and writers. */
  Sharing?: ClusterVolumeSpecAccessModeSharing | (string & {});
  /** Options for using this volume as a Mount-type volume. Either MountVolume or BlockVolume, but not both, must be present. properties: FsType: type: "string" description: | Specifies the filesystem type for the mount volume. Optional. MountFlags: type: "array" description: | Flags to pass when mounting the volume. Optional. items: type: "string" BlockVolume: type: "object" description: | Options for using this volume as a Block-type volume. Intentionally empty. */
  MountVolume?: unknown;
  /** Swarm Secrets that are passed to the CSI storage plugin when operating on this volume. */
  Secrets?: ClusterVolumeSpecAccessModeSecretsList;
  /** Requirements for the accessible topology of the volume. These fields are optional. For an in-depth description of what these fields mean, see the CSI specification. */
  AccessibilityRequirements?: ClusterVolumeSpecAccessModeAccessibilityRequirements;
  /** The desired capacity that the volume should be created with. If empty, the plugin will decide the capacity. */
  CapacityRange?: ClusterVolumeSpecAccessModeCapacityRange;
  /** The availability of the volume for use in tasks. - `active` The volume is fully available for scheduling on the cluster - `pause` No new workloads should use the volume, but existing workloads are not stopped. - `drain` All workloads using this volume should be stopped and rescheduled, and no new ones should be started. */
  Availability?: ClusterVolumeSpecAccessModeAvailability | (string & {});
}
export const ClusterVolumeSpecAccessMode = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Scope: S.optional(ClusterVolumeSpecAccessModeScope),
    Sharing: S.optional(ClusterVolumeSpecAccessModeSharing),
    MountVolume: S.optional(S.Unknown),
    Secrets: S.optional(ClusterVolumeSpecAccessModeSecretsList),
    AccessibilityRequirements: S.optional(
      ClusterVolumeSpecAccessModeAccessibilityRequirements,
    ),
    CapacityRange: S.optional(ClusterVolumeSpecAccessModeCapacityRange),
    Availability: S.optional(ClusterVolumeSpecAccessModeAvailability),
  }),
).annotate({
  identifier: "ClusterVolumeSpecAccessMode",
}) as any as S.Schema<ClusterVolumeSpecAccessMode>;

/** Cluster-specific options used to create the volume. */
export interface ClusterVolumeSpec {
  /** Group defines the volume group of this volume. Volumes belonging to the same group can be referred to by group name when creating Services. Referring to a volume by group instructs Swarm to treat volumes in that group interchangeably for the purpose of scheduling. Volumes with an empty string for a group technically all belong to the same, emptystring group. */
  Group?: string;
  /** Defines how the volume is used by tasks. */
  AccessMode?: ClusterVolumeSpecAccessMode;
}
export const ClusterVolumeSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Group: S.optional(S.String),
    AccessMode: S.optional(ClusterVolumeSpecAccessMode),
  }),
).annotate({
  identifier: "ClusterVolumeSpec",
}) as any as S.Schema<ClusterVolumeSpec>;

export interface CreateVolumeRequest {
  /** The new volume's name. If not specified, Docker generates a name. */
  Name?: string;
  /** Name of the volume driver to use. */
  Driver?: string;
  /** A mapping of driver options and values. These options are passed directly to the driver and are driver specific. */
  DriverOpts?: CreateVolumeRequestDriverOptsMap;
  /** User-defined key/value metadata. */
  Labels?: CreateVolumeRequestLabelsMap;
  ClusterVolumeSpec?: ClusterVolumeSpec;
}
export const CreateVolumeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Driver: S.optional(S.String),
    DriverOpts: S.optional(CreateVolumeRequestDriverOptsMap),
    Labels: S.optional(CreateVolumeRequestLabelsMap),
    ClusterVolumeSpec: S.optional(ClusterVolumeSpec),
  }).pipe(T.Http({ method: "POST", uri: "/volumes/create", code: 200 })),
).annotate({
  identifier: "CreateVolumeRequest",
}) as any as S.Schema<CreateVolumeRequest>;

/** Low-level details about the volume, provided by the volume driver. Details are returned as a map with key/value pairs: `{"key":"value","key2":"value2"}`. The `Status` field is optional, and is omitted if the volume driver does not support this feature. */
export type VolumeStatusMap = { [key: string]: unknown | undefined };
export const VolumeStatusMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<VolumeStatusMap>;

/** User-defined key/value metadata. */
export type VolumeLabelsMap = { [key: string]: string | undefined };
export const VolumeLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<VolumeLabelsMap>;

/** The level at which the volume exists. Either `global` for cluster-wide, or `local` for machine level. */
export type VolumeScope = "local" | "global";
export const VolumeScope = S.String;

/** A map of strings to strings returned from the storage plugin when the volume is created. */
export type ClusterVolumeInfoVolumeContextMap = {
  [key: string]: string | undefined;
};
export const ClusterVolumeInfoVolumeContextMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ClusterVolumeInfoVolumeContextMap>;

/** The topology this volume is actually accessible from. */
export type ClusterVolumeInfoAccessibleTopologyList = Array<Topology>;
export const ClusterVolumeInfoAccessibleTopologyList = /*@__PURE__*/ S.Array(
  Topology,
) as any as S.Schema<ClusterVolumeInfoAccessibleTopologyList>;

/** Information about the global status of the volume. */
export interface ClusterVolumeInfo {
  /** The capacity of the volume in bytes. A value of 0 indicates that the capacity is unknown. */
  CapacityBytes?: number;
  /** A map of strings to strings returned from the storage plugin when the volume is created. */
  VolumeContext?: ClusterVolumeInfoVolumeContextMap;
  /** The ID of the volume as returned by the CSI storage plugin. This is distinct from the volume's ID as provided by Docker. This ID is never used by the user when communicating with Docker to refer to this volume. If the ID is blank, then the Volume has not been successfully created in the plugin yet. */
  VolumeID?: string;
  /** The topology this volume is actually accessible from. */
  AccessibleTopology?: ClusterVolumeInfoAccessibleTopologyList;
}
export const ClusterVolumeInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    CapacityBytes: S.optional(S.Number),
    VolumeContext: S.optional(ClusterVolumeInfoVolumeContextMap),
    VolumeID: S.optional(S.String),
    AccessibleTopology: S.optional(ClusterVolumeInfoAccessibleTopologyList),
  }),
).annotate({
  identifier: "ClusterVolumeInfo",
}) as any as S.Schema<ClusterVolumeInfo>;

/** The published state of the volume. * `pending-publish` The volume should be published to this node, but the call to the controller plugin to do so has not yet been successfully completed. * `published` The volume is published successfully to the node. * `pending-node-unpublish` The volume should be unpublished from the node, and the manager is awaiting confirmation from the worker that it has done so. * `pending-controller-unpublish` The volume is successfully unpublished from the node, but has not yet been successfully unpublished on the controller. */
export type ClusterVolumePublishStatusItemState =
  | "pending-publish"
  | "published"
  | "pending-node-unpublish"
  | "pending-controller-unpublish";
export const ClusterVolumePublishStatusItemState = S.String;

/** A map of strings to strings returned by the CSI controller plugin when a volume is published. */
export type ClusterVolumePublishStatusItemPublishContextMap = {
  [key: string]: string | undefined;
};
export const ClusterVolumePublishStatusItemPublishContextMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<ClusterVolumePublishStatusItemPublishContextMap>;

export interface ClusterVolumePublishStatusItem {
  /** The ID of the Swarm node the volume is published on. */
  NodeID?: string;
  /** The published state of the volume. * `pending-publish` The volume should be published to this node, but the call to the controller plugin to do so has not yet been successfully completed. * `published` The volume is published successfully to the node. * `pending-node-unpublish` The volume should be unpublished from the node, and the manager is awaiting confirmation from the worker that it has done so. * `pending-controller-unpublish` The volume is successfully unpublished from the node, but has not yet been successfully unpublished on the controller. */
  State?: ClusterVolumePublishStatusItemState;
  /** A map of strings to strings returned by the CSI controller plugin when a volume is published. */
  PublishContext?: ClusterVolumePublishStatusItemPublishContextMap;
}
export const ClusterVolumePublishStatusItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NodeID: S.optional(S.String),
    State: S.optional(ClusterVolumePublishStatusItemState),
    PublishContext: S.optional(ClusterVolumePublishStatusItemPublishContextMap),
  }),
).annotate({
  identifier: "ClusterVolumePublishStatusItem",
}) as any as S.Schema<ClusterVolumePublishStatusItem>;

/** The status of the volume as it pertains to its publishing and use on specific nodes */
export type ClusterVolumePublishStatusList =
  Array<ClusterVolumePublishStatusItem>;
export const ClusterVolumePublishStatusList = /*@__PURE__*/ S.Array(
  ClusterVolumePublishStatusItem,
) as any as S.Schema<ClusterVolumePublishStatusList>;

/** Options and information specific to, and only present on, Swarm CSI cluster volumes. */
export interface ClusterVolume {
  /** The Swarm ID of this volume. Because cluster volumes are Swarm objects, they have an ID, unlike non-cluster volumes. This ID can be used to refer to the Volume instead of the name. */
  ID?: string;
  Version?: ObjectVersion;
  CreatedAt?: string;
  UpdatedAt?: string;
  Spec?: ClusterVolumeSpec;
  /** Information about the global status of the volume. */
  Info?: ClusterVolumeInfo;
  /** The status of the volume as it pertains to its publishing and use on specific nodes */
  PublishStatus?: ClusterVolumePublishStatusList;
}
export const ClusterVolume = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Version: S.optional(ObjectVersion),
    CreatedAt: S.optional(S.String),
    UpdatedAt: S.optional(S.String),
    Spec: S.optional(ClusterVolumeSpec),
    Info: S.optional(ClusterVolumeInfo),
    PublishStatus: S.optional(ClusterVolumePublishStatusList),
  }),
).annotate({ identifier: "ClusterVolume" }) as any as S.Schema<ClusterVolume>;

/** The driver specific options used when creating the volume. */
export type VolumeOptionsMap = { [key: string]: string | undefined };
export const VolumeOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<VolumeOptionsMap>;

/** Usage details about the volume. This information is used by the `GET /system/df` endpoint, and omitted in other endpoints. */
export interface VolumeUsageData {
  /** Amount of disk space used by the volume (in bytes). This information is only available for volumes created with the `"local"` volume driver. For volumes created with other volume drivers, this field is set to `-1` ("not available") */
  Size: number;
  /** The number of containers referencing this volume. This field is set to `-1` if the reference-count is not available. */
  RefCount: number;
}
export const VolumeUsageData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Size: S.Number,
    RefCount: S.Number,
  }),
).annotate({
  identifier: "VolumeUsageData",
}) as any as S.Schema<VolumeUsageData>;

export interface Volume {
  /** Name of the volume. */
  Name: string;
  /** Name of the volume driver used by the volume. */
  Driver: string;
  /** Mount path of the volume on the host. */
  Mountpoint: string;
  /** Date/Time the volume was created. */
  CreatedAt?: string;
  /** Low-level details about the volume, provided by the volume driver. Details are returned as a map with key/value pairs: `{"key":"value","key2":"value2"}`. The `Status` field is optional, and is omitted if the volume driver does not support this feature. */
  Status?: VolumeStatusMap;
  /** User-defined key/value metadata. */
  Labels: VolumeLabelsMap;
  /** The level at which the volume exists. Either `global` for cluster-wide, or `local` for machine level. */
  Scope: VolumeScope;
  ClusterVolume?: ClusterVolume;
  /** The driver specific options used when creating the volume. */
  Options: VolumeOptionsMap;
  /** Usage details about the volume. This information is used by the `GET /system/df` endpoint, and omitted in other endpoints. */
  UsageData?: VolumeUsageData | null;
}
export const Volume = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Driver: S.String,
    Mountpoint: S.String,
    CreatedAt: S.optional(S.String),
    Status: S.optional(VolumeStatusMap),
    Labels: VolumeLabelsMap,
    Scope: VolumeScope,
    ClusterVolume: S.optional(ClusterVolume),
    Options: VolumeOptionsMap,
    UsageData: S.optional(S.NullOr(VolumeUsageData)),
  }),
).annotate({ identifier: "Volume" }) as any as S.Schema<Volume>;

export interface DeleteConfigRequest {
  /** ID of the config */
  id: string;
}
export const DeleteConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/configs/{id}", code: 200 })),
).annotate({
  identifier: "DeleteConfigRequest",
}) as any as S.Schema<DeleteConfigRequest>;

export interface DeleteConfigResponse {}
export const DeleteConfigResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteConfigResponse",
}) as any as S.Schema<DeleteConfigResponse>;

export interface DeleteContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Remove anonymous volumes associated with the container. */
  v?: boolean;
  /** If the container is running, kill it before removing it. */
  force?: boolean;
  /** Remove the specified link associated with the container. */
  link?: boolean;
}
export const DeleteContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    v: S.optional(S.Boolean.pipe(T.Query())),
    force: S.optional(S.Boolean.pipe(T.Query())),
    link: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "DELETE", uri: "/containers/{id}", code: 200 })),
).annotate({
  identifier: "DeleteContainerRequest",
}) as any as S.Schema<DeleteContainerRequest>;

export interface DeleteContainerResponse {}
export const DeleteContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteContainerResponse",
}) as any as S.Schema<DeleteContainerResponse>;

export type DeleteImageRequestPlatformsList = Array<string>;
export const DeleteImageRequestPlatformsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeleteImageRequestPlatformsList>;

export interface DeleteImageRequest {
  /** Image name or ID */
  name: string;
  /** Remove the image even if it is being used by stopped containers or has other tags */
  force?: boolean;
  /** Do not delete untagged parent images */
  noprune?: boolean;
  /** Select platform-specific content to delete. Multiple values are accepted. Each platform is a OCI platform encoded as a JSON string. */
  platforms?: DeleteImageRequestPlatformsList;
}
export const DeleteImageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    force: S.optional(S.Boolean.pipe(T.Query())),
    noprune: S.optional(S.Boolean.pipe(T.Query())),
    platforms: S.optional(DeleteImageRequestPlatformsList.pipe(T.Query())),
  }).pipe(T.Http({ method: "DELETE", uri: "/images/{name}", code: 200 })),
).annotate({
  identifier: "DeleteImageRequest",
}) as any as S.Schema<DeleteImageRequest>;

export interface ImageDeleteResponseItem {
  /** The image ID of an image that was untagged */
  Untagged?: string;
  /** The image ID of an image that was deleted */
  Deleted?: string;
}
export const ImageDeleteResponseItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Untagged: S.optional(S.String),
    Deleted: S.optional(S.String),
  }),
).annotate({
  identifier: "ImageDeleteResponseItem",
}) as any as S.Schema<ImageDeleteResponseItem>;

export type DeleteImageResponseBodyList = Array<ImageDeleteResponseItem>;
export const DeleteImageResponseBodyList = /*@__PURE__*/ S.Array(
  ImageDeleteResponseItem,
) as any as S.Schema<DeleteImageResponseBodyList>;

export type DeleteImageResponse = DeleteImageResponseBodyList;
export const DeleteImageResponse = /*@__PURE__*/ S.suspend(() =>
  DeleteImageResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteImageResponse",
}) as any as S.Schema<DeleteImageResponse>;

export interface DeleteNetworkRequest {
  /** Network ID or name */
  id: string;
}
export const DeleteNetworkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/networks/{id}", code: 200 })),
).annotate({
  identifier: "DeleteNetworkRequest",
}) as any as S.Schema<DeleteNetworkRequest>;

export interface DeleteNetworkResponse {}
export const DeleteNetworkResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteNetworkResponse",
}) as any as S.Schema<DeleteNetworkResponse>;

export interface DeleteNodeRequest {
  /** The ID or name of the node */
  id: string;
  /** Force remove a node from the swarm */
  force?: boolean;
}
export const DeleteNodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "DELETE", uri: "/nodes/{id}", code: 200 })),
).annotate({
  identifier: "DeleteNodeRequest",
}) as any as S.Schema<DeleteNodeRequest>;

export interface DeleteNodeResponse {}
export const DeleteNodeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteNodeResponse",
}) as any as S.Schema<DeleteNodeResponse>;

export interface DeletePluginRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  name: string;
  /** Disable the plugin before removing. This may result in issues if the plugin is in use by a container. */
  force?: boolean;
}
export const DeletePluginRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "DELETE", uri: "/plugins/{name}", code: 200 })),
).annotate({
  identifier: "DeletePluginRequest",
}) as any as S.Schema<DeletePluginRequest>;

export type PluginMountSettableList = Array<string>;
export const PluginMountSettableList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginMountSettableList>;

export type PluginMountOptionsList = Array<string>;
export const PluginMountOptionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginMountOptionsList>;

export interface PluginMount {
  Name: string;
  Description: string;
  Settable: PluginMountSettableList;
  Source: string;
  Destination: string;
  Type: string;
  Options: PluginMountOptionsList;
}
export const PluginMount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Description: S.String,
    Settable: PluginMountSettableList,
    Source: S.String,
    Destination: S.String,
    Type: S.String,
    Options: PluginMountOptionsList,
  }),
).annotate({ identifier: "PluginMount" }) as any as S.Schema<PluginMount>;

export type PluginSettingsMountsList = Array<PluginMount>;
export const PluginSettingsMountsList = /*@__PURE__*/ S.Array(
  PluginMount,
) as any as S.Schema<PluginSettingsMountsList>;

export type PluginSettingsEnvList = Array<string>;
export const PluginSettingsEnvList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginSettingsEnvList>;

export type PluginSettingsArgsList = Array<string>;
export const PluginSettingsArgsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginSettingsArgsList>;

export type PluginDeviceSettableList = Array<string>;
export const PluginDeviceSettableList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginDeviceSettableList>;

export interface PluginDevice {
  Name: string;
  Description: string;
  Settable: PluginDeviceSettableList;
  Path: string;
}
export const PluginDevice = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Description: S.String,
    Settable: PluginDeviceSettableList,
    Path: S.String,
  }),
).annotate({ identifier: "PluginDevice" }) as any as S.Schema<PluginDevice>;

export type PluginSettingsDevicesList = Array<PluginDevice>;
export const PluginSettingsDevicesList = /*@__PURE__*/ S.Array(
  PluginDevice,
) as any as S.Schema<PluginSettingsDevicesList>;

/** user-configurable settings for the plugin. */
export interface PluginSettings {
  Mounts: PluginSettingsMountsList;
  Env: PluginSettingsEnvList;
  Args: PluginSettingsArgsList;
  Devices: PluginSettingsDevicesList;
}
export const PluginSettings = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Mounts: PluginSettingsMountsList,
    Env: PluginSettingsEnvList,
    Args: PluginSettingsArgsList,
    Devices: PluginSettingsDevicesList,
  }),
).annotate({ identifier: "PluginSettings" }) as any as S.Schema<PluginSettings>;

export type PluginConfigInterfaceTypesList = Array<string>;
export const PluginConfigInterfaceTypesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginConfigInterfaceTypesList>;

/** Protocol to use for clients connecting to the plugin. */
export type PluginConfigInterfaceProtocolScheme = "" | "moby.plugins.http/v1";
export const PluginConfigInterfaceProtocolScheme = S.String;

/** The interface between Docker and the plugin */
export interface PluginConfigInterface {
  Types: PluginConfigInterfaceTypesList;
  Socket: string;
  /** Protocol to use for clients connecting to the plugin. */
  ProtocolScheme?: PluginConfigInterfaceProtocolScheme;
}
export const PluginConfigInterface = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Types: PluginConfigInterfaceTypesList,
    Socket: S.String,
    ProtocolScheme: S.optional(PluginConfigInterfaceProtocolScheme),
  }),
).annotate({
  identifier: "PluginConfigInterface",
}) as any as S.Schema<PluginConfigInterface>;

export type PluginConfigEntrypointList = Array<string>;
export const PluginConfigEntrypointList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginConfigEntrypointList>;

export interface PluginConfigUser {
  UID?: number;
  GID?: number;
}
export const PluginConfigUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    UID: S.optional(S.Number),
    GID: S.optional(S.Number),
  }),
).annotate({
  identifier: "PluginConfigUser",
}) as any as S.Schema<PluginConfigUser>;

export interface PluginConfigNetwork {
  Type: string;
}
export const PluginConfigNetwork = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Type: S.String,
  }),
).annotate({
  identifier: "PluginConfigNetwork",
}) as any as S.Schema<PluginConfigNetwork>;

export type PluginConfigLinuxCapabilitiesList = Array<string>;
export const PluginConfigLinuxCapabilitiesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginConfigLinuxCapabilitiesList>;

export type PluginConfigLinuxDevicesList = Array<PluginDevice>;
export const PluginConfigLinuxDevicesList = /*@__PURE__*/ S.Array(
  PluginDevice,
) as any as S.Schema<PluginConfigLinuxDevicesList>;

export interface PluginConfigLinux {
  Capabilities: PluginConfigLinuxCapabilitiesList;
  AllowAllDevices: boolean;
  Devices: PluginConfigLinuxDevicesList;
}
export const PluginConfigLinux = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Capabilities: PluginConfigLinuxCapabilitiesList,
    AllowAllDevices: S.Boolean,
    Devices: PluginConfigLinuxDevicesList,
  }),
).annotate({
  identifier: "PluginConfigLinux",
}) as any as S.Schema<PluginConfigLinux>;

export type PluginConfigMountsList = Array<PluginMount>;
export const PluginConfigMountsList = /*@__PURE__*/ S.Array(
  PluginMount,
) as any as S.Schema<PluginConfigMountsList>;

export type PluginEnvSettableList = Array<string>;
export const PluginEnvSettableList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginEnvSettableList>;

export interface PluginEnv {
  Name: string;
  Description: string;
  Settable: PluginEnvSettableList;
  Value: string;
}
export const PluginEnv = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Description: S.String,
    Settable: PluginEnvSettableList,
    Value: S.String,
  }),
).annotate({ identifier: "PluginEnv" }) as any as S.Schema<PluginEnv>;

export type PluginConfigEnvList = Array<PluginEnv>;
export const PluginConfigEnvList = /*@__PURE__*/ S.Array(
  PluginEnv,
) as any as S.Schema<PluginConfigEnvList>;

export type PluginConfigArgsSettableList = Array<string>;
export const PluginConfigArgsSettableList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginConfigArgsSettableList>;

export type PluginConfigArgsValueList = Array<string>;
export const PluginConfigArgsValueList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginConfigArgsValueList>;

export interface PluginConfigArgs {
  Name: string;
  Description: string;
  Settable: PluginConfigArgsSettableList;
  Value: PluginConfigArgsValueList;
}
export const PluginConfigArgs = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Description: S.String,
    Settable: PluginConfigArgsSettableList,
    Value: PluginConfigArgsValueList,
  }),
).annotate({
  identifier: "PluginConfigArgs",
}) as any as S.Schema<PluginConfigArgs>;

export type PluginConfigRootfsDiffIdsList = Array<string>;
export const PluginConfigRootfsDiffIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginConfigRootfsDiffIdsList>;

export interface PluginConfigRootfs {
  type?: string;
  diff_ids?: PluginConfigRootfsDiffIdsList;
}
export const PluginConfigRootfs = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(S.String),
    diff_ids: S.optional(PluginConfigRootfsDiffIdsList),
  }),
).annotate({
  identifier: "PluginConfigRootfs",
}) as any as S.Schema<PluginConfigRootfs>;

/** The config of a plugin. */
export interface PluginConfig {
  Description: string;
  Documentation: string;
  /** The interface between Docker and the plugin */
  Interface: PluginConfigInterface;
  Entrypoint: PluginConfigEntrypointList;
  WorkDir: string;
  User?: PluginConfigUser;
  Network: PluginConfigNetwork;
  Linux: PluginConfigLinux;
  PropagatedMount: string;
  IpcHost: boolean;
  PidHost: boolean;
  Mounts: PluginConfigMountsList;
  Env: PluginConfigEnvList;
  Args: PluginConfigArgs;
  rootfs?: PluginConfigRootfs;
}
export const PluginConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Description: S.String,
    Documentation: S.String,
    Interface: PluginConfigInterface,
    Entrypoint: PluginConfigEntrypointList,
    WorkDir: S.String,
    User: S.optional(PluginConfigUser),
    Network: PluginConfigNetwork,
    Linux: PluginConfigLinux,
    PropagatedMount: S.String,
    IpcHost: S.Boolean,
    PidHost: S.Boolean,
    Mounts: PluginConfigMountsList,
    Env: PluginConfigEnvList,
    Args: PluginConfigArgs,
    rootfs: S.optional(PluginConfigRootfs),
  }),
).annotate({ identifier: "PluginConfig" }) as any as S.Schema<PluginConfig>;

/** A plugin for the Engine API */
export interface Plugin {
  Id?: string;
  Name: string;
  /** True if the plugin is running. False if the plugin is not running, only installed. */
  Enabled: boolean;
  /** user-configurable settings for the plugin. */
  Settings: PluginSettings;
  /** plugin remote reference used to push/pull the plugin */
  PluginReference?: string;
  /** The config of a plugin. */
  Config: PluginConfig;
}
export const Plugin = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.optional(S.String),
    Name: S.String,
    Enabled: S.Boolean,
    Settings: PluginSettings,
    PluginReference: S.optional(S.String),
    Config: PluginConfig,
  }),
).annotate({ identifier: "Plugin" }) as any as S.Schema<Plugin>;

export interface DeleteSecretRequest {
  /** ID of the secret */
  id: string;
}
export const DeleteSecretRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/secrets/{id}", code: 200 })),
).annotate({
  identifier: "DeleteSecretRequest",
}) as any as S.Schema<DeleteSecretRequest>;

export interface DeleteSecretResponse {}
export const DeleteSecretResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteSecretResponse",
}) as any as S.Schema<DeleteSecretResponse>;

export interface DeleteServiceRequest {
  /** ID or name of service. */
  id: string;
}
export const DeleteServiceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/services/{id}", code: 200 })),
).annotate({
  identifier: "DeleteServiceRequest",
}) as any as S.Schema<DeleteServiceRequest>;

export interface DeleteServiceResponse {}
export const DeleteServiceResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteServiceResponse",
}) as any as S.Schema<DeleteServiceResponse>;

export interface DeleteVolumeRequest {
  /** Volume name or ID */
  name: string;
  /** Force the removal of the volume */
  force?: boolean;
}
export const DeleteVolumeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "DELETE", uri: "/volumes/{name}", code: 200 })),
).annotate({
  identifier: "DeleteVolumeRequest",
}) as any as S.Schema<DeleteVolumeRequest>;

export interface DeleteVolumeResponse {}
export const DeleteVolumeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteVolumeResponse",
}) as any as S.Schema<DeleteVolumeResponse>;

export interface DisablePluginRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  name: string;
  /** Force disable a plugin even if still in use. */
  force?: boolean;
}
export const DisablePluginRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "POST", uri: "/plugins/{name}/disable", code: 200 }),
  ),
).annotate({
  identifier: "DisablePluginRequest",
}) as any as S.Schema<DisablePluginRequest>;

export interface DisablePluginResponse {}
export const DisablePluginResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DisablePluginResponse",
}) as any as S.Schema<DisablePluginResponse>;

export interface DisconnectNetworkRequest {
  /** Network ID or name */
  id: string;
  /** The ID or name of the container to disconnect from the network. */
  Container: string;
  /** Force the container to disconnect from the network. */
  Force?: boolean;
}
export const DisconnectNetworkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    Container: S.String,
    Force: S.optional(S.Boolean),
  }).pipe(
    T.Http({ method: "POST", uri: "/networks/{id}/disconnect", code: 200 }),
  ),
).annotate({
  identifier: "DisconnectNetworkRequest",
}) as any as S.Schema<DisconnectNetworkRequest>;

export interface DisconnectNetworkResponse {}
export const DisconnectNetworkResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DisconnectNetworkResponse",
}) as any as S.Schema<DisconnectNetworkResponse>;

export interface DistributionInspectRequest {
  /** Image name or id */
  name: string;
}
export const DistributionInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/distribution/{name}/json", code: 200 }),
  ),
).annotate({
  identifier: "DistributionInspectRequest",
}) as any as S.Schema<DistributionInspectRequest>;

/** An array containing all platforms supported by the image. */
export type DistributionInspectPlatformsList = Array<OCIPlatform>;
export const DistributionInspectPlatformsList = /*@__PURE__*/ S.Array(
  OCIPlatform,
) as any as S.Schema<DistributionInspectPlatformsList>;

/** Describes the result obtained from contacting the registry to retrieve image metadata. */
export interface DistributionInspect {
  Descriptor: OCIDescriptor;
  /** An array containing all platforms supported by the image. */
  Platforms: DistributionInspectPlatformsList;
}
export const DistributionInspect = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Descriptor: OCIDescriptor,
    Platforms: DistributionInspectPlatformsList,
  }),
).annotate({
  identifier: "DistributionInspect",
}) as any as S.Schema<DistributionInspect>;

export interface EnablePluginRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  name: string;
  /** Set the HTTP client timeout (in seconds) */
  timeout?: number;
}
export const EnablePluginRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    timeout: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/plugins/{name}/enable", code: 200 })),
).annotate({
  identifier: "EnablePluginRequest",
}) as any as S.Schema<EnablePluginRequest>;

export interface EnablePluginResponse {}
export const EnablePluginResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "EnablePluginResponse",
}) as any as S.Schema<EnablePluginResponse>;

/** Initial console size, as an `[height, width]` array. */
export type ExecContainerRequestConsoleSizeList = Array<number>;
export const ExecContainerRequestConsoleSizeList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<ExecContainerRequestConsoleSizeList>;

/** A list of environment variables in the form `["VAR=value", ...]`. */
export type ExecContainerRequestEnvList = Array<string>;
export const ExecContainerRequestEnvList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ExecContainerRequestEnvList>;

/** Command to run, as a string or array of strings. */
export type ExecContainerRequestCmdList = Array<string>;
export const ExecContainerRequestCmdList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ExecContainerRequestCmdList>;

export interface ExecContainerRequest {
  /** ID or name of container */
  id: string;
  /** Attach to `stdin` of the exec command. */
  AttachStdin?: boolean;
  /** Attach to `stdout` of the exec command. */
  AttachStdout?: boolean;
  /** Attach to `stderr` of the exec command. */
  AttachStderr?: boolean;
  /** Initial console size, as an `[height, width]` array. */
  ConsoleSize?: ExecContainerRequestConsoleSizeList | null;
  /** Override the key sequence for detaching a container. Format is a single character `[a-Z]` or `ctrl-<value>` where `<value>` is one of: `a-z`, `@`, `^`, `[`, `,` or `_`. */
  DetachKeys?: string;
  /** Allocate a pseudo-TTY. */
  Tty?: boolean;
  /** A list of environment variables in the form `["VAR=value", ...]`. */
  Env?: ExecContainerRequestEnvList;
  /** Command to run, as a string or array of strings. */
  Cmd?: ExecContainerRequestCmdList;
  /** Runs the exec process with extended privileges. */
  Privileged?: boolean;
  /** The user, and optionally, group to run the exec process inside the container. Format is one of: `user`, `user:group`, `uid`, or `uid:gid`. */
  User?: string;
  /** The working directory for the exec process inside the container. */
  WorkingDir?: string;
}
export const ExecContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    AttachStdin: S.optional(S.Boolean),
    AttachStdout: S.optional(S.Boolean),
    AttachStderr: S.optional(S.Boolean),
    ConsoleSize: S.optional(S.NullOr(ExecContainerRequestConsoleSizeList)),
    DetachKeys: S.optional(S.String),
    Tty: S.optional(S.Boolean),
    Env: S.optional(ExecContainerRequestEnvList),
    Cmd: S.optional(ExecContainerRequestCmdList),
    Privileged: S.optional(S.Boolean),
    User: S.optional(S.String),
    WorkingDir: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/containers/{id}/exec", code: 200 })),
).annotate({
  identifier: "ExecContainerRequest",
}) as any as S.Schema<ExecContainerRequest>;

export interface ExecInspectRequest {
  /** Exec instance ID */
  id: string;
}
export const ExecInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/exec/{id}/json", code: 200 })),
).annotate({
  identifier: "ExecInspectRequest",
}) as any as S.Schema<ExecInspectRequest>;

export type ProcessConfigArgumentsList = Array<string>;
export const ProcessConfigArgumentsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ProcessConfigArgumentsList>;

export interface ProcessConfig {
  privileged?: boolean;
  user?: string;
  tty?: boolean;
  entrypoint?: string;
  arguments?: ProcessConfigArgumentsList;
}
export const ProcessConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    privileged: S.optional(S.Boolean),
    user: S.optional(S.String),
    tty: S.optional(S.Boolean),
    entrypoint: S.optional(S.String),
    arguments: S.optional(ProcessConfigArgumentsList),
  }),
).annotate({ identifier: "ProcessConfig" }) as any as S.Schema<ProcessConfig>;

export interface ExecInspectResponse {
  CanRemove?: boolean;
  DetachKeys?: string;
  ID?: string;
  Running?: boolean;
  ExitCode?: number;
  ProcessConfig?: ProcessConfig;
  OpenStdin?: boolean;
  OpenStderr?: boolean;
  OpenStdout?: boolean;
  ContainerID?: string;
  /** The system process ID for the exec process. */
  Pid?: number;
}
export const ExecInspectResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    CanRemove: S.optional(S.Boolean),
    DetachKeys: S.optional(S.String),
    ID: S.optional(S.String),
    Running: S.optional(S.Boolean),
    ExitCode: S.optional(S.Number),
    ProcessConfig: S.optional(ProcessConfig),
    OpenStdin: S.optional(S.Boolean),
    OpenStderr: S.optional(S.Boolean),
    OpenStdout: S.optional(S.Boolean),
    ContainerID: S.optional(S.String),
    Pid: S.optional(S.Number),
  }),
).annotate({
  identifier: "ExecInspectResponse",
}) as any as S.Schema<ExecInspectResponse>;

export interface ExecResizeRequest {
  /** Exec instance ID */
  id: string;
  /** Height of the TTY session in characters */
  h: number;
  /** Width of the TTY session in characters */
  w: number;
}
export const ExecResizeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    h: S.Number.pipe(T.Query()),
    w: S.Number.pipe(T.Query()),
  }).pipe(T.Http({ method: "POST", uri: "/exec/{id}/resize", code: 200 })),
).annotate({
  identifier: "ExecResizeRequest",
}) as any as S.Schema<ExecResizeRequest>;

export interface ExecResizeResponse {}
export const ExecResizeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ExecResizeResponse",
}) as any as S.Schema<ExecResizeResponse>;

/** Initial console size, as an `[height, width]` array. */
export type ExecStartRequestConsoleSizeList = Array<number>;
export const ExecStartRequestConsoleSizeList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<ExecStartRequestConsoleSizeList>;

export interface ExecStartRequest {
  /** Exec instance ID */
  id: string;
  /** Detach from the command. */
  Detach?: boolean;
  /** Allocate a pseudo-TTY. */
  Tty?: boolean;
  /** Initial console size, as an `[height, width]` array. */
  ConsoleSize?: ExecStartRequestConsoleSizeList | null;
}
export const ExecStartRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    Detach: S.optional(S.Boolean),
    Tty: S.optional(S.Boolean),
    ConsoleSize: S.optional(S.NullOr(ExecStartRequestConsoleSizeList)),
  }).pipe(T.Http({ method: "POST", uri: "/exec/{id}/start", code: 200 })),
).annotate({
  identifier: "ExecStartRequest",
}) as any as S.Schema<ExecStartRequest>;

export interface ExecStartResponse {}
export const ExecStartResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ExecStartResponse",
}) as any as S.Schema<ExecStartResponse>;

export interface ExportContainerRequest {
  /** ID or name of the container */
  id: string;
}
export const ExportContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/containers/{id}/export", code: 200 })),
).annotate({
  identifier: "ExportContainerRequest",
}) as any as S.Schema<ExportContainerRequest>;

export interface ExportContainerResponse {}
export const ExportContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ExportContainerResponse",
}) as any as S.Schema<ExportContainerResponse>;

export type GetImageRequestPlatformList = Array<string>;
export const GetImageRequestPlatformList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<GetImageRequestPlatformList>;

export interface GetImageRequest {
  /** Image name or ID */
  name: string;
  /** JSON encoded OCI platform describing a platform which will be used to select a platform-specific image to be saved if the image is multi-platform. If not provided, the full multi-platform image will be saved. Example: `{"os": "linux", "architecture": "arm", "variant": "v5"}` */
  platform?: GetImageRequestPlatformList;
}
export const GetImageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    platform: S.optional(GetImageRequestPlatformList.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/images/{name}/get", code: 200 })),
).annotate({
  identifier: "GetImageRequest",
}) as any as S.Schema<GetImageRequest>;

export type GetImageResponse = string;
export const GetImageResponse = /*@__PURE__*/ S.suspend(() =>
  S.String.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "GetImageResponse",
}) as any as S.Schema<GetImageResponse>;

export interface GetPluginPrivilegesRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  remote: string;
}
export const GetPluginPrivilegesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    remote: S.String.pipe(T.Query()),
  }).pipe(T.Http({ method: "GET", uri: "/plugins/privileges", code: 200 })),
).annotate({
  identifier: "GetPluginPrivilegesRequest",
}) as any as S.Schema<GetPluginPrivilegesRequest>;

export type GetPluginPrivilegesResponseBodyList = Array<PluginPrivilege>;
export const GetPluginPrivilegesResponseBodyList = /*@__PURE__*/ S.Array(
  PluginPrivilege,
) as any as S.Schema<GetPluginPrivilegesResponseBodyList>;

export type GetPluginPrivilegesResponse = GetPluginPrivilegesResponseBodyList;
export const GetPluginPrivilegesResponse = /*@__PURE__*/ S.suspend(() =>
  GetPluginPrivilegesResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "GetPluginPrivilegesResponse",
}) as any as S.Schema<GetPluginPrivilegesResponse>;

export type ImageAttestationsRequestPlatformList = Array<string>;
export const ImageAttestationsRequestPlatformList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageAttestationsRequestPlatformList>;

export type ImageAttestationsRequestTypeList = Array<string>;
export const ImageAttestationsRequestTypeList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageAttestationsRequestTypeList>;

export interface ImageAttestationsRequest {
  /** Image name or id */
  name: string;
  /** JSON-encoded OCI platform to select the image variant whose attestations to return. If omitted, the daemon's default (host) platform is used. Only one platform value is currently accepted; passing more than one returns an error. The parameter is declared as an array so the wire shape can accept multiple values in the future without an API version bump. Example: `{"os": "linux", "architecture": "amd64"}` */
  platform?: ImageAttestationsRequestPlatformList;
  /** In-toto predicate type URI to filter returned statements. May be repeated to accept any of several predicate types. If omitted, all statements are returned. Example: `type=https://slsa.dev/provenance/v0.2&type=https://spdx.dev/Document` */
  type?: ImageAttestationsRequestTypeList;
  /** Include the verbatim in-toto statement body in each returned entry. Defaults to false; when omitted or false, only the descriptor and predicate type are returned and statement blobs are not read. */
  statement?: boolean;
}
export const ImageAttestationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    platform: S.optional(ImageAttestationsRequestPlatformList.pipe(T.Query())),
    type: S.optional(ImageAttestationsRequestTypeList.pipe(T.Query())),
    statement: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/images/{name}/attestations", code: 200 }),
  ),
).annotate({
  identifier: "ImageAttestationsRequest",
}) as any as S.Schema<ImageAttestationsRequest>;

/** AttestationStatement is a single in-toto statement attached to an image. */
export interface AttestationStatement {
  Descriptor: OCIDescriptor;
  /** The in-toto predicate type URI of this statement. */
  PredicateType: string;
  /** The verbatim in-toto statement JSON. Only included when the caller opts in via the `statement=true` query parameter; otherwise absent. */
  Statement?: unknown | null;
}
export const AttestationStatement = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Descriptor: OCIDescriptor,
    PredicateType: S.String,
    Statement: S.optional(S.NullOr(S.Unknown)),
  }),
).annotate({
  identifier: "AttestationStatement",
}) as any as S.Schema<AttestationStatement>;

export type ImageAttestationsResponseBodyList = Array<AttestationStatement>;
export const ImageAttestationsResponseBodyList = /*@__PURE__*/ S.Array(
  AttestationStatement,
) as any as S.Schema<ImageAttestationsResponseBodyList>;

export type ImageAttestationsResponse = ImageAttestationsResponseBodyList;
export const ImageAttestationsResponse = /*@__PURE__*/ S.suspend(() =>
  ImageAttestationsResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ImageAttestationsResponse",
}) as any as S.Schema<ImageAttestationsResponse>;

export type ImageBuildRequestVersion = "1" | "2";
export const ImageBuildRequestVersion = S.String;

export type ImageBuildRequestContentType = "application/x-tar";
export const ImageBuildRequestContentType = S.String;

export interface ImageBuildRequest {
  /** Path within the build context to the `Dockerfile`. This is ignored if `remote` is specified and points to an external `Dockerfile`. */
  dockerfile?: string;
  /** A name and optional tag to apply to the image in the `name:tag` format. If you omit the tag the default `latest` value is assumed. You can provide several `t` parameters. */
  t?: string;
  /** Extra hosts to add to /etc/hosts */
  extrahosts?: string;
  /** A Git repository URI or HTTP/HTTPS context URI. If the URI points to a single text file, the file’s contents are placed into a file called `Dockerfile` and the image is built from that file. If the URI points to a tarball, the file is downloaded by the daemon and the contents therein used as the context for the build. If the URI points to a tarball and the `dockerfile` parameter is also specified, there must be a file with the corresponding path inside the tarball. */
  remote?: string;
  /** Suppress verbose build output. */
  q?: boolean;
  /** Do not use the cache when building the image. */
  nocache?: boolean;
  /** JSON array of images used for build cache resolution. */
  cachefrom?: string;
  /** Attempt to pull the image even if an older image exists locally. */
  pull?: string;
  /** Remove intermediate containers after a successful build. */
  rm?: boolean;
  /** Always remove intermediate containers, even upon failure. */
  forcerm?: boolean;
  /** Set memory limit for build. */
  memory?: number;
  /** Total memory (memory + swap). Set as `-1` to disable swap. */
  memswap?: number;
  /** CPU shares (relative weight). */
  cpushares?: number;
  /** CPUs in which to allow execution (e.g., `0-3`, `0,1`). */
  cpusetcpus?: string;
  /** The length of a CPU period in microseconds. */
  cpuperiod?: number;
  /** Microseconds of CPU time that the container can get in a CPU period. */
  cpuquota?: number;
  /** JSON map of string pairs for build-time variables. Users pass these values at build-time. Docker uses the buildargs as the environment context for commands run via the `Dockerfile` RUN instruction, or for variable expansion in other `Dockerfile` instructions. This is not meant for passing secret values. For example, the build arg `FOO=bar` would become `{"FOO":"bar"}` in JSON. This would result in the query parameter `buildargs={"FOO":"bar"}`. Note that `{"FOO":"bar"}` should be URI component encoded. [Read more about the buildargs instruction.](https://docs.docker.com/engine/reference/builder/#arg) */
  buildargs?: string;
  /** Size of `/dev/shm` in bytes. The size must be greater than 0. If omitted the system uses 64MB. */
  shmsize?: number;
  /** Squash the resulting images layers into a single layer. *(Experimental release only.)* */
  squash?: boolean;
  /** Arbitrary key/value labels to set on the image, as a JSON map of string pairs. */
  labels?: string;
  /** Sets the networking mode for the run commands during build. Supported standard values are: `bridge`, `host`, `none`, and `container:<name|id>`. Any other value is taken as a custom network's name or ID to which this container should connect to. */
  networkmode?: string;
  /** Platform in the format os[/arch[/variant]] */
  platform?: string;
  /** Target build stage */
  target?: string;
  /** BuildKit output configuration in the format of a stringified JSON array of objects. Each object must have two top-level properties: `Type` and `Attrs`. The `Type` property must be set to 'moby'. The `Attrs` property is a map of attributes for the BuildKit output configuration. See https://docs.docker.com/build/exporters/oci-docker/ for more information. Example: ``` [{"Type":"moby","Attrs":{"type":"image","force-compression":"true","compression":"zstd"}}] ``` */
  outputs?: string;
  /** Version of the builder backend to use. - `1` is the first generation classic (deprecated) builder in the Docker daemon (default) - `2` is [BuildKit](https://github.com/moby/buildkit) */
  version?: ImageBuildRequestVersion | (string & {});
  contentType?: ImageBuildRequestContentType | (string & {});
  /** This is a base64-encoded JSON object with auth configurations for multiple registries that a build may refer to. The key is a registry URL, and the value is an auth configuration object, [as described in the authentication section](#section/Authentication). For example: ``` { "docker.example.com": { "username": "janedoe", "password": "hunter2" }, "https://index.docker.io/v1/": { "username": "mobydock", "password": "conta1n3rize14" } } ``` Only the registry domain name (and port if not the default 443) are required. However, for legacy reasons, the Docker Hub registry must be specified with both a `https://` prefix and a `/v1/` suffix even though Docker will prefer to use the v2 registry API. */
  xRegistryConfig?: string;
  body?: string;
}
export const ImageBuildRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    dockerfile: S.optional(S.String.pipe(T.Query())),
    t: S.optional(S.String.pipe(T.Query())),
    extrahosts: S.optional(S.String.pipe(T.Query())),
    remote: S.optional(S.String.pipe(T.Query())),
    q: S.optional(S.Boolean.pipe(T.Query())),
    nocache: S.optional(S.Boolean.pipe(T.Query())),
    cachefrom: S.optional(S.String.pipe(T.Query())),
    pull: S.optional(S.String.pipe(T.Query())),
    rm: S.optional(S.Boolean.pipe(T.Query())),
    forcerm: S.optional(S.Boolean.pipe(T.Query())),
    memory: S.optional(S.Number.pipe(T.Query())),
    memswap: S.optional(S.Number.pipe(T.Query())),
    cpushares: S.optional(S.Number.pipe(T.Query())),
    cpusetcpus: S.optional(S.String.pipe(T.Query())),
    cpuperiod: S.optional(S.Number.pipe(T.Query())),
    cpuquota: S.optional(S.Number.pipe(T.Query())),
    buildargs: S.optional(S.String.pipe(T.Query())),
    shmsize: S.optional(S.Number.pipe(T.Query())),
    squash: S.optional(S.Boolean.pipe(T.Query())),
    labels: S.optional(S.String.pipe(T.Query())),
    networkmode: S.optional(S.String.pipe(T.Query())),
    platform: S.optional(S.String.pipe(T.Query())),
    target: S.optional(S.String.pipe(T.Query())),
    outputs: S.optional(S.String.pipe(T.Query())),
    version: S.optional(ImageBuildRequestVersion.pipe(T.Query())),
    contentType: S.optional(
      ImageBuildRequestContentType.pipe(T.Header("Content-type")),
    ),
    xRegistryConfig: S.optional(S.String.pipe(T.Header("X-Registry-Config"))),
    body: S.optional(S.String.pipe(T.HttpBody())),
  }).pipe(T.Http({ method: "POST", uri: "/build", code: 200 })),
).annotate({
  identifier: "ImageBuildRequest",
}) as any as S.Schema<ImageBuildRequest>;

export interface ImageBuildResponse {}
export const ImageBuildResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ImageBuildResponse",
}) as any as S.Schema<ImageBuildResponse>;

export type ImageGetAllRequestNamesList = Array<string>;
export const ImageGetAllRequestNamesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageGetAllRequestNamesList>;

export type ImageGetAllRequestPlatformList = Array<string>;
export const ImageGetAllRequestPlatformList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageGetAllRequestPlatformList>;

export interface ImageGetAllRequest {
  /** Image names to filter by */
  names?: ImageGetAllRequestNamesList;
  /** JSON encoded OCI platform(s) which will be used to select the platform-specific image(s) to be saved if the image is multi-platform. If not provided, the full multi-platform image will be saved. Example: `{"os": "linux", "architecture": "arm", "variant": "v5"}` */
  platform?: ImageGetAllRequestPlatformList;
}
export const ImageGetAllRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    names: S.optional(ImageGetAllRequestNamesList.pipe(T.Query())),
    platform: S.optional(ImageGetAllRequestPlatformList.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/images/get", code: 200 })),
).annotate({
  identifier: "ImageGetAllRequest",
}) as any as S.Schema<ImageGetAllRequest>;

export type ImageGetAllResponse = string;
export const ImageGetAllResponse = /*@__PURE__*/ S.suspend(() =>
  S.String.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ImageGetAllResponse",
}) as any as S.Schema<ImageGetAllResponse>;

export interface ImageHistoryRequest {
  /** Image name or ID */
  name: string;
  /** JSON-encoded OCI platform to select the platform-variant. If omitted, it defaults to any locally available platform, prioritizing the daemon's host platform. If the daemon provides a multi-platform image store, this selects the platform-variant to show the history for. If the image is a single-platform image, or if the multi-platform image does not provide a variant matching the given platform, an error is returned. Example: `{"os": "linux", "architecture": "arm", "variant": "v5"}` */
  platform?: string;
}
export const ImageHistoryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    platform: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/images/{name}/history", code: 200 })),
).annotate({
  identifier: "ImageHistoryRequest",
}) as any as S.Schema<ImageHistoryRequest>;

export type ImageHistoryResponseItemTagsList = Array<string>;
export const ImageHistoryResponseItemTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageHistoryResponseItemTagsList>;

/** individual image layer information in response to ImageHistory operation */
export interface ImageHistoryResponseItem {
  Id: string;
  Created: number;
  CreatedBy: string;
  Tags: ImageHistoryResponseItemTagsList;
  Size: number;
  Comment: string;
}
export const ImageHistoryResponseItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.String,
    Created: S.Number,
    CreatedBy: S.String,
    Tags: ImageHistoryResponseItemTagsList,
    Size: S.Number,
    Comment: S.String,
  }),
).annotate({
  identifier: "ImageHistoryResponseItem",
}) as any as S.Schema<ImageHistoryResponseItem>;

export type ImageHistoryResponseBodyList = Array<ImageHistoryResponseItem>;
export const ImageHistoryResponseBodyList = /*@__PURE__*/ S.Array(
  ImageHistoryResponseItem,
) as any as S.Schema<ImageHistoryResponseBodyList>;

export type ImageHistoryResponse = ImageHistoryResponseBodyList;
export const ImageHistoryResponse = /*@__PURE__*/ S.suspend(() =>
  ImageHistoryResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ImageHistoryResponse",
}) as any as S.Schema<ImageHistoryResponse>;

export interface ImageInspectRequest {
  /** Image name or id */
  name: string;
  /** Include Manifests in the image summary. The `manifests` and `platform` options are mutually exclusive, and an error is produced if both are set. */
  manifests?: boolean;
  /** JSON-encoded OCI platform to select the platform-variant. If omitted, it defaults to any locally available platform, prioritizing the daemon's host platform. If the daemon provides a multi-platform image store, this selects the platform-variant to show inspect. If the image is a single-platform image, or if the multi-platform image does not provide a variant matching the given platform, an error is returned. The `platform` and `manifests` options are mutually exclusive, and an error is produced if both are set. Example: `{"os": "linux", "architecture": "arm", "variant": "v5"}` */
  platform?: string;
}
export const ImageInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    manifests: S.optional(S.Boolean.pipe(T.Query())),
    platform: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/images/{name}/json", code: 200 })),
).annotate({
  identifier: "ImageInspectRequest",
}) as any as S.Schema<ImageInspectRequest>;

export interface ImageManifestSummarySize {
  /** Total is the total size (in bytes) of all the locally present data (both distributable and non-distributable) that's related to this manifest and its children. This equal to the sum of [Content] size AND all the sizes in the [Size] struct present in the Kind-specific data struct. For example, for an image kind (Kind == "image") this would include the size of the image content and unpacked image snapshots ([Size.Content] + [ImageData.Size.Unpacked]). */
  Total: number;
  /** Content is the size (in bytes) of all the locally present content in the content store (e.g. image config, layers) referenced by this manifest and its children. This only includes blobs in the content store. */
  Content: number;
}
export const ImageManifestSummarySize = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Total: S.Number,
    Content: S.Number,
  }),
).annotate({
  identifier: "ImageManifestSummarySize",
}) as any as S.Schema<ImageManifestSummarySize>;

/** The kind of the manifest. kind | description -------------|----------------------------------------------------------- image | Image manifest that can be used to start a container. attestation | Attestation manifest produced by the Buildkit builder for a specific image manifest. */
export type ImageManifestSummaryKind = "image" | "attestation" | "unknown";
export const ImageManifestSummaryKind = S.String;

/** SignatureTimestampType is the type of timestamp used in the signature. */
export type SignatureTimestampType = "Tlog" | "TimestampAuthority";
export const SignatureTimestampType = S.String;

/** SignatureTimestamp contains information about a verified signed timestamp for an image signature. */
export interface SignatureTimestamp {
  Type?: SignatureTimestampType;
  URI?: string;
  Timestamp?: string;
}
export const SignatureTimestamp = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Type: S.optional(SignatureTimestampType),
    URI: S.optional(S.String),
    Timestamp: S.optional(S.String),
  }),
).annotate({
  identifier: "SignatureTimestamp",
}) as any as S.Schema<SignatureTimestamp>;

/** Timestamps contains a list of verified signed timestamps for the signature. */
export type SignatureIdentityTimestampsList = Array<SignatureTimestamp>;
export const SignatureIdentityTimestampsList = /*@__PURE__*/ S.Array(
  SignatureTimestamp,
) as any as S.Schema<SignatureIdentityTimestampsList>;

/** KnownSignerIdentity is an identifier for a special signer identity that is known to the implementation. */
export type KnownSignerIdentity = "DHI";
export const KnownSignerIdentity = S.String;

/** SignerIdentity contains information about the signer certificate used to sign the image. */
export interface SignerIdentity {
  /** CertificateIssuer is the certificate issuer. */
  CertificateIssuer?: string;
  /** SubjectAlternativeName is the certificate subject alternative name. */
  SubjectAlternativeName?: string;
  /** The OIDC issuer. Should match `iss` claim of ID token or, in the case of a federated login like Dex it should match the issuer URL of the upstream issuer. The issuer is not set the extensions are invalid and will fail to render. */
  Issuer?: string;
  /** Reference to specific build instructions that are responsible for signing. */
  BuildSignerURI?: string;
  /** Immutable reference to the specific version of the build instructions that is responsible for signing. */
  BuildSignerDigest?: string;
  /** Specifies whether the build took place in platform-hosted cloud infrastructure or customer/self-hosted infrastructure. */
  RunnerEnvironment?: string;
  /** Source repository URL that the build was based on. */
  SourceRepositoryURI?: string;
  /** Immutable reference to a specific version of the source code that the build was based upon. */
  SourceRepositoryDigest?: string;
  /** Source Repository Ref that the build run was based upon. */
  SourceRepositoryRef?: string;
  /** Immutable identifier for the source repository the workflow was based upon. */
  SourceRepositoryIdentifier?: string;
  /** Source repository owner URL of the owner of the source repository that the build was based on. */
  SourceRepositoryOwnerURI?: string;
  /** Immutable identifier for the owner of the source repository that the workflow was based upon. */
  SourceRepositoryOwnerIdentifier?: string;
  /** Build Config URL to the top-level/initiating build instructions. */
  BuildConfigURI?: string;
  /** Immutable reference to the specific version of the top-level/initiating build instructions. */
  BuildConfigDigest?: string;
  /** Event or action that initiated the build. */
  BuildTrigger?: string;
  /** Run Invocation URL to uniquely identify the build execution. */
  RunInvocationURI?: string;
  /** Source repository visibility at the time of signing the certificate. */
  SourceRepositoryVisibilityAtSigning?: string;
}
export const SignerIdentity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    CertificateIssuer: S.optional(S.String),
    SubjectAlternativeName: S.optional(S.String),
    Issuer: S.optional(S.String),
    BuildSignerURI: S.optional(S.String),
    BuildSignerDigest: S.optional(S.String),
    RunnerEnvironment: S.optional(S.String),
    SourceRepositoryURI: S.optional(S.String),
    SourceRepositoryDigest: S.optional(S.String),
    SourceRepositoryRef: S.optional(S.String),
    SourceRepositoryIdentifier: S.optional(S.String),
    SourceRepositoryOwnerURI: S.optional(S.String),
    SourceRepositoryOwnerIdentifier: S.optional(S.String),
    BuildConfigURI: S.optional(S.String),
    BuildConfigDigest: S.optional(S.String),
    BuildTrigger: S.optional(S.String),
    RunInvocationURI: S.optional(S.String),
    SourceRepositoryVisibilityAtSigning: S.optional(S.String),
  }),
).annotate({ identifier: "SignerIdentity" }) as any as S.Schema<SignerIdentity>;

/** SignatureType is the type of signature format. */
export type SignatureType = "bundle-v0.3" | "simplesigning-v1";
export const SignatureType = S.String;

/** Warnings contains any warnings that occurred during signature verification. For example, if there was no internet connectivity and cached trust roots were used. Warning does not indicate a failed verification but may point to configuration issues. */
export type SignatureIdentityWarningsList = Array<string>;
export const SignatureIdentityWarningsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SignatureIdentityWarningsList>;

/** SignatureIdentity contains the properties of verified signatures for the image. */
export interface SignatureIdentity {
  /** Name is a textual description summarizing the type of signature. */
  Name?: string;
  /** Timestamps contains a list of verified signed timestamps for the signature. */
  Timestamps?: SignatureIdentityTimestampsList;
  /** KnownSigner is an identifier for a special signer identity that is known to the implementation. */
  KnownSigner?: KnownSignerIdentity;
  /** DockerReference is the Docker image reference associated with the signature. This is an optional field only present in older hashedrecord signatures. */
  DockerReference?: string;
  /** Signer contains information about the signer certificate used to sign the image. */
  Signer?: SignerIdentity;
  /** SignatureType is the type of signature format. E.g. "bundle-v0.3" or "hashedrecord". */
  SignatureType?: SignatureType;
  /** Error contains error information if signature verification failed. Other fields will be empty in this case. */
  Error?: string;
  /** Warnings contains any warnings that occurred during signature verification. For example, if there was no internet connectivity and cached trust roots were used. Warning does not indicate a failed verification but may point to configuration issues. */
  Warnings?: SignatureIdentityWarningsList;
}
export const SignatureIdentity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Timestamps: S.optional(SignatureIdentityTimestampsList),
    KnownSigner: S.optional(KnownSignerIdentity),
    DockerReference: S.optional(S.String),
    Signer: S.optional(SignerIdentity),
    SignatureType: S.optional(SignatureType),
    Error: S.optional(S.String),
    Warnings: S.optional(SignatureIdentityWarningsList),
  }),
).annotate({
  identifier: "SignatureIdentity",
}) as any as S.Schema<SignatureIdentity>;

/** Signature contains the properties of verified signatures for the image. */
export type IdentitySignatureList = Array<SignatureIdentity>;
export const IdentitySignatureList = /*@__PURE__*/ S.Array(
  SignatureIdentity,
) as any as S.Schema<IdentitySignatureList>;

/** PullIdentity contains remote location information if image was created via pull. If image was pulled via mirror, this contains the original repository location. */
export interface PullIdentity {
  /** Repository is the remote repository location the image was pulled from. */
  Repository?: string;
}
export const PullIdentity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Repository: S.optional(S.String),
  }),
).annotate({ identifier: "PullIdentity" }) as any as S.Schema<PullIdentity>;

/** Pull contains remote location information if image was created via pull. If image was pulled via mirror, this contains the original repository location. After successful push this images also contains the pushed repository location. */
export type IdentityPullList = Array<PullIdentity>;
export const IdentityPullList = /*@__PURE__*/ S.Array(
  PullIdentity,
) as any as S.Schema<IdentityPullList>;

/** BuildIdentity contains build reference information if image was created via build. */
export interface BuildIdentity {
  /** Ref is the identifier for the build request. This reference can be used to look up the build details in BuildKit history API. */
  Ref?: string;
  /** CreatedAt is the time when the build ran. */
  CreatedAt?: string;
}
export const BuildIdentity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Ref: S.optional(S.String),
    CreatedAt: S.optional(S.String),
  }),
).annotate({ identifier: "BuildIdentity" }) as any as S.Schema<BuildIdentity>;

/** Build contains build reference information if image was created via build. */
export type IdentityBuildList = Array<BuildIdentity>;
export const IdentityBuildList = /*@__PURE__*/ S.Array(
  BuildIdentity,
) as any as S.Schema<IdentityBuildList>;

/** Identity holds information about the identity and origin of the image. This is trusted information verified by the daemon and cannot be modified by tagging an image to a different name. */
export interface Identity {
  /** Signature contains the properties of verified signatures for the image. */
  Signature?: IdentitySignatureList;
  /** Pull contains remote location information if image was created via pull. If image was pulled via mirror, this contains the original repository location. After successful push this images also contains the pushed repository location. */
  Pull?: IdentityPullList;
  /** Build contains build reference information if image was created via build. */
  Build?: IdentityBuildList;
}
export const Identity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Signature: S.optional(IdentitySignatureList),
    Pull: S.optional(IdentityPullList),
    Build: S.optional(IdentityBuildList),
  }),
).annotate({ identifier: "Identity" }) as any as S.Schema<Identity>;

/** The IDs of the containers that are using this image. */
export type ImageManifestSummaryImageDataContainersList = Array<string>;
export const ImageManifestSummaryImageDataContainersList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ImageManifestSummaryImageDataContainersList>;

export interface ImageManifestSummaryImageDataSize {
  /** Unpacked is the size (in bytes) of the locally unpacked (uncompressed) image content that's directly usable by the containers running this image. It's independent of the distributable content - e.g. the image might still have an unpacked data that's still used by some container even when the distributable/compressed content is already gone. */
  Unpacked: number;
}
export const ImageManifestSummaryImageDataSize = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Unpacked: S.Number,
  }),
).annotate({
  identifier: "ImageManifestSummaryImageDataSize",
}) as any as S.Schema<ImageManifestSummaryImageDataSize>;

/** The image data for the image manifest. This field is only populated when Kind is "image". */
export interface ImageManifestSummaryImageData {
  /** OCI platform of the image. This will be the platform specified in the manifest descriptor from the index/manifest list. If it's not available, it will be obtained from the image config. */
  Platform: OCIPlatform | null;
  /** Identity holds information about the identity and origin of this image. For image list responses, this can duplicate Build/Pull fields across image manifests, because those parts are image-level metadata. */
  Identity?: Identity | null;
  /** The IDs of the containers that are using this image. */
  Containers: ImageManifestSummaryImageDataContainersList;
  Size: ImageManifestSummaryImageDataSize;
}
export const ImageManifestSummaryImageData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Platform: S.NullOr(OCIPlatform),
    Identity: S.optional(S.NullOr(Identity)),
    Containers: ImageManifestSummaryImageDataContainersList,
    Size: ImageManifestSummaryImageDataSize,
  }),
).annotate({
  identifier: "ImageManifestSummaryImageData",
}) as any as S.Schema<ImageManifestSummaryImageData>;

/** The image data for the attestation manifest. This field is only populated when Kind is "attestation". */
export interface ImageManifestSummaryAttestationData {
  /** The digest of the image manifest that this attestation is for. */
  For: string;
}
export const ImageManifestSummaryAttestationData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    For: S.String,
  }),
).annotate({
  identifier: "ImageManifestSummaryAttestationData",
}) as any as S.Schema<ImageManifestSummaryAttestationData>;

/** ImageManifestSummary represents a summary of an image manifest. */
export interface ImageManifestSummary {
  /** ID is the content-addressable ID of an image and is the same as the digest of the image manifest. */
  ID: string;
  Descriptor: OCIDescriptor;
  /** Indicates whether all the child content (image config, layers) is fully available locally. */
  Available: boolean;
  Size: ImageManifestSummarySize;
  /** The kind of the manifest. kind | description -------------|----------------------------------------------------------- image | Image manifest that can be used to start a container. attestation | Attestation manifest produced by the Buildkit builder for a specific image manifest. */
  Kind: ImageManifestSummaryKind;
  /** The image data for the image manifest. This field is only populated when Kind is "image". */
  ImageData?: ImageManifestSummaryImageData | null;
  /** The image data for the attestation manifest. This field is only populated when Kind is "attestation". */
  AttestationData?: ImageManifestSummaryAttestationData | null;
}
export const ImageManifestSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.String,
    Descriptor: OCIDescriptor,
    Available: S.Boolean,
    Size: ImageManifestSummarySize,
    Kind: ImageManifestSummaryKind,
    ImageData: S.optional(S.NullOr(ImageManifestSummaryImageData)),
    AttestationData: S.optional(S.NullOr(ImageManifestSummaryAttestationData)),
  }),
).annotate({
  identifier: "ImageManifestSummary",
}) as any as S.Schema<ImageManifestSummary>;

/** Manifests is a list of image manifests available in this image. It provides a more detailed view of the platform-specific image manifests or other image-attached data like build attestations. Only available if the daemon provides a multi-platform image store and the `manifests` option is set in the inspect request. WARNING: This is experimental and may change at any time without any backward compatibility. */
export type ImageInspectManifestsList = Array<ImageManifestSummary>;
export const ImageInspectManifestsList = /*@__PURE__*/ S.Array(
  ImageManifestSummary,
) as any as S.Schema<ImageInspectManifestsList>;

/** List of image names/tags in the local image cache that reference this image. Multiple image tags can refer to the same image, and this list may be empty if no tags reference the image, in which case the image is "untagged", in which case it can still be referenced by its ID. */
export type ImageInspectRepoTagsList = Array<string>;
export const ImageInspectRepoTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageInspectRepoTagsList>;

/** List of content-addressable digests of locally available image manifests that the image is referenced from. Multiple manifests can refer to the same image. These digests are usually only available if the image was either pulled from a registry, or if the image was pushed to a registry, which is when the manifest is generated and its digest calculated. */
export type ImageInspectRepoDigestsList = Array<string>;
export const ImageInspectRepoDigestsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageInspectRepoDigestsList>;

/** An object mapping ports to an empty object in the form: `{"<port>/<tcp|udp|sctp>": {}}` */
export type ImageConfigExposedPortsMap = { [key: string]: unknown | undefined };
export const ImageConfigExposedPortsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ImageConfigExposedPortsMap>;

/** A list of environment variables to set inside the container in the form `["VAR=value", ...]`. A variable without `=` is removed from the environment, rather than to have an empty value. */
export type ImageConfigEnvList = Array<string>;
export const ImageConfigEnvList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageConfigEnvList>;

/** Command to run specified as a string or an array of strings. */
export type ImageConfigCmdList = Array<string>;
export const ImageConfigCmdList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageConfigCmdList>;

/** An object mapping mount point paths inside the container to empty objects. */
export type ImageConfigVolumesMap = { [key: string]: unknown | undefined };
export const ImageConfigVolumesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ImageConfigVolumesMap>;

/** The entry point for the container as a string or an array of strings. If the array consists of exactly one empty string (`[""]`) then the entry point is reset to system default (i.e., the entry point used by docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`). */
export type ImageConfigEntrypointList = Array<string>;
export const ImageConfigEntrypointList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageConfigEntrypointList>;

/** `ONBUILD` metadata that were defined in the image's `Dockerfile`. */
export type ImageConfigOnBuildList = Array<string>;
export const ImageConfigOnBuildList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageConfigOnBuildList>;

/** User-defined key/value metadata. */
export type ImageConfigLabelsMap = { [key: string]: string | undefined };
export const ImageConfigLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ImageConfigLabelsMap>;

/** Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell. */
export type ImageConfigShellList = Array<string>;
export const ImageConfigShellList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageConfigShellList>;

/** Configuration of the image. These fields are used as defaults when starting a container from the image. */
export interface ImageConfig {
  /** The user that commands are run as inside the container. */
  User?: string;
  /** An object mapping ports to an empty object in the form: `{"<port>/<tcp|udp|sctp>": {}}` */
  ExposedPorts?: ImageConfigExposedPortsMap | null;
  /** A list of environment variables to set inside the container in the form `["VAR=value", ...]`. A variable without `=` is removed from the environment, rather than to have an empty value. */
  Env?: ImageConfigEnvList;
  /** Command to run specified as a string or an array of strings. */
  Cmd?: ImageConfigCmdList;
  Healthcheck?: HealthConfig;
  /** Command is already escaped (Windows only) */
  ArgsEscaped?: boolean | null;
  /** An object mapping mount point paths inside the container to empty objects. */
  Volumes?: ImageConfigVolumesMap;
  /** The working directory for commands to run in. */
  WorkingDir?: string;
  /** The entry point for the container as a string or an array of strings. If the array consists of exactly one empty string (`[""]`) then the entry point is reset to system default (i.e., the entry point used by docker when there is no `ENTRYPOINT` instruction in the `Dockerfile`). */
  Entrypoint?: ImageConfigEntrypointList;
  /** `ONBUILD` metadata that were defined in the image's `Dockerfile`. */
  OnBuild?: ImageConfigOnBuildList | null;
  /** User-defined key/value metadata. */
  Labels?: ImageConfigLabelsMap;
  /** Signal to stop a container as a string or unsigned integer. */
  StopSignal?: string | null;
  /** Shell for when `RUN`, `CMD`, and `ENTRYPOINT` uses a shell. */
  Shell?: ImageConfigShellList | null;
}
export const ImageConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    User: S.optional(S.String),
    ExposedPorts: S.optional(S.NullOr(ImageConfigExposedPortsMap)),
    Env: S.optional(ImageConfigEnvList),
    Cmd: S.optional(ImageConfigCmdList),
    Healthcheck: S.optional(HealthConfig),
    ArgsEscaped: S.optional(S.NullOr(S.Boolean)),
    Volumes: S.optional(ImageConfigVolumesMap),
    WorkingDir: S.optional(S.String),
    Entrypoint: S.optional(ImageConfigEntrypointList),
    OnBuild: S.optional(S.NullOr(ImageConfigOnBuildList)),
    Labels: S.optional(ImageConfigLabelsMap),
    StopSignal: S.optional(S.NullOr(S.String)),
    Shell: S.optional(S.NullOr(ImageConfigShellList)),
  }),
).annotate({ identifier: "ImageConfig" }) as any as S.Schema<ImageConfig>;

export type ImageInspectRootFSLayersList = Array<string>;
export const ImageInspectRootFSLayersList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageInspectRootFSLayersList>;

/** Information about the image's RootFS, including the layer IDs. */
export interface ImageInspectRootFS {
  Type: string;
  Layers?: ImageInspectRootFSLayersList;
}
export const ImageInspectRootFS = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Type: S.String,
    Layers: S.optional(ImageInspectRootFSLayersList),
  }),
).annotate({
  identifier: "ImageInspectRootFS",
}) as any as S.Schema<ImageInspectRootFS>;

/** Additional metadata of the image in the local cache. This information is local to the daemon, and not part of the image itself. */
export interface ImageInspectMetadata {
  /** Date and time at which the image was last tagged in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. This information is only available if the image was tagged locally, and omitted otherwise. */
  LastTagTime?: string | null;
}
export const ImageInspectMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    LastTagTime: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "ImageInspectMetadata",
}) as any as S.Schema<ImageInspectMetadata>;

/** Information about an image in the local image cache. */
export interface ImageInspect {
  /** ID is the content-addressable ID of an image. This identifier is a content-addressable digest calculated from the image's configuration (which includes the digests of layers used by the image). Note that this digest differs from the `RepoDigests` below, which holds digests of image manifests that reference the image. */
  Id?: string;
  /** Descriptor is an OCI descriptor of the image target. In case of a multi-platform image, this descriptor points to the OCI index or a manifest list. This field is only present if the daemon provides a multi-platform image store. WARNING: This is experimental and may change at any time without any backward compatibility. */
  Descriptor?: OCIDescriptor | null;
  /** Manifests is a list of image manifests available in this image. It provides a more detailed view of the platform-specific image manifests or other image-attached data like build attestations. Only available if the daemon provides a multi-platform image store and the `manifests` option is set in the inspect request. WARNING: This is experimental and may change at any time without any backward compatibility. */
  Manifests?: ImageInspectManifestsList | null;
  /** Identity holds information about the identity and origin of the image. This is trusted information verified by the daemon and cannot be modified by tagging an image to a different name. */
  Identity?: Identity | null;
  /** List of image names/tags in the local image cache that reference this image. Multiple image tags can refer to the same image, and this list may be empty if no tags reference the image, in which case the image is "untagged", in which case it can still be referenced by its ID. */
  RepoTags?: ImageInspectRepoTagsList;
  /** List of content-addressable digests of locally available image manifests that the image is referenced from. Multiple manifests can refer to the same image. These digests are usually only available if the image was either pulled from a registry, or if the image was pushed to a registry, which is when the manifest is generated and its digest calculated. */
  RepoDigests?: ImageInspectRepoDigestsList;
  /** Optional message that was set when committing or importing the image. */
  Comment?: string | null;
  /** Date and time at which the image was created, formatted in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. This information is only available if present in the image, and omitted otherwise. */
  Created?: string | null;
  /** Name of the author that was specified when committing the image, or as specified through MAINTAINER (deprecated) in the Dockerfile. */
  Author?: string | null;
  Config?: ImageConfig;
  /** Hardware CPU architecture that the image runs on. */
  Architecture?: string;
  /** CPU architecture variant (presently ARM-only). */
  Variant?: string | null;
  /** Operating System the image is built to run on. */
  Os?: string;
  /** Operating System version the image is built to run on (especially for Windows). */
  OsVersion?: string | null;
  /** Total size of the image variant, including all layers it is composed of. For multi-platform images, this is the size of the platform variant selected by the `platform` query parameter. If no platform is specified, the daemon selects a platform as described in the `platform` parameter. When using the containerd image store, this includes both the image content that's present locally and the unpacked snapshot data for the selected variant. Image data may be shared between images, so this size does not indicate how much space would be reclaimed by deleting the image. */
  Size?: number;
  GraphDriver?: DriverData | null;
  /** Information about the image's RootFS, including the layer IDs. */
  RootFS?: ImageInspectRootFS;
  /** Additional metadata of the image in the local cache. This information is local to the daemon, and not part of the image itself. */
  Metadata?: ImageInspectMetadata;
}
export const ImageInspect = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.optional(S.String),
    Descriptor: S.optional(S.NullOr(OCIDescriptor)),
    Manifests: S.optional(S.NullOr(ImageInspectManifestsList)),
    Identity: S.optional(S.NullOr(Identity)),
    RepoTags: S.optional(ImageInspectRepoTagsList),
    RepoDigests: S.optional(ImageInspectRepoDigestsList),
    Comment: S.optional(S.NullOr(S.String)),
    Created: S.optional(S.NullOr(S.String)),
    Author: S.optional(S.NullOr(S.String)),
    Config: S.optional(ImageConfig),
    Architecture: S.optional(S.String),
    Variant: S.optional(S.NullOr(S.String)),
    Os: S.optional(S.String),
    OsVersion: S.optional(S.NullOr(S.String)),
    Size: S.optional(S.Number),
    GraphDriver: S.optional(S.NullOr(DriverData)),
    RootFS: S.optional(ImageInspectRootFS),
    Metadata: S.optional(ImageInspectMetadata),
  }),
).annotate({ identifier: "ImageInspect" }) as any as S.Schema<ImageInspect>;

export type ImageLoadRequestPlatformList = Array<string>;
export const ImageLoadRequestPlatformList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageLoadRequestPlatformList>;

export interface ImageLoadRequest {
  /** Suppress progress details during load. */
  quiet?: boolean;
  /** JSON encoded OCI platform(s) which will be used to select the platform-specific image(s) to load if the image is multi-platform. If not provided, the full multi-platform image will be loaded. Example: `{"os": "linux", "architecture": "arm", "variant": "v5"}` */
  platform?: ImageLoadRequestPlatformList;
  body?: string;
}
export const ImageLoadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    quiet: S.optional(S.Boolean.pipe(T.Query())),
    platform: S.optional(ImageLoadRequestPlatformList.pipe(T.Query())),
    body: S.optional(S.String.pipe(T.HttpBody())),
  }).pipe(T.Http({ method: "POST", uri: "/images/load", code: 200 })),
).annotate({
  identifier: "ImageLoadRequest",
}) as any as S.Schema<ImageLoadRequest>;

export interface ImageLoadResponse {}
export const ImageLoadResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ImageLoadResponse",
}) as any as S.Schema<ImageLoadResponse>;

export interface ImagePruneRequest {
  /** Filters to process on the prune list, encoded as JSON (a `map[string][]string`). Available filters: - `dangling=<boolean>` When set to `true` (or `1`), prune only unused *and* untagged images. When set to `false` (or `0`), all unused images are pruned. - `until=<string>` Prune images created before this timestamp. The `<timestamp>` can be Unix timestamps, date formatted timestamps, or Go duration strings (e.g. `10m`, `1h30m`) computed relative to the daemon machine’s time. - `label` (`label=<key>`, `label=<key>=<value>`, `label!=<key>`, or `label!=<key>=<value>`) Prune images with (or without, in case `label!=...` is used) the specified labels. */
  filters?: string;
}
export const ImagePruneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/images/prune", code: 200 })),
).annotate({
  identifier: "ImagePruneRequest",
}) as any as S.Schema<ImagePruneRequest>;

/** Images that were deleted */
export type ImagePruneResponseImagesDeletedList =
  Array<ImageDeleteResponseItem>;
export const ImagePruneResponseImagesDeletedList = /*@__PURE__*/ S.Array(
  ImageDeleteResponseItem,
) as any as S.Schema<ImagePruneResponseImagesDeletedList>;

export interface ImagePruneResponse {
  /** Images that were deleted */
  ImagesDeleted?: ImagePruneResponseImagesDeletedList;
  /** Disk space reclaimed in bytes */
  SpaceReclaimed?: number;
}
export const ImagePruneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ImagesDeleted: S.optional(ImagePruneResponseImagesDeletedList),
    SpaceReclaimed: S.optional(S.Number),
  }),
).annotate({
  identifier: "ImagePruneResponse",
}) as any as S.Schema<ImagePruneResponse>;

export interface ImagePushRequest {
  /** Name of the image to push. For example, `registry.example.com/myimage`. The image must be present in the local image store with the same name. The name should be provided without tag; if a tag is provided, it is ignored. For example, `registry.example.com/myimage:latest` is considered equivalent to `registry.example.com/myimage`. Use the `tag` parameter to specify the tag to push. */
  name: string;
  /** Tag of the image to push. For example, `latest`. If no tag is provided, all tags of the given image that are present in the local image store are pushed. */
  tag?: string;
  /** JSON-encoded OCI platform to select the platform-variant to push. If not provided, all available variants will attempt to be pushed. If the daemon provides a multi-platform image store, this selects the platform-variant to push to the registry. If the image is a single-platform image, or if the multi-platform image does not provide a variant matching the given platform, an error is returned. Example: `{"os": "linux", "architecture": "arm", "variant": "v5"}` */
  platform?: string;
  /** A base64url-encoded auth configuration. Refer to the [authentication section](#section/Authentication) for details. */
  xRegistryAuth: string;
}
export const ImagePushRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    tag: S.optional(S.String.pipe(T.Query())),
    platform: S.optional(S.String.pipe(T.Query())),
    xRegistryAuth: S.String.pipe(T.Header("X-Registry-Auth")),
  }).pipe(T.Http({ method: "POST", uri: "/images/{name}/push", code: 200 })),
).annotate({
  identifier: "ImagePushRequest",
}) as any as S.Schema<ImagePushRequest>;

export interface ImagePushResponse {}
export const ImagePushResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ImagePushResponse",
}) as any as S.Schema<ImagePushResponse>;

export interface ImageTagRequest {
  /** Image name or ID to tag. */
  name: string;
  /** The repository to tag in. For example, `someuser/someimage`. */
  repo?: string;
  /** The name of the new tag. */
  tag?: string;
}
export const ImageTagRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    repo: S.optional(S.String.pipe(T.Query())),
    tag: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/images/{name}/tag", code: 200 })),
).annotate({
  identifier: "ImageTagRequest",
}) as any as S.Schema<ImageTagRequest>;

export interface ImageTagResponse {}
export const ImageTagResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ImageTagResponse",
}) as any as S.Schema<ImageTagResponse>;

/** Addresses of manager nodes already participating in the swarm. */
export type JoinSwarmRequestRemoteAddrsList = Array<string>;
export const JoinSwarmRequestRemoteAddrsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<JoinSwarmRequestRemoteAddrsList>;

export interface JoinSwarmRequest {
  /** Listen address used for inter-manager communication if the node gets promoted to manager, as well as determining the networking interface used for the VXLAN Tunnel Endpoint (VTEP). This is required for joining a swarm. If the port number is omitted, the default swarm listening port is used. */
  ListenAddr: string;
  /** Externally reachable address advertised to other nodes. This can either be an address/port combination in the form `192.168.1.1:4567`, or an interface followed by a port number, like `eth0:4567`. If the port number is omitted, the port number from the listen address is used. If `AdvertiseAddr` is not specified, it will be automatically detected when possible. */
  AdvertiseAddr?: string;
  /** Address or interface to use for data path traffic (format: `<ip|interface>`), for example, `192.168.1.1`, or an interface, like `eth0`. If `DataPathAddr` is unspecified, the same address as `AdvertiseAddr` is used. The `DataPathAddr` specifies the address that global scope network drivers will publish towards other nodes in order to reach the containers running on this node. Using this parameter it is possible to separate the container data traffic from the management traffic of the cluster. */
  DataPathAddr?: string;
  /** Addresses of manager nodes already participating in the swarm. */
  RemoteAddrs: JoinSwarmRequestRemoteAddrsList;
  /** Secret token for joining this swarm. */
  JoinToken: string;
}
export const JoinSwarmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ListenAddr: S.String,
    AdvertiseAddr: S.optional(S.String),
    DataPathAddr: S.optional(S.String),
    RemoteAddrs: JoinSwarmRequestRemoteAddrsList,
    JoinToken: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/swarm/join", code: 200 })),
).annotate({
  identifier: "JoinSwarmRequest",
}) as any as S.Schema<JoinSwarmRequest>;

export interface JoinSwarmResponse {}
export const JoinSwarmResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "JoinSwarmResponse",
}) as any as S.Schema<JoinSwarmResponse>;

export interface KillContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Signal to send to the container as an integer or string (e.g. `SIGINT`). */
  signal?: string;
}
export const KillContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    signal: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/containers/{id}/kill", code: 200 })),
).annotate({
  identifier: "KillContainerRequest",
}) as any as S.Schema<KillContainerRequest>;

export interface KillContainerResponse {}
export const KillContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "KillContainerResponse",
}) as any as S.Schema<KillContainerResponse>;

export interface LeaveSwarmRequest {
  /** Force leave swarm, even if this is the last manager or that it will break the cluster. */
  force?: boolean;
}
export const LeaveSwarmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/swarm/leave", code: 200 })),
).annotate({
  identifier: "LeaveSwarmRequest",
}) as any as S.Schema<LeaveSwarmRequest>;

export interface LeaveSwarmResponse {}
export const LeaveSwarmResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "LeaveSwarmResponse",
}) as any as S.Schema<LeaveSwarmResponse>;

export interface ListConfigRequest {
  /** A JSON encoded value of the filters (a `map[string][]string`) to process on the configs list. Available filters: - `id=<config id>` - `label=<key> or label=<key>=value` - `name=<config name>` - `names=<config name>` */
  filters?: string;
}
export const ListConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/configs", code: 200 })),
).annotate({
  identifier: "ListConfigRequest",
}) as any as S.Schema<ListConfigRequest>;

export type ListConfigResponseBodyList = Array<Config>;
export const ListConfigResponseBodyList = /*@__PURE__*/ S.Array(
  Config,
) as any as S.Schema<ListConfigResponseBodyList>;

export type ListConfigResponse = ListConfigResponseBodyList;
export const ListConfigResponse = /*@__PURE__*/ S.suspend(() =>
  ListConfigResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListConfigResponse",
}) as any as S.Schema<ListConfigResponse>;

export interface ListContainerRequest {
  /** Return all containers. By default, only running containers are shown. */
  all?: boolean;
  /** Return this number of most recently created containers, including non-running ones. */
  limit?: number;
  /** Return the size of container as fields `SizeRw` and `SizeRootFs`. */
  size?: boolean;
  /** Filters to process on the container list, encoded as JSON (a `map[string][]string`). For example, `{"status": ["paused"]}` will only return paused containers. Available filters: - `ancestor`=(`<image-name>[:<tag>]`, `<image id>`, or `<image@digest>`) - `before`=(`<container id>` or `<container name>`) - `expose`=(`<port>[/<proto>]`|`<startport-endport>/[<proto>]`) - `exited=<int>` containers with exit code of `<int>` - `health`=(`starting`|`healthy`|`unhealthy`|`none`) - `id=<ID>` a container's ID - `isolation=`(`default`|`process`|`hyperv`) (Windows daemon only) - `is-task=`(`true`|`false`) - `label=key` or `label="key=value"` of a container label - `name=<name>` a container's name - `network`=(`<network id>` or `<network name>`) - `publish`=(`<port>[/<proto>]`|`<startport-endport>/[<proto>]`) - `since`=(`<container id>` or `<container name>`) - `status=`(`created`|`restarting`|`running`|`removing`|`paused`|`exited`|`dead`) - `volume`=(`<volume name>` or `<mount point destination>`) */
  filters?: string;
}
export const ListContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    all: S.optional(S.Boolean.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    size: S.optional(S.Boolean.pipe(T.Query())),
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/containers/json", code: 200 })),
).annotate({
  identifier: "ListContainerRequest",
}) as any as S.Schema<ListContainerRequest>;

/** The names associated with this container. Most containers have a single name, but when using legacy "links", the container can have multiple names. For historic reasons, names are prefixed with a forward-slash (`/`). */
export type ContainerSummaryNamesList = Array<string>;
export const ContainerSummaryNamesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerSummaryNamesList>;

export type PortSummaryType = "tcp" | "udp" | "sctp";
export const PortSummaryType = S.String;

/** Describes a port-mapping between the container and the host. */
export interface PortSummary {
  /** Host IP address that the container's port is mapped to */
  IP?: string;
  /** Port on the container */
  PrivatePort: number;
  /** Port exposed on the host */
  PublicPort?: number;
  Type: PortSummaryType;
}
export const PortSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    IP: S.optional(S.String),
    PrivatePort: S.Number,
    PublicPort: S.optional(S.Number),
    Type: PortSummaryType,
  }),
).annotate({ identifier: "PortSummary" }) as any as S.Schema<PortSummary>;

/** Port-mappings for the container. */
export type ContainerSummaryPortsList = Array<PortSummary>;
export const ContainerSummaryPortsList = /*@__PURE__*/ S.Array(
  PortSummary,
) as any as S.Schema<ContainerSummaryPortsList>;

/** User-defined key/value metadata. */
export type ContainerSummaryLabelsMap = { [key: string]: string | undefined };
export const ContainerSummaryLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ContainerSummaryLabelsMap>;

/** The state of this container. */
export type ContainerSummaryState =
  | "created"
  | "running"
  | "paused"
  | "restarting"
  | "exited"
  | "removing"
  | "dead";
export const ContainerSummaryState = S.String;

/** Arbitrary key-value metadata attached to the container. */
export type ContainerSummaryHostConfigAnnotationsMap = {
  [key: string]: string | undefined;
};
export const ContainerSummaryHostConfigAnnotationsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ContainerSummaryHostConfigAnnotationsMap>;

/** Summary of host-specific runtime information of the container. This is a reduced set of information in the container's "HostConfig" as available in the container "inspect" response. */
export interface ContainerSummaryHostConfig {
  /** Networking mode (`host`, `none`, `container:<id>`) or name of the primary network the container is using. This field is primarily for backward compatibility. The container can be connected to multiple networks for which information can be found in the `NetworkSettings.Networks` field, which enumerates settings per network. */
  NetworkMode?: string;
  /** Arbitrary key-value metadata attached to the container. */
  Annotations?: ContainerSummaryHostConfigAnnotationsMap | null;
}
export const ContainerSummaryHostConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NetworkMode: S.optional(S.String),
    Annotations: S.optional(S.NullOr(ContainerSummaryHostConfigAnnotationsMap)),
  }),
).annotate({
  identifier: "ContainerSummaryHostConfig",
}) as any as S.Schema<ContainerSummaryHostConfig>;

/** Summary of network-settings for each network the container is attached to. */
export type ContainerSummaryNetworkSettingsNetworksMap = {
  [key: string]: EndpointSettings | undefined;
};
export const ContainerSummaryNetworkSettingsNetworksMap =
  /*@__PURE__*/ S.Record(
    S.String,
    EndpointSettings,
  ) as any as S.Schema<ContainerSummaryNetworkSettingsNetworksMap>;

/** Summary of the container's network settings */
export interface ContainerSummaryNetworkSettings {
  /** Summary of network-settings for each network the container is attached to. */
  Networks?: ContainerSummaryNetworkSettingsNetworksMap;
}
export const ContainerSummaryNetworkSettings = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Networks: S.optional(ContainerSummaryNetworkSettingsNetworksMap),
  }),
).annotate({
  identifier: "ContainerSummaryNetworkSettings",
}) as any as S.Schema<ContainerSummaryNetworkSettings>;

/** List of mounts used by the container. */
export type ContainerSummaryMountsList = Array<MountPoint>;
export const ContainerSummaryMountsList = /*@__PURE__*/ S.Array(
  MountPoint,
) as any as S.Schema<ContainerSummaryMountsList>;

/** the health status of the container */
export type ContainerSummaryHealthStatus =
  | "none"
  | "starting"
  | "healthy"
  | "unhealthy";
export const ContainerSummaryHealthStatus = S.String;

/** Summary of health status Added in v1.52, before that version all container summary not include Health. After this attribute introduced, it includes containers with no health checks configured, or containers that are not running with none */
export interface ContainerSummaryHealth {
  /** the health status of the container */
  Status?: ContainerSummaryHealthStatus;
  /** FailingStreak is the number of consecutive failures */
  FailingStreak?: number;
}
export const ContainerSummaryHealth = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Status: S.optional(ContainerSummaryHealthStatus),
    FailingStreak: S.optional(S.Number),
  }),
).annotate({
  identifier: "ContainerSummaryHealth",
}) as any as S.Schema<ContainerSummaryHealth>;

export interface ContainerSummary {
  /** The ID of this container as a 128-bit (64-character) hexadecimal string (32 bytes). */
  Id?: string;
  /** The names associated with this container. Most containers have a single name, but when using legacy "links", the container can have multiple names. For historic reasons, names are prefixed with a forward-slash (`/`). */
  Names?: ContainerSummaryNamesList;
  /** The name or ID of the image used to create the container. This field shows the image reference as was specified when creating the container, which can be in its canonical form (e.g., `docker.io/library/ubuntu:latest` or `docker.io/library/ubuntu@sha256:72297848456d5d37d1262630108ab308d3e9ec7ed1c3286a32fe09856619a782`), short form (e.g., `ubuntu:latest`)), or the ID(-prefix) of the image (e.g., `72297848456d`). The content of this field can be updated at runtime if the image used to create the container is untagged, in which case the field is updated to contain the the image ID (digest) it was resolved to in its canonical, non-truncated form (e.g., `sha256:72297848456d5d37d1262630108ab308d3e9ec7ed1c3286a32fe09856619a782`). */
  Image?: string;
  /** The ID (digest) of the image that this container was created from. */
  ImageID?: string;
  /** OCI descriptor of the platform-specific manifest of the image the container was created from. Note: Only available if the daemon provides a multi-platform image store. This field is not populated in the `GET /system/df` endpoint. */
  ImageManifestDescriptor?: OCIDescriptor | null;
  /** Command to run when starting the container */
  Command?: string;
  /** Date and time at which the container was created as a Unix timestamp (number of seconds since EPOCH). */
  Created?: number;
  /** Port-mappings for the container. */
  Ports?: ContainerSummaryPortsList;
  /** The size of files that have been created or changed by this container. This field is omitted by default, and only set when size is requested in the API request. */
  SizeRw?: number | null;
  /** The total size of all files in the read-only layers from the image that the container uses. These layers can be shared between containers. This field is omitted by default, and only set when size is requested in the API request. */
  SizeRootFs?: number | null;
  /** User-defined key/value metadata. */
  Labels?: ContainerSummaryLabelsMap;
  /** The state of this container. */
  State?: ContainerSummaryState;
  /** Additional human-readable status of this container (e.g. `Exit 0`) */
  Status?: string;
  /** Summary of host-specific runtime information of the container. This is a reduced set of information in the container's "HostConfig" as available in the container "inspect" response. */
  HostConfig?: ContainerSummaryHostConfig;
  /** Summary of the container's network settings */
  NetworkSettings?: ContainerSummaryNetworkSettings;
  /** List of mounts used by the container. */
  Mounts?: ContainerSummaryMountsList;
  /** Summary of health status Added in v1.52, before that version all container summary not include Health. After this attribute introduced, it includes containers with no health checks configured, or containers that are not running with none */
  Health?: ContainerSummaryHealth;
}
export const ContainerSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.optional(S.String),
    Names: S.optional(ContainerSummaryNamesList),
    Image: S.optional(S.String),
    ImageID: S.optional(S.String),
    ImageManifestDescriptor: S.optional(S.NullOr(OCIDescriptor)),
    Command: S.optional(S.String),
    Created: S.optional(S.Number),
    Ports: S.optional(ContainerSummaryPortsList),
    SizeRw: S.optional(S.NullOr(S.Number)),
    SizeRootFs: S.optional(S.NullOr(S.Number)),
    Labels: S.optional(ContainerSummaryLabelsMap),
    State: S.optional(ContainerSummaryState),
    Status: S.optional(S.String),
    HostConfig: S.optional(ContainerSummaryHostConfig),
    NetworkSettings: S.optional(ContainerSummaryNetworkSettings),
    Mounts: S.optional(ContainerSummaryMountsList),
    Health: S.optional(ContainerSummaryHealth),
  }),
).annotate({
  identifier: "ContainerSummary",
}) as any as S.Schema<ContainerSummary>;

export type ListContainerResponseBodyList = Array<ContainerSummary>;
export const ListContainerResponseBodyList = /*@__PURE__*/ S.Array(
  ContainerSummary,
) as any as S.Schema<ListContainerResponseBodyList>;

export type ListContainerResponse = ListContainerResponseBodyList;
export const ListContainerResponse = /*@__PURE__*/ S.suspend(() =>
  ListContainerResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListContainerResponse",
}) as any as S.Schema<ListContainerResponse>;

export interface ListImageRequest {
  /** Show all images. Only images from a final layer (no children) are shown by default. */
  all?: boolean;
  /** A JSON encoded value of the filters (a `map[string][]string`) to process on the images list. Available filters: - `before`=(`<image-name>[:<tag>]`, `<image id>` or `<image@digest>`) - `dangling=true` - `label=key` or `label="key=value"` of an image label - `reference`=(`<image-name>[:<tag>]`) - `since`=(`<image-name>[:<tag>]`, `<image id>` or `<image@digest>`) - `until=<timestamp>` */
  filters?: string;
  /** Compute and show shared size as a `SharedSize` field on each image. */
  shared_size?: boolean;
  /** Show digest information as a `RepoDigests` field on each image. */
  digests?: boolean;
  /** Include `Manifests` in the image summary. */
  manifests?: boolean;
  /** Include `Identity` in each manifest summary. Requires `manifests=1`. */
  identity?: boolean;
}
export const ListImageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    all: S.optional(S.Boolean.pipe(T.Query())),
    filters: S.optional(S.String.pipe(T.Query())),
    shared_size: S.optional(S.Boolean.pipe(T.Query("shared-size"))),
    digests: S.optional(S.Boolean.pipe(T.Query())),
    manifests: S.optional(S.Boolean.pipe(T.Query())),
    identity: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/images/json", code: 200 })),
).annotate({
  identifier: "ListImageRequest",
}) as any as S.Schema<ListImageRequest>;

/** List of image names/tags in the local image cache that reference this image. Multiple image tags can refer to the same image, and this list may be empty if no tags reference the image, in which case the image is "untagged", in which case it can still be referenced by its ID. */
export type ImageSummaryRepoTagsList = Array<string>;
export const ImageSummaryRepoTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageSummaryRepoTagsList>;

/** List of content-addressable digests of locally available image manifests that the image is referenced from. Multiple manifests can refer to the same image. These digests are usually only available if the image was either pulled from a registry, or if the image was pushed to a registry, which is when the manifest is generated and its digest calculated. */
export type ImageSummaryRepoDigestsList = Array<string>;
export const ImageSummaryRepoDigestsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ImageSummaryRepoDigestsList>;

/** User-defined key/value metadata. */
export type ImageSummaryLabelsMap = { [key: string]: string | undefined };
export const ImageSummaryLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ImageSummaryLabelsMap>;

/** Manifests is a list of manifests available in this image. It provides a more detailed view of the platform-specific image manifests or other image-attached data like build attestations. WARNING: This is experimental and may change at any time without any backward compatibility. */
export type ImageSummaryManifestsList = Array<ImageManifestSummary>;
export const ImageSummaryManifestsList = /*@__PURE__*/ S.Array(
  ImageManifestSummary,
) as any as S.Schema<ImageSummaryManifestsList>;

export interface ImageSummary {
  /** ID is the content-addressable ID of an image. This identifier is a content-addressable digest calculated from the image's configuration (which includes the digests of layers used by the image). Note that this digest differs from the `RepoDigests` below, which holds digests of image manifests that reference the image. */
  Id: string;
  /** ID of the parent image. Depending on how the image was created, this field may be empty and is only set for images that were built/created locally. This field is empty if the image was pulled from an image registry. */
  ParentId: string;
  /** List of image names/tags in the local image cache that reference this image. Multiple image tags can refer to the same image, and this list may be empty if no tags reference the image, in which case the image is "untagged", in which case it can still be referenced by its ID. */
  RepoTags: ImageSummaryRepoTagsList;
  /** List of content-addressable digests of locally available image manifests that the image is referenced from. Multiple manifests can refer to the same image. These digests are usually only available if the image was either pulled from a registry, or if the image was pushed to a registry, which is when the manifest is generated and its digest calculated. */
  RepoDigests: ImageSummaryRepoDigestsList;
  /** Date and time at which the image was created as a Unix timestamp (number of seconds since EPOCH). */
  Created: number;
  /** Total size of the image including all layers it is composed of. */
  Size: number;
  /** Total size of image layers that are shared between this image and other images. This size is not calculated by default. `-1` indicates that the value has not been set / calculated. */
  SharedSize: number;
  /** User-defined key/value metadata. */
  Labels: ImageSummaryLabelsMap;
  /** Number of containers using this image. Includes both stopped and running containers. `-1` indicates that the value has not been set / calculated. */
  Containers: number;
  /** Manifests is a list of manifests available in this image. It provides a more detailed view of the platform-specific image manifests or other image-attached data like build attestations. WARNING: This is experimental and may change at any time without any backward compatibility. */
  Manifests?: ImageSummaryManifestsList;
  /** Descriptor is an OCI descriptor of the image target. In case of a multi-platform image, this descriptor points to the OCI index or a manifest list. This field is only present if the daemon provides a multi-platform image store. WARNING: This is experimental and may change at any time without any backward compatibility. */
  Descriptor?: OCIDescriptor | null;
}
export const ImageSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Id: S.String,
    ParentId: S.String,
    RepoTags: ImageSummaryRepoTagsList,
    RepoDigests: ImageSummaryRepoDigestsList,
    Created: S.Number,
    Size: S.Number,
    SharedSize: S.Number,
    Labels: ImageSummaryLabelsMap,
    Containers: S.Number,
    Manifests: S.optional(ImageSummaryManifestsList),
    Descriptor: S.optional(S.NullOr(OCIDescriptor)),
  }),
).annotate({ identifier: "ImageSummary" }) as any as S.Schema<ImageSummary>;

export type ListImageResponseBodyList = Array<ImageSummary>;
export const ListImageResponseBodyList = /*@__PURE__*/ S.Array(
  ImageSummary,
) as any as S.Schema<ListImageResponseBodyList>;

export type ListImageResponse = ListImageResponseBodyList;
export const ListImageResponse = /*@__PURE__*/ S.suspend(() =>
  ListImageResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListImageResponse",
}) as any as S.Schema<ListImageResponse>;

export interface ListNetworkRequest {
  /** JSON encoded value of the filters (a `map[string][]string`) to process on the networks list. Available filters: - `dangling=<boolean>` When set to `true` (or `1`), returns all networks that are not in use by a container. When set to `false` (or `0`), only networks that are in use by one or more containers are returned. - `driver=<driver-name>` Matches a network's driver. - `id=<network-id>` Matches all or part of a network ID. - `label=<key>` or `label=<key>=<value>` of a network label. - `name=<network-name>` Matches all or part of a network name. - `scope=["swarm"|"global"|"local"]` Filters networks by scope (`swarm`, `global`, or `local`). - `type=["custom"|"builtin"]` Filters networks by type. The `custom` keyword returns all user-defined networks. */
  filters?: string;
}
export const ListNetworkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/networks", code: 200 })),
).annotate({
  identifier: "ListNetworkRequest",
}) as any as S.Schema<ListNetworkRequest>;

/** Network-specific options uses when creating the network. */
export type NetworkOptionsMap = { [key: string]: string | undefined };
export const NetworkOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<NetworkOptionsMap>;

/** Metadata specific to the network being created. */
export type NetworkLabelsMap = { [key: string]: string | undefined };
export const NetworkLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<NetworkLabelsMap>;

/** represents one peer of an overlay network. */
export interface PeerInfo {
  /** ID of the peer-node in the Swarm cluster. */
  Name?: string;
  /** IP-address of the peer-node in the Swarm cluster. */
  IP?: string;
}
export const PeerInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    IP: S.optional(S.String),
  }),
).annotate({ identifier: "PeerInfo" }) as any as S.Schema<PeerInfo>;

/** List of peer nodes for an overlay network. This field is only present for overlay networks, and omitted for other network types. */
export type NetworkPeersList = Array<PeerInfo>;
export const NetworkPeersList = /*@__PURE__*/ S.Array(
  PeerInfo,
) as any as S.Schema<NetworkPeersList>;

export interface Network {
  /** Name of the network. */
  Name?: string;
  /** ID that uniquely identifies a network on a single machine. */
  Id?: string;
  /** Date and time at which the network was created in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  Created?: string;
  /** The level at which the network exists (e.g. `swarm` for cluster-wide or `local` for machine level) */
  Scope?: string;
  /** The name of the driver used to create the network (e.g. `bridge`, `overlay`). */
  Driver?: string;
  /** Whether the network was created with IPv4 enabled. */
  EnableIPv4?: boolean;
  /** Whether the network was created with IPv6 enabled. */
  EnableIPv6?: boolean;
  /** The network's IP Address Management. */
  IPAM?: IPAM;
  /** Whether the network is created to only allow internal networking connectivity. */
  Internal?: boolean;
  /** Whether a global / swarm scope network is manually attachable by regular containers from workers in swarm mode. */
  Attachable?: boolean;
  /** Whether the network is providing the routing-mesh for the swarm cluster. */
  Ingress?: boolean;
  ConfigFrom?: ConfigReference;
  /** Whether the network is a config-only network. Config-only networks are placeholder networks for network configurations to be used by other networks. Config-only networks cannot be used directly to run containers or services. */
  ConfigOnly?: boolean;
  /** Network-specific options uses when creating the network. */
  Options?: NetworkOptionsMap;
  /** Metadata specific to the network being created. */
  Labels?: NetworkLabelsMap;
  /** List of peer nodes for an overlay network. This field is only present for overlay networks, and omitted for other network types. */
  Peers?: NetworkPeersList;
}
export const Network = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Id: S.optional(S.String),
    Created: S.optional(S.String),
    Scope: S.optional(S.String),
    Driver: S.optional(S.String),
    EnableIPv4: S.optional(S.Boolean),
    EnableIPv6: S.optional(S.Boolean),
    IPAM: S.optional(IPAM),
    Internal: S.optional(S.Boolean),
    Attachable: S.optional(S.Boolean),
    Ingress: S.optional(S.Boolean),
    ConfigFrom: S.optional(ConfigReference),
    ConfigOnly: S.optional(S.Boolean),
    Options: S.optional(NetworkOptionsMap),
    Labels: S.optional(NetworkLabelsMap),
    Peers: S.optional(NetworkPeersList),
  }),
).annotate({ identifier: "Network" }) as any as S.Schema<Network>;

export type ListNetworkResponseBodyList = Array<Network>;
export const ListNetworkResponseBodyList = /*@__PURE__*/ S.Array(
  Network,
) as any as S.Schema<ListNetworkResponseBodyList>;

export type ListNetworkResponse = ListNetworkResponseBodyList;
export const ListNetworkResponse = /*@__PURE__*/ S.suspend(() =>
  ListNetworkResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListNetworkResponse",
}) as any as S.Schema<ListNetworkResponse>;

export interface ListNodeRequest {
  /** Filters to process on the nodes list, encoded as JSON (a `map[string][]string`). Available filters: - `id=<node id>` - `label=<engine label>` - `membership=`(`accepted`|`pending`)` - `name=<node name>` - `node.label=<node label>` - `role=`(`manager`|`worker`)` */
  filters?: string;
}
export const ListNodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/nodes", code: 200 })),
).annotate({
  identifier: "ListNodeRequest",
}) as any as S.Schema<ListNodeRequest>;

/** User-defined key/value metadata. */
export type NodeSpecLabelsMap = { [key: string]: string | undefined };
export const NodeSpecLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<NodeSpecLabelsMap>;

/** Role of the node. */
export type NodeSpecRole = "worker" | "manager";
export const NodeSpecRole = S.String;

/** Availability of the node. */
export type NodeSpecAvailability = "active" | "pause" | "drain";
export const NodeSpecAvailability = S.String;

export interface NodeSpec {
  /** Name for the node. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: NodeSpecLabelsMap;
  /** Role of the node. */
  Role?: NodeSpecRole;
  /** Availability of the node. */
  Availability?: NodeSpecAvailability;
}
export const NodeSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Labels: S.optional(NodeSpecLabelsMap),
    Role: S.optional(NodeSpecRole),
    Availability: S.optional(NodeSpecAvailability),
  }),
).annotate({ identifier: "NodeSpec" }) as any as S.Schema<NodeSpec>;

export type EngineDescriptionLabelsMap = { [key: string]: string | undefined };
export const EngineDescriptionLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<EngineDescriptionLabelsMap>;

export interface EngineDescriptionPluginsItem {
  Type?: string;
  Name?: string;
}
export const EngineDescriptionPluginsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Type: S.optional(S.String),
    Name: S.optional(S.String),
  }),
).annotate({
  identifier: "EngineDescriptionPluginsItem",
}) as any as S.Schema<EngineDescriptionPluginsItem>;

export type EngineDescriptionPluginsList = Array<EngineDescriptionPluginsItem>;
export const EngineDescriptionPluginsList = /*@__PURE__*/ S.Array(
  EngineDescriptionPluginsItem,
) as any as S.Schema<EngineDescriptionPluginsList>;

/** EngineDescription provides information about an engine. */
export interface EngineDescription {
  EngineVersion?: string;
  Labels?: EngineDescriptionLabelsMap;
  Plugins?: EngineDescriptionPluginsList;
}
export const EngineDescription = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    EngineVersion: S.optional(S.String),
    Labels: S.optional(EngineDescriptionLabelsMap),
    Plugins: S.optional(EngineDescriptionPluginsList),
  }),
).annotate({
  identifier: "EngineDescription",
}) as any as S.Schema<EngineDescription>;

/** Information about the issuer of leaf TLS certificates and the trusted root CA certificate. */
export interface TLSInfo {
  /** The root CA certificate(s) that are used to validate leaf TLS certificates. */
  TrustRoot?: string;
  /** The base64-url-safe-encoded raw subject bytes of the issuer. */
  CertIssuerSubject?: string;
  /** The base64-url-safe-encoded raw public key bytes of the issuer. */
  CertIssuerPublicKey?: string;
}
export const TLSInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    TrustRoot: S.optional(S.String),
    CertIssuerSubject: S.optional(S.String),
    CertIssuerPublicKey: S.optional(S.String),
  }),
).annotate({ identifier: "TLSInfo" }) as any as S.Schema<TLSInfo>;

/** NodeDescription encapsulates the properties of the Node as reported by the agent. */
export interface NodeDescription {
  Hostname?: string;
  Platform?: Platform;
  Resources?: ResourceObject;
  Engine?: EngineDescription;
  TLSInfo?: TLSInfo;
}
export const NodeDescription = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Hostname: S.optional(S.String),
    Platform: S.optional(Platform),
    Resources: S.optional(ResourceObject),
    Engine: S.optional(EngineDescription),
    TLSInfo: S.optional(TLSInfo),
  }),
).annotate({
  identifier: "NodeDescription",
}) as any as S.Schema<NodeDescription>;

/** NodeState represents the state of a node. */
export type NodeState = "unknown" | "down" | "ready" | "disconnected";
export const NodeState = S.String;

/** NodeStatus represents the status of a node. It provides the current status of the node, as seen by the manager. */
export interface NodeStatus {
  State?: NodeState;
  Message?: string;
  /** IP address of the node. */
  Addr?: string;
}
export const NodeStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    State: S.optional(NodeState),
    Message: S.optional(S.String),
    Addr: S.optional(S.String),
  }),
).annotate({ identifier: "NodeStatus" }) as any as S.Schema<NodeStatus>;

/** Reachability represents the reachability of a node. */
export type Reachability = "unknown" | "unreachable" | "reachable";
export const Reachability = S.String;

/** ManagerStatus represents the status of a manager. It provides the current status of a node's manager component, if the node is a manager. */
export interface ManagerStatus {
  Leader?: boolean;
  Reachability?: Reachability;
  /** The IP address and port at which the manager is reachable. */
  Addr?: string;
}
export const ManagerStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Leader: S.optional(S.Boolean),
    Reachability: S.optional(Reachability),
    Addr: S.optional(S.String),
  }),
).annotate({ identifier: "ManagerStatus" }) as any as S.Schema<ManagerStatus>;

export interface Node {
  ID?: string;
  Version?: ObjectVersion;
  /** Date and time at which the node was added to the swarm in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  CreatedAt?: string;
  /** Date and time at which the node was last updated in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  UpdatedAt?: string;
  Spec?: NodeSpec;
  Description?: NodeDescription;
  Status?: NodeStatus;
  ManagerStatus?: ManagerStatus | null;
}
export const Node = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Version: S.optional(ObjectVersion),
    CreatedAt: S.optional(S.String),
    UpdatedAt: S.optional(S.String),
    Spec: S.optional(NodeSpec),
    Description: S.optional(NodeDescription),
    Status: S.optional(NodeStatus),
    ManagerStatus: S.optional(S.NullOr(ManagerStatus)),
  }),
).annotate({ identifier: "Node" }) as any as S.Schema<Node>;

export type ListNodeResponseBodyList = Array<Node>;
export const ListNodeResponseBodyList = /*@__PURE__*/ S.Array(
  Node,
) as any as S.Schema<ListNodeResponseBodyList>;

export type ListNodeResponse = ListNodeResponseBodyList;
export const ListNodeResponse = /*@__PURE__*/ S.suspend(() =>
  ListNodeResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListNodeResponse",
}) as any as S.Schema<ListNodeResponse>;

export interface ListPluginRequest {
  /** A JSON encoded value of the filters (a `map[string][]string`) to process on the plugin list. Available filters: - `capability=<capability name>` - `enable=<true>|<false>` */
  filters?: string;
}
export const ListPluginRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/plugins", code: 200 })),
).annotate({
  identifier: "ListPluginRequest",
}) as any as S.Schema<ListPluginRequest>;

export type ListPluginResponseBodyList = Array<Plugin>;
export const ListPluginResponseBodyList = /*@__PURE__*/ S.Array(
  Plugin,
) as any as S.Schema<ListPluginResponseBodyList>;

export type ListPluginResponse = ListPluginResponseBodyList;
export const ListPluginResponse = /*@__PURE__*/ S.suspend(() =>
  ListPluginResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListPluginResponse",
}) as any as S.Schema<ListPluginResponse>;

export interface ListSecretRequest {
  /** A JSON encoded value of the filters (a `map[string][]string`) to process on the secrets list. Available filters: - `id=<secret id>` - `label=<key> or label=<key>=value` - `name=<secret name>` - `names=<secret name>` */
  filters?: string;
}
export const ListSecretRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/secrets", code: 200 })),
).annotate({
  identifier: "ListSecretRequest",
}) as any as S.Schema<ListSecretRequest>;

/** User-defined key/value metadata. */
export type SecretSpecLabelsMap = { [key: string]: string | undefined };
export const SecretSpecLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<SecretSpecLabelsMap>;

export interface SecretSpec {
  /** User-defined name of the secret. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: SecretSpecLabelsMap;
  /** Data is the data to store as a secret, formatted as a standard base64-encoded ([RFC 4648](https://tools.ietf.org/html/rfc4648#section-4)) string. It must be empty if the Driver field is set, in which case the data is loaded from an external secret store. The maximum allowed size is 500KB, as defined in [MaxSecretSize](https://pkg.go.dev/github.com/moby/swarmkit/v2@v2.0.0/api/validation#MaxSecretSize). This field is only used to _create_ a secret, and is not returned by other endpoints. */
  Data?: string;
  /** Name of the secrets driver used to fetch the secret's value from an external secret store. */
  Driver?: Driver;
  /** Templating driver, if applicable Templating controls whether and how to evaluate the config payload as a template. If no driver is set, no templating is used. */
  Templating?: Driver;
}
export const SecretSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Labels: S.optional(SecretSpecLabelsMap),
    Data: S.optional(S.String),
    Driver: S.optional(Driver),
    Templating: S.optional(Driver),
  }),
).annotate({ identifier: "SecretSpec" }) as any as S.Schema<SecretSpec>;

export interface Secret {
  ID?: string;
  Version?: ObjectVersion;
  CreatedAt?: string;
  UpdatedAt?: string;
  Spec?: SecretSpec;
}
export const Secret = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Version: S.optional(ObjectVersion),
    CreatedAt: S.optional(S.String),
    UpdatedAt: S.optional(S.String),
    Spec: S.optional(SecretSpec),
  }),
).annotate({ identifier: "Secret" }) as any as S.Schema<Secret>;

export type ListSecretResponseBodyList = Array<Secret>;
export const ListSecretResponseBodyList = /*@__PURE__*/ S.Array(
  Secret,
) as any as S.Schema<ListSecretResponseBodyList>;

export type ListSecretResponse = ListSecretResponseBodyList;
export const ListSecretResponse = /*@__PURE__*/ S.suspend(() =>
  ListSecretResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListSecretResponse",
}) as any as S.Schema<ListSecretResponse>;

export interface ListServiceRequest {
  /** A JSON encoded value of the filters (a `map[string][]string`) to process on the services list. Available filters: - `id=<service id>` - `label=<service label>` - `mode=["replicated"|"global"]` - `name=<service name>` */
  filters?: string;
  /** Include service status, with count of running and desired tasks. */
  status?: boolean;
}
export const ListServiceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
    status: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/services", code: 200 })),
).annotate({
  identifier: "ListServiceRequest",
}) as any as S.Schema<ListServiceRequest>;

/** User-defined key/value metadata. */
export type ServiceSpecLabelsMap = { [key: string]: string | undefined };
export const ServiceSpecLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ServiceSpecLabelsMap>;

export type ServiceSpecModeReplicated = CreateServiceRequestModeReplicated;
export const ServiceSpecModeReplicated = CreateServiceRequestModeReplicated;

/** The mode used for services with a finite number of tasks that run to a completed state. */
export type ServiceSpecModeReplicatedJob =
  CreateServiceRequestModeReplicatedJob;
export const ServiceSpecModeReplicatedJob =
  CreateServiceRequestModeReplicatedJob;

/** Scheduling mode for the service. */
export type ServiceSpecMode = CreateServiceRequestMode;
export const ServiceSpecMode = CreateServiceRequestMode;

/** Action to take if an updated task fails to run, or stops running during the update. */
export type ServiceSpecUpdateConfigFailureAction =
  | "continue"
  | "pause"
  | "rollback";
export const ServiceSpecUpdateConfigFailureAction = S.String;

/** The order of operations when rolling out an updated task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
export type ServiceSpecUpdateConfigOrder = "stop-first" | "start-first";
export const ServiceSpecUpdateConfigOrder = S.String;

/** Specification for the update strategy of the service. */
export interface ServiceSpecUpdateConfig {
  /** Maximum number of tasks to be updated in one iteration (0 means unlimited parallelism). */
  Parallelism?: number;
  /** Amount of time between updates, in nanoseconds. */
  Delay?: number;
  /** Action to take if an updated task fails to run, or stops running during the update. */
  FailureAction?: ServiceSpecUpdateConfigFailureAction;
  /** Amount of time to monitor each updated task for failures, in nanoseconds. */
  Monitor?: number;
  /** The fraction of tasks that may fail during an update before the failure action is invoked, specified as a floating point number between 0 and 1. */
  MaxFailureRatio?: number;
  /** The order of operations when rolling out an updated task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
  Order?: ServiceSpecUpdateConfigOrder;
}
export const ServiceSpecUpdateConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Parallelism: S.optional(S.Number),
    Delay: S.optional(S.Number),
    FailureAction: S.optional(ServiceSpecUpdateConfigFailureAction),
    Monitor: S.optional(S.Number),
    MaxFailureRatio: S.optional(S.Number),
    Order: S.optional(ServiceSpecUpdateConfigOrder),
  }),
).annotate({
  identifier: "ServiceSpecUpdateConfig",
}) as any as S.Schema<ServiceSpecUpdateConfig>;

/** Action to take if an rolled back task fails to run, or stops running during the rollback. */
export type ServiceSpecRollbackConfigFailureAction = "continue" | "pause";
export const ServiceSpecRollbackConfigFailureAction = S.String;

/** The order of operations when rolling back a task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
export type ServiceSpecRollbackConfigOrder = "stop-first" | "start-first";
export const ServiceSpecRollbackConfigOrder = S.String;

/** Specification for the rollback strategy of the service. */
export interface ServiceSpecRollbackConfig {
  /** Maximum number of tasks to be rolled back in one iteration (0 means unlimited parallelism). */
  Parallelism?: number;
  /** Amount of time between rollback iterations, in nanoseconds. */
  Delay?: number;
  /** Action to take if an rolled back task fails to run, or stops running during the rollback. */
  FailureAction?: ServiceSpecRollbackConfigFailureAction;
  /** Amount of time to monitor each rolled back task for failures, in nanoseconds. */
  Monitor?: number;
  /** The fraction of tasks that may fail during a rollback before the failure action is invoked, specified as a floating point number between 0 and 1. */
  MaxFailureRatio?: number;
  /** The order of operations when rolling back a task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
  Order?: ServiceSpecRollbackConfigOrder;
}
export const ServiceSpecRollbackConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Parallelism: S.optional(S.Number),
    Delay: S.optional(S.Number),
    FailureAction: S.optional(ServiceSpecRollbackConfigFailureAction),
    Monitor: S.optional(S.Number),
    MaxFailureRatio: S.optional(S.Number),
    Order: S.optional(ServiceSpecRollbackConfigOrder),
  }),
).annotate({
  identifier: "ServiceSpecRollbackConfig",
}) as any as S.Schema<ServiceSpecRollbackConfig>;

/** Specifies which networks the service should attach to. Deprecated: This field is deprecated since v1.44. The Networks field in TaskSpec should be used instead. */
export type ServiceSpecNetworksList = Array<NetworkAttachmentConfig>;
export const ServiceSpecNetworksList = /*@__PURE__*/ S.Array(
  NetworkAttachmentConfig,
) as any as S.Schema<ServiceSpecNetworksList>;

/** User modifiable configuration for a service. */
export interface ServiceSpec {
  /** Name of the service. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: ServiceSpecLabelsMap;
  TaskTemplate?: TaskSpec;
  /** Scheduling mode for the service. */
  Mode?: CreateServiceRequestMode;
  /** Specification for the update strategy of the service. */
  UpdateConfig?: ServiceSpecUpdateConfig;
  /** Specification for the rollback strategy of the service. */
  RollbackConfig?: ServiceSpecRollbackConfig;
  /** Specifies which networks the service should attach to. Deprecated: This field is deprecated since v1.44. The Networks field in TaskSpec should be used instead. */
  Networks?: ServiceSpecNetworksList;
  EndpointSpec?: EndpointSpec;
}
export const ServiceSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Labels: S.optional(ServiceSpecLabelsMap),
    TaskTemplate: S.optional(TaskSpec),
    Mode: S.optional(CreateServiceRequestMode),
    UpdateConfig: S.optional(ServiceSpecUpdateConfig),
    RollbackConfig: S.optional(ServiceSpecRollbackConfig),
    Networks: S.optional(ServiceSpecNetworksList),
    EndpointSpec: S.optional(EndpointSpec),
  }),
).annotate({ identifier: "ServiceSpec" }) as any as S.Schema<ServiceSpec>;

export type ServiceEndpointPortsList = Array<EndpointPortConfig>;
export const ServiceEndpointPortsList = /*@__PURE__*/ S.Array(
  EndpointPortConfig,
) as any as S.Schema<ServiceEndpointPortsList>;

export interface ServiceEndpointVirtualIPsItem {
  NetworkID?: string;
  Addr?: string;
}
export const ServiceEndpointVirtualIPsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NetworkID: S.optional(S.String),
    Addr: S.optional(S.String),
  }),
).annotate({
  identifier: "ServiceEndpointVirtualIPsItem",
}) as any as S.Schema<ServiceEndpointVirtualIPsItem>;

export type ServiceEndpointVirtualIPsList =
  Array<ServiceEndpointVirtualIPsItem>;
export const ServiceEndpointVirtualIPsList = /*@__PURE__*/ S.Array(
  ServiceEndpointVirtualIPsItem,
) as any as S.Schema<ServiceEndpointVirtualIPsList>;

export interface ServiceEndpoint {
  Spec?: EndpointSpec;
  Ports?: ServiceEndpointPortsList;
  VirtualIPs?: ServiceEndpointVirtualIPsList;
}
export const ServiceEndpoint = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Spec: S.optional(EndpointSpec),
    Ports: S.optional(ServiceEndpointPortsList),
    VirtualIPs: S.optional(ServiceEndpointVirtualIPsList),
  }),
).annotate({
  identifier: "ServiceEndpoint",
}) as any as S.Schema<ServiceEndpoint>;

export type ServiceUpdateStatusState = "updating" | "paused" | "completed";
export const ServiceUpdateStatusState = S.String;

/** The status of a service update. */
export interface ServiceUpdateStatus {
  State?: ServiceUpdateStatusState;
  StartedAt?: string;
  CompletedAt?: string;
  Message?: string;
}
export const ServiceUpdateStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    State: S.optional(ServiceUpdateStatusState),
    StartedAt: S.optional(S.String),
    CompletedAt: S.optional(S.String),
    Message: S.optional(S.String),
  }),
).annotate({
  identifier: "ServiceUpdateStatus",
}) as any as S.Schema<ServiceUpdateStatus>;

/** The status of the service's tasks. Provided only when requested as part of a ServiceList operation. */
export interface ServiceServiceStatus {
  /** The number of tasks for the service currently in the Running state. */
  RunningTasks?: number;
  /** The number of tasks for the service desired to be running. For replicated services, this is the replica count from the service spec. For global services, this is computed by taking count of all tasks for the service with a Desired State other than Shutdown. */
  DesiredTasks?: number;
  /** The number of tasks for a job that are in the Completed state. This field must be cross-referenced with the service type, as the value of 0 may mean the service is not in a job mode, or it may mean the job-mode service has no tasks yet Completed. */
  CompletedTasks?: number;
}
export const ServiceServiceStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    RunningTasks: S.optional(S.Number),
    DesiredTasks: S.optional(S.Number),
    CompletedTasks: S.optional(S.Number),
  }),
).annotate({
  identifier: "ServiceServiceStatus",
}) as any as S.Schema<ServiceServiceStatus>;

/** The status of the service when it is in one of ReplicatedJob or GlobalJob modes. Absent on Replicated and Global mode services. The JobIteration is an ObjectVersion, but unlike the Service's version, does not need to be sent with an update request. */
export interface ServiceJobStatus {
  /** JobIteration is a value increased each time a Job is executed, successfully or otherwise. "Executed", in this case, means the job as a whole has been started, not that an individual Task has been launched. A job is "Executed" when its ServiceSpec is updated. JobIteration can be used to disambiguate Tasks belonging to different executions of a job. Though JobIteration will increase with each subsequent execution, it may not necessarily increase by 1, and so JobIteration should not be used to */
  JobIteration?: ObjectVersion;
  /** The last time, as observed by the server, that this job was started. */
  LastExecution?: string;
}
export const ServiceJobStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    JobIteration: S.optional(ObjectVersion),
    LastExecution: S.optional(S.String),
  }),
).annotate({
  identifier: "ServiceJobStatus",
}) as any as S.Schema<ServiceJobStatus>;

export interface Service {
  ID?: string;
  Version?: ObjectVersion;
  CreatedAt?: string;
  UpdatedAt?: string;
  Spec?: ServiceSpec;
  Endpoint?: ServiceEndpoint;
  /** The status of a service update. */
  UpdateStatus?: ServiceUpdateStatus;
  /** The status of the service's tasks. Provided only when requested as part of a ServiceList operation. */
  ServiceStatus?: ServiceServiceStatus;
  /** The status of the service when it is in one of ReplicatedJob or GlobalJob modes. Absent on Replicated and Global mode services. The JobIteration is an ObjectVersion, but unlike the Service's version, does not need to be sent with an update request. */
  JobStatus?: ServiceJobStatus;
}
export const Service = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Version: S.optional(ObjectVersion),
    CreatedAt: S.optional(S.String),
    UpdatedAt: S.optional(S.String),
    Spec: S.optional(ServiceSpec),
    Endpoint: S.optional(ServiceEndpoint),
    UpdateStatus: S.optional(ServiceUpdateStatus),
    ServiceStatus: S.optional(ServiceServiceStatus),
    JobStatus: S.optional(ServiceJobStatus),
  }),
).annotate({ identifier: "Service" }) as any as S.Schema<Service>;

export type ListServiceResponseBodyList = Array<Service>;
export const ListServiceResponseBodyList = /*@__PURE__*/ S.Array(
  Service,
) as any as S.Schema<ListServiceResponseBodyList>;

export type ListServiceResponse = ListServiceResponseBodyList;
export const ListServiceResponse = /*@__PURE__*/ S.suspend(() =>
  ListServiceResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListServiceResponse",
}) as any as S.Schema<ListServiceResponse>;

export interface ListTaskRequest {
  /** A JSON encoded value of the filters (a `map[string][]string`) to process on the tasks list. Available filters: - `desired-state=(running | shutdown | accepted)` - `id=<task id>` - `label=key` or `label="key=value"` - `name=<task name>` - `node=<node id or name>` - `service=<service name>` */
  filters?: string;
}
export const ListTaskRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/tasks", code: 200 })),
).annotate({
  identifier: "ListTaskRequest",
}) as any as S.Schema<ListTaskRequest>;

/** User-defined key/value metadata. */
export type TaskLabelsMap = { [key: string]: string | undefined };
export const TaskLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<TaskLabelsMap>;

export type TaskState =
  | "new"
  | "allocated"
  | "pending"
  | "assigned"
  | "accepted"
  | "preparing"
  | "ready"
  | "starting"
  | "running"
  | "complete"
  | "shutdown"
  | "failed"
  | "rejected"
  | "remove"
  | "orphaned";
export const TaskState = S.String;

/** represents the status of a container. */
export interface ContainerStatus {
  ContainerID?: string;
  PID?: number;
  ExitCode?: number;
}
export const ContainerStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ContainerID: S.optional(S.String),
    PID: S.optional(S.Number),
    ExitCode: S.optional(S.Number),
  }),
).annotate({
  identifier: "ContainerStatus",
}) as any as S.Schema<ContainerStatus>;

export type PortStatusPortsList = Array<EndpointPortConfig>;
export const PortStatusPortsList = /*@__PURE__*/ S.Array(
  EndpointPortConfig,
) as any as S.Schema<PortStatusPortsList>;

/** represents the port status of a task's host ports whose service has published host ports */
export interface PortStatus {
  Ports?: PortStatusPortsList;
}
export const PortStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Ports: S.optional(PortStatusPortsList),
  }),
).annotate({ identifier: "PortStatus" }) as any as S.Schema<PortStatus>;

/** represents the status of a task. */
export interface TaskStatus {
  Timestamp?: string;
  State?: TaskState;
  Message?: string;
  Err?: string;
  ContainerStatus?: ContainerStatus;
  PortStatus?: PortStatus;
}
export const TaskStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Timestamp: S.optional(S.String),
    State: S.optional(TaskState),
    Message: S.optional(S.String),
    Err: S.optional(S.String),
    ContainerStatus: S.optional(ContainerStatus),
    PortStatus: S.optional(PortStatus),
  }),
).annotate({ identifier: "TaskStatus" }) as any as S.Schema<TaskStatus>;

/** The IP addresses (in CIDR notation) assigned to the task on this network. To maintain backward compatibility this field accepts CIDR notation, but only the IP address is used. */
export type NetworkAttachmentAddressesList = Array<string>;
export const NetworkAttachmentAddressesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<NetworkAttachmentAddressesList>;

/** Specifies how a task is attached to a network, and the addresses the task was assigned on that network. */
export interface NetworkAttachment {
  Network?: Network;
  /** The IP addresses (in CIDR notation) assigned to the task on this network. To maintain backward compatibility this field accepts CIDR notation, but only the IP address is used. */
  Addresses?: NetworkAttachmentAddressesList;
}
export const NetworkAttachment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Network: S.optional(Network),
    Addresses: S.optional(NetworkAttachmentAddressesList),
  }),
).annotate({
  identifier: "NetworkAttachment",
}) as any as S.Schema<NetworkAttachment>;

/** The networks that this task is attached to, and the addresses the task was assigned on each of them. */
export type TaskNetworksAttachmentsList = Array<NetworkAttachment>;
export const TaskNetworksAttachmentsList = /*@__PURE__*/ S.Array(
  NetworkAttachment,
) as any as S.Schema<TaskNetworksAttachmentsList>;

export interface Task {
  /** The ID of the task. */
  ID?: string;
  Version?: ObjectVersion;
  CreatedAt?: string;
  UpdatedAt?: string;
  /** Name of the task. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: TaskLabelsMap;
  Spec?: TaskSpec;
  /** The ID of the service this task is part of. */
  ServiceID?: string;
  Slot?: number;
  /** The ID of the node that this task is on. */
  NodeID?: string;
  AssignedGenericResources?: GenericResources;
  Status?: TaskStatus;
  DesiredState?: TaskState;
  /** If the Service this Task belongs to is a job-mode service, contains the JobIteration of the Service this Task was created for. Absent if the Task was created for a Replicated or Global Service. */
  JobIteration?: ObjectVersion;
  /** The networks that this task is attached to, and the addresses the task was assigned on each of them. */
  NetworksAttachments?: TaskNetworksAttachmentsList;
}
export const Task = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Version: S.optional(ObjectVersion),
    CreatedAt: S.optional(S.String),
    UpdatedAt: S.optional(S.String),
    Name: S.optional(S.String),
    Labels: S.optional(TaskLabelsMap),
    Spec: S.optional(TaskSpec),
    ServiceID: S.optional(S.String),
    Slot: S.optional(S.Number),
    NodeID: S.optional(S.String),
    AssignedGenericResources: S.optional(GenericResources),
    Status: S.optional(TaskStatus),
    DesiredState: S.optional(TaskState),
    JobIteration: S.optional(ObjectVersion),
    NetworksAttachments: S.optional(TaskNetworksAttachmentsList),
  }),
).annotate({ identifier: "Task" }) as any as S.Schema<Task>;

export type ListTaskResponseBodyList = Array<Task>;
export const ListTaskResponseBodyList = /*@__PURE__*/ S.Array(
  Task,
) as any as S.Schema<ListTaskResponseBodyList>;

export type ListTaskResponse = ListTaskResponseBodyList;
export const ListTaskResponse = /*@__PURE__*/ S.suspend(() =>
  ListTaskResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListTaskResponse",
}) as any as S.Schema<ListTaskResponse>;

export interface ListVolumeRequest {
  /** JSON encoded value of the filters (a `map[string][]string`) to process on the volumes list. Available filters: - `dangling=<boolean>` When set to `true` (or `1`), returns all volumes that are not in use by a container. When set to `false` (or `0`), only volumes that are in use by one or more containers are returned. - `driver=<volume-driver-name>` Matches volumes based on their driver. - `label=<key>` or `label=<key>:<value>` Matches volumes based on the presence of a `label` alone or a `label` and a value. - `name=<volume-name>` Matches all or part of a volume name. */
  filters?: string;
}
export const ListVolumeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/volumes", code: 200 })),
).annotate({
  identifier: "ListVolumeRequest",
}) as any as S.Schema<ListVolumeRequest>;

/** List of volumes */
export type VolumeListResponseVolumesList = Array<Volume>;
export const VolumeListResponseVolumesList = /*@__PURE__*/ S.Array(
  Volume,
) as any as S.Schema<VolumeListResponseVolumesList>;

/** Warnings that occurred when fetching the list of volumes. */
export type VolumeListResponseWarningsList = Array<string>;
export const VolumeListResponseWarningsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<VolumeListResponseWarningsList>;

/** Volume list response */
export interface VolumeListResponse {
  /** List of volumes */
  Volumes?: VolumeListResponseVolumesList;
  /** Warnings that occurred when fetching the list of volumes. */
  Warnings?: VolumeListResponseWarningsList;
}
export const VolumeListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Volumes: S.optional(VolumeListResponseVolumesList),
    Warnings: S.optional(VolumeListResponseWarningsList),
  }),
).annotate({
  identifier: "VolumeListResponse",
}) as any as S.Schema<VolumeListResponse>;

export interface NetworkInspectRequest {
  /** Network ID or name */
  id: string;
  /** Detailed inspect output for troubleshooting */
  verbose?: boolean;
  /** Filter the network by scope (swarm, global, or local) */
  scope?: string;
}
export const NetworkInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    verbose: S.optional(S.Boolean.pipe(T.Query())),
    scope: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/networks/{id}", code: 200 })),
).annotate({
  identifier: "NetworkInspectRequest",
}) as any as S.Schema<NetworkInspectRequest>;

/** Network-specific options uses when creating the network. */
export type NetworkInspectResponseOptionsMap = {
  [key: string]: string | undefined;
};
export const NetworkInspectResponseOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<NetworkInspectResponseOptionsMap>;

/** Metadata specific to the network being created. */
export type NetworkInspectResponseLabelsMap = {
  [key: string]: string | undefined;
};
export const NetworkInspectResponseLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<NetworkInspectResponseLabelsMap>;

/** List of peer nodes for an overlay network. This field is only present for overlay networks, and omitted for other network types. */
export type NetworkInspectResponsePeersList = Array<PeerInfo>;
export const NetworkInspectResponsePeersList = /*@__PURE__*/ S.Array(
  PeerInfo,
) as any as S.Schema<NetworkInspectResponsePeersList>;

/** contains network resources allocated and used for a container in a network. */
export interface EndpointResource {
  Name?: string;
  EndpointID?: string;
  MacAddress?: string;
  IPv4Address?: string;
  IPv6Address?: string;
}
export const EndpointResource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    EndpointID: S.optional(S.String),
    MacAddress: S.optional(S.String),
    IPv4Address: S.optional(S.String),
    IPv6Address: S.optional(S.String),
  }),
).annotate({
  identifier: "EndpointResource",
}) as any as S.Schema<EndpointResource>;

/** Contains endpoints attached to the network. */
export type NetworkInspectResponseContainersMap = {
  [key: string]: EndpointResource | undefined;
};
export const NetworkInspectResponseContainersMap = /*@__PURE__*/ S.Record(
  S.String,
  EndpointResource,
) as any as S.Schema<NetworkInspectResponseContainersMap>;

/** List of services using the network. This field is only present for swarm scope networks, and omitted for local scope networks. */
export type NetworkInspectResponseServicesMap = {
  [key: string]: unknown | undefined;
};
export const NetworkInspectResponseServicesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<NetworkInspectResponseServicesMap>;

export interface SubnetStatus {
  /** Number of IP addresses in the subnet that are in use or reserved and are therefore unavailable for allocation, saturating at 2<sup>64</sup> - 1. */
  IPsInUse?: number;
  /** Number of IP addresses within the network's IPRange for the subnet that are available for allocation, saturating at 2<sup>64</sup> - 1. */
  DynamicIPsAvailable?: number;
}
export const SubnetStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    IPsInUse: S.optional(S.Number),
    DynamicIPsAvailable: S.optional(S.Number),
  }),
).annotate({ identifier: "SubnetStatus" }) as any as S.Schema<SubnetStatus>;

export type IPAMStatusSubnetsMap = { [key: string]: SubnetStatus | undefined };
export const IPAMStatusSubnetsMap = /*@__PURE__*/ S.Record(
  S.String,
  SubnetStatus,
) as any as S.Schema<IPAMStatusSubnetsMap>;

export interface IPAMStatus {
  Subnets?: IPAMStatusSubnetsMap;
}
export const IPAMStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Subnets: S.optional(IPAMStatusSubnetsMap),
  }),
).annotate({ identifier: "IPAMStatus" }) as any as S.Schema<IPAMStatus>;

/** provides runtime information about the network such as the number of allocated IPs. */
export interface NetworkStatus {
  IPAM?: IPAMStatus;
}
export const NetworkStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    IPAM: S.optional(IPAMStatus),
  }),
).annotate({ identifier: "NetworkStatus" }) as any as S.Schema<NetworkStatus>;

export interface NetworkInspectResponse {
  /** Name of the network. */
  Name?: string;
  /** ID that uniquely identifies a network on a single machine. */
  Id?: string;
  /** Date and time at which the network was created in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  Created?: string;
  /** The level at which the network exists (e.g. `swarm` for cluster-wide or `local` for machine level) */
  Scope?: string;
  /** The name of the driver used to create the network (e.g. `bridge`, `overlay`). */
  Driver?: string;
  /** Whether the network was created with IPv4 enabled. */
  EnableIPv4?: boolean;
  /** Whether the network was created with IPv6 enabled. */
  EnableIPv6?: boolean;
  /** The network's IP Address Management. */
  IPAM?: IPAM;
  /** Whether the network is created to only allow internal networking connectivity. */
  Internal?: boolean;
  /** Whether a global / swarm scope network is manually attachable by regular containers from workers in swarm mode. */
  Attachable?: boolean;
  /** Whether the network is providing the routing-mesh for the swarm cluster. */
  Ingress?: boolean;
  ConfigFrom?: ConfigReference;
  /** Whether the network is a config-only network. Config-only networks are placeholder networks for network configurations to be used by other networks. Config-only networks cannot be used directly to run containers or services. */
  ConfigOnly?: boolean;
  /** Network-specific options uses when creating the network. */
  Options?: NetworkInspectResponseOptionsMap;
  /** Metadata specific to the network being created. */
  Labels?: NetworkInspectResponseLabelsMap;
  /** List of peer nodes for an overlay network. This field is only present for overlay networks, and omitted for other network types. */
  Peers?: NetworkInspectResponsePeersList;
  /** Contains endpoints attached to the network. */
  Containers?: NetworkInspectResponseContainersMap;
  /** List of services using the network. This field is only present for swarm scope networks, and omitted for local scope networks. */
  Services?: NetworkInspectResponseServicesMap;
  /** provides runtime information about the network such as the number of allocated IPs. */
  Status?: NetworkStatus;
}
export const NetworkInspectResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Id: S.optional(S.String),
    Created: S.optional(S.String),
    Scope: S.optional(S.String),
    Driver: S.optional(S.String),
    EnableIPv4: S.optional(S.Boolean),
    EnableIPv6: S.optional(S.Boolean),
    IPAM: S.optional(IPAM),
    Internal: S.optional(S.Boolean),
    Attachable: S.optional(S.Boolean),
    Ingress: S.optional(S.Boolean),
    ConfigFrom: S.optional(ConfigReference),
    ConfigOnly: S.optional(S.Boolean),
    Options: S.optional(NetworkInspectResponseOptionsMap),
    Labels: S.optional(NetworkInspectResponseLabelsMap),
    Peers: S.optional(NetworkInspectResponsePeersList),
    Containers: S.optional(NetworkInspectResponseContainersMap),
    Services: S.optional(NetworkInspectResponseServicesMap),
    Status: S.optional(NetworkStatus),
  }),
).annotate({
  identifier: "NetworkInspectResponse",
}) as any as S.Schema<NetworkInspectResponse>;

export interface NetworkPruneRequest {
  /** Filters to process on the prune list, encoded as JSON (a `map[string][]string`). Available filters: - `until=<timestamp>` Prune networks created before this timestamp. The `<timestamp>` can be Unix timestamps, date formatted timestamps, or Go duration strings (e.g. `10m`, `1h30m`) computed relative to the daemon machine’s time. - `label` (`label=<key>`, `label=<key>=<value>`, `label!=<key>`, or `label!=<key>=<value>`) Prune networks with (or without, in case `label!=...` is used) the specified labels. */
  filters?: string;
}
export const NetworkPruneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/networks/prune", code: 200 })),
).annotate({
  identifier: "NetworkPruneRequest",
}) as any as S.Schema<NetworkPruneRequest>;

/** Networks that were deleted */
export type NetworkPruneResponseNetworksDeletedList = Array<string>;
export const NetworkPruneResponseNetworksDeletedList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<NetworkPruneResponseNetworksDeletedList>;

export interface NetworkPruneResponse {
  /** Networks that were deleted */
  NetworksDeleted?: NetworkPruneResponseNetworksDeletedList;
}
export const NetworkPruneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NetworksDeleted: S.optional(NetworkPruneResponseNetworksDeletedList),
  }),
).annotate({
  identifier: "NetworkPruneResponse",
}) as any as S.Schema<NetworkPruneResponse>;

export interface NodeInspectRequest {
  /** The ID or name of the node */
  id: string;
}
export const NodeInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/nodes/{id}", code: 200 })),
).annotate({
  identifier: "NodeInspectRequest",
}) as any as S.Schema<NodeInspectRequest>;

export interface PauseContainerRequest {
  /** ID or name of the container */
  id: string;
}
export const PauseContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "POST", uri: "/containers/{id}/pause", code: 200 })),
).annotate({
  identifier: "PauseContainerRequest",
}) as any as S.Schema<PauseContainerRequest>;

export interface PauseContainerResponse {}
export const PauseContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PauseContainerResponse",
}) as any as S.Schema<PauseContainerResponse>;

export interface PingSystemRequest {}
export const PingSystemRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/_ping", code: 200 })),
).annotate({
  identifier: "PingSystemRequest",
}) as any as S.Schema<PingSystemRequest>;

export type PingSystemResponse = string;
export const PingSystemResponse = /*@__PURE__*/ S.suspend(() =>
  S.String.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "PingSystemResponse",
}) as any as S.Schema<PingSystemResponse>;

export interface PluginInspectRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  name: string;
}
export const PluginInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/plugins/{name}/json", code: 200 })),
).annotate({
  identifier: "PluginInspectRequest",
}) as any as S.Schema<PluginInspectRequest>;

export type PluginPullRequestBodyList = Array<PluginPrivilege>;
export const PluginPullRequestBodyList = /*@__PURE__*/ S.Array(
  PluginPrivilege,
) as any as S.Schema<PluginPullRequestBodyList>;

export interface PluginPullRequest {
  /** Remote reference for plugin to install. The `:latest` tag is optional, and is used as the default if omitted. */
  remote: string;
  /** Local name for the pulled plugin. The `:latest` tag is optional, and is used as the default if omitted. */
  name?: string;
  /** A base64url-encoded auth configuration to use when pulling a plugin from a registry. Refer to the [authentication section](#section/Authentication) for details. */
  xRegistryAuth?: string;
  body?: PluginPullRequestBodyList;
}
export const PluginPullRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    remote: S.String.pipe(T.Query()),
    name: S.optional(S.String.pipe(T.Query())),
    xRegistryAuth: S.optional(S.String.pipe(T.Header("X-Registry-Auth"))),
    body: S.optional(PluginPullRequestBodyList.pipe(T.HttpBody())),
  }).pipe(T.Http({ method: "POST", uri: "/plugins/pull", code: 200 })),
).annotate({
  identifier: "PluginPullRequest",
}) as any as S.Schema<PluginPullRequest>;

export interface PluginPullResponse {}
export const PluginPullResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PluginPullResponse",
}) as any as S.Schema<PluginPullResponse>;

export interface PluginPushRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  name: string;
}
export const PluginPushRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "POST", uri: "/plugins/{name}/push", code: 200 })),
).annotate({
  identifier: "PluginPushRequest",
}) as any as S.Schema<PluginPushRequest>;

export interface PluginPushResponse {}
export const PluginPushResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PluginPushResponse",
}) as any as S.Schema<PluginPushResponse>;

export type PluginUpgradeRequestBodyList = Array<PluginPrivilege>;
export const PluginUpgradeRequestBodyList = /*@__PURE__*/ S.Array(
  PluginPrivilege,
) as any as S.Schema<PluginUpgradeRequestBodyList>;

export interface PluginUpgradeRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  name: string;
  /** Remote reference to upgrade to. The `:latest` tag is optional, and is used as the default if omitted. */
  remote: string;
  /** A base64url-encoded auth configuration to use when pulling a plugin from a registry. Refer to the [authentication section](#section/Authentication) for details. */
  xRegistryAuth?: string;
  body?: PluginUpgradeRequestBodyList;
}
export const PluginUpgradeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    remote: S.String.pipe(T.Query()),
    xRegistryAuth: S.optional(S.String.pipe(T.Header("X-Registry-Auth"))),
    body: S.optional(PluginUpgradeRequestBodyList.pipe(T.HttpBody())),
  }).pipe(
    T.Http({ method: "POST", uri: "/plugins/{name}/upgrade", code: 200 }),
  ),
).annotate({
  identifier: "PluginUpgradeRequest",
}) as any as S.Schema<PluginUpgradeRequest>;

export interface PluginUpgradeResponse {}
export const PluginUpgradeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PluginUpgradeResponse",
}) as any as S.Schema<PluginUpgradeResponse>;

export interface PutContainerArchiveRequest {
  /** ID or name of the container */
  id: string;
  /** Path to a directory in the container to extract the archive’s contents into. */
  path: string;
  /** If `1`, `true`, or `True` then it will be an error if unpacking the given content would cause an existing directory to be replaced with a non-directory and vice versa. */
  noOverwriteDirNonDir?: string;
  /** If `1`, `true`, then it will copy UID/GID maps to the dest file or dir */
  copyUIDGID?: string;
  body: string;
}
export const PutContainerArchiveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
    noOverwriteDirNonDir: S.optional(S.String.pipe(T.Query())),
    copyUIDGID: S.optional(S.String.pipe(T.Query())),
    body: S.String.pipe(T.HttpBody()),
  }).pipe(
    T.Http({ method: "PUT", uri: "/containers/{id}/archive", code: 200 }),
  ),
).annotate({
  identifier: "PutContainerArchiveRequest",
}) as any as S.Schema<PutContainerArchiveRequest>;

export interface PutContainerArchiveResponse {}
export const PutContainerArchiveResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PutContainerArchiveResponse",
}) as any as S.Schema<PutContainerArchiveResponse>;

export interface RenameContainerRequest {
  /** ID or name of the container */
  id: string;
  /** New name for the container */
  name: string;
}
export const RenameContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({ method: "POST", uri: "/containers/{id}/rename", code: 200 }),
  ),
).annotate({
  identifier: "RenameContainerRequest",
}) as any as S.Schema<RenameContainerRequest>;

export interface RenameContainerResponse {}
export const RenameContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "RenameContainerResponse",
}) as any as S.Schema<RenameContainerResponse>;

export interface ResizeContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Height of the TTY session in characters */
  h: number;
  /** Width of the TTY session in characters */
  w: number;
}
export const ResizeContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    h: S.Number.pipe(T.Query()),
    w: S.Number.pipe(T.Query()),
  }).pipe(
    T.Http({ method: "POST", uri: "/containers/{id}/resize", code: 200 }),
  ),
).annotate({
  identifier: "ResizeContainerRequest",
}) as any as S.Schema<ResizeContainerRequest>;

export interface ResizeContainerResponse {}
export const ResizeContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ResizeContainerResponse",
}) as any as S.Schema<ResizeContainerResponse>;

export interface RestartContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Signal to send to the container as an integer or string (e.g. `SIGINT`). */
  signal?: string;
  /** Number of seconds to wait before killing the container */
  t?: number;
}
export const RestartContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    signal: S.optional(S.String.pipe(T.Query())),
    t: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "POST", uri: "/containers/{id}/restart", code: 200 }),
  ),
).annotate({
  identifier: "RestartContainerRequest",
}) as any as S.Schema<RestartContainerRequest>;

export interface RestartContainerResponse {}
export const RestartContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "RestartContainerResponse",
}) as any as S.Schema<RestartContainerResponse>;

export interface SearchImageRequest {
  /** Term to search */
  term: string;
  /** Maximum number of results to return */
  limit?: number;
  /** A JSON encoded value of the filters (a `map[string][]string`) to process on the images list. Available filters: - `is-official=(true|false)` - `stars=<number>` Matches images that has at least 'number' stars. */
  filters?: string;
}
export const SearchImageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    term: S.String.pipe(T.Query()),
    limit: S.optional(S.Number.pipe(T.Query())),
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/images/search", code: 200 })),
).annotate({
  identifier: "SearchImageRequest",
}) as any as S.Schema<SearchImageRequest>;

export interface SearchImageResponseBodyItem {
  description?: string;
  is_official?: boolean;
  /** Whether this repository has automated builds enabled. <p><br /></p> > **Deprecated**: This field is deprecated and will always be "false". */
  is_automated?: boolean;
  name?: string;
  star_count?: number;
}
export const SearchImageResponseBodyItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    is_official: S.optional(S.Boolean),
    is_automated: S.optional(S.Boolean),
    name: S.optional(S.String),
    star_count: S.optional(S.Number),
  }),
).annotate({
  identifier: "SearchImageResponseBodyItem",
}) as any as S.Schema<SearchImageResponseBodyItem>;

export type SearchImageResponseBodyList = Array<SearchImageResponseBodyItem>;
export const SearchImageResponseBodyList = /*@__PURE__*/ S.Array(
  SearchImageResponseBodyItem,
) as any as S.Schema<SearchImageResponseBodyList>;

export type SearchImageResponse = SearchImageResponseBodyList;
export const SearchImageResponse = /*@__PURE__*/ S.suspend(() =>
  SearchImageResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "SearchImageResponse",
}) as any as S.Schema<SearchImageResponse>;

export interface SecretInspectRequest {
  /** ID of the secret */
  id: string;
}
export const SecretInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/secrets/{id}", code: 200 })),
).annotate({
  identifier: "SecretInspectRequest",
}) as any as S.Schema<SecretInspectRequest>;

export interface ServiceInspectRequest {
  /** ID or name of service. */
  id: string;
  /** Fill empty fields with default values. */
  insertDefaults?: boolean;
}
export const ServiceInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    insertDefaults: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/services/{id}", code: 200 })),
).annotate({
  identifier: "ServiceInspectRequest",
}) as any as S.Schema<ServiceInspectRequest>;

export interface ServiceLogsRequest {
  /** ID or name of the service */
  id: string;
  /** Show service context and extra details provided to logs. */
  details?: boolean;
  /** Keep connection after returning logs. */
  follow?: boolean;
  /** Return logs from `stdout` */
  stdout?: boolean;
  /** Return logs from `stderr` */
  stderr?: boolean;
  /** Only return logs since this time, as a UNIX timestamp */
  since?: number;
  /** Add timestamps to every log line */
  timestamps?: boolean;
  /** Only return this number of log lines from the end of the logs. Specify as an integer or `all` to output all log lines. */
  tail?: string;
}
export const ServiceLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    details: S.optional(S.Boolean.pipe(T.Query())),
    follow: S.optional(S.Boolean.pipe(T.Query())),
    stdout: S.optional(S.Boolean.pipe(T.Query())),
    stderr: S.optional(S.Boolean.pipe(T.Query())),
    since: S.optional(S.Number.pipe(T.Query())),
    timestamps: S.optional(S.Boolean.pipe(T.Query())),
    tail: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/services/{id}/logs", code: 200 })),
).annotate({
  identifier: "ServiceLogsRequest",
}) as any as S.Schema<ServiceLogsRequest>;

export type ServiceLogsResponse = string;
export const ServiceLogsResponse = /*@__PURE__*/ S.suspend(() =>
  S.String.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ServiceLogsResponse",
}) as any as S.Schema<ServiceLogsResponse>;

export interface SessionRequest {}
export const SessionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "POST", uri: "/session", code: 200 })),
).annotate({ identifier: "SessionRequest" }) as any as S.Schema<SessionRequest>;

export interface SessionResponse {}
export const SessionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "SessionResponse",
}) as any as S.Schema<SessionResponse>;

export type SetPluginRequestBodyList = Array<string>;
export const SetPluginRequestBodyList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SetPluginRequestBodyList>;

export interface SetPluginRequest {
  /** The name of the plugin. The `:latest` tag is optional, and is the default if omitted. */
  name: string;
  body?: SetPluginRequestBodyList;
}
export const SetPluginRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    body: S.optional(SetPluginRequestBodyList.pipe(T.HttpBody())),
  }).pipe(T.Http({ method: "POST", uri: "/plugins/{name}/set", code: 200 })),
).annotate({
  identifier: "SetPluginRequest",
}) as any as S.Schema<SetPluginRequest>;

export interface SetPluginResponse {}
export const SetPluginResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "SetPluginResponse",
}) as any as S.Schema<SetPluginResponse>;

export interface StartContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Override the key sequence for detaching a container. Format is a single character `[a-Z]` or `ctrl-<value>` where `<value>` is one of: `a-z`, `@`, `^`, `[`, `,` or `_`. */
  detachKeys?: string;
}
export const StartContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    detachKeys: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/containers/{id}/start", code: 200 })),
).annotate({
  identifier: "StartContainerRequest",
}) as any as S.Schema<StartContainerRequest>;

export interface StartContainerResponse {}
export const StartContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "StartContainerResponse",
}) as any as S.Schema<StartContainerResponse>;

export interface StopContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Signal to send to the container as an integer or string (e.g. `SIGINT`). */
  signal?: string;
  /** Number of seconds to wait before killing the container */
  t?: number;
}
export const StopContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    signal: S.optional(S.String.pipe(T.Query())),
    t: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/containers/{id}/stop", code: 200 })),
).annotate({
  identifier: "StopContainerRequest",
}) as any as S.Schema<StopContainerRequest>;

export interface StopContainerResponse {}
export const StopContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "StopContainerResponse",
}) as any as S.Schema<StopContainerResponse>;

/** Default Address Pool specifies default subnet pools for global scope networks. */
export type SwarmInitRequestDefaultAddrPoolList = Array<string>;
export const SwarmInitRequestDefaultAddrPoolList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SwarmInitRequestDefaultAddrPoolList>;

/** User-defined key/value metadata. */
export type SwarmSpecLabelsMap = { [key: string]: string | undefined };
export const SwarmSpecLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<SwarmSpecLabelsMap>;

/** Orchestration configuration. */
export interface SwarmSpecOrchestration {
  /** The number of historic tasks to keep per instance or node. If negative, never remove completed or failed tasks. */
  TaskHistoryRetentionLimit?: number;
}
export const SwarmSpecOrchestration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    TaskHistoryRetentionLimit: S.optional(S.Number),
  }),
).annotate({
  identifier: "SwarmSpecOrchestration",
}) as any as S.Schema<SwarmSpecOrchestration>;

/** Raft configuration. */
export interface SwarmSpecRaft {
  /** The number of log entries between snapshots. */
  SnapshotInterval?: number;
  /** The number of snapshots to keep beyond the current snapshot. */
  KeepOldSnapshots?: number;
  /** The number of log entries to keep around to sync up slow followers after a snapshot is created. */
  LogEntriesForSlowFollowers?: number;
  /** The number of ticks that a follower will wait for a message from the leader before becoming a candidate and starting an election. `ElectionTick` must be greater than `HeartbeatTick`. A tick currently defaults to one second, so these translate directly to seconds currently, but this is NOT guaranteed. */
  ElectionTick?: number;
  /** The number of ticks between heartbeats. Every HeartbeatTick ticks, the leader will send a heartbeat to the followers. A tick currently defaults to one second, so these translate directly to seconds currently, but this is NOT guaranteed. */
  HeartbeatTick?: number;
}
export const SwarmSpecRaft = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    SnapshotInterval: S.optional(S.Number),
    KeepOldSnapshots: S.optional(S.Number),
    LogEntriesForSlowFollowers: S.optional(S.Number),
    ElectionTick: S.optional(S.Number),
    HeartbeatTick: S.optional(S.Number),
  }),
).annotate({ identifier: "SwarmSpecRaft" }) as any as S.Schema<SwarmSpecRaft>;

/** Dispatcher configuration. */
export interface SwarmSpecDispatcher {
  /** The delay for an agent to send a heartbeat to the dispatcher. */
  HeartbeatPeriod?: number;
}
export const SwarmSpecDispatcher = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    HeartbeatPeriod: S.optional(S.Number),
  }),
).annotate({
  identifier: "SwarmSpecDispatcher",
}) as any as S.Schema<SwarmSpecDispatcher>;

/** Protocol for communication with the external CA (currently only `cfssl` is supported). */
export type SwarmSpecCAConfigExternalCAsItemProtocol = "cfssl";
export const SwarmSpecCAConfigExternalCAsItemProtocol = S.String;

/** An object with key/value pairs that are interpreted as protocol-specific options for the external CA driver. */
export type SwarmSpecCAConfigExternalCAsItemOptionsMap = {
  [key: string]: string | undefined;
};
export const SwarmSpecCAConfigExternalCAsItemOptionsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<SwarmSpecCAConfigExternalCAsItemOptionsMap>;

export interface SwarmSpecCAConfigExternalCAsItem {
  /** Protocol for communication with the external CA (currently only `cfssl` is supported). */
  Protocol?: SwarmSpecCAConfigExternalCAsItemProtocol | (string & {});
  /** URL where certificate signing requests should be sent. */
  URL?: string;
  /** An object with key/value pairs that are interpreted as protocol-specific options for the external CA driver. */
  Options?: SwarmSpecCAConfigExternalCAsItemOptionsMap;
  /** The root CA certificate (in PEM format) this external CA uses to issue TLS certificates (assumed to be to the current swarm root CA certificate if not provided). */
  CACert?: string;
}
export const SwarmSpecCAConfigExternalCAsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Protocol: S.optional(SwarmSpecCAConfigExternalCAsItemProtocol),
    URL: S.optional(S.String),
    Options: S.optional(SwarmSpecCAConfigExternalCAsItemOptionsMap),
    CACert: S.optional(S.String),
  }),
).annotate({
  identifier: "SwarmSpecCAConfigExternalCAsItem",
}) as any as S.Schema<SwarmSpecCAConfigExternalCAsItem>;

/** Configuration for forwarding signing requests to an external certificate authority. */
export type SwarmSpecCAConfigExternalCAsList =
  Array<SwarmSpecCAConfigExternalCAsItem>;
export const SwarmSpecCAConfigExternalCAsList = /*@__PURE__*/ S.Array(
  SwarmSpecCAConfigExternalCAsItem,
) as any as S.Schema<SwarmSpecCAConfigExternalCAsList>;

/** CA configuration. */
export interface SwarmSpecCAConfig {
  /** The duration node certificates are issued for. */
  NodeCertExpiry?: number;
  /** Configuration for forwarding signing requests to an external certificate authority. */
  ExternalCAs?: SwarmSpecCAConfigExternalCAsList;
  /** The desired signing CA certificate for all swarm node TLS leaf certificates, in PEM format. */
  SigningCACert?: string;
  /** The desired signing CA key for all swarm node TLS leaf certificates, in PEM format. */
  SigningCAKey?: string;
  /** An integer whose purpose is to force swarm to generate a new signing CA certificate and key, if none have been specified in `SigningCACert` and `SigningCAKey` */
  ForceRotate?: number;
}
export const SwarmSpecCAConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NodeCertExpiry: S.optional(S.Number),
    ExternalCAs: S.optional(SwarmSpecCAConfigExternalCAsList),
    SigningCACert: S.optional(S.String),
    SigningCAKey: S.optional(S.String),
    ForceRotate: S.optional(S.Number),
  }),
).annotate({
  identifier: "SwarmSpecCAConfig",
}) as any as S.Schema<SwarmSpecCAConfig>;

/** Parameters related to encryption-at-rest. */
export interface SwarmSpecEncryptionConfig {
  /** If set, generate a key and use it to lock data stored on the managers. */
  AutoLockManagers?: boolean;
}
export const SwarmSpecEncryptionConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    AutoLockManagers: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "SwarmSpecEncryptionConfig",
}) as any as S.Schema<SwarmSpecEncryptionConfig>;

/** Driver-specific options for the selected log driver, specified as key/value pairs. */
export type SwarmSpecTaskDefaultsLogDriverOptionsMap = {
  [key: string]: string | undefined;
};
export const SwarmSpecTaskDefaultsLogDriverOptionsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<SwarmSpecTaskDefaultsLogDriverOptionsMap>;

/** The log driver to use for tasks created in the orchestrator if unspecified by a service. Updating this value only affects new tasks. Existing tasks continue to use their previously configured log driver until recreated. */
export interface SwarmSpecTaskDefaultsLogDriver {
  /** The log driver to use as a default for new tasks. */
  Name?: string;
  /** Driver-specific options for the selected log driver, specified as key/value pairs. */
  Options?: SwarmSpecTaskDefaultsLogDriverOptionsMap;
}
export const SwarmSpecTaskDefaultsLogDriver = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Options: S.optional(SwarmSpecTaskDefaultsLogDriverOptionsMap),
  }),
).annotate({
  identifier: "SwarmSpecTaskDefaultsLogDriver",
}) as any as S.Schema<SwarmSpecTaskDefaultsLogDriver>;

/** Defaults for creating tasks in this cluster. */
export interface SwarmSpecTaskDefaults {
  /** The log driver to use for tasks created in the orchestrator if unspecified by a service. Updating this value only affects new tasks. Existing tasks continue to use their previously configured log driver until recreated. */
  LogDriver?: SwarmSpecTaskDefaultsLogDriver;
}
export const SwarmSpecTaskDefaults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    LogDriver: S.optional(SwarmSpecTaskDefaultsLogDriver),
  }),
).annotate({
  identifier: "SwarmSpecTaskDefaults",
}) as any as S.Schema<SwarmSpecTaskDefaults>;

/** User modifiable swarm configuration. */
export interface SwarmSpec {
  /** Name of the swarm. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: SwarmSpecLabelsMap;
  /** Orchestration configuration. */
  Orchestration?: SwarmSpecOrchestration | null;
  /** Raft configuration. */
  Raft?: SwarmSpecRaft;
  /** Dispatcher configuration. */
  Dispatcher?: SwarmSpecDispatcher | null;
  /** CA configuration. */
  CAConfig?: SwarmSpecCAConfig | null;
  /** Parameters related to encryption-at-rest. */
  EncryptionConfig?: SwarmSpecEncryptionConfig;
  /** Defaults for creating tasks in this cluster. */
  TaskDefaults?: SwarmSpecTaskDefaults;
}
export const SwarmSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Labels: S.optional(SwarmSpecLabelsMap),
    Orchestration: S.optional(S.NullOr(SwarmSpecOrchestration)),
    Raft: S.optional(SwarmSpecRaft),
    Dispatcher: S.optional(S.NullOr(SwarmSpecDispatcher)),
    CAConfig: S.optional(S.NullOr(SwarmSpecCAConfig)),
    EncryptionConfig: S.optional(SwarmSpecEncryptionConfig),
    TaskDefaults: S.optional(SwarmSpecTaskDefaults),
  }),
).annotate({ identifier: "SwarmSpec" }) as any as S.Schema<SwarmSpec>;

export interface SwarmInitRequest {
  /** Listen address used for inter-manager communication, as well as determining the networking interface used for the VXLAN Tunnel Endpoint (VTEP). This can either be an address/port combination in the form `192.168.1.1:4567`, or an interface followed by a port number, like `eth0:4567`. If the port number is omitted, the default swarm listening port is used. */
  ListenAddr?: string;
  /** Externally reachable address advertised to other nodes. This can either be an address/port combination in the form `192.168.1.1:4567`, or an interface followed by a port number, like `eth0:4567`. If the port number is omitted, the port number from the listen address is used. If `AdvertiseAddr` is not specified, it will be automatically detected when possible. */
  AdvertiseAddr?: string;
  /** Address or interface to use for data path traffic (format: `<ip|interface>`), for example, `192.168.1.1`, or an interface, like `eth0`. If `DataPathAddr` is unspecified, the same address as `AdvertiseAddr` is used. The `DataPathAddr` specifies the address that global scope network drivers will publish towards other nodes in order to reach the containers running on this node. Using this parameter it is possible to separate the container data traffic from the management traffic of the cluster. */
  DataPathAddr?: string;
  /** DataPathPort specifies the data path port number for data traffic. Acceptable port range is 1024 to 49151. if no port is set or is set to 0, default port 4789 will be used. */
  DataPathPort?: number;
  /** Default Address Pool specifies default subnet pools for global scope networks. */
  DefaultAddrPool?: SwarmInitRequestDefaultAddrPoolList;
  /** Force creation of a new swarm. */
  ForceNewCluster?: boolean;
  /** SubnetSize specifies the subnet size of the networks created from the default subnet pool. */
  SubnetSize?: number;
  Spec?: SwarmSpec;
}
export const SwarmInitRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ListenAddr: S.optional(S.String),
    AdvertiseAddr: S.optional(S.String),
    DataPathAddr: S.optional(S.String),
    DataPathPort: S.optional(S.Number),
    DefaultAddrPool: S.optional(SwarmInitRequestDefaultAddrPoolList),
    ForceNewCluster: S.optional(S.Boolean),
    SubnetSize: S.optional(S.Number),
    Spec: S.optional(SwarmSpec),
  }).pipe(T.Http({ method: "POST", uri: "/swarm/init", code: 200 })),
).annotate({
  identifier: "SwarmInitRequest",
}) as any as S.Schema<SwarmInitRequest>;

export type SwarmInitResponse = string;
export const SwarmInitResponse = /*@__PURE__*/ S.suspend(() =>
  S.String.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "SwarmInitResponse",
}) as any as S.Schema<SwarmInitResponse>;

export interface SwarmInspectRequest {}
export const SwarmInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/swarm", code: 200 })),
).annotate({
  identifier: "SwarmInspectRequest",
}) as any as S.Schema<SwarmInspectRequest>;

/** Default Address Pool specifies default subnet pools for global scope networks. */
export type SwarmInspectResponseDefaultAddrPoolList = Array<string>;
export const SwarmInspectResponseDefaultAddrPoolList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SwarmInspectResponseDefaultAddrPoolList>;

/** JoinTokens contains the tokens workers and managers need to join the swarm. */
export interface JoinTokens {
  /** The token workers can use to join the swarm. */
  Worker?: string;
  /** The token managers can use to join the swarm. */
  Manager?: string;
}
export const JoinTokens = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Worker: S.optional(S.String),
    Manager: S.optional(S.String),
  }),
).annotate({ identifier: "JoinTokens" }) as any as S.Schema<JoinTokens>;

export interface SwarmInspectResponse {
  /** The ID of the swarm. */
  ID?: string;
  Version?: ObjectVersion;
  /** Date and time at which the swarm was initialised in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  CreatedAt?: string;
  /** Date and time at which the swarm was last updated in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  UpdatedAt?: string;
  Spec?: SwarmSpec;
  TLSInfo?: TLSInfo;
  /** Whether there is currently a root CA rotation in progress for the swarm */
  RootRotationInProgress?: boolean;
  /** DataPathPort specifies the data path port number for data traffic. Acceptable port range is 1024 to 49151. If no port is set or is set to 0, the default port (4789) is used. */
  DataPathPort?: number;
  /** Default Address Pool specifies default subnet pools for global scope networks. */
  DefaultAddrPool?: SwarmInspectResponseDefaultAddrPoolList;
  /** SubnetSize specifies the subnet size of the networks created from the default subnet pool. */
  SubnetSize?: number;
  JoinTokens?: JoinTokens;
}
export const SwarmInspectResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Version: S.optional(ObjectVersion),
    CreatedAt: S.optional(S.String),
    UpdatedAt: S.optional(S.String),
    Spec: S.optional(SwarmSpec),
    TLSInfo: S.optional(TLSInfo),
    RootRotationInProgress: S.optional(S.Boolean),
    DataPathPort: S.optional(S.Number),
    DefaultAddrPool: S.optional(SwarmInspectResponseDefaultAddrPoolList),
    SubnetSize: S.optional(S.Number),
    JoinTokens: S.optional(JoinTokens),
  }),
).annotate({
  identifier: "SwarmInspectResponse",
}) as any as S.Schema<SwarmInspectResponse>;

export interface SwarmUnlockkeyRequest {}
export const SwarmUnlockkeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/swarm/unlockkey", code: 200 }),
  ),
).annotate({
  identifier: "SwarmUnlockkeyRequest",
}) as any as S.Schema<SwarmUnlockkeyRequest>;

export interface SwarmUnlockkeyResponse {
  /** The swarm's unlock key. */
  UnlockKey?: string;
}
export const SwarmUnlockkeyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    UnlockKey: S.optional(S.String),
  }),
).annotate({
  identifier: "SwarmUnlockkeyResponse",
}) as any as S.Schema<SwarmUnlockkeyResponse>;

export interface SystemAuthRequest {
  username?: string;
  password?: string | Redacted.Redacted<string>;
  serveraddress?: string;
}
export const SystemAuthRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.optional(S.String),
    password: S.optional(S.String.pipe(T.SensitiveValue({}))),
    serveraddress: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/auth", code: 200 })),
).annotate({
  identifier: "SystemAuthRequest",
}) as any as S.Schema<SystemAuthRequest>;

/** An identity token was generated successfully. */
export interface AuthResponse {
  /** The status of the authentication */
  Status: string;
  /** An opaque token used to authenticate a user after a successful login */
  IdentityToken?: string;
}
export const AuthResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Status: S.String,
    IdentityToken: S.optional(S.String),
  }),
).annotate({ identifier: "AuthResponse" }) as any as S.Schema<AuthResponse>;

export type SystemDataUsageRequestTypeItem =
  | "container"
  | "image"
  | "volume"
  | "build-cache";
export const SystemDataUsageRequestTypeItem = S.String;

export type SystemDataUsageRequestTypeList = Array<
  SystemDataUsageRequestTypeItem | (string & {})
>;
export const SystemDataUsageRequestTypeList = /*@__PURE__*/ S.Array(
  SystemDataUsageRequestTypeItem,
) as any as S.Schema<SystemDataUsageRequestTypeList>;

export interface SystemDataUsageRequest {
  /** Object types, for which to compute and return data. */
  type?: SystemDataUsageRequestTypeList;
  /** Show detailed information on space usage. */
  verbose?: boolean;
}
export const SystemDataUsageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(SystemDataUsageRequestTypeList.pipe(T.Query())),
    verbose: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/system/df", code: 200 })),
).annotate({
  identifier: "SystemDataUsageRequest",
}) as any as S.Schema<SystemDataUsageRequest>;

/** List of image summaries. */
export type ImagesDiskUsageItemsList = Array<unknown>;
export const ImagesDiskUsageItemsList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ImagesDiskUsageItemsList>;

/** represents system data usage for image resources. */
export interface ImagesDiskUsage {
  /** Count of active images. */
  ActiveCount?: number;
  /** Count of all images. */
  TotalCount?: number;
  /** Disk space that can be reclaimed by removing unused images. */
  Reclaimable?: number;
  /** Disk space in use by images. */
  TotalSize?: number;
  /** List of image summaries. */
  Items?: ImagesDiskUsageItemsList;
}
export const ImagesDiskUsage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ActiveCount: S.optional(S.Number),
    TotalCount: S.optional(S.Number),
    Reclaimable: S.optional(S.Number),
    TotalSize: S.optional(S.Number),
    Items: S.optional(ImagesDiskUsageItemsList),
  }),
).annotate({
  identifier: "ImagesDiskUsage",
}) as any as S.Schema<ImagesDiskUsage>;

/** List of container summaries. */
export type ContainersDiskUsageItemsList = Array<unknown>;
export const ContainersDiskUsageItemsList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ContainersDiskUsageItemsList>;

/** represents system data usage information for container resources. */
export interface ContainersDiskUsage {
  /** Count of active containers. */
  ActiveCount?: number;
  /** Count of all containers. */
  TotalCount?: number;
  /** Disk space that can be reclaimed by removing inactive containers. */
  Reclaimable?: number;
  /** Disk space in use by containers. */
  TotalSize?: number;
  /** List of container summaries. */
  Items?: ContainersDiskUsageItemsList;
}
export const ContainersDiskUsage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ActiveCount: S.optional(S.Number),
    TotalCount: S.optional(S.Number),
    Reclaimable: S.optional(S.Number),
    TotalSize: S.optional(S.Number),
    Items: S.optional(ContainersDiskUsageItemsList),
  }),
).annotate({
  identifier: "ContainersDiskUsage",
}) as any as S.Schema<ContainersDiskUsage>;

/** List of volumes. */
export type VolumesDiskUsageItemsList = Array<unknown>;
export const VolumesDiskUsageItemsList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<VolumesDiskUsageItemsList>;

/** represents system data usage for volume resources. */
export interface VolumesDiskUsage {
  /** Count of active volumes. */
  ActiveCount?: number;
  /** Count of all volumes. */
  TotalCount?: number;
  /** Disk space that can be reclaimed by removing inactive volumes. */
  Reclaimable?: number;
  /** Disk space in use by volumes. */
  TotalSize?: number;
  /** List of volumes. */
  Items?: VolumesDiskUsageItemsList;
}
export const VolumesDiskUsage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ActiveCount: S.optional(S.Number),
    TotalCount: S.optional(S.Number),
    Reclaimable: S.optional(S.Number),
    TotalSize: S.optional(S.Number),
    Items: S.optional(VolumesDiskUsageItemsList),
  }),
).annotate({
  identifier: "VolumesDiskUsage",
}) as any as S.Schema<VolumesDiskUsage>;

/** List of build cache records. */
export type BuildCacheDiskUsageItemsList = Array<unknown>;
export const BuildCacheDiskUsageItemsList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<BuildCacheDiskUsageItemsList>;

/** represents system data usage for build cache resources. */
export interface BuildCacheDiskUsage {
  /** Count of active build cache records. */
  ActiveCount?: number;
  /** Count of all build cache records. */
  TotalCount?: number;
  /** Disk space that can be reclaimed by removing inactive build cache records. */
  Reclaimable?: number;
  /** Disk space in use by build cache records. */
  TotalSize?: number;
  /** List of build cache records. */
  Items?: BuildCacheDiskUsageItemsList;
}
export const BuildCacheDiskUsage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ActiveCount: S.optional(S.Number),
    TotalCount: S.optional(S.Number),
    Reclaimable: S.optional(S.Number),
    TotalSize: S.optional(S.Number),
    Items: S.optional(BuildCacheDiskUsageItemsList),
  }),
).annotate({
  identifier: "BuildCacheDiskUsage",
}) as any as S.Schema<BuildCacheDiskUsage>;

export interface SystemDataUsageResponse {
  ImageUsage?: ImagesDiskUsage;
  ContainerUsage?: ContainersDiskUsage;
  VolumeUsage?: VolumesDiskUsage;
  BuildCacheUsage?: BuildCacheDiskUsage;
}
export const SystemDataUsageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ImageUsage: S.optional(ImagesDiskUsage),
    ContainerUsage: S.optional(ContainersDiskUsage),
    VolumeUsage: S.optional(VolumesDiskUsage),
    BuildCacheUsage: S.optional(BuildCacheDiskUsage),
  }),
).annotate({
  identifier: "SystemDataUsageResponse",
}) as any as S.Schema<SystemDataUsageResponse>;

export interface SystemEventsRequest {
  /** Show events created since this timestamp then stream new events. */
  since?: string;
  /** Show events created until this timestamp then stop streaming. */
  until?: string;
  /** A JSON encoded value of filters (a `map[string][]string`) to process on the event list. Available filters: - `config=<string>` config name or ID - `container=<string>` container name or ID - `daemon=<string>` daemon name or ID - `event=<string>` event type - `image=<string>` image name or ID - `label=<string>` image or container label - `network=<string>` network name or ID - `node=<string>` node ID - `plugin`=<string> plugin name or ID - `scope`=<string> local or swarm - `secret=<string>` secret name or ID - `service=<string>` service name or ID - `type=<string>` object to filter by, one of `container`, `image`, `volume`, `network`, `daemon`, `plugin`, `node`, `service`, `secret` or `config` - `volume=<string>` volume name */
  filters?: string;
}
export const SystemEventsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    since: S.optional(S.String.pipe(T.Query())),
    until: S.optional(S.String.pipe(T.Query())),
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/events", code: 200 })),
).annotate({
  identifier: "SystemEventsRequest",
}) as any as S.Schema<SystemEventsRequest>;

/** The type of object emitting the event */
export type EventMessageType =
  | "builder"
  | "config"
  | "container"
  | "daemon"
  | "image"
  | "network"
  | "node"
  | "plugin"
  | "secret"
  | "service"
  | "volume";
export const EventMessageType = S.String;

/** Various key/value attributes of the object, depending on its type. */
export type EventActorAttributesMap = { [key: string]: string | undefined };
export const EventActorAttributesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<EventActorAttributesMap>;

/** Actor describes something that generates events, like a container, network, or a volume. */
export interface EventActor {
  /** The ID of the object emitting the event */
  ID?: string;
  /** Various key/value attributes of the object, depending on its type. */
  Attributes?: EventActorAttributesMap;
}
export const EventActor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Attributes: S.optional(EventActorAttributesMap),
  }),
).annotate({ identifier: "EventActor" }) as any as S.Schema<EventActor>;

/** Scope of the event. Engine events are `local` scope. Cluster (Swarm) events are `swarm` scope. */
export type EventMessageScope = "local" | "swarm";
export const EventMessageScope = S.String;

/** EventMessage represents the information an event contains. */
export interface EventMessage {
  /** The type of object emitting the event */
  Type?: EventMessageType;
  /** The type of event */
  Action?: string;
  Actor?: EventActor;
  /** Scope of the event. Engine events are `local` scope. Cluster (Swarm) events are `swarm` scope. */
  scope?: EventMessageScope;
  /** Timestamp of event */
  time?: number;
  /** Timestamp of event, with nanosecond accuracy */
  timeNano?: number;
}
export const EventMessage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Type: S.optional(EventMessageType),
    Action: S.optional(S.String),
    Actor: S.optional(EventActor),
    scope: S.optional(EventMessageScope),
    time: S.optional(S.Number),
    timeNano: S.optional(S.Number),
  }),
).annotate({ identifier: "EventMessage" }) as any as S.Schema<EventMessage>;

export interface SystemInfo2Request {}
export const SystemInfo2Request = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/info", code: 200 })),
).annotate({
  identifier: "SystemInfo2Request",
}) as any as S.Schema<SystemInfo2Request>;

export type SystemInfoDriverStatusItemList = Array<string>;
export const SystemInfoDriverStatusItemList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SystemInfoDriverStatusItemList>;

/** Information specific to the storage driver, provided as "label" / "value" pairs. This information is provided by the storage driver, and formatted in a way consistent with the output of `docker info` on the command line. <p><br /></p> > **Note**: The information returned in this field, including the > formatting of values and labels, should not be considered stable, > and may change without notice. */
export type SystemInfoDriverStatusList = Array<SystemInfoDriverStatusItemList>;
export const SystemInfoDriverStatusList = /*@__PURE__*/ S.Array(
  SystemInfoDriverStatusItemList,
) as any as S.Schema<SystemInfoDriverStatusList>;

/** Names of available volume-drivers, and network-driver plugins. */
export type PluginsInfoVolumeList = Array<string>;
export const PluginsInfoVolumeList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginsInfoVolumeList>;

/** Names of available network-drivers, and network-driver plugins. */
export type PluginsInfoNetworkList = Array<string>;
export const PluginsInfoNetworkList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginsInfoNetworkList>;

/** Names of available authorization plugins. */
export type PluginsInfoAuthorizationList = Array<string>;
export const PluginsInfoAuthorizationList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginsInfoAuthorizationList>;

/** Names of available logging-drivers, and logging-driver plugins. */
export type PluginsInfoLogList = Array<string>;
export const PluginsInfoLogList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PluginsInfoLogList>;

/** Available plugins per type. <p><br /></p> > **Note**: Only unmanaged (V1) plugins are included in this list. > V1 plugins are "lazily" loaded, and are not returned in this list > if there is no resource using the plugin. */
export interface PluginsInfo {
  /** Names of available volume-drivers, and network-driver plugins. */
  Volume?: PluginsInfoVolumeList;
  /** Names of available network-drivers, and network-driver plugins. */
  Network?: PluginsInfoNetworkList;
  /** Names of available authorization plugins. */
  Authorization?: PluginsInfoAuthorizationList;
  /** Names of available logging-drivers, and logging-driver plugins. */
  Log?: PluginsInfoLogList;
}
export const PluginsInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Volume: S.optional(PluginsInfoVolumeList),
    Network: S.optional(PluginsInfoNetworkList),
    Authorization: S.optional(PluginsInfoAuthorizationList),
    Log: S.optional(PluginsInfoLogList),
  }),
).annotate({ identifier: "PluginsInfo" }) as any as S.Schema<PluginsInfo>;

/** The driver to use for managing cgroups. */
export type SystemInfoCgroupDriver = "cgroupfs" | "systemd" | "none";
export const SystemInfoCgroupDriver = S.String;

/** The version of the cgroup. */
export type SystemInfoCgroupVersion = "1" | "2";
export const SystemInfoCgroupVersion = S.String;

/** List of IP ranges of insecure registries, using the CIDR syntax ([RFC 4632](https://tools.ietf.org/html/4632)). Insecure registries accept un-encrypted (HTTP) and/or untrusted (HTTPS with certificates from unknown CAs) communication. By default, local registries (`::1/128` and `127.0.0.0/8`) are configured as insecure. All other registries are secure. Communicating with an insecure registry is not possible if the daemon assumes that registry is secure. This configuration override this behavior, insecure communication with registries whose resolved IP address is within the subnet described by the CIDR syntax. Registries can also be marked insecure by hostname. Those registries are listed under `IndexConfigs` and have their `Secure` field set to `false`. > **Warning**: Using this option can be useful when running a local > registry, but introduces security vulnerabilities. This option > should therefore ONLY be used for testing purposes. For increased > security, users should add their CA to their system's list of trusted > CAs instead of enabling this option. */
export type RegistryServiceConfigInsecureRegistryCIDRsList = Array<string>;
export const RegistryServiceConfigInsecureRegistryCIDRsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<RegistryServiceConfigInsecureRegistryCIDRsList>;

/** List of mirrors, expressed as URIs. */
export type IndexInfoMirrorsList = Array<string>;
export const IndexInfoMirrorsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<IndexInfoMirrorsList>;

/** IndexInfo contains information about a registry. */
export interface IndexInfo {
  /** Name of the registry, such as "docker.io". */
  Name?: string;
  /** List of mirrors, expressed as URIs. */
  Mirrors?: IndexInfoMirrorsList;
  /** Indicates if the registry is part of the list of insecure registries. If `false`, the registry is insecure. Insecure registries accept un-encrypted (HTTP) and/or untrusted (HTTPS with certificates from unknown CAs) communication. > **Warning**: Insecure registries can be useful when running a local > registry. However, because its use creates security vulnerabilities > it should ONLY be enabled for testing purposes. For increased > security, users should add their CA to their system's list of > trusted CAs instead of enabling this option. */
  Secure?: boolean;
  /** Indicates whether this is an official registry (i.e., Docker Hub / docker.io) */
  Official?: boolean;
}
export const IndexInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.optional(S.String),
    Mirrors: S.optional(IndexInfoMirrorsList),
    Secure: S.optional(S.Boolean),
    Official: S.optional(S.Boolean),
  }),
).annotate({ identifier: "IndexInfo" }) as any as S.Schema<IndexInfo>;

export type RegistryServiceConfigIndexConfigsMap = {
  [key: string]: IndexInfo | undefined;
};
export const RegistryServiceConfigIndexConfigsMap = /*@__PURE__*/ S.Record(
  S.String,
  IndexInfo,
) as any as S.Schema<RegistryServiceConfigIndexConfigsMap>;

/** List of registry URLs that act as a mirror for the official (`docker.io`) registry. */
export type RegistryServiceConfigMirrorsList = Array<string>;
export const RegistryServiceConfigMirrorsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<RegistryServiceConfigMirrorsList>;

/** RegistryServiceConfig stores daemon registry services configuration. */
export interface RegistryServiceConfig {
  /** List of IP ranges of insecure registries, using the CIDR syntax ([RFC 4632](https://tools.ietf.org/html/4632)). Insecure registries accept un-encrypted (HTTP) and/or untrusted (HTTPS with certificates from unknown CAs) communication. By default, local registries (`::1/128` and `127.0.0.0/8`) are configured as insecure. All other registries are secure. Communicating with an insecure registry is not possible if the daemon assumes that registry is secure. This configuration override this behavior, insecure communication with registries whose resolved IP address is within the subnet described by the CIDR syntax. Registries can also be marked insecure by hostname. Those registries are listed under `IndexConfigs` and have their `Secure` field set to `false`. > **Warning**: Using this option can be useful when running a local > registry, but introduces security vulnerabilities. This option > should therefore ONLY be used for testing purposes. For increased > security, users should add their CA to their system's list of trusted > CAs instead of enabling this option. */
  InsecureRegistryCIDRs?: RegistryServiceConfigInsecureRegistryCIDRsList;
  IndexConfigs?: RegistryServiceConfigIndexConfigsMap;
  /** List of registry URLs that act as a mirror for the official (`docker.io`) registry. */
  Mirrors?: RegistryServiceConfigMirrorsList;
}
export const RegistryServiceConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    InsecureRegistryCIDRs: S.optional(
      RegistryServiceConfigInsecureRegistryCIDRsList,
    ),
    IndexConfigs: S.optional(RegistryServiceConfigIndexConfigsMap),
    Mirrors: S.optional(RegistryServiceConfigMirrorsList),
  }),
).annotate({
  identifier: "RegistryServiceConfig",
}) as any as S.Schema<RegistryServiceConfig>;

/** User-defined labels (key/value metadata) as set on the daemon. <p><br /></p> > **Note**: When part of a Swarm, nodes can both have _daemon_ labels, > set through the daemon configuration, and _node_ labels, set from a > manager node in the Swarm. Node labels are not included in this > field. Node labels can be retrieved using the `/nodes/(id)` endpoint > on a manager node in the Swarm. */
export type SystemInfoLabelsList = Array<string>;
export const SystemInfoLabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SystemInfoLabelsList>;

/** List of command-line arguments to pass to the runtime when invoked. */
export type RuntimeRuntimeArgsList = Array<string>;
export const RuntimeRuntimeArgsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<RuntimeRuntimeArgsList>;

/** Information specific to the runtime. While this API specification does not define data provided by runtimes, the following well-known properties may be provided by runtimes: `org.opencontainers.runtime-spec.features`: features structure as defined in the [OCI Runtime Specification](https://github.com/opencontainers/runtime-spec/blob/main/features.md), in a JSON string representation. <p><br /></p> > **Note**: The information returned in this field, including the > formatting of values and labels, should not be considered stable, > and may change without notice. */
export type RuntimeStatusMap = { [key: string]: string | undefined };
export const RuntimeStatusMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<RuntimeStatusMap>;

/** Runtime describes an [OCI compliant](https://github.com/opencontainers/runtime-spec) runtime. The runtime is invoked by the daemon via the `containerd` daemon. OCI runtimes act as an interface to the Linux kernel namespaces, cgroups, and SELinux. */
export interface Runtime {
  /** Name and, optional, path, of the OCI executable binary. If the path is omitted, the daemon searches the host's `$PATH` for the binary and uses the first result. */
  path?: string;
  /** List of command-line arguments to pass to the runtime when invoked. */
  runtimeArgs?: RuntimeRuntimeArgsList | null;
  /** Information specific to the runtime. While this API specification does not define data provided by runtimes, the following well-known properties may be provided by runtimes: `org.opencontainers.runtime-spec.features`: features structure as defined in the [OCI Runtime Specification](https://github.com/opencontainers/runtime-spec/blob/main/features.md), in a JSON string representation. <p><br /></p> > **Note**: The information returned in this field, including the > formatting of values and labels, should not be considered stable, > and may change without notice. */
  status?: RuntimeStatusMap | null;
}
export const Runtime = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.optional(S.String),
    runtimeArgs: S.optional(S.NullOr(RuntimeRuntimeArgsList)),
    status: S.optional(S.NullOr(RuntimeStatusMap)),
  }),
).annotate({ identifier: "Runtime" }) as any as S.Schema<Runtime>;

/** List of [OCI compliant](https://github.com/opencontainers/runtime-spec) runtimes configured on the daemon. Keys hold the "name" used to reference the runtime. The Docker daemon relies on an OCI compliant runtime (invoked via the `containerd` daemon) as its interface to the Linux kernel namespaces, cgroups, and SELinux. The default runtime is `runc`, and automatically configured. Additional runtimes can be configured by the user and will be listed here. */
export type SystemInfoRuntimesMap = { [key: string]: Runtime | undefined };
export const SystemInfoRuntimesMap = /*@__PURE__*/ S.Record(
  S.String,
  Runtime,
) as any as S.Schema<SystemInfoRuntimesMap>;

/** Current local status of this node. */
export type LocalNodeState =
  | ""
  | "inactive"
  | "pending"
  | "active"
  | "error"
  | "locked";
export const LocalNodeState = S.String;

/** Represents a peer-node in the swarm */
export interface PeerNode {
  /** Unique identifier of for this node in the swarm. */
  NodeID?: string;
  /** IP address and ports at which this node can be reached. */
  Addr?: string;
}
export const PeerNode = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NodeID: S.optional(S.String),
    Addr: S.optional(S.String),
  }),
).annotate({ identifier: "PeerNode" }) as any as S.Schema<PeerNode>;

/** List of ID's and addresses of other managers in the swarm. */
export type SwarmInfoRemoteManagersList = Array<PeerNode>;
export const SwarmInfoRemoteManagersList = /*@__PURE__*/ S.Array(
  PeerNode,
) as any as S.Schema<SwarmInfoRemoteManagersList>;

/** Default Address Pool specifies default subnet pools for global scope networks. */
export type ClusterInfoDefaultAddrPoolList = Array<string>;
export const ClusterInfoDefaultAddrPoolList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ClusterInfoDefaultAddrPoolList>;

/** ClusterInfo represents information about the swarm as is returned by the "/info" endpoint. Join-tokens are not included. */
export interface ClusterInfo {
  /** The ID of the swarm. */
  ID?: string;
  Version?: ObjectVersion;
  /** Date and time at which the swarm was initialised in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  CreatedAt?: string;
  /** Date and time at which the swarm was last updated in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  UpdatedAt?: string;
  Spec?: SwarmSpec;
  TLSInfo?: TLSInfo;
  /** Whether there is currently a root CA rotation in progress for the swarm */
  RootRotationInProgress?: boolean;
  /** DataPathPort specifies the data path port number for data traffic. Acceptable port range is 1024 to 49151. If no port is set or is set to 0, the default port (4789) is used. */
  DataPathPort?: number;
  /** Default Address Pool specifies default subnet pools for global scope networks. */
  DefaultAddrPool?: ClusterInfoDefaultAddrPoolList;
  /** SubnetSize specifies the subnet size of the networks created from the default subnet pool. */
  SubnetSize?: number;
}
export const ClusterInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Version: S.optional(ObjectVersion),
    CreatedAt: S.optional(S.String),
    UpdatedAt: S.optional(S.String),
    Spec: S.optional(SwarmSpec),
    TLSInfo: S.optional(TLSInfo),
    RootRotationInProgress: S.optional(S.Boolean),
    DataPathPort: S.optional(S.Number),
    DefaultAddrPool: S.optional(ClusterInfoDefaultAddrPoolList),
    SubnetSize: S.optional(S.Number),
  }),
).annotate({ identifier: "ClusterInfo" }) as any as S.Schema<ClusterInfo>;

/** Represents generic information about swarm. */
export interface SwarmInfo {
  /** Unique identifier of for this node in the swarm. */
  NodeID?: string;
  /** IP address at which this node can be reached by other nodes in the swarm. */
  NodeAddr?: string;
  LocalNodeState?: LocalNodeState;
  ControlAvailable?: boolean;
  Error?: string;
  /** List of ID's and addresses of other managers in the swarm. */
  RemoteManagers?: SwarmInfoRemoteManagersList | null;
  /** Total number of nodes in the swarm. */
  Nodes?: number | null;
  /** Total number of managers in the swarm. */
  Managers?: number | null;
  Cluster?: ClusterInfo | null;
}
export const SwarmInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NodeID: S.optional(S.String),
    NodeAddr: S.optional(S.String),
    LocalNodeState: S.optional(LocalNodeState),
    ControlAvailable: S.optional(S.Boolean),
    Error: S.optional(S.String),
    RemoteManagers: S.optional(S.NullOr(SwarmInfoRemoteManagersList)),
    Nodes: S.optional(S.NullOr(S.Number)),
    Managers: S.optional(S.NullOr(S.Number)),
    Cluster: S.optional(S.NullOr(ClusterInfo)),
  }),
).annotate({ identifier: "SwarmInfo" }) as any as S.Schema<SwarmInfo>;

/** Represents the isolation technology to use as a default for containers. The supported values are platform-specific. If no isolation value is specified on daemon start, on Windows client, the default is `hyperv`, and on Windows server, the default is `process`. This option is currently not used on other platforms. */
export type SystemInfoIsolation = "default" | "hyperv" | "process" | "";
export const SystemInfoIsolation = S.String;

/** Commit holds the Git-commit (SHA1) that a binary was built from, as reported in the version-string of external tools, such as `containerd`, or `runC`. */
export interface Commit {
  /** Actual commit ID of external tool. */
  ID?: string;
}
export const Commit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
  }),
).annotate({ identifier: "Commit" }) as any as S.Schema<Commit>;

/** List of security features that are enabled on the daemon, such as apparmor, seccomp, SELinux, user-namespaces (userns), rootless and no-new-privileges. Additional configuration options for each security feature may be present, and are included as a comma-separated list of key/value pairs. */
export type SystemInfoSecurityOptionsList = Array<string>;
export const SystemInfoSecurityOptionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SystemInfoSecurityOptionsList>;

export interface SystemInfoDefaultAddressPoolsItem {
  /** The network address in CIDR format */
  Base?: string;
  /** The network pool size */
  Size?: number;
}
export const SystemInfoDefaultAddressPoolsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Base: S.optional(S.String),
    Size: S.optional(S.Number),
  }),
).annotate({
  identifier: "SystemInfoDefaultAddressPoolsItem",
}) as any as S.Schema<SystemInfoDefaultAddressPoolsItem>;

/** List of custom default address pools for local networks, which can be specified in the daemon.json file or dockerd option. Example: a Base "10.10.0.0/16" with Size 24 will define the set of 256 10.10.[0-255].0/24 address pools. */
export type SystemInfoDefaultAddressPoolsList =
  Array<SystemInfoDefaultAddressPoolsItem>;
export const SystemInfoDefaultAddressPoolsList = /*@__PURE__*/ S.Array(
  SystemInfoDefaultAddressPoolsItem,
) as any as S.Schema<SystemInfoDefaultAddressPoolsList>;

export type FirewallInfoInfoItemList = Array<string>;
export const FirewallInfoInfoItemList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<FirewallInfoInfoItemList>;

/** Information about the firewall backend, provided as "label" / "value" pairs. <p><br /></p> > **Note**: The information returned in this field, including the > formatting of values and labels, should not be considered stable, > and may change without notice. */
export type FirewallInfoInfoList = Array<FirewallInfoInfoItemList>;
export const FirewallInfoInfoList = /*@__PURE__*/ S.Array(
  FirewallInfoInfoItemList,
) as any as S.Schema<FirewallInfoInfoList>;

/** Information about the daemon's firewalling configuration. This field is currently only used on Linux, and omitted on other platforms. */
export interface FirewallInfo {
  /** The name of the firewall backend driver. */
  Driver?: string;
  /** Information about the firewall backend, provided as "label" / "value" pairs. <p><br /></p> > **Note**: The information returned in this field, including the > formatting of values and labels, should not be considered stable, > and may change without notice. */
  Info?: FirewallInfoInfoList;
}
export const FirewallInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Driver: S.optional(S.String),
    Info: S.optional(FirewallInfoInfoList),
  }),
).annotate({ identifier: "FirewallInfo" }) as any as S.Schema<FirewallInfo>;

/** DeviceInfo represents a device that can be used by a container. */
export interface DeviceInfo {
  /** The origin device driver. */
  Source?: string;
  /** The unique identifier for the device within its source driver. For CDI devices, this would be an FQDN like "vendor.com/gpu=0". */
  ID?: string;
}
export const DeviceInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Source: S.optional(S.String),
    ID: S.optional(S.String),
  }),
).annotate({ identifier: "DeviceInfo" }) as any as S.Schema<DeviceInfo>;

/** List of devices discovered by device drivers. Each device includes information about its source driver, kind, name, and additional driver-specific attributes. */
export type SystemInfoDiscoveredDevicesList = Array<DeviceInfo>;
export const SystemInfoDiscoveredDevicesList = /*@__PURE__*/ S.Array(
  DeviceInfo,
) as any as S.Schema<SystemInfoDiscoveredDevicesList>;

export type NRIInfoInfoItemList = Array<string>;
export const NRIInfoInfoItemList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<NRIInfoInfoItemList>;

/** Information about NRI, provided as "label" / "value" pairs. <p><br /></p> > **Note**: The information returned in this field, including the > formatting of values and labels, should not be considered stable, > and may change without notice. */
export type NRIInfoInfoList = Array<NRIInfoInfoItemList>;
export const NRIInfoInfoList = /*@__PURE__*/ S.Array(
  NRIInfoInfoItemList,
) as any as S.Schema<NRIInfoInfoList>;

/** Information about the Node Resource Interface (NRI). This field is only present if NRI is enabled. */
export interface NRIInfo {
  /** Information about NRI, provided as "label" / "value" pairs. <p><br /></p> > **Note**: The information returned in this field, including the > formatting of values and labels, should not be considered stable, > and may change without notice. */
  Info?: NRIInfoInfoList;
}
export const NRIInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Info: S.optional(NRIInfoInfoList),
  }),
).annotate({ identifier: "NRIInfo" }) as any as S.Schema<NRIInfo>;

/** List of warnings / informational messages about missing features, or issues related to the daemon configuration. These messages can be printed by the client as information to the user. */
export type SystemInfoWarningsList = Array<string>;
export const SystemInfoWarningsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SystemInfoWarningsList>;

/** List of directories where (Container Device Interface) CDI specifications are located. These specifications define vendor-specific modifications to an OCI runtime specification for a container being created. An empty list indicates that CDI device injection is disabled. Note that since using CDI device injection requires the daemon to have experimental enabled. For non-experimental daemons an empty list will always be returned. */
export type SystemInfoCDISpecDirsList = Array<string>;
export const SystemInfoCDISpecDirsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SystemInfoCDISpecDirsList>;

/** The namespaces that the daemon uses for running containers and plugins in containerd. These namespaces can be configured in the daemon configuration, and are considered to be used exclusively by the daemon, Tampering with the containerd instance may cause unexpected behavior. As these namespaces are considered to be exclusively accessed by the daemon, it is not recommended to change these values, or to change them to a value that is used by other systems, such as cri-containerd. */
export interface ContainerdInfoNamespaces {
  /** The default containerd namespace used for containers managed by the daemon. The default namespace for containers is "moby", but will be suffixed with the `<uid>.<gid>` of the remapped `root` if user-namespaces are enabled and the containerd image-store is used. */
  Containers?: string;
  /** The default containerd namespace used for plugins managed by the daemon. The default namespace for plugins is "plugins.moby", but will be suffixed with the `<uid>.<gid>` of the remapped `root` if user-namespaces are enabled and the containerd image-store is used. */
  Plugins?: string;
}
export const ContainerdInfoNamespaces = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Containers: S.optional(S.String),
    Plugins: S.optional(S.String),
  }),
).annotate({
  identifier: "ContainerdInfoNamespaces",
}) as any as S.Schema<ContainerdInfoNamespaces>;

/** Information for connecting to the containerd instance that is used by the daemon. This is included for debugging purposes only. */
export interface ContainerdInfo {
  /** The address of the containerd socket. */
  Address?: string;
  /** The namespaces that the daemon uses for running containers and plugins in containerd. These namespaces can be configured in the daemon configuration, and are considered to be used exclusively by the daemon, Tampering with the containerd instance may cause unexpected behavior. As these namespaces are considered to be exclusively accessed by the daemon, it is not recommended to change these values, or to change them to a value that is used by other systems, such as cri-containerd. */
  Namespaces?: ContainerdInfoNamespaces;
}
export const ContainerdInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Address: S.optional(S.String),
    Namespaces: S.optional(ContainerdInfoNamespaces),
  }),
).annotate({ identifier: "ContainerdInfo" }) as any as S.Schema<ContainerdInfo>;

export interface SystemInfo {
  /** Unique identifier of the daemon. <p><br /></p> > **Note**: The format of the ID itself is not part of the API, and > should not be considered stable. */
  ID?: string;
  /** Total number of containers on the host. */
  Containers?: number;
  /** Number of containers with status `"running"`. */
  ContainersRunning?: number;
  /** Number of containers with status `"paused"`. */
  ContainersPaused?: number;
  /** Number of containers with status `"stopped"`. */
  ContainersStopped?: number;
  /** Total number of images on the host. Both _tagged_ and _untagged_ (dangling) images are counted. */
  Images?: number;
  /** Name of the storage driver in use. */
  Driver?: string;
  /** Information specific to the storage driver, provided as "label" / "value" pairs. This information is provided by the storage driver, and formatted in a way consistent with the output of `docker info` on the command line. <p><br /></p> > **Note**: The information returned in this field, including the > formatting of values and labels, should not be considered stable, > and may change without notice. */
  DriverStatus?: SystemInfoDriverStatusList;
  /** Root directory of persistent Docker state. Defaults to `/var/lib/docker` on Linux, and `C:\ProgramData\docker` on Windows. */
  DockerRootDir?: string;
  Plugins?: PluginsInfo;
  /** Indicates if the host has memory limit support enabled. */
  MemoryLimit?: boolean;
  /** Indicates if the host has memory swap limit support enabled. */
  SwapLimit?: boolean;
  /** Indicates if CPU CFS(Completely Fair Scheduler) period is supported by the host. */
  CpuCfsPeriod?: boolean;
  /** Indicates if CPU CFS(Completely Fair Scheduler) quota is supported by the host. */
  CpuCfsQuota?: boolean;
  /** Indicates if CPU Shares limiting is supported by the host. */
  CPUShares?: boolean;
  /** Indicates if CPUsets (cpuset.cpus, cpuset.mems) are supported by the host. See [cpuset(7)](https://www.kernel.org/doc/Documentation/cgroup-v1/cpusets.txt) */
  CPUSet?: boolean;
  /** Indicates if the host kernel has PID limit support enabled. */
  PidsLimit?: boolean;
  /** Indicates if OOM killer disable is supported on the host. */
  OomKillDisable?: boolean;
  /** Indicates IPv4 forwarding is enabled. */
  IPv4Forwarding?: boolean;
  /** Indicates if the daemon is running in debug-mode / with debug-level logging enabled. */
  Debug?: boolean;
  /** The total number of file Descriptors in use by the daemon process. This information is only returned if debug-mode is enabled. */
  NFd?: number;
  /** The number of goroutines that currently exist. This information is only returned if debug-mode is enabled. */
  NGoroutines?: number;
  /** Current system-time in [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format with nano-seconds. */
  SystemTime?: string;
  /** The logging driver to use as a default for new containers. */
  LoggingDriver?: string;
  /** The driver to use for managing cgroups. */
  CgroupDriver?: SystemInfoCgroupDriver;
  /** The version of the cgroup. */
  CgroupVersion?: SystemInfoCgroupVersion;
  /** Number of event listeners subscribed. */
  NEventsListener?: number;
  /** Kernel version of the host. On Linux, this information obtained from `uname`. On Windows this information is queried from the <kbd>HKEY_LOCAL_MACHINE\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion\\</kbd> registry value, for example _"10.0 14393 (14393.1198.amd64fre.rs1_release_sec.170427-1353)"_. */
  KernelVersion?: string;
  /** Name of the host's operating system, for example: "Ubuntu 24.04 LTS" or "Windows Server 2016 Datacenter" */
  OperatingSystem?: string;
  /** Version of the host's operating system <p><br /></p> > **Note**: The information returned in this field, including its > very existence, and the formatting of values, should not be considered > stable, and may change without notice. */
  OSVersion?: string;
  /** Generic type of the operating system of the host, as returned by the Go runtime (`GOOS`). Currently returned values are "linux" and "windows". A full list of possible values can be found in the [Go documentation](https://go.dev/doc/install/source#environment). */
  OSType?: string;
  /** Hardware architecture of the host, as returned by the operating system. This is equivalent to the output of `uname -m` on Linux. Unlike `Arch` (from `/version`), this reports the machine's native architecture, which can differ from the Go runtime architecture when running a binary compiled for a different architecture (for example, a 32-bit binary running on 64-bit hardware). */
  Architecture?: string;
  /** The number of logical CPUs usable by the daemon. The number of available CPUs is checked by querying the operating system when the daemon starts. Changes to operating system CPU allocation after the daemon is started are not reflected. */
  NCPU?: number;
  /** Total amount of physical memory available on the host, in bytes. */
  MemTotal?: number;
  /** Address / URL of the index server that is used for image search, and as a default for user authentication for Docker Hub and Docker Cloud. */
  IndexServerAddress?: string;
  RegistryConfig?: RegistryServiceConfig | null;
  GenericResources?: GenericResources;
  /** HTTP-proxy configured for the daemon. This value is obtained from the [`HTTP_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html) environment variable. Credentials ([user info component](https://tools.ietf.org/html/rfc3986#section-3.2.1)) in the proxy URL are masked in the API response. Containers do not automatically inherit this configuration. */
  HttpProxy?: string;
  /** HTTPS-proxy configured for the daemon. This value is obtained from the [`HTTPS_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html) environment variable. Credentials ([user info component](https://tools.ietf.org/html/rfc3986#section-3.2.1)) in the proxy URL are masked in the API response. Containers do not automatically inherit this configuration. */
  HttpsProxy?: string;
  /** Comma-separated list of domain extensions for which no proxy should be used. This value is obtained from the [`NO_PROXY`](https://www.gnu.org/software/wget/manual/html_node/Proxies.html) environment variable. Containers do not automatically inherit this configuration. */
  NoProxy?: string;
  /** Hostname of the host. */
  Name?: string;
  /** User-defined labels (key/value metadata) as set on the daemon. <p><br /></p> > **Note**: When part of a Swarm, nodes can both have _daemon_ labels, > set through the daemon configuration, and _node_ labels, set from a > manager node in the Swarm. Node labels are not included in this > field. Node labels can be retrieved using the `/nodes/(id)` endpoint > on a manager node in the Swarm. */
  Labels?: SystemInfoLabelsList;
  /** Indicates if experimental features are enabled on the daemon. */
  ExperimentalBuild?: boolean;
  /** Version string of the daemon. */
  ServerVersion?: string;
  /** List of [OCI compliant](https://github.com/opencontainers/runtime-spec) runtimes configured on the daemon. Keys hold the "name" used to reference the runtime. The Docker daemon relies on an OCI compliant runtime (invoked via the `containerd` daemon) as its interface to the Linux kernel namespaces, cgroups, and SELinux. The default runtime is `runc`, and automatically configured. Additional runtimes can be configured by the user and will be listed here. */
  Runtimes?: SystemInfoRuntimesMap;
  /** Name of the default OCI runtime that is used when starting containers. The default can be overridden per-container at create time. */
  DefaultRuntime?: string;
  Swarm?: SwarmInfo;
  /** Indicates if live restore is enabled. If enabled, containers are kept running when the daemon is shutdown or upon daemon start if running containers are detected. */
  LiveRestoreEnabled?: boolean;
  /** Represents the isolation technology to use as a default for containers. The supported values are platform-specific. If no isolation value is specified on daemon start, on Windows client, the default is `hyperv`, and on Windows server, the default is `process`. This option is currently not used on other platforms. */
  Isolation?: SystemInfoIsolation;
  /** Name and, optional, path of the `docker-init` binary. If the path is omitted, the daemon searches the host's `$PATH` for the binary and uses the first result. */
  InitBinary?: string;
  ContainerdCommit?: Commit;
  RuncCommit?: Commit;
  InitCommit?: Commit;
  /** List of security features that are enabled on the daemon, such as apparmor, seccomp, SELinux, user-namespaces (userns), rootless and no-new-privileges. Additional configuration options for each security feature may be present, and are included as a comma-separated list of key/value pairs. */
  SecurityOptions?: SystemInfoSecurityOptionsList;
  /** Reports a summary of the product license on the daemon. If a commercial license has been applied to the daemon, information such as number of nodes, and expiration are included. */
  ProductLicense?: string;
  /** List of custom default address pools for local networks, which can be specified in the daemon.json file or dockerd option. Example: a Base "10.10.0.0/16" with Size 24 will define the set of 256 10.10.[0-255].0/24 address pools. */
  DefaultAddressPools?: SystemInfoDefaultAddressPoolsList;
  FirewallBackend?: FirewallInfo | null;
  /** List of devices discovered by device drivers. Each device includes information about its source driver, kind, name, and additional driver-specific attributes. */
  DiscoveredDevices?: SystemInfoDiscoveredDevicesList;
  NRI?: NRIInfo | null;
  /** List of warnings / informational messages about missing features, or issues related to the daemon configuration. These messages can be printed by the client as information to the user. */
  Warnings?: SystemInfoWarningsList;
  /** List of directories where (Container Device Interface) CDI specifications are located. These specifications define vendor-specific modifications to an OCI runtime specification for a container being created. An empty list indicates that CDI device injection is disabled. Note that since using CDI device injection requires the daemon to have experimental enabled. For non-experimental daemons an empty list will always be returned. */
  CDISpecDirs?: SystemInfoCDISpecDirsList;
  Containerd?: ContainerdInfo | null;
}
export const SystemInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ID: S.optional(S.String),
    Containers: S.optional(S.Number),
    ContainersRunning: S.optional(S.Number),
    ContainersPaused: S.optional(S.Number),
    ContainersStopped: S.optional(S.Number),
    Images: S.optional(S.Number),
    Driver: S.optional(S.String),
    DriverStatus: S.optional(SystemInfoDriverStatusList),
    DockerRootDir: S.optional(S.String),
    Plugins: S.optional(PluginsInfo),
    MemoryLimit: S.optional(S.Boolean),
    SwapLimit: S.optional(S.Boolean),
    CpuCfsPeriod: S.optional(S.Boolean),
    CpuCfsQuota: S.optional(S.Boolean),
    CPUShares: S.optional(S.Boolean),
    CPUSet: S.optional(S.Boolean),
    PidsLimit: S.optional(S.Boolean),
    OomKillDisable: S.optional(S.Boolean),
    IPv4Forwarding: S.optional(S.Boolean),
    Debug: S.optional(S.Boolean),
    NFd: S.optional(S.Number),
    NGoroutines: S.optional(S.Number),
    SystemTime: S.optional(S.String),
    LoggingDriver: S.optional(S.String),
    CgroupDriver: S.optional(SystemInfoCgroupDriver),
    CgroupVersion: S.optional(SystemInfoCgroupVersion),
    NEventsListener: S.optional(S.Number),
    KernelVersion: S.optional(S.String),
    OperatingSystem: S.optional(S.String),
    OSVersion: S.optional(S.String),
    OSType: S.optional(S.String),
    Architecture: S.optional(S.String),
    NCPU: S.optional(S.Number),
    MemTotal: S.optional(S.Number),
    IndexServerAddress: S.optional(S.String),
    RegistryConfig: S.optional(S.NullOr(RegistryServiceConfig)),
    GenericResources: S.optional(GenericResources),
    HttpProxy: S.optional(S.String),
    HttpsProxy: S.optional(S.String),
    NoProxy: S.optional(S.String),
    Name: S.optional(S.String),
    Labels: S.optional(SystemInfoLabelsList),
    ExperimentalBuild: S.optional(S.Boolean),
    ServerVersion: S.optional(S.String),
    Runtimes: S.optional(SystemInfoRuntimesMap),
    DefaultRuntime: S.optional(S.String),
    Swarm: S.optional(SwarmInfo),
    LiveRestoreEnabled: S.optional(S.Boolean),
    Isolation: S.optional(SystemInfoIsolation),
    InitBinary: S.optional(S.String),
    ContainerdCommit: S.optional(Commit),
    RuncCommit: S.optional(Commit),
    InitCommit: S.optional(Commit),
    SecurityOptions: S.optional(SystemInfoSecurityOptionsList),
    ProductLicense: S.optional(S.String),
    DefaultAddressPools: S.optional(SystemInfoDefaultAddressPoolsList),
    FirewallBackend: S.optional(S.NullOr(FirewallInfo)),
    DiscoveredDevices: S.optional(SystemInfoDiscoveredDevicesList),
    NRI: S.optional(S.NullOr(NRIInfo)),
    Warnings: S.optional(SystemInfoWarningsList),
    CDISpecDirs: S.optional(SystemInfoCDISpecDirsList),
    Containerd: S.optional(S.NullOr(ContainerdInfo)),
  }),
).annotate({ identifier: "SystemInfo" }) as any as S.Schema<SystemInfo>;

export interface SystemVersion2Request {}
export const SystemVersion2Request = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/version", code: 200 })),
).annotate({
  identifier: "SystemVersion2Request",
}) as any as S.Schema<SystemVersion2Request>;

export interface SystemVersionPlatform {
  Name: string;
}
export const SystemVersionPlatform = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
  }),
).annotate({
  identifier: "SystemVersionPlatform",
}) as any as S.Schema<SystemVersionPlatform>;

export interface SystemVersionComponentsItem {
  /** Name of the component */
  Name: string;
  /** Version of the component */
  Version: string;
  /** Key/value pairs of strings with additional information about the component. These values are intended for informational purposes only, and their content is not defined, and not part of the API specification. These messages can be printed by the client as information to the user. */
  Details?: unknown | null;
}
export const SystemVersionComponentsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Name: S.String,
    Version: S.String,
    Details: S.optional(S.NullOr(S.Unknown)),
  }),
).annotate({
  identifier: "SystemVersionComponentsItem",
}) as any as S.Schema<SystemVersionComponentsItem>;

/** Information about system components */
export type SystemVersionComponentsList = Array<SystemVersionComponentsItem>;
export const SystemVersionComponentsList = /*@__PURE__*/ S.Array(
  SystemVersionComponentsItem,
) as any as S.Schema<SystemVersionComponentsList>;

/** Response of Engine API: GET "/version" */
export interface SystemVersion {
  Platform?: SystemVersionPlatform;
  /** Information about system components */
  Components?: SystemVersionComponentsList;
  /** The version of the daemon */
  Version?: string;
  /** The default (and highest) API version that is supported by the daemon */
  ApiVersion?: string;
  /** The minimum API version that is supported by the daemon */
  MinAPIVersion?: string;
  /** The Git commit of the source code that was used to build the daemon */
  GitCommit?: string;
  /** The version Go used to compile the daemon, and the version of the Go runtime in use. */
  GoVersion?: string;
  /** The operating system that the daemon is running on ("linux" or "windows") */
  Os?: string;
  /** Architecture of the daemon, as returned by the Go runtime (`GOARCH`). A full list of possible values can be found in the [Go documentation](https://go.dev/doc/install/source#environment). */
  Arch?: string;
  /** The kernel version (`uname -r`) that the daemon is running on. This field is omitted when empty. */
  KernelVersion?: string;
  /** Indicates if the daemon is started with experimental features enabled. This field is omitted when empty / false. */
  Experimental?: boolean;
  /** The date and time that the daemon was compiled. */
  BuildTime?: string;
}
export const SystemVersion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Platform: S.optional(SystemVersionPlatform),
    Components: S.optional(SystemVersionComponentsList),
    Version: S.optional(S.String),
    ApiVersion: S.optional(S.String),
    MinAPIVersion: S.optional(S.String),
    GitCommit: S.optional(S.String),
    GoVersion: S.optional(S.String),
    Os: S.optional(S.String),
    Arch: S.optional(S.String),
    KernelVersion: S.optional(S.String),
    Experimental: S.optional(S.Boolean),
    BuildTime: S.optional(S.String),
  }),
).annotate({ identifier: "SystemVersion" }) as any as S.Schema<SystemVersion>;

export interface TaskInspectRequest {
  /** ID of the task */
  id: string;
}
export const TaskInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/tasks/{id}", code: 200 })),
).annotate({
  identifier: "TaskInspectRequest",
}) as any as S.Schema<TaskInspectRequest>;

export interface TaskLogsRequest {
  /** ID of the task */
  id: string;
  /** Show task context and extra details provided to logs. */
  details?: boolean;
  /** Keep connection after returning logs. */
  follow?: boolean;
  /** Return logs from `stdout` */
  stdout?: boolean;
  /** Return logs from `stderr` */
  stderr?: boolean;
  /** Only return logs since this time, as a UNIX timestamp */
  since?: number;
  /** Add timestamps to every log line */
  timestamps?: boolean;
  /** Only return this number of log lines from the end of the logs. Specify as an integer or `all` to output all log lines. */
  tail?: string;
}
export const TaskLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    details: S.optional(S.Boolean.pipe(T.Query())),
    follow: S.optional(S.Boolean.pipe(T.Query())),
    stdout: S.optional(S.Boolean.pipe(T.Query())),
    stderr: S.optional(S.Boolean.pipe(T.Query())),
    since: S.optional(S.Number.pipe(T.Query())),
    timestamps: S.optional(S.Boolean.pipe(T.Query())),
    tail: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/tasks/{id}/logs", code: 200 })),
).annotate({
  identifier: "TaskLogsRequest",
}) as any as S.Schema<TaskLogsRequest>;

export type TaskLogsResponse = string;
export const TaskLogsResponse = /*@__PURE__*/ S.suspend(() =>
  S.String.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "TaskLogsResponse",
}) as any as S.Schema<TaskLogsResponse>;

export interface UnlockSwarmRequest {
  /** The swarm's unlock key. */
  UnlockKey?: string;
}
export const UnlockSwarmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    UnlockKey: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/swarm/unlock", code: 200 })),
).annotate({
  identifier: "UnlockSwarmRequest",
}) as any as S.Schema<UnlockSwarmRequest>;

export interface UnlockSwarmResponse {}
export const UnlockSwarmResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UnlockSwarmResponse",
}) as any as S.Schema<UnlockSwarmResponse>;

export interface UnpauseContainerRequest {
  /** ID or name of the container */
  id: string;
}
export const UnpauseContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "POST", uri: "/containers/{id}/unpause", code: 200 }),
  ),
).annotate({
  identifier: "UnpauseContainerRequest",
}) as any as S.Schema<UnpauseContainerRequest>;

export interface UnpauseContainerResponse {}
export const UnpauseContainerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UnpauseContainerResponse",
}) as any as S.Schema<UnpauseContainerResponse>;

/** User-defined key/value metadata. */
export type UpdateConfigRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const UpdateConfigRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateConfigRequestLabelsMap>;

export interface UpdateConfigRequest {
  /** The ID or name of the config */
  id: string;
  /** The version number of the config object being updated. This is required to avoid conflicting writes. */
  version: number;
  /** User-defined name of the config. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: UpdateConfigRequestLabelsMap;
  /** Data is the data to store as a config, formatted as a standard base64-encoded ([RFC 4648](https://tools.ietf.org/html/rfc4648#section-4)) string. The maximum allowed size is 1000KB, as defined in [MaxConfigSize](https://pkg.go.dev/github.com/moby/swarmkit/v2@v2.0.0-20250103191802-8c1959736554/manager/controlapi#MaxConfigSize). */
  Data?: string;
  /** Templating driver, if applicable Templating controls whether and how to evaluate the config payload as a template. If no driver is set, no templating is used. */
  Templating?: Driver;
}
export const UpdateConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    version: S.Number.pipe(T.Query()),
    Name: S.optional(S.String),
    Labels: S.optional(UpdateConfigRequestLabelsMap),
    Data: S.optional(S.String),
    Templating: S.optional(Driver),
  }).pipe(T.Http({ method: "POST", uri: "/configs/{id}/update", code: 200 })),
).annotate({
  identifier: "UpdateConfigRequest",
}) as any as S.Schema<UpdateConfigRequest>;

export interface UpdateConfigResponse {}
export const UpdateConfigResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UpdateConfigResponse",
}) as any as S.Schema<UpdateConfigResponse>;

export type UpdateContainerRequestBlkioWeightDeviceItem =
  HostConfigBlkioWeightDeviceItem;
export const UpdateContainerRequestBlkioWeightDeviceItem =
  HostConfigBlkioWeightDeviceItem;

/** Block IO weight (relative device weight) in the form: ``` [{"Path": "device_path", "Weight": weight}] ``` */
export type UpdateContainerRequestBlkioWeightDeviceList =
  Array<HostConfigBlkioWeightDeviceItem>;
export const UpdateContainerRequestBlkioWeightDeviceList =
  /*@__PURE__*/ S.Array(
    HostConfigBlkioWeightDeviceItem,
  ) as any as S.Schema<UpdateContainerRequestBlkioWeightDeviceList>;

/** Limit read rate (bytes per second) from a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
export type UpdateContainerRequestBlkioDeviceReadBpsList =
  Array<ThrottleDevice>;
export const UpdateContainerRequestBlkioDeviceReadBpsList =
  /*@__PURE__*/ S.Array(
    ThrottleDevice,
  ) as any as S.Schema<UpdateContainerRequestBlkioDeviceReadBpsList>;

/** Limit write rate (bytes per second) to a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
export type UpdateContainerRequestBlkioDeviceWriteBpsList =
  Array<ThrottleDevice>;
export const UpdateContainerRequestBlkioDeviceWriteBpsList =
  /*@__PURE__*/ S.Array(
    ThrottleDevice,
  ) as any as S.Schema<UpdateContainerRequestBlkioDeviceWriteBpsList>;

/** Limit read rate (IO per second) from a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
export type UpdateContainerRequestBlkioDeviceReadIOpsList =
  Array<ThrottleDevice>;
export const UpdateContainerRequestBlkioDeviceReadIOpsList =
  /*@__PURE__*/ S.Array(
    ThrottleDevice,
  ) as any as S.Schema<UpdateContainerRequestBlkioDeviceReadIOpsList>;

/** Limit write rate (IO per second) to a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
export type UpdateContainerRequestBlkioDeviceWriteIOpsList =
  Array<ThrottleDevice>;
export const UpdateContainerRequestBlkioDeviceWriteIOpsList =
  /*@__PURE__*/ S.Array(
    ThrottleDevice,
  ) as any as S.Schema<UpdateContainerRequestBlkioDeviceWriteIOpsList>;

/** A list of devices to add to the container. */
export type UpdateContainerRequestDevicesList = Array<DeviceMapping>;
export const UpdateContainerRequestDevicesList = /*@__PURE__*/ S.Array(
  DeviceMapping,
) as any as S.Schema<UpdateContainerRequestDevicesList>;

/** a list of cgroup rules to apply to the container */
export type UpdateContainerRequestDeviceCgroupRulesList = Array<string>;
export const UpdateContainerRequestDeviceCgroupRulesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateContainerRequestDeviceCgroupRulesList>;

/** A list of requests for devices to be sent to device drivers. */
export type UpdateContainerRequestDeviceRequestsList = Array<DeviceRequest>;
export const UpdateContainerRequestDeviceRequestsList = /*@__PURE__*/ S.Array(
  DeviceRequest,
) as any as S.Schema<UpdateContainerRequestDeviceRequestsList>;

export type UpdateContainerRequestUlimitsItem = HostConfigUlimitsItem;
export const UpdateContainerRequestUlimitsItem = HostConfigUlimitsItem;

/** A list of resource limits to set in the container. For example: ``` {"Name": "nofile", "Soft": 1024, "Hard": 2048} ``` */
export type UpdateContainerRequestUlimitsList = Array<HostConfigUlimitsItem>;
export const UpdateContainerRequestUlimitsList = /*@__PURE__*/ S.Array(
  HostConfigUlimitsItem,
) as any as S.Schema<UpdateContainerRequestUlimitsList>;

export interface UpdateContainerRequest {
  /** ID or name of the container */
  id: string;
  /** An integer value representing this container's relative CPU weight versus other containers. */
  CpuShares?: number;
  /** Memory limit in bytes. */
  Memory?: number;
  /** Path to `cgroups` under which the container's `cgroup` is created. If the path is not absolute, the path is considered to be relative to the `cgroups` path of the init process. Cgroups are created if they do not already exist. */
  CgroupParent?: string;
  /** Block IO weight (relative weight). */
  BlkioWeight?: number;
  /** Block IO weight (relative device weight) in the form: ``` [{"Path": "device_path", "Weight": weight}] ``` */
  BlkioWeightDevice?: UpdateContainerRequestBlkioWeightDeviceList;
  /** Limit read rate (bytes per second) from a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
  BlkioDeviceReadBps?: UpdateContainerRequestBlkioDeviceReadBpsList;
  /** Limit write rate (bytes per second) to a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
  BlkioDeviceWriteBps?: UpdateContainerRequestBlkioDeviceWriteBpsList;
  /** Limit read rate (IO per second) from a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
  BlkioDeviceReadIOps?: UpdateContainerRequestBlkioDeviceReadIOpsList;
  /** Limit write rate (IO per second) to a device, in the form: ``` [{"Path": "device_path", "Rate": rate}] ``` */
  BlkioDeviceWriteIOps?: UpdateContainerRequestBlkioDeviceWriteIOpsList;
  /** The length of a CPU period in microseconds. */
  CpuPeriod?: number;
  /** Microseconds of CPU time that the container can get in a CPU period. */
  CpuQuota?: number;
  /** The length of a CPU real-time period in microseconds. Set to 0 to allocate no time allocated to real-time tasks. */
  CpuRealtimePeriod?: number;
  /** The length of a CPU real-time runtime in microseconds. Set to 0 to allocate no time allocated to real-time tasks. */
  CpuRealtimeRuntime?: number;
  /** CPUs in which to allow execution (e.g., `0-3`, `0,1`). */
  CpusetCpus?: string;
  /** Memory nodes (MEMs) in which to allow execution (0-3, 0,1). Only effective on NUMA systems. */
  CpusetMems?: string;
  /** A list of devices to add to the container. */
  Devices?: UpdateContainerRequestDevicesList;
  /** a list of cgroup rules to apply to the container */
  DeviceCgroupRules?: UpdateContainerRequestDeviceCgroupRulesList;
  /** A list of requests for devices to be sent to device drivers. */
  DeviceRequests?: UpdateContainerRequestDeviceRequestsList;
  /** Memory soft limit in bytes. */
  MemoryReservation?: number;
  /** Total memory limit (memory + swap). Set as `-1` to enable unlimited swap. */
  MemorySwap?: number;
  /** Tune a container's memory swappiness behavior. Accepts an integer between 0 and 100. */
  MemorySwappiness?: number;
  /** CPU quota in units of 10<sup>-9</sup> CPUs. */
  NanoCpus?: number;
  /** Disable OOM Killer for the container. */
  OomKillDisable?: boolean;
  /** Run an init inside the container that forwards signals and reaps processes. This field is omitted if empty, and the default (as configured on the daemon) is used. */
  Init?: boolean | null;
  /** Tune a container's PIDs limit. Set `0` or `-1` for unlimited, or `null` to not change. */
  PidsLimit?: number | null;
  /** A list of resource limits to set in the container. For example: ``` {"Name": "nofile", "Soft": 1024, "Hard": 2048} ``` */
  Ulimits?: UpdateContainerRequestUlimitsList;
  /** The number of usable CPUs (Windows only). On Windows Server containers, the processor resource controls are mutually exclusive. The order of precedence is `CPUCount` first, then `CPUShares`, and `CPUPercent` last. */
  CpuCount?: number;
  /** The usable percentage of the available CPUs (Windows only). On Windows Server containers, the processor resource controls are mutually exclusive. The order of precedence is `CPUCount` first, then `CPUShares`, and `CPUPercent` last. */
  CpuPercent?: number;
  /** Maximum IOps for the container system drive (Windows only) */
  IOMaximumIOps?: number;
  /** Maximum IO in bytes per second for the container system drive (Windows only). */
  IOMaximumBandwidth?: number;
  RestartPolicy?: RestartPolicy;
}
export const UpdateContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    CpuShares: S.optional(S.Number),
    Memory: S.optional(S.Number),
    CgroupParent: S.optional(S.String),
    BlkioWeight: S.optional(S.Number),
    BlkioWeightDevice: S.optional(UpdateContainerRequestBlkioWeightDeviceList),
    BlkioDeviceReadBps: S.optional(
      UpdateContainerRequestBlkioDeviceReadBpsList,
    ),
    BlkioDeviceWriteBps: S.optional(
      UpdateContainerRequestBlkioDeviceWriteBpsList,
    ),
    BlkioDeviceReadIOps: S.optional(
      UpdateContainerRequestBlkioDeviceReadIOpsList,
    ),
    BlkioDeviceWriteIOps: S.optional(
      UpdateContainerRequestBlkioDeviceWriteIOpsList,
    ),
    CpuPeriod: S.optional(S.Number),
    CpuQuota: S.optional(S.Number),
    CpuRealtimePeriod: S.optional(S.Number),
    CpuRealtimeRuntime: S.optional(S.Number),
    CpusetCpus: S.optional(S.String),
    CpusetMems: S.optional(S.String),
    Devices: S.optional(UpdateContainerRequestDevicesList),
    DeviceCgroupRules: S.optional(UpdateContainerRequestDeviceCgroupRulesList),
    DeviceRequests: S.optional(UpdateContainerRequestDeviceRequestsList),
    MemoryReservation: S.optional(S.Number),
    MemorySwap: S.optional(S.Number),
    MemorySwappiness: S.optional(S.Number),
    NanoCpus: S.optional(S.Number),
    OomKillDisable: S.optional(S.Boolean),
    Init: S.optional(S.NullOr(S.Boolean)),
    PidsLimit: S.optional(S.NullOr(S.Number)),
    Ulimits: S.optional(UpdateContainerRequestUlimitsList),
    CpuCount: S.optional(S.Number),
    CpuPercent: S.optional(S.Number),
    IOMaximumIOps: S.optional(S.Number),
    IOMaximumBandwidth: S.optional(S.Number),
    RestartPolicy: S.optional(RestartPolicy),
  }).pipe(
    T.Http({ method: "POST", uri: "/containers/{id}/update", code: 200 }),
  ),
).annotate({
  identifier: "UpdateContainerRequest",
}) as any as S.Schema<UpdateContainerRequest>;

/** Warnings encountered when updating the container. */
export type ContainerUpdateResponseWarningsList = Array<string>;
export const ContainerUpdateResponseWarningsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ContainerUpdateResponseWarningsList>;

/** Response for a successful container-update. */
export interface ContainerUpdateResponse {
  /** Warnings encountered when updating the container. */
  Warnings?: ContainerUpdateResponseWarningsList;
}
export const ContainerUpdateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Warnings: S.optional(ContainerUpdateResponseWarningsList),
  }),
).annotate({
  identifier: "ContainerUpdateResponse",
}) as any as S.Schema<ContainerUpdateResponse>;

/** User-defined key/value metadata. */
export type UpdateNodeRequestLabelsMap = { [key: string]: string | undefined };
export const UpdateNodeRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateNodeRequestLabelsMap>;

/** Role of the node. */
export type UpdateNodeRequestRole = "worker" | "manager";
export const UpdateNodeRequestRole = S.String;

/** Availability of the node. */
export type UpdateNodeRequestAvailability = "active" | "pause" | "drain";
export const UpdateNodeRequestAvailability = S.String;

export interface UpdateNodeRequest {
  /** The ID of the node */
  id: string;
  /** The version number of the node object being updated. This is required to avoid conflicting writes. */
  version: number;
  /** Name for the node. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: UpdateNodeRequestLabelsMap;
  /** Role of the node. */
  Role?: UpdateNodeRequestRole | (string & {});
  /** Availability of the node. */
  Availability?: UpdateNodeRequestAvailability | (string & {});
}
export const UpdateNodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    version: S.Number.pipe(T.Query()),
    Name: S.optional(S.String),
    Labels: S.optional(UpdateNodeRequestLabelsMap),
    Role: S.optional(UpdateNodeRequestRole),
    Availability: S.optional(UpdateNodeRequestAvailability),
  }).pipe(T.Http({ method: "POST", uri: "/nodes/{id}/update", code: 200 })),
).annotate({
  identifier: "UpdateNodeRequest",
}) as any as S.Schema<UpdateNodeRequest>;

export interface UpdateNodeResponse {}
export const UpdateNodeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UpdateNodeResponse",
}) as any as S.Schema<UpdateNodeResponse>;

/** User-defined key/value metadata. */
export type UpdateSecretRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const UpdateSecretRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateSecretRequestLabelsMap>;

export interface UpdateSecretRequest {
  /** The ID or name of the secret */
  id: string;
  /** The version number of the secret object being updated. This is required to avoid conflicting writes. */
  version: number;
  /** User-defined name of the secret. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: UpdateSecretRequestLabelsMap;
  /** Data is the data to store as a secret, formatted as a standard base64-encoded ([RFC 4648](https://tools.ietf.org/html/rfc4648#section-4)) string. It must be empty if the Driver field is set, in which case the data is loaded from an external secret store. The maximum allowed size is 500KB, as defined in [MaxSecretSize](https://pkg.go.dev/github.com/moby/swarmkit/v2@v2.0.0/api/validation#MaxSecretSize). This field is only used to _create_ a secret, and is not returned by other endpoints. */
  Data?: string;
  /** Name of the secrets driver used to fetch the secret's value from an external secret store. */
  Driver?: Driver;
  /** Templating driver, if applicable Templating controls whether and how to evaluate the config payload as a template. If no driver is set, no templating is used. */
  Templating?: Driver;
}
export const UpdateSecretRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    version: S.Number.pipe(T.Query()),
    Name: S.optional(S.String),
    Labels: S.optional(UpdateSecretRequestLabelsMap),
    Data: S.optional(S.String),
    Driver: S.optional(Driver),
    Templating: S.optional(Driver),
  }).pipe(T.Http({ method: "POST", uri: "/secrets/{id}/update", code: 200 })),
).annotate({
  identifier: "UpdateSecretRequest",
}) as any as S.Schema<UpdateSecretRequest>;

export interface UpdateSecretResponse {}
export const UpdateSecretResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UpdateSecretResponse",
}) as any as S.Schema<UpdateSecretResponse>;

export type UpdateServiceRequestRegistryAuthFrom = "spec" | "previous-spec";
export const UpdateServiceRequestRegistryAuthFrom = S.String;

/** User-defined key/value metadata. */
export type UpdateServiceRequestLabelsMap = {
  [key: string]: string | undefined;
};
export const UpdateServiceRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateServiceRequestLabelsMap>;

export type UpdateServiceRequestModeReplicated =
  CreateServiceRequestModeReplicated;
export const UpdateServiceRequestModeReplicated =
  CreateServiceRequestModeReplicated;

/** The mode used for services with a finite number of tasks that run to a completed state. */
export type UpdateServiceRequestModeReplicatedJob =
  CreateServiceRequestModeReplicatedJob;
export const UpdateServiceRequestModeReplicatedJob =
  CreateServiceRequestModeReplicatedJob;

/** Scheduling mode for the service. */
export type UpdateServiceRequestMode = CreateServiceRequestMode;
export const UpdateServiceRequestMode = CreateServiceRequestMode;

/** Action to take if an updated task fails to run, or stops running during the update. */
export type UpdateServiceRequestUpdateConfigFailureAction =
  | "continue"
  | "pause"
  | "rollback";
export const UpdateServiceRequestUpdateConfigFailureAction = S.String;

/** The order of operations when rolling out an updated task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
export type UpdateServiceRequestUpdateConfigOrder =
  | "stop-first"
  | "start-first";
export const UpdateServiceRequestUpdateConfigOrder = S.String;

/** Specification for the update strategy of the service. */
export interface UpdateServiceRequestUpdateConfig {
  /** Maximum number of tasks to be updated in one iteration (0 means unlimited parallelism). */
  Parallelism?: number;
  /** Amount of time between updates, in nanoseconds. */
  Delay?: number;
  /** Action to take if an updated task fails to run, or stops running during the update. */
  FailureAction?: UpdateServiceRequestUpdateConfigFailureAction | (string & {});
  /** Amount of time to monitor each updated task for failures, in nanoseconds. */
  Monitor?: number;
  /** The fraction of tasks that may fail during an update before the failure action is invoked, specified as a floating point number between 0 and 1. */
  MaxFailureRatio?: number;
  /** The order of operations when rolling out an updated task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
  Order?: UpdateServiceRequestUpdateConfigOrder | (string & {});
}
export const UpdateServiceRequestUpdateConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Parallelism: S.optional(S.Number),
    Delay: S.optional(S.Number),
    FailureAction: S.optional(UpdateServiceRequestUpdateConfigFailureAction),
    Monitor: S.optional(S.Number),
    MaxFailureRatio: S.optional(S.Number),
    Order: S.optional(UpdateServiceRequestUpdateConfigOrder),
  }),
).annotate({
  identifier: "UpdateServiceRequestUpdateConfig",
}) as any as S.Schema<UpdateServiceRequestUpdateConfig>;

/** Action to take if an rolled back task fails to run, or stops running during the rollback. */
export type UpdateServiceRequestRollbackConfigFailureAction =
  | "continue"
  | "pause";
export const UpdateServiceRequestRollbackConfigFailureAction = S.String;

/** The order of operations when rolling back a task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
export type UpdateServiceRequestRollbackConfigOrder =
  | "stop-first"
  | "start-first";
export const UpdateServiceRequestRollbackConfigOrder = S.String;

/** Specification for the rollback strategy of the service. */
export interface UpdateServiceRequestRollbackConfig {
  /** Maximum number of tasks to be rolled back in one iteration (0 means unlimited parallelism). */
  Parallelism?: number;
  /** Amount of time between rollback iterations, in nanoseconds. */
  Delay?: number;
  /** Action to take if an rolled back task fails to run, or stops running during the rollback. */
  FailureAction?:
    | UpdateServiceRequestRollbackConfigFailureAction
    | (string & {});
  /** Amount of time to monitor each rolled back task for failures, in nanoseconds. */
  Monitor?: number;
  /** The fraction of tasks that may fail during a rollback before the failure action is invoked, specified as a floating point number between 0 and 1. */
  MaxFailureRatio?: number;
  /** The order of operations when rolling back a task. Either the old task is shut down before the new task is started, or the new task is started before the old task is shut down. */
  Order?: UpdateServiceRequestRollbackConfigOrder | (string & {});
}
export const UpdateServiceRequestRollbackConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Parallelism: S.optional(S.Number),
    Delay: S.optional(S.Number),
    FailureAction: S.optional(UpdateServiceRequestRollbackConfigFailureAction),
    Monitor: S.optional(S.Number),
    MaxFailureRatio: S.optional(S.Number),
    Order: S.optional(UpdateServiceRequestRollbackConfigOrder),
  }),
).annotate({
  identifier: "UpdateServiceRequestRollbackConfig",
}) as any as S.Schema<UpdateServiceRequestRollbackConfig>;

/** Specifies which networks the service should attach to. Deprecated: This field is deprecated since v1.44. The Networks field in TaskSpec should be used instead. */
export type UpdateServiceRequestNetworksList = Array<NetworkAttachmentConfig>;
export const UpdateServiceRequestNetworksList = /*@__PURE__*/ S.Array(
  NetworkAttachmentConfig,
) as any as S.Schema<UpdateServiceRequestNetworksList>;

export interface UpdateServiceRequest {
  /** ID or name of service. */
  id: string;
  /** The version number of the service object being updated. This is required to avoid conflicting writes. This version number should be the value as currently set on the service *before* the update. You can find the current version by calling `GET /services/{id}` */
  version: number;
  /** If the `X-Registry-Auth` header is not specified, this parameter indicates where to find registry authorization credentials. */
  registryAuthFrom?: UpdateServiceRequestRegistryAuthFrom | (string & {});
  /** Set to this parameter to `previous` to cause a server-side rollback to the previous service spec. The supplied spec will be ignored in this case. */
  rollback?: string;
  /** A base64url-encoded auth configuration for pulling from private registries. Refer to the [authentication section](#section/Authentication) for details. */
  xRegistryAuth?: string;
  /** Name of the service. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: UpdateServiceRequestLabelsMap;
  TaskTemplate?: TaskSpec;
  /** Scheduling mode for the service. */
  Mode?: CreateServiceRequestMode;
  /** Specification for the update strategy of the service. */
  UpdateConfig?: UpdateServiceRequestUpdateConfig;
  /** Specification for the rollback strategy of the service. */
  RollbackConfig?: UpdateServiceRequestRollbackConfig;
  /** Specifies which networks the service should attach to. Deprecated: This field is deprecated since v1.44. The Networks field in TaskSpec should be used instead. */
  Networks?: UpdateServiceRequestNetworksList;
  EndpointSpec?: EndpointSpec;
}
export const UpdateServiceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    version: S.Number.pipe(T.Query()),
    registryAuthFrom: S.optional(
      UpdateServiceRequestRegistryAuthFrom.pipe(T.Query()),
    ),
    rollback: S.optional(S.String.pipe(T.Query())),
    xRegistryAuth: S.optional(S.String.pipe(T.Header("X-Registry-Auth"))),
    Name: S.optional(S.String),
    Labels: S.optional(UpdateServiceRequestLabelsMap),
    TaskTemplate: S.optional(TaskSpec),
    Mode: S.optional(CreateServiceRequestMode),
    UpdateConfig: S.optional(UpdateServiceRequestUpdateConfig),
    RollbackConfig: S.optional(UpdateServiceRequestRollbackConfig),
    Networks: S.optional(UpdateServiceRequestNetworksList),
    EndpointSpec: S.optional(EndpointSpec),
  }).pipe(T.Http({ method: "POST", uri: "/services/{id}/update", code: 200 })),
).annotate({
  identifier: "UpdateServiceRequest",
}) as any as S.Schema<UpdateServiceRequest>;

/** Optional warning messages */
export type ServiceUpdateResponseWarningsList = Array<string>;
export const ServiceUpdateResponseWarningsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ServiceUpdateResponseWarningsList>;

export interface ServiceUpdateResponse {
  /** Optional warning messages */
  Warnings?: ServiceUpdateResponseWarningsList;
}
export const ServiceUpdateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Warnings: S.optional(ServiceUpdateResponseWarningsList),
  }),
).annotate({
  identifier: "ServiceUpdateResponse",
}) as any as S.Schema<ServiceUpdateResponse>;

/** User-defined key/value metadata. */
export type UpdateSwarmRequestLabelsMap = { [key: string]: string | undefined };
export const UpdateSwarmRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateSwarmRequestLabelsMap>;

/** Orchestration configuration. */
export type UpdateSwarmRequestOrchestration = SwarmSpecOrchestration;
export const UpdateSwarmRequestOrchestration = SwarmSpecOrchestration;

/** Raft configuration. */
export type UpdateSwarmRequestRaft = SwarmSpecRaft;
export const UpdateSwarmRequestRaft = SwarmSpecRaft;

/** Dispatcher configuration. */
export type UpdateSwarmRequestDispatcher = SwarmSpecDispatcher;
export const UpdateSwarmRequestDispatcher = SwarmSpecDispatcher;

/** Protocol for communication with the external CA (currently only `cfssl` is supported). */
export type UpdateSwarmRequestCAConfigExternalCAsItemProtocol = "cfssl";
export const UpdateSwarmRequestCAConfigExternalCAsItemProtocol = S.String;

/** An object with key/value pairs that are interpreted as protocol-specific options for the external CA driver. */
export type UpdateSwarmRequestCAConfigExternalCAsItemOptionsMap = {
  [key: string]: string | undefined;
};
export const UpdateSwarmRequestCAConfigExternalCAsItemOptionsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<UpdateSwarmRequestCAConfigExternalCAsItemOptionsMap>;

export interface UpdateSwarmRequestCAConfigExternalCAsItem {
  /** Protocol for communication with the external CA (currently only `cfssl` is supported). */
  Protocol?: UpdateSwarmRequestCAConfigExternalCAsItemProtocol | (string & {});
  /** URL where certificate signing requests should be sent. */
  URL?: string;
  /** An object with key/value pairs that are interpreted as protocol-specific options for the external CA driver. */
  Options?: UpdateSwarmRequestCAConfigExternalCAsItemOptionsMap;
  /** The root CA certificate (in PEM format) this external CA uses to issue TLS certificates (assumed to be to the current swarm root CA certificate if not provided). */
  CACert?: string;
}
export const UpdateSwarmRequestCAConfigExternalCAsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      Protocol: S.optional(UpdateSwarmRequestCAConfigExternalCAsItemProtocol),
      URL: S.optional(S.String),
      Options: S.optional(UpdateSwarmRequestCAConfigExternalCAsItemOptionsMap),
      CACert: S.optional(S.String),
    }),
  ).annotate({
    identifier: "UpdateSwarmRequestCAConfigExternalCAsItem",
  }) as any as S.Schema<UpdateSwarmRequestCAConfigExternalCAsItem>;

/** Configuration for forwarding signing requests to an external certificate authority. */
export type UpdateSwarmRequestCAConfigExternalCAsList =
  Array<UpdateSwarmRequestCAConfigExternalCAsItem>;
export const UpdateSwarmRequestCAConfigExternalCAsList = /*@__PURE__*/ S.Array(
  UpdateSwarmRequestCAConfigExternalCAsItem,
) as any as S.Schema<UpdateSwarmRequestCAConfigExternalCAsList>;

/** CA configuration. */
export interface UpdateSwarmRequestCAConfig {
  /** The duration node certificates are issued for. */
  NodeCertExpiry?: number;
  /** Configuration for forwarding signing requests to an external certificate authority. */
  ExternalCAs?: UpdateSwarmRequestCAConfigExternalCAsList;
  /** The desired signing CA certificate for all swarm node TLS leaf certificates, in PEM format. */
  SigningCACert?: string;
  /** The desired signing CA key for all swarm node TLS leaf certificates, in PEM format. */
  SigningCAKey?: string;
  /** An integer whose purpose is to force swarm to generate a new signing CA certificate and key, if none have been specified in `SigningCACert` and `SigningCAKey` */
  ForceRotate?: number;
}
export const UpdateSwarmRequestCAConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    NodeCertExpiry: S.optional(S.Number),
    ExternalCAs: S.optional(UpdateSwarmRequestCAConfigExternalCAsList),
    SigningCACert: S.optional(S.String),
    SigningCAKey: S.optional(S.String),
    ForceRotate: S.optional(S.Number),
  }),
).annotate({
  identifier: "UpdateSwarmRequestCAConfig",
}) as any as S.Schema<UpdateSwarmRequestCAConfig>;

/** Parameters related to encryption-at-rest. */
export type UpdateSwarmRequestEncryptionConfig = SwarmSpecEncryptionConfig;
export const UpdateSwarmRequestEncryptionConfig = SwarmSpecEncryptionConfig;

/** Driver-specific options for the selected log driver, specified as key/value pairs. */
export type UpdateSwarmRequestTaskDefaultsLogDriverOptionsMap = {
  [key: string]: string | undefined;
};
export const UpdateSwarmRequestTaskDefaultsLogDriverOptionsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<UpdateSwarmRequestTaskDefaultsLogDriverOptionsMap>;

/** The log driver to use for tasks created in the orchestrator if unspecified by a service. Updating this value only affects new tasks. Existing tasks continue to use their previously configured log driver until recreated. */
export interface UpdateSwarmRequestTaskDefaultsLogDriver {
  /** The log driver to use as a default for new tasks. */
  Name?: string;
  /** Driver-specific options for the selected log driver, specified as key/value pairs. */
  Options?: UpdateSwarmRequestTaskDefaultsLogDriverOptionsMap;
}
export const UpdateSwarmRequestTaskDefaultsLogDriver = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      Name: S.optional(S.String),
      Options: S.optional(UpdateSwarmRequestTaskDefaultsLogDriverOptionsMap),
    }),
).annotate({
  identifier: "UpdateSwarmRequestTaskDefaultsLogDriver",
}) as any as S.Schema<UpdateSwarmRequestTaskDefaultsLogDriver>;

/** Defaults for creating tasks in this cluster. */
export interface UpdateSwarmRequestTaskDefaults {
  /** The log driver to use for tasks created in the orchestrator if unspecified by a service. Updating this value only affects new tasks. Existing tasks continue to use their previously configured log driver until recreated. */
  LogDriver?: UpdateSwarmRequestTaskDefaultsLogDriver;
}
export const UpdateSwarmRequestTaskDefaults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    LogDriver: S.optional(UpdateSwarmRequestTaskDefaultsLogDriver),
  }),
).annotate({
  identifier: "UpdateSwarmRequestTaskDefaults",
}) as any as S.Schema<UpdateSwarmRequestTaskDefaults>;

export interface UpdateSwarmRequest {
  /** The version number of the swarm object being updated. This is required to avoid conflicting writes. */
  version: number;
  /** Rotate the worker join token. */
  rotateWorkerToken?: boolean;
  /** Rotate the manager join token. */
  rotateManagerToken?: boolean;
  /** Rotate the manager unlock key. */
  rotateManagerUnlockKey?: boolean;
  /** Name of the swarm. */
  Name?: string;
  /** User-defined key/value metadata. */
  Labels?: UpdateSwarmRequestLabelsMap;
  /** Orchestration configuration. */
  Orchestration?: SwarmSpecOrchestration | null;
  /** Raft configuration. */
  Raft?: SwarmSpecRaft;
  /** Dispatcher configuration. */
  Dispatcher?: SwarmSpecDispatcher | null;
  /** CA configuration. */
  CAConfig?: UpdateSwarmRequestCAConfig | null;
  /** Parameters related to encryption-at-rest. */
  EncryptionConfig?: SwarmSpecEncryptionConfig;
  /** Defaults for creating tasks in this cluster. */
  TaskDefaults?: UpdateSwarmRequestTaskDefaults;
}
export const UpdateSwarmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    version: S.Number.pipe(T.Query()),
    rotateWorkerToken: S.optional(S.Boolean.pipe(T.Query())),
    rotateManagerToken: S.optional(S.Boolean.pipe(T.Query())),
    rotateManagerUnlockKey: S.optional(S.Boolean.pipe(T.Query())),
    Name: S.optional(S.String),
    Labels: S.optional(UpdateSwarmRequestLabelsMap),
    Orchestration: S.optional(S.NullOr(SwarmSpecOrchestration)),
    Raft: S.optional(SwarmSpecRaft),
    Dispatcher: S.optional(S.NullOr(SwarmSpecDispatcher)),
    CAConfig: S.optional(S.NullOr(UpdateSwarmRequestCAConfig)),
    EncryptionConfig: S.optional(SwarmSpecEncryptionConfig),
    TaskDefaults: S.optional(UpdateSwarmRequestTaskDefaults),
  }).pipe(T.Http({ method: "POST", uri: "/swarm/update", code: 200 })),
).annotate({
  identifier: "UpdateSwarmRequest",
}) as any as S.Schema<UpdateSwarmRequest>;

export interface UpdateSwarmResponse {}
export const UpdateSwarmResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UpdateSwarmResponse",
}) as any as S.Schema<UpdateSwarmResponse>;

export interface UpdateVolumeRequest {
  /** The name or ID of the volume */
  name: string;
  /** The version number of the volume being updated. This is required to avoid conflicting writes. Found in the volume's `ClusterVolume` field. */
  version: number;
  Spec?: ClusterVolumeSpec;
}
export const UpdateVolumeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    version: S.Number.pipe(T.Query()),
    Spec: S.optional(ClusterVolumeSpec),
  }).pipe(T.Http({ method: "PUT", uri: "/volumes/{name}", code: 200 })),
).annotate({
  identifier: "UpdateVolumeRequest",
}) as any as S.Schema<UpdateVolumeRequest>;

export interface UpdateVolumeResponse {}
export const UpdateVolumeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UpdateVolumeResponse",
}) as any as S.Schema<UpdateVolumeResponse>;

export interface VolumeInspectRequest {
  /** Volume name or ID */
  name: string;
}
export const VolumeInspectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/volumes/{name}", code: 200 })),
).annotate({
  identifier: "VolumeInspectRequest",
}) as any as S.Schema<VolumeInspectRequest>;

export interface VolumePruneRequest {
  /** Filters to process on the prune list, encoded as JSON (a `map[string][]string`). Available filters: - `label` (`label=<key>`, `label=<key>=<value>`, `label!=<key>`, or `label!=<key>=<value>`) Prune volumes with (or without, in case `label!=...` is used) the specified labels. - `all` (`all=true`) - Consider all (local) volumes for pruning and not just anonymous volumes. */
  filters?: string;
}
export const VolumePruneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filters: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/volumes/prune", code: 200 })),
).annotate({
  identifier: "VolumePruneRequest",
}) as any as S.Schema<VolumePruneRequest>;

/** Volumes that were deleted */
export type VolumePruneResponseVolumesDeletedList = Array<string>;
export const VolumePruneResponseVolumesDeletedList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<VolumePruneResponseVolumesDeletedList>;

export interface VolumePruneResponse {
  /** Volumes that were deleted */
  VolumesDeleted?: VolumePruneResponseVolumesDeletedList;
  /** Disk space reclaimed in bytes */
  SpaceReclaimed?: number;
}
export const VolumePruneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    VolumesDeleted: S.optional(VolumePruneResponseVolumesDeletedList),
    SpaceReclaimed: S.optional(S.Number),
  }),
).annotate({
  identifier: "VolumePruneResponse",
}) as any as S.Schema<VolumePruneResponse>;

export type WaitContainerRequestCondition =
  | "not-running"
  | "next-exit"
  | "removed";
export const WaitContainerRequestCondition = S.String;

export interface WaitContainerRequest {
  /** ID or name of the container */
  id: string;
  /** Wait until a container state reaches the given condition. Defaults to `not-running` if omitted or empty. */
  condition?: WaitContainerRequestCondition | (string & {});
}
export const WaitContainerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    condition: S.optional(WaitContainerRequestCondition.pipe(T.Query())),
  }).pipe(T.Http({ method: "POST", uri: "/containers/{id}/wait", code: 200 })),
).annotate({
  identifier: "WaitContainerRequest",
}) as any as S.Schema<WaitContainerRequest>;

/** container waiting error, if any */
export interface ContainerWaitExitError {
  /** Details of an error */
  Message?: string;
}
export const ContainerWaitExitError = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    Message: S.optional(S.String),
  }),
).annotate({
  identifier: "ContainerWaitExitError",
}) as any as S.Schema<ContainerWaitExitError>;

/** OK response to ContainerWait operation */
export interface ContainerWaitResponse {
  /** Exit code of the container */
  StatusCode: number;
  Error?: ContainerWaitExitError;
}
export const ContainerWaitResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    StatusCode: S.Number,
    Error: S.optional(ContainerWaitExitError),
  }),
).annotate({
  identifier: "ContainerWaitResponse",
}) as any as S.Schema<ContainerWaitResponse>;

export type ArchiveContainerError = BadRequest | NotFound | DockerOpError;
/** Get an archive of a filesystem resource in a container Get a tar archive of a resource in the filesystem of container id. */
export const archiveContainer: API.OperationMethod<
  ArchiveContainerRequest,
  ArchiveContainerResponse,
  ArchiveContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ArchiveContainerRequest,
  output: ArchiveContainerResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type AttachContainerError = BadRequest | NotFound | DockerOpError;
/** Attach to a container Attach to a container to read its output or send it input. You can attach to the same container multiple times and you can reattach to containers that have been detached. Either the `stream` or `logs` parameter must be `true` for this endpoint to do anything. See the [documentation for the `docker attach` command](https://docs.docker.com/engine/reference/commandline/attach/) for more details. ### Hijacking This endpoint hijacks the HTTP connection to transport `stdin`, `stdout`, and `stderr` on the same socket. This is the response from the daemon for an attach request: ``` HTTP/1.1 200 OK Content-Type: application/vnd.docker.raw-stream [STREAM] ``` After the headers and two new lines, the TCP connection can now be used for raw, bidirectional communication between the client and server. To hint potential proxies about connection hijacking, the Docker client can also optionally send connection upgrade headers. For example, the client sends this request to upgrade the connection: ``` POST /containers/16253994b7c4/attach?stream=1&stdout=1 HTTP/1.1 Upgrade: tcp Connection: Upgrade ``` The Docker daemon will respond with a `101 UPGRADED` response, and will similarly follow with the raw stream: ``` HTTP/1.1 101 UPGRADED Content-Type: application/vnd.docker.raw-stream Connection: Upgrade Upgrade: tcp [STREAM] ``` ### Stream format When the TTY setting is disabled in [`POST /containers/create`](#operation/ContainerCreate), the HTTP Content-Type header is set to application/vnd.docker.multiplexed-stream and the stream over the hijacked connected is multiplexed to separate out `stdout` and `stderr`. The stream consists of a series of frames, each containing a header and a payload. The header contains the information which the stream writes (`stdout` or `stderr`). It also contains the size of the associated frame encoded in the last four bytes (`uint32`). It is encoded on the first eight bytes like this: ```go header := [8]byte{STREAM_TYPE, 0, 0, 0, SIZE1, SIZE2, SIZE3, SIZE4} ``` `STREAM_TYPE` can be: - 0: `stdin` (is written on `stdout`) - 1: `stdout` - 2: `stderr` `SIZE1, SIZE2, SIZE3, SIZE4` are the four bytes of the `uint32` size encoded as big endian. Following the header is the payload, which is the specified number of bytes of `STREAM_TYPE`. The simplest way to implement this protocol is the following: 1. Read 8 bytes. 2. Choose `stdout` or `stderr` depending on the first byte. 3. Extract the frame size from the last four bytes. 4. Read the extracted size and output it on the correct output. 5. Goto 1. ### Stream format when using a TTY When the TTY setting is enabled in [`POST /containers/create`](#operation/ContainerCreate), the stream is not multiplexed. The data exchanged over the hijacked connection is simply the raw data from the process PTY and client's `stdin`. */
export const attachContainer: API.OperationMethod<
  AttachContainerRequest,
  AttachContainerResponse,
  AttachContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AttachContainerRequest,
  output: AttachContainerResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type BuildPruneError = DockerOpError;
/** Delete builder cache */
export const buildPrune: API.OperationMethod<
  BuildPruneRequest,
  BuildPruneResponse,
  BuildPruneError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: BuildPruneRequest,
  output: BuildPruneResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CommitImageError = NotFound | DockerOpError;
/** Create a new image from a container */
export const commitImage: API.OperationMethod<
  CommitImageRequest,
  IDResponse,
  CommitImageError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CommitImageRequest,
  output: IDResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ConfigInspectError = NotFound | DockerOpError;
/** Inspect a config */
export const configInspect: API.OperationMethod<
  ConfigInspectRequest,
  Config,
  ConfigInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ConfigInspectRequest,
  output: Config,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ConnectNetworkError =
  | BadRequest
  | Forbidden
  | NotFound
  | DockerOpError;
/** Connect a container to a network The network must be either a local-scoped network or a swarm-scoped network with the `attachable` option set. A network cannot be re-attached to a running container */
export const connectNetwork: API.OperationMethod<
  ConnectNetworkRequest,
  ConnectNetworkResponse,
  ConnectNetworkError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ConnectNetworkRequest,
  output: ConnectNetworkResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ContainerAttachWebsocketError =
  | BadRequest
  | NotFound
  | DockerOpError;
/** Attach to a container via a websocket */
export const containerAttachWebsocket: API.OperationMethod<
  ContainerAttachWebsocketRequest,
  ContainerAttachWebsocketResponse,
  ContainerAttachWebsocketError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContainerAttachWebsocketRequest,
  output: ContainerAttachWebsocketResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ContainerChangesError = NotFound | DockerOpError;
/** Get changes on a container’s filesystem Returns which files in a container's filesystem have been added, deleted, or modified. The `Kind` of modification can be one of: - `0`: Modified ("C") - `1`: Added ("A") - `2`: Deleted ("D") */
export const containerChanges: API.OperationMethod<
  ContainerChangesRequest,
  ContainerChangesResponse,
  ContainerChangesError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContainerChangesRequest,
  output: ContainerChangesResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ContainerInspectError = NotFound | DockerOpError;
/** Inspect a container Return low-level information about a container. */
export const containerInspect: API.OperationMethod<
  ContainerInspectRequest,
  ContainerInspectResponse,
  ContainerInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContainerInspectRequest,
  output: ContainerInspectResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ContainerLogsError = NotFound | DockerOpError;
/** Get container logs Get `stdout` and `stderr` logs from a container. Note: This endpoint works only for containers with the `json-file` or `journald` logging driver. */
export const containerLogs: API.OperationMethod<
  ContainerLogsRequest,
  ContainerLogsResponse,
  ContainerLogsError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContainerLogsRequest,
  output: ContainerLogsResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ContainerPruneError = DockerOpError;
/** Delete stopped containers */
export const containerPrune: API.OperationMethod<
  ContainerPruneRequest,
  ContainerPruneResponse,
  ContainerPruneError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContainerPruneRequest,
  output: ContainerPruneResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ContainerStatsError = NotFound | DockerOpError;
/** Get container stats based on resource usage This endpoint returns a live stream of a container’s resource usage statistics. The `precpu_stats` is the CPU statistic of the *previous* read, and is used to calculate the CPU usage percentage. It is not an exact copy of the `cpu_stats` field. If either `precpu_stats.online_cpus` or `cpu_stats.online_cpus` is nil then for compatibility with older daemons the length of the corresponding `cpu_usage.percpu_usage` array should be used. On a cgroup v2 host, the following fields are not set * `blkio_stats`: all fields other than `io_service_bytes_recursive` * `cpu_stats`: `cpu_usage.percpu_usage` * `memory_stats`: `max_usage` and `failcnt` Also, `memory_stats.stats` fields are incompatible with cgroup v1. To calculate the values shown by the `stats` command of the docker cli tool the following formulas can be used: * used_memory = `memory_stats.usage - memory_stats.stats.cache` (cgroups v1) * used_memory = `memory_stats.usage - memory_stats.stats.inactive_file` (cgroups v2) * available_memory = `memory_stats.limit` * Memory usage % = `(used_memory / available_memory) * 100.0` * cpu_delta = `cpu_stats.cpu_usage.total_usage - precpu_stats.cpu_usage.total_usage` * system_cpu_delta = `cpu_stats.system_cpu_usage - precpu_stats.system_cpu_usage` * number_cpus = `length(cpu_stats.cpu_usage.percpu_usage)` or `cpu_stats.online_cpus` * CPU usage % = `(cpu_delta / system_cpu_delta) * number_cpus * 100.0` */
export const containerStats: API.OperationMethod<
  ContainerStatsRequest,
  ContainerStatsResponse,
  ContainerStatsError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContainerStatsRequest,
  output: ContainerStatsResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ContainerTopError = NotFound | DockerOpError;
/** List processes running inside a container On Unix systems, this is done by running the `ps` command. This endpoint is not supported on Windows. */
export const containerTop: API.OperationMethod<
  ContainerTopRequest,
  ContainerTopResponse,
  ContainerTopError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContainerTopRequest,
  output: ContainerTopResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CreateConfigError = Conflict | DockerOpError;
/** Create a config */
export const createConfig: API.OperationMethod<
  CreateConfigRequest,
  IDResponse,
  CreateConfigError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateConfigRequest,
  output: IDResponse,
  errors: [Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CreateContainerError =
  | BadRequest
  | NotFound
  | Conflict
  | DockerOpError;
/** Create a container */
export const createContainer: API.OperationMethod<
  CreateContainerRequest,
  ContainerCreateResponse,
  CreateContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateContainerRequest,
  output: ContainerCreateResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CreateImageError = NotFound | DockerOpError;
/** Create an image Pull or import an image. */
export const createImage: API.OperationMethod<
  CreateImageRequest,
  CreateImageResponse,
  CreateImageError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateImageRequest,
  output: CreateImageResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CreateNetworkError =
  | BadRequest
  | Forbidden
  | NotFound
  | DockerOpError;
/** Create a network */
export const createNetwork: API.OperationMethod<
  CreateNetworkRequest,
  NetworkCreateResponse,
  CreateNetworkError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateNetworkRequest,
  output: NetworkCreateResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CreatePluginError = DockerOpError;
/** Create a plugin */
export const createPlugin: API.OperationMethod<
  CreatePluginRequest,
  CreatePluginResponse,
  CreatePluginError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreatePluginRequest,
  output: CreatePluginResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CreateSecretError = Conflict | DockerOpError;
/** Create a secret */
export const createSecret: API.OperationMethod<
  CreateSecretRequest,
  IDResponse,
  CreateSecretError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSecretRequest,
  output: IDResponse,
  errors: [Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CreateServiceError =
  | BadRequest
  | Forbidden
  | Conflict
  | DockerOpError;
/** Create a service */
export const createService: API.OperationMethod<
  CreateServiceRequest,
  ServiceCreateResponse,
  CreateServiceError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateServiceRequest,
  output: ServiceCreateResponse,
  errors: [BadRequest, Forbidden, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type CreateVolumeError = DockerOpError;
/** Create a volume */
export const createVolume: API.OperationMethod<
  CreateVolumeRequest,
  Volume,
  CreateVolumeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateVolumeRequest,
  output: Volume,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeleteConfigError = NotFound | DockerOpError;
/** Delete a config */
export const deleteConfig: API.OperationMethod<
  DeleteConfigRequest,
  DeleteConfigResponse,
  DeleteConfigError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteConfigRequest,
  output: DeleteConfigResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeleteContainerError =
  | BadRequest
  | NotFound
  | Conflict
  | DockerOpError;
/** Remove a container */
export const deleteContainer: API.OperationMethod<
  DeleteContainerRequest,
  DeleteContainerResponse,
  DeleteContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteContainerRequest,
  output: DeleteContainerResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeleteImageError = NotFound | Conflict | DockerOpError;
/** Remove an image Remove an image, along with any untagged parent images that were referenced by that image. Images can't be removed if they have descendant images, are being used by a running container or are being used by a build. */
export const deleteImage: API.OperationMethod<
  DeleteImageRequest,
  DeleteImageResponse,
  DeleteImageError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteImageRequest,
  output: DeleteImageResponse,
  errors: [NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkError = Forbidden | NotFound | DockerOpError;
/** Remove a network */
export const deleteNetwork: API.OperationMethod<
  DeleteNetworkRequest,
  DeleteNetworkResponse,
  DeleteNetworkError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkRequest,
  output: DeleteNetworkResponse,
  errors: [Forbidden, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeleteNodeError = NotFound | DockerOpError;
/** Delete a node */
export const deleteNode: API.OperationMethod<
  DeleteNodeRequest,
  DeleteNodeResponse,
  DeleteNodeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNodeRequest,
  output: DeleteNodeResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeletePluginError = NotFound | DockerOpError;
/** Remove a plugin */
export const deletePlugin: API.OperationMethod<
  DeletePluginRequest,
  Plugin,
  DeletePluginError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeletePluginRequest,
  output: Plugin,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeleteSecretError = NotFound | DockerOpError;
/** Delete a secret */
export const deleteSecret: API.OperationMethod<
  DeleteSecretRequest,
  DeleteSecretResponse,
  DeleteSecretError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteSecretRequest,
  output: DeleteSecretResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeleteServiceError = NotFound | DockerOpError;
/** Delete a service */
export const deleteService: API.OperationMethod<
  DeleteServiceRequest,
  DeleteServiceResponse,
  DeleteServiceError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteServiceRequest,
  output: DeleteServiceResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DeleteVolumeError = NotFound | Conflict | DockerOpError;
/** Remove a volume Instruct the driver to remove the volume. */
export const deleteVolume: API.OperationMethod<
  DeleteVolumeRequest,
  DeleteVolumeResponse,
  DeleteVolumeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteVolumeRequest,
  output: DeleteVolumeResponse,
  errors: [NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DisablePluginError = NotFound | DockerOpError;
/** Disable a plugin */
export const disablePlugin: API.OperationMethod<
  DisablePluginRequest,
  DisablePluginResponse,
  DisablePluginError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DisablePluginRequest,
  output: DisablePluginResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DisconnectNetworkError = Forbidden | NotFound | DockerOpError;
/** Disconnect a container from a network */
export const disconnectNetwork: API.OperationMethod<
  DisconnectNetworkRequest,
  DisconnectNetworkResponse,
  DisconnectNetworkError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DisconnectNetworkRequest,
  output: DisconnectNetworkResponse,
  errors: [Forbidden, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type DistributionInspect2Error = DockerOpError;
/** Get image information from the registry Return image digest and platform information by contacting the registry. */
export const distributionInspect2: API.OperationMethod<
  DistributionInspectRequest,
  DistributionInspect,
  DistributionInspect2Error,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DistributionInspectRequest,
  output: DistributionInspect,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type EnablePluginError = NotFound | DockerOpError;
/** Enable a plugin */
export const enablePlugin: API.OperationMethod<
  EnablePluginRequest,
  EnablePluginResponse,
  EnablePluginError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EnablePluginRequest,
  output: EnablePluginResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ExecContainerError = NotFound | Conflict | DockerOpError;
/** Create an exec instance Run a command inside a running container. */
export const execContainer: API.OperationMethod<
  ExecContainerRequest,
  IDResponse,
  ExecContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExecContainerRequest,
  output: IDResponse,
  errors: [NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ExecInspectError = NotFound | DockerOpError;
/** Inspect an exec instance Return low-level information about an exec instance. */
export const execInspect: API.OperationMethod<
  ExecInspectRequest,
  ExecInspectResponse,
  ExecInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExecInspectRequest,
  output: ExecInspectResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ExecResizeError = BadRequest | NotFound | DockerOpError;
/** Resize an exec instance Resize the TTY session used by an exec instance. This endpoint only works if `tty` was specified as part of creating and starting the exec instance. */
export const execResize: API.OperationMethod<
  ExecResizeRequest,
  ExecResizeResponse,
  ExecResizeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExecResizeRequest,
  output: ExecResizeResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ExecStartError = NotFound | Conflict | DockerOpError;
/** Start an exec instance Starts a previously set up exec instance. If detach is true, this endpoint returns immediately after starting the command. Otherwise, it sets up an interactive session with the command. */
export const execStart: API.OperationMethod<
  ExecStartRequest,
  ExecStartResponse,
  ExecStartError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExecStartRequest,
  output: ExecStartResponse,
  errors: [NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ExportContainerError = NotFound | DockerOpError;
/** Export a container Export the contents of a container as a tarball. */
export const exportContainer: API.OperationMethod<
  ExportContainerRequest,
  ExportContainerResponse,
  ExportContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExportContainerRequest,
  output: ExportContainerResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type GetImageError = DockerOpError;
/** Export an image Get a tarball containing all images and metadata for a repository. If `name` is a specific name and tag (e.g. `ubuntu:latest`), then only that image (and its parents) are returned. If `name` is an image ID, similarly only that image (and its parents) are returned, but with the exclusion of the `repositories` file in the tarball, as there were no image names referenced. ### Image tarball format An image tarball contains [Content as defined in the OCI Image Layout Specification](https://github.com/opencontainers/image-spec/blob/v1.1.1/image-layout.md#content). Additionally, includes the manifest.json file associated with a backwards compatible docker save format. If the tarball defines a repository, the tarball should also include a `repositories` file at the root that contains a list of repository and tag names mapped to layer IDs. ```json { "hello-world": { "latest": "565a9d68a73f6706862bfe8409a7f659776d4d60a8d096eb4a3cbce6999cc2a1" } } ``` */
export const getImage: API.OperationMethod<
  GetImageRequest,
  GetImageResponse,
  GetImageError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetImageRequest,
  output: GetImageResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type GetPluginPrivilegesError = DockerOpError;
/** Get plugin privileges */
export const getPluginPrivileges: API.OperationMethod<
  GetPluginPrivilegesRequest,
  GetPluginPrivilegesResponse,
  GetPluginPrivilegesError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetPluginPrivilegesRequest,
  output: GetPluginPrivilegesResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImageAttestationsError = BadRequest | NotFound | DockerOpError;
/** Get attestation statements for an image Return the in-toto attestation statements attached to the image for the given platform. The daemon locates the attestation manifest(s) that reference the matching platform image manifest, reads their statement layers, and returns the verbatim statement JSON together with layer metadata. If the image has no attestations an empty array is returned. */
export const imageAttestations: API.OperationMethod<
  ImageAttestationsRequest,
  ImageAttestationsResponse,
  ImageAttestationsError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImageAttestationsRequest,
  output: ImageAttestationsResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImageBuildError = BadRequest | DockerOpError;
/** Build an image Build an image from a tar archive with a `Dockerfile` in it. The `Dockerfile` specifies how the image is built from the tar archive. It is typically in the archive's root, but can be at a different path or have a different name by specifying the `dockerfile` parameter. [See the `Dockerfile` reference for more information](https://docs.docker.com/engine/reference/builder/). The Docker daemon performs a preliminary validation of the `Dockerfile` before starting the build, and returns an error if the syntax is incorrect. After that, each instruction is run one-by-one until the ID of the new image is output. The build is canceled if the client drops the connection by quitting or being killed. */
export const imageBuild: API.OperationMethod<
  ImageBuildRequest,
  ImageBuildResponse,
  ImageBuildError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImageBuildRequest,
  output: ImageBuildResponse,
  errors: [BadRequest, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImageGetAllError = DockerOpError;
/** Export several images Get a tarball containing all images and metadata for several image repositories. For each value of the `names` parameter: if it is a specific name and tag (e.g. `ubuntu:latest`), then only that image (and its parents) are returned; if it is an image ID, similarly only that image (and its parents) are returned and there would be no names referenced in the 'repositories' file for this image ID. For details on the format, see the [export image endpoint](#operation/ImageGet). */
export const imageGetAll: API.OperationMethod<
  ImageGetAllRequest,
  ImageGetAllResponse,
  ImageGetAllError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImageGetAllRequest,
  output: ImageGetAllResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImageHistoryError = NotFound | DockerOpError;
/** Get the history of an image Return parent layers of an image. */
export const imageHistory: API.OperationMethod<
  ImageHistoryRequest,
  ImageHistoryResponse,
  ImageHistoryError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImageHistoryRequest,
  output: ImageHistoryResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImageInspect2Error = NotFound | DockerOpError;
/** Inspect an image Return low-level information about an image. */
export const imageInspect2: API.OperationMethod<
  ImageInspectRequest,
  ImageInspect,
  ImageInspect2Error,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImageInspectRequest,
  output: ImageInspect,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImageLoadError = DockerOpError;
/** Import images Load a set of images and tags into a repository. For details on the format, see the [export image endpoint](#operation/ImageGet). */
export const imageLoad: API.OperationMethod<
  ImageLoadRequest,
  ImageLoadResponse,
  ImageLoadError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImageLoadRequest,
  output: ImageLoadResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImagePruneError = DockerOpError;
/** Delete unused images */
export const imagePrune: API.OperationMethod<
  ImagePruneRequest,
  ImagePruneResponse,
  ImagePruneError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImagePruneRequest,
  output: ImagePruneResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImagePushError = NotFound | DockerOpError;
/** Push an image Push an image to a registry. If you wish to push an image on to a private registry, that image must already have a tag which references the registry. For example, `registry.example.com/myimage:latest`. The push is cancelled if the HTTP connection is closed. */
export const imagePush: API.OperationMethod<
  ImagePushRequest,
  ImagePushResponse,
  ImagePushError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImagePushRequest,
  output: ImagePushResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ImageTagError = BadRequest | NotFound | Conflict | DockerOpError;
/** Tag an image Create a tag that refers to a source image. This creates an additional reference (tag) to the source image. The tag can include a different repository name and/or tag. If the repository or tag already exists, it will be overwritten. */
export const imageTag: API.OperationMethod<
  ImageTagRequest,
  ImageTagResponse,
  ImageTagError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ImageTagRequest,
  output: ImageTagResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type JoinSwarmError = BadRequest | DockerOpError;
/** Join an existing swarm */
export const joinSwarm: API.OperationMethod<
  JoinSwarmRequest,
  JoinSwarmResponse,
  JoinSwarmError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: JoinSwarmRequest,
  output: JoinSwarmResponse,
  errors: [BadRequest, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type KillContainerError = NotFound | Conflict | DockerOpError;
/** Kill a container Send a POSIX signal to a container, defaulting to killing to the container. */
export const killContainer: API.OperationMethod<
  KillContainerRequest,
  KillContainerResponse,
  KillContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KillContainerRequest,
  output: KillContainerResponse,
  errors: [NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type LeaveSwarmError = DockerOpError;
/** Leave a swarm */
export const leaveSwarm: API.OperationMethod<
  LeaveSwarmRequest,
  LeaveSwarmResponse,
  LeaveSwarmError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LeaveSwarmRequest,
  output: LeaveSwarmResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListConfigError = DockerOpError;
/** List configs */
export const listConfig: API.OperationMethod<
  ListConfigRequest,
  ListConfigResponse,
  ListConfigError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListConfigRequest,
  output: ListConfigResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListContainerError = BadRequest | DockerOpError;
/** List containers Returns a list of containers. For details on the format, see the [inspect endpoint](#operation/ContainerInspect). Note that it uses a different, smaller representation of a container than inspecting a single container. For example, the list of linked containers is not propagated . */
export const listContainer: API.OperationMethod<
  ListContainerRequest,
  ListContainerResponse,
  ListContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListContainerRequest,
  output: ListContainerResponse,
  errors: [BadRequest, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListImageError = DockerOpError;
/** List Images Returns a list of images on the server. Note that it uses a different, smaller representation of an image than inspecting a single image. */
export const listImage: API.OperationMethod<
  ListImageRequest,
  ListImageResponse,
  ListImageError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListImageRequest,
  output: ListImageResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListNetworkError = DockerOpError;
/** List networks Returns a list of networks. For details on the format, see the [network inspect endpoint](#operation/NetworkInspect). Note that it uses a different, smaller representation of a network than inspecting a single network. For example, the list of containers attached to the network is not propagated in API versions 1.28 and up. */
export const listNetwork: API.OperationMethod<
  ListNetworkRequest,
  ListNetworkResponse,
  ListNetworkError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNetworkRequest,
  output: ListNetworkResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListNodeError = DockerOpError;
/** List nodes */
export const listNode: API.OperationMethod<
  ListNodeRequest,
  ListNodeResponse,
  ListNodeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNodeRequest,
  output: ListNodeResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListPluginError = DockerOpError;
/** List plugins Returns information about installed plugins. */
export const listPlugin: API.OperationMethod<
  ListPluginRequest,
  ListPluginResponse,
  ListPluginError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListPluginRequest,
  output: ListPluginResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListSecretError = DockerOpError;
/** List secrets */
export const listSecret: API.OperationMethod<
  ListSecretRequest,
  ListSecretResponse,
  ListSecretError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSecretRequest,
  output: ListSecretResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListServiceError = DockerOpError;
/** List services */
export const listService: API.OperationMethod<
  ListServiceRequest,
  ListServiceResponse,
  ListServiceError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListServiceRequest,
  output: ListServiceResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListTaskError = DockerOpError;
/** List tasks */
export const listTask: API.OperationMethod<
  ListTaskRequest,
  ListTaskResponse,
  ListTaskError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListTaskRequest,
  output: ListTaskResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ListVolumeError = DockerOpError;
/** List volumes */
export const listVolume: API.OperationMethod<
  ListVolumeRequest,
  VolumeListResponse,
  ListVolumeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListVolumeRequest,
  output: VolumeListResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type NetworkInspectError = NotFound | DockerOpError;
/** Inspect a network */
export const networkInspect: API.OperationMethod<
  NetworkInspectRequest,
  NetworkInspectResponse,
  NetworkInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: NetworkInspectRequest,
  output: NetworkInspectResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type NetworkPruneError = DockerOpError;
/** Delete unused networks */
export const networkPrune: API.OperationMethod<
  NetworkPruneRequest,
  NetworkPruneResponse,
  NetworkPruneError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: NetworkPruneRequest,
  output: NetworkPruneResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type NodeInspectError = NotFound | DockerOpError;
/** Inspect a node */
export const nodeInspect: API.OperationMethod<
  NodeInspectRequest,
  Node,
  NodeInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: NodeInspectRequest,
  output: Node,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type PauseContainerError = NotFound | DockerOpError;
/** Pause a container Use the freezer cgroup to suspend all processes in a container. Traditionally, when suspending a process the `SIGSTOP` signal is used, which is observable by the process being suspended. With the freezer cgroup the process is unaware, and unable to capture, that it is being suspended, and subsequently resumed. */
export const pauseContainer: API.OperationMethod<
  PauseContainerRequest,
  PauseContainerResponse,
  PauseContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PauseContainerRequest,
  output: PauseContainerResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type PingSystemError = DockerOpError;
/** Ping This is a dummy endpoint you can use to test if the server is accessible. */
export const pingSystem: API.OperationMethod<
  PingSystemRequest,
  PingSystemResponse,
  PingSystemError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PingSystemRequest,
  output: PingSystemResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type PluginInspectError = NotFound | DockerOpError;
/** Inspect a plugin */
export const pluginInspect: API.OperationMethod<
  PluginInspectRequest,
  Plugin,
  PluginInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PluginInspectRequest,
  output: Plugin,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type PluginPullError = DockerOpError;
/** Install a plugin Pulls and installs a plugin. After the plugin is installed, it can be enabled using the [`POST /plugins/{name}/enable` endpoint](#operation/PostPluginsEnable). */
export const pluginPull: API.OperationMethod<
  PluginPullRequest,
  PluginPullResponse,
  PluginPullError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PluginPullRequest,
  output: PluginPullResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type PluginPushError = NotFound | DockerOpError;
/** Push a plugin Push a plugin to the registry. */
export const pluginPush: API.OperationMethod<
  PluginPushRequest,
  PluginPushResponse,
  PluginPushError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PluginPushRequest,
  output: PluginPushResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type PluginUpgradeError = NotFound | DockerOpError;
/** Upgrade a plugin */
export const pluginUpgrade: API.OperationMethod<
  PluginUpgradeRequest,
  PluginUpgradeResponse,
  PluginUpgradeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PluginUpgradeRequest,
  output: PluginUpgradeResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type PutContainerArchiveError =
  | BadRequest
  | Forbidden
  | NotFound
  | DockerOpError;
/** Extract an archive of files or folders to a directory in a container Upload a tar archive to be extracted to a path in the filesystem of container id. `path` parameter is asserted to be a directory. If it exists as a file, 400 error will be returned with message "not a directory". */
export const putContainerArchive: API.OperationMethod<
  PutContainerArchiveRequest,
  PutContainerArchiveResponse,
  PutContainerArchiveError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PutContainerArchiveRequest,
  output: PutContainerArchiveResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type RenameContainerError = NotFound | Conflict | DockerOpError;
/** Rename a container */
export const renameContainer: API.OperationMethod<
  RenameContainerRequest,
  RenameContainerResponse,
  RenameContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RenameContainerRequest,
  output: RenameContainerResponse,
  errors: [NotFound, Conflict, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ResizeContainerError = NotFound | DockerOpError;
/** Resize a container TTY Resize the TTY for a container. */
export const resizeContainer: API.OperationMethod<
  ResizeContainerRequest,
  ResizeContainerResponse,
  ResizeContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ResizeContainerRequest,
  output: ResizeContainerResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type RestartContainerError = NotFound | DockerOpError;
/** Restart a container */
export const restartContainer: API.OperationMethod<
  RestartContainerRequest,
  RestartContainerResponse,
  RestartContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RestartContainerRequest,
  output: RestartContainerResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SearchImageError = DockerOpError;
/** Search images Search for an image on Docker Hub. */
export const searchImage: API.OperationMethod<
  SearchImageRequest,
  SearchImageResponse,
  SearchImageError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SearchImageRequest,
  output: SearchImageResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SecretInspectError = NotFound | DockerOpError;
/** Inspect a secret */
export const secretInspect: API.OperationMethod<
  SecretInspectRequest,
  Secret,
  SecretInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SecretInspectRequest,
  output: Secret,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ServiceInspectError = NotFound | DockerOpError;
/** Inspect a service */
export const serviceInspect: API.OperationMethod<
  ServiceInspectRequest,
  Service,
  ServiceInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ServiceInspectRequest,
  output: Service,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type ServiceLogsError = NotFound | DockerOpError;
/** Get service logs Get `stdout` and `stderr` logs from a service. See also [`/containers/{id}/logs`](#operation/ContainerLogs). **Note**: This endpoint works only for services with the `local`, `json-file` or `journald` logging drivers. */
export const serviceLogs: API.OperationMethod<
  ServiceLogsRequest,
  ServiceLogsResponse,
  ServiceLogsError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ServiceLogsRequest,
  output: ServiceLogsResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SessionError = BadRequest | DockerOpError;
/** Initialize interactive session Start a new interactive session with a server. Session allows server to call back to the client for advanced capabilities. > **Deprecated**: This endpoint is deprecated and will be removed in a future version. > Server should support gRPC directly on the listening socket. ### Hijacking This endpoint hijacks the HTTP connection to HTTP2 transport that allows the client to expose gPRC services on that connection. For example, the client sends this request to upgrade the connection: ``` POST /session HTTP/1.1 Upgrade: h2c Connection: Upgrade ``` The Docker daemon responds with a `101 UPGRADED` response follow with the raw stream: ``` HTTP/1.1 101 UPGRADED Connection: Upgrade Upgrade: h2c ``` */
export const session: API.OperationMethod<
  SessionRequest,
  SessionResponse,
  SessionError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SessionRequest,
  output: SessionResponse,
  errors: [BadRequest, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SetPluginError = NotFound | DockerOpError;
/** Configure a plugin */
export const setPlugin: API.OperationMethod<
  SetPluginRequest,
  SetPluginResponse,
  SetPluginError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetPluginRequest,
  output: SetPluginResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type StartContainerError = BadRequest | NotFound | DockerOpError;
/** Start a container */
export const startContainer: API.OperationMethod<
  StartContainerRequest,
  StartContainerResponse,
  StartContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StartContainerRequest,
  output: StartContainerResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type StopContainerError = NotFound | DockerOpError;
/** Stop a container */
export const stopContainer: API.OperationMethod<
  StopContainerRequest,
  StopContainerResponse,
  StopContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StopContainerRequest,
  output: StopContainerResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SwarmInitError = BadRequest | DockerOpError;
/** Initialize a new swarm */
export const swarmInit: API.OperationMethod<
  SwarmInitRequest,
  SwarmInitResponse,
  SwarmInitError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SwarmInitRequest,
  output: SwarmInitResponse,
  errors: [BadRequest, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SwarmInspectError = NotFound | DockerOpError;
/** Inspect swarm */
export const swarmInspect: API.OperationMethod<
  SwarmInspectRequest,
  SwarmInspectResponse,
  SwarmInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SwarmInspectRequest,
  output: SwarmInspectResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SwarmUnlockkeyError = DockerOpError;
/** Get the unlock key */
export const swarmUnlockkey: API.OperationMethod<
  SwarmUnlockkeyRequest,
  SwarmUnlockkeyResponse,
  SwarmUnlockkeyError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SwarmUnlockkeyRequest,
  output: SwarmUnlockkeyResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SystemAuthError = DockerOpError;
/** Check auth configuration Validate credentials for a registry and, if available, get an identity token for accessing the registry without password. */
export const systemAuth: API.OperationMethod<
  SystemAuthRequest,
  AuthResponse,
  SystemAuthError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SystemAuthRequest,
  output: AuthResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SystemDataUsageError = DockerOpError;
/** Get data usage information */
export const systemDataUsage: API.OperationMethod<
  SystemDataUsageRequest,
  SystemDataUsageResponse,
  SystemDataUsageError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SystemDataUsageRequest,
  output: SystemDataUsageResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SystemEventsError = BadRequest | DockerOpError;
/** Monitor events Stream real-time events from the server. Various objects within Docker report events when something happens to them. Containers report these events: `attach`, `commit`, `copy`, `create`, `destroy`, `detach`, `die`, `exec_create`, `exec_detach`, `exec_start`, `exec_die`, `export`, `health_status`, `kill`, `oom`, `pause`, `rename`, `resize`, `restart`, `start`, `stop`, `top`, `unpause`, `update`, and `prune` Images report these events: `create`, `delete`, `import`, `load`, `pull`, `push`, `save`, `tag`, `untag`, and `prune` Volumes report these events: `create`, `mount`, `unmount`, `destroy`, and `prune` Networks report these events: `create`, `connect`, `disconnect`, `destroy`, `update`, `remove`, and `prune` The Docker daemon reports these events: `reload` Services report these events: `create`, `update`, and `remove` Nodes report these events: `create`, `update`, and `remove` Secrets report these events: `create`, `update`, and `remove` Configs report these events: `create`, `update`, and `remove` The Builder reports `prune` events */
export const systemEvents: API.OperationMethod<
  SystemEventsRequest,
  EventMessage,
  SystemEventsError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SystemEventsRequest,
  output: EventMessage,
  errors: [BadRequest, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SystemInfo2Error = DockerOpError;
/** Get system information */
export const systemInfo2: API.OperationMethod<
  SystemInfo2Request,
  SystemInfo,
  SystemInfo2Error,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SystemInfo2Request,
  output: SystemInfo,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type SystemVersion2Error = DockerOpError;
/** Get version Returns the version of Docker that is running and various information about the system that Docker is running on. */
export const systemVersion2: API.OperationMethod<
  SystemVersion2Request,
  SystemVersion,
  SystemVersion2Error,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SystemVersion2Request,
  output: SystemVersion,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type TaskInspectError = NotFound | DockerOpError;
/** Inspect a task */
export const taskInspect: API.OperationMethod<
  TaskInspectRequest,
  Task,
  TaskInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TaskInspectRequest,
  output: Task,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type TaskLogsError = NotFound | DockerOpError;
/** Get task logs Get `stdout` and `stderr` logs from a task. See also [`/containers/{id}/logs`](#operation/ContainerLogs). **Note**: This endpoint works only for services with the `local`, `json-file` or `journald` logging drivers. */
export const taskLogs: API.OperationMethod<
  TaskLogsRequest,
  TaskLogsResponse,
  TaskLogsError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TaskLogsRequest,
  output: TaskLogsResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UnlockSwarmError = DockerOpError;
/** Unlock a locked manager */
export const unlockSwarm: API.OperationMethod<
  UnlockSwarmRequest,
  UnlockSwarmResponse,
  UnlockSwarmError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UnlockSwarmRequest,
  output: UnlockSwarmResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UnpauseContainerError = NotFound | DockerOpError;
/** Unpause a container Resume a container which has been paused. */
export const unpauseContainer: API.OperationMethod<
  UnpauseContainerRequest,
  UnpauseContainerResponse,
  UnpauseContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UnpauseContainerRequest,
  output: UnpauseContainerResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UpdateConfigError = BadRequest | NotFound | DockerOpError;
/** Update a Config */
export const updateConfig: API.OperationMethod<
  UpdateConfigRequest,
  UpdateConfigResponse,
  UpdateConfigError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateConfigRequest,
  output: UpdateConfigResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UpdateContainerError = NotFound | DockerOpError;
/** Update a container Change various configuration options of a container without having to recreate it. */
export const updateContainer: API.OperationMethod<
  UpdateContainerRequest,
  ContainerUpdateResponse,
  UpdateContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateContainerRequest,
  output: ContainerUpdateResponse,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UpdateNodeError = BadRequest | NotFound | DockerOpError;
/** Update a node */
export const updateNode: API.OperationMethod<
  UpdateNodeRequest,
  UpdateNodeResponse,
  UpdateNodeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateNodeRequest,
  output: UpdateNodeResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UpdateSecretError = BadRequest | NotFound | DockerOpError;
/** Update a Secret */
export const updateSecret: API.OperationMethod<
  UpdateSecretRequest,
  UpdateSecretResponse,
  UpdateSecretError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateSecretRequest,
  output: UpdateSecretResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UpdateServiceError = BadRequest | NotFound | DockerOpError;
/** Update a service */
export const updateService: API.OperationMethod<
  UpdateServiceRequest,
  ServiceUpdateResponse,
  UpdateServiceError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateServiceRequest,
  output: ServiceUpdateResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UpdateSwarmError = BadRequest | DockerOpError;
/** Update a swarm */
export const updateSwarm: API.OperationMethod<
  UpdateSwarmRequest,
  UpdateSwarmResponse,
  UpdateSwarmError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateSwarmRequest,
  output: UpdateSwarmResponse,
  errors: [BadRequest, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type UpdateVolumeError = BadRequest | NotFound | DockerOpError;
/** "Update a volume. Valid only for Swarm cluster volumes" */
export const updateVolume: API.OperationMethod<
  UpdateVolumeRequest,
  UpdateVolumeResponse,
  UpdateVolumeError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateVolumeRequest,
  output: UpdateVolumeResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type VolumeInspectError = NotFound | DockerOpError;
/** Inspect a volume */
export const volumeInspect: API.OperationMethod<
  VolumeInspectRequest,
  Volume,
  VolumeInspectError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: VolumeInspectRequest,
  output: Volume,
  errors: [NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type VolumePruneError = DockerOpError;
/** Delete unused volumes */
export const volumePrune: API.OperationMethod<
  VolumePruneRequest,
  VolumePruneResponse,
  VolumePruneError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: VolumePruneRequest,
  output: VolumePruneResponse,
  errors: [UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));

export type WaitContainerError = BadRequest | NotFound | DockerOpError;
/** Wait for a container Block until a container stops, then returns the exit code. */
export const waitContainer: API.OperationMethod<
  WaitContainerRequest,
  ContainerWaitResponse,
  WaitContainerError,
  DockerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WaitContainerRequest,
  output: ContainerWaitResponse,
  errors: [BadRequest, NotFound, UnknownDockerError],
  protocol: DockerProtocol,
  retry: Retry.Retry,
}));
